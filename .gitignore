# build & deps
node_modules/
dist/
.astro/
.output/
# локальное состояние wrangler: создаётся при любом его запуске, включая
# --dry-run. В репозиторий не идёт — это кэш машины, а не проекта.
.wrangler/

# окружение и секреты — репозиторий публичный, это не формальность
.env
.env.*
!.env.example

# кэши инструментов контура
.jscpd/
coverage/
.coverage
coverage.json
.mypy_cache/
.ruff_cache/
__pycache__/

# macOS
.DS_Store
