# Канон-контур — карта канонов

**Входная точка набора канонов.** Если в корне лежат несколько `*.md` канонов —
**начни отсюда**, потом иди по ссылкам. Этот файл короткий специально.

> Язык: проза — русский; имена файлов / пути — английские.

---

## 1. Что это (одна картинка)

```text
AGENT_STACK.md          ← ты здесь (карта)
        │
        ▼
① AGENT_DELIVERY_HARNESS.md   — КАК вести работу (фазы, DoD, stop-gates, evals, метрики)
        │
        ▼
② CODE_QUALITY_GATES.md       — КАК не портить код (lint / types / secrets) + CI-workflow
        │
        ▼
③ OKF_KNOWLEDGE_BUNDLE.md     — ГДЕ правда домена (knowledge bundle) + гейт code↔canon
        ╎
        ╎ ④ CI + гейт мержа — СКВОЗНОЙ слой: один прогон проверяет все три
        ╎    (контракт — Delivery §10.4; реализация — CQG §8)
        ▼
   зелёные гейты на СЛИТОМ состоянии = единственное, что закрывает поставку
```

**④ — не четвёртый файл, а место запуска первых трёх.** Локальные хуки обходятся
(`git commit -n`, `STRICT=0`, «хук не установлен»), поэтому «Done = oracles» без CI
означает «Done = самооценка». Разворачивается вместе с ②, обслуживает всё.

**Платного тарифа не требует.** Серверная защита ветки платная (GitHub: rulesets —
Team+, приватный репо — Pro+), поэтому гейт мержа лежит **в репозитории**:
`merge_guard.sh` гоняет гейты на слитом состоянии и мержит только зелёное,
`needs:` не пускает красное в прод, pre-push поднимает цену обхода. Защита ветки —
усиление сверху (CQG §8.5.3), а не предпосылка.

| # | Файл | Версия | Отвечает на | Не делает |
|---|---|---|---|---|
| ① | [AGENT_DELIVERY_HARNESS.md](AGENT_DELIVERY_HARNESS.md) | `delivery@1.84` | Что строить, в каком порядке, когда «готово»; evals; hooks; CI-контракт; права на действия; журналы поведения; метрики | Не задаёт ruff/eslint; не формат OKF |
| ② | [CODE_QUALITY_GATES.md](CODE_QUALITY_GATES.md) | `cqg@2.28` | Форма кода, commit-гейты, CI-workflow, secrets в git | Не ведёт поставку фичи; не knowledge wiki |
| ③ | [OKF_KNOWLEDGE_BUNDLE.md](OKF_KNOWLEDGE_BUNDLE.md) | `okf@1.17` | Канон смысла (политики, ADR, метрики) | Не фазы delivery; не lint-пороги; не тела skills |

Версии — из шапок соответствующих файлов. При развёртывании они пишутся в проект
одной строкой (`stack: delivery@1.68, cqg@2.01, okf@absent`) в
`delivery/CONSTITUTION.md` и `delivery/active/STATUS.md`: без этого нельзя сказать,
какую ревизию канона репозиторий получил и что менялось с тех пор.

**Эта карта:** `stack-map@1.51` · 2026-08-18. Владеет двумя сквозными вещами:
приёмкой контура (**§6**) и самопроверкой канонов (**§7** + Приложение A).

### 1.1. Словарь (читать до всего остального)

Два слова несут всю нагрузку. Путаница между ними — самая частая, потому что
описание и описываемое легко смешать.

| Термин | Что это | Чем не является |
|---|---|---|
| **Каноны** | Четыре `*.md` — источник правды. Копируются в проект, читаются, обновляются по версии | Не работают сами. Канон ничего не проверяет |
| **Контур** | То, что развёрнуто **в проекте** по канонам: `delivery/`, `scripts/**`, `knowledge/`, гейты, CI, гейт мержа. Это оно ловит ошибки | Не документация. Контур — работающая машинерия |
| **Слой** | ① процесс · ② форма кода · ③ канон домена · ④ CI и гейт мержа (сквозной) | Не файл: слой = канон + его часть контура |
| **Технологический стек** | Язык и инструменты **проекта** (Python+TS, Swift, Go) | Не про каноны. В этих документах слово «стек» значит **только** это |
| **Гейт** | Механическая проверка: exit 0/1, без участия модели | Не ревью и не «спроси LLM» |
| **Oracle** | Всё, что отвечает «готово или нет»: гейты + тесты + smoke + фазовые артефакты | Шире гейта |
| **Payload** | Вторая половина каждого канона: шаблоны и дословные исходники | Не читается в обычной работе (§3) |

Вместе набор называется **канон-контур**; в разговоре достаточно «контур», когда
речь о развёрнутом, и «каноны», когда о документах:

> «разверни контур в проекте» · «контур боевой / контур weak» ·
> «каноны обновились до `cqg@1.14`» · «технологический стек проекта — Swift»

**Про поле `stack:`** в `STATUS.md` и `CONSTITUTION.md`: имя историческое, хранит
**версии канонов** (`stack: delivery@1.18, cqg@1.14, okf@absent`). Не переименовано
сознательно — иначе пришлось бы править уже развёрнутые проекты; читай его как
`canon:`. То же про имена файлов `AGENT_STACK.md`, `stack_selftest.py`,
`STACK-ACCEPTANCE.md`.

---

## 2. Движение агента

### A. Первый заход в проект (развернуть контур)

Делай **строго по порядку**. Не начинай с ② или ③.

1. Прочитай этот файл целиком.
2. Разверни **① Delivery** по его §7 (дерево `delivery/`, hook в `AGENTS.md`).
3. Если человек просит полный контур — разверни **② CQG** по его §5 **все 11 шагов**
   (конфиг копируется дословно, не «по смыслу»; шаги 8–10 — CI, гейт мержа,
   защита ветки; шаг 11 — снимок канонов и его самопроверка, §7.1).
4. Затем **③ OKF** по его §0→§5 (у OKF свой preflight: сверка upstream SPEC).
5. Поставь **④ гейт мержа** по CQG §8.5: `merge_guard.sh` + pre-push + `needs:`
   (на любом тарифе); защиту ветки — если тариф даёт (§8.5.3).
6. В `delivery/CONSTITUTION.md` проставь указатели и версии: CQG/OKF/CI deployed.
7. Прогони приёмку — **§6** ниже. Пока чеклист не зелёный, контур не боевой.

Команда человеку для полного контура (одна, копируется целиком):

> «Разверни канон-контур по `AGENT_STACK.md` в этом проекте, строго по порядку:
> ① Delivery (`AGENT_DELIVERY_HARNESS.md` §7): дерево `delivery/`, `CONSTITUTION.md`
> под этот проект, `scripts/delivery_check.py` (Приложение B),
> `scripts/delivery_metrics.py` (Приложение C), hook A.5 в `AGENTS.md`.
> ② CQG (`CODE_QUALITY_GATES.md` §5, **все 11 шагов**): скрипты Приложения A,
> конфиги Приложения B — `.pre-commit-config.yaml` **копируй дословно**, не собирай
> «по смыслу», иначе гейт теряется молча (**что именно «дословно» — CQG §5, врезка:
> транспорт хука менять можно, состав гейтов и строку вызова нельзя; оффлайн-профиль
> на `local`-хуках законен**); baseline'ы, `pre-commit install`,
> `scripts/lint/check_baseline_ratchet.sh` (§8.2), `check_gate_coverage.sh` (мета-гейт)
> и `.github/workflows/quality.yml` (§8.3). Сверься с блоком «Применимость» — если
> технологический стек проекта не Python+TS — адаптируй паттерны, сохранив
> механику ратчета, и пройди таблицу §3 построчно.
> ③ OKF (`OKF_KNOWLEDGE_BUNDLE.md`): сначала §0 preflight (сверить upstream SPEC),
> затем bundle `knowledge/`, `scripts/okf_validate.py` (Приложение B),
> `scripts/okf_sync_gate.py` (Приложение C) с заполненным `implementation:` хотя бы
> у одного concept'а, weekly-workflow `--check-stale`.
> ④ Гейт мержа по CQG §8.5: **обязательно** `scripts/merge_guard.sh` (§8.5.2),
> `scripts/lint/check_ci_status.sh`, pre-push hook и `needs:` у downstream-джоб —
> это работает на любом тарифе. Защиту ветки (§8.5.3) предлагай только если тариф
> её даёт — тогда скажи, что нужно сделать руками в UI.
> **`ci-oracles` заполняй по факту прогона, а не по факту наличия workflow:**
> сначала `gh run list --workflow quality --branch <ветка>`, и если красный —
> значение `weak` + blocker в STATUS, а поставку не закрывать.
> Запиши версии в `stack:` (constitution + STATUS), разверни снимок канонов и его
> самопроверку по **CQG §5 шаг 11** (вариант C по §7.1 — дефолт, выбирай сам:
> `docs/canon/` + `stack_selftest.py` рядом + `BREAKER_EXCLUDE` + шаг
> `Canon payload selftest` в workflow) и закончи приёмкой по `AGENT_STACK.md` §6:
> покажи мне чеклист с отметками и что осталось weak.»

Нужен только каркас процесса, без ②③ — скажи «разверни только Delivery по §7».

### A′. ⚠ Три пункта, которые агент НЕ закроет сам

Отмечено здесь специально: это не дефекты канонов и не то, что «забыли доделать» —
это места, где развёртывание **упирается в реальность проекта**. Агент,
разворачивающий контур, обязан их назвать вслух, а не тихо обойти.

Проверено практикой: на первом реальном развёртывании пункт 3 был обойдён
молча — инвариант остался непокрытым, а приёмка показала «всё зелено».

| # | Пункт | Что происходит | Кто закрывает |
|---|---|---|---|
| **1** | **Раскладка путей** | Первый прогон на чужой раскладке **зелёный, а не красный** — замерено: 7 гейтов из 9 выходят 0, потому что «нет каталога» для них законный мягкий пропуск (§4.3b: ложное красное дороже пропуска). Опасность обратная ожидаемой: контур выглядит покрытым, не просмотрев ни файла. Смотреть надо не на exit code, а на **число просмотренных файлов** в каждой строке вывода (одно законное исключение — правило, выбирающее файлы по имени нарушителя: CQG §6) | Агент — настройкой env по **CQG §6** (см. там «Первый прогон»), при другом технологическом стеке — адаптацией паттернов по блоку «Применимость». Приёмка §6 ниже требует непустого числа просмотренных именно поэтому |
| **2** | **Гейт мержа** | Серверная защита ветки **платная** (GitHub: rulesets — Team+, приватный репо — Pro+). Поэтому база — **в репозитории**: мерж только через `merge_guard.sh`, тяжёлые проверки на `pre-push`, downstream через `needs:` (**CQG §8.5.1–8.5.2**) | Агент — кладёт `merge_guard.sh` и pre-push hook. Человек — **если тариф позволяет**, включает защиту ветки (§8.5.3) как усиление |
| **3** | **Где лежат каноны** | Если четыре `*.md` не в репозитории (лежат в отдельном каталоге и не коммитятся) — CI их не видит, `stack_selftest` в CI невозможен, инвариант остаётся непокрытым | Агент — выбирает вариант A/B/C из **§7.1** и **пишет выбор в STATUS**; молчаливое B = дыра, выглядящая как покрытие |

**Запрещённые «решения» пункта 1:** отключить гейт, поставить `STRICT=0` в конфиг,
переснять baseline вверх, чтобы стало зелено. Красный первый прогон означает
«пути не настроены» **или** «в коде правда есть нарушения» — различить это и
есть работа развёртывания.

Про пункт 2 в `STATUS.md` пиши честное состояние (Delivery §10.4):
`deployed` — есть серверный гейт; `tooling` — гейт в репозитории, тариф не даёт
серверного (**нормальный рабочий режим**, не поддавки); `weak` — CI нет вовсе.
Что на бесплатном тарифе закрыть нечем: **force-push и обход админом** — этот
остаток пишется в STATUS, а не замалчивается.

### B. Обычная работа (фича / баг) — контур уже есть

1. Открой `delivery/active/STATUS.md` — фаза и класс S/M/L.
2. Следуй фазам ①: specify → plan → tasks → implement → verify → converge → handoff  
   (короткий путь для S — см. Delivery §2.2; подфазы clarify/analyze — там же).
3. При письме кода держи ② (гейты / pre-commit) зелёными — и помни, что
   закрывает поставку **зелёный CI**, а не локальный прогон (Delivery §10.4).
4. Канон домена не выдумывай — читай `knowledge/` (③); при смене инварианта
   обнови concept + log.
5. **Done** только по DoD Delivery (oracles PASS), не по самооценке.

### C. Если чего-то ещё нет

| Нет в репо | Действие |
|---|---|
| Нет `delivery/` | Сначала ①, не пиши большую фичу «в лоб» |
| Нет CQG / pre-commit | Работает; STATUS: `shape-oracles: weak`; предложи развернуть ② |
| **Нет CI** | STATUS: `ci-oracles: weak`; на классе L нужен human waiver. Локальные гейты обходятся (`commit -n`), поэтому Verifier обязан приложить прогон в чистом клоне — Delivery §10.4, CQG §8 |
| Нет `knowledge/` | Работает; не выдумывай «как принято» — спроси человека или предложи ③ |

---

## 3. Порядок чтения в сессии (чеклист)

- [ ] `AGENT_STACK.md` (этот файл)
- [ ] `delivery/active/STATUS.md` (если delivery уже развёрнут)
- [ ] Нужный кусок ① / ② / ③ **по таблице маршрутизации** Delivery §0.3a
      (`kind` → что читать, что обязательно, какие гейты сработают) — не все три
      целиком каждый раз
- [ ] `knowledge/references/repo-map.md` — как устроен **этот** репозиторий
      (входные точки, слои, грабли; OKF §3.5a). Если OKF не развёрнут — пропустить
- [ ] `delivery/archive/INDEX.md` — уроки по путям, которые собираешься трогать
      (Delivery §2.2a): строки, чьи пути пересекаются с твоим диффом
- [ ] Для доменного вопроса: `knowledge/index.md` → links (если OKF есть)

⚠ **Маршрут по `kind` живёт в ОДНОМ месте — Delivery §0.3a**, и здесь стоит
ссылка, а не копия. Это удерживается прогоном (`stack-map@1.44`): вторая таблица
по `kind` в карте роняет сьют, а оглавление Delivery §0.3 сверяется с реальными
секциями. Внешняя рецензия предлагала «слить три маршрутизатора»; замер показал,
что сливать нечего — §0.3 это оглавление, §0.3a маршрут, а чеклист ниже уже
ссылается на него. Но держалось это внимательностью автора, а не механикой.

Первые два пункта — про процесс, следующие два — про **этот** проект. Пропуск
третьего и четвёртого и есть «переоткрывать уже понятое»: карта отвечает, как
репозиторий устроен, индекс архива — на чём в нём уже спотыкались.

**Канон vs payload.** У каждого из ①②③ вторая половина файла — **bootstrap
payload** (шаблоны и дословные исходники скриптов), помеченный баннером
`⬇ Ниже — BOOTSTRAP PAYLOAD`. В обычной работе он не нужен:

| Файл | Канон (читать по задаче) | Payload (только при развёртывании) |
|---|---|---|
| Delivery | §0–§14, ~2420 строк | Приложения A–C, ~3750 |
| CQG | §0–§8, ~2960 строк | Приложения A–B, ~7440 |
| OKF | §0–§7, ~800 строк | Приложения A–C, ~773 |

<!-- Числа сверяются МЕХАНИКОЙ (stack-map@1.43): `selftest_sizes.py` меряет файлы
     по баннеру ⬇ BOOTSTRAP PAYLOAD и роняет самопроверку при расхождении свыше
     10%. До этого здесь стояла просьба «пересчитывай здесь же» — правило прозой,
     и оно сгнило: объявления разошлись с фактом в 1.6–5.1 раза, причём про CQG
     канон утверждал две разные цифры сразу. Правило без гейта живёт, пока его
     читают. Пересчитать: python3 stack_selftest.py . -->

Delivery вырос, поэтому читать его целиком уже нерационально даже в «каноне» —
начинай с **§0.3a** (маршрутизация по `kind`): она называет секции под конкретную
задачу.

Читай приложение целиком **только** когда разворачиваешь слой или правишь
конкретный шаблон/скрипт. Тянуть весь файл в контекст ради вопроса «а можно ли
так писать» — не надо.

---

## 4. Куда класть в репо

Скопируй в **корень проекта** рядом с тремя канонами (или сначала только эту
карту + ①). В `AGENTS.md` / Cursor rule достаточно блока:

```markdown
## Canon stack
Start at `AGENT_STACK.md`, then follow links:
Delivery → Code Quality Gates → OKF.
Active work: `delivery/active/STATUS.md` (field `stack:` = deployed canon versions).
Done is closed by a green CI run, never by a local pass (Delivery §10.4).
```

---

## 5. Куда смотреть по пробелам 2026

| Тема | Файл / путь |
|---|---|
| Метрики / cost / rework | [AGENT_DELIVERY_HARNESS.md](AGENT_DELIVERY_HARNESS.md) **§9** (+ A.10) |
| Stop-on-red, protect-main, hooks | Delivery **§10** (+ A.11) |
| CI как неподделываемый слой | Delivery **§10.4** (контракт) + [CODE_QUALITY_GATES.md](CODE_QUALITY_GATES.md) **§8** (workflow, `check_baseline_ratchet.sh`) |
| Версии канонов в проекте | `stack:` в `delivery/active/STATUS.md` и `delivery/CONSTITUTION.md` (Delivery A.1–A.2); шапки каждого канона |
| Worktree / sandbox | Delivery **§5.1** |
| Eval / smoke suite | Delivery **§6**, `delivery/evals/smoke/` |
| Secrets в git, mechanical security | [CODE_QUALITY_GATES.md](CODE_QUALITY_GATES.md) **§2.7** |
| HITL prod / опасные действия | Delivery **§4.3**, **§10.3** |
| Skills / длинные промпты | `skills/README.md` + CQG `inline-prompt`; указатель Delivery **§11** |
| Гейт code↔canon, freshness | [OKF_KNOWLEDGE_BUNDLE.md](OKF_KNOWLEDGE_BUNDLE.md) **Приложение C** (`okf_sync_gate.py`), §7.2 |
| Circuit breakers механически | Delivery **§3.4** → `delivery_check.py --diff-base` |
| Builder ≠ Verifier механически | Delivery **§5.2** + branch protection CQG **§8.5** |
| Branch protection / required checks | CQG **§8.5** |

---

## 6. Приёмка боевого контура

Контур **боевой**, когда у каждого инварианта есть исполнитель, а не абзац.
Прогоняется после развёртывания и после любой правки канонов.

### ⚠ 6.0. Сначала полнота, потом зелёность

**Слепое пятно этой процедуры, вскрытое на первом реальном развёртывании.**
`pre-commit run --all-files` доказывает исправность хуков, **перечисленных** в
конфиге, и молчит о недостающих: невключённый гейт неотличим от проходящего.
Быстрый чек-лист ниже — тоже фиксированный список команд, поэтому забытый гейт
не попадает ни туда, ни сюда, и приёмка честно показывает «7 из 7 зелёных».

Порядок приёмки — **три вопроса, именно в этом порядке** (Delivery §3.1a):

```bash
# 1. Всё ли на месте?
bash scripts/lint/check_gate_coverage.sh   # каждый гейт подключён к pre-commit/CI
python stack_selftest.py                   # payload канонов не сломан
python scripts/delivery_check.py           # артефакты фазы на месте

# 2. Исполнилось ли оно? — смотреть ЛОГ прогона, а не факт «шаг есть в файле»
gh run view --log | grep -c 'gate-coverage\|baseline-ratchet\|diff coverage'
#    Упавший шаг обрывает джобу: гейты ниже не запускаются, и в логе их просто нет

# 3. Зелено ли оно — и ГДЕ зелено?
bash scripts/lint/check_ci_status.sh "$(git rev-parse --abbrev-ref HEAD)"
pre-commit run --all-files
```

Второй вопрос — из второго реального развёртывания: четыре гейта не выполнялись
за всё время, потому что стояли за шагом, который крашился. Приёмка «зелено?» этого
не видит, «подключено?» — тоже.

**Общее правило:** любой чек-лист проверяет то, что в нём написано, и только если
его кто-то исполняет. Сначала «**кто это читает?**», потом «зелено ли оно».
Дыра всегда оказывалась там, где артефакт существовал, но его никто не вызывал и
не парсил.

Пометки в колонке «Кто»: **auto** — прибито скриптом и работает на любом тарифе;
**tune** — требует настройки под проект (§2.A′ пункт 1); **measure** — гейта нет,
исполнитель обязан привести **число** (секунды, минуты, штуки), а не слово
«проверил»; **human** — вне репозитория (§2.A′ пункт 2). Ни одна строка чеклиста
**не требует платного тарифа**: там, где серверный гейт платный, база —
`merge_guard.sh` и `needs:` (CQG §8.5), а защита ветки идёт усилением.

**Чем строка закрывается — правило, а не вкус.** Замер восьмого и девятого прогонов
дал ровно ту поверхность, где контроль держался на честности исполнителя, а не на
механике, и она перечислима:

| Метка | Закрывается | НЕ закрывается |
|---|---|---|
| `auto` | зелёным выводом **постоянного** исполнителя (шаг CI, хук) | «команда есть»; ручным прогоном во время приёмки |
| `tune` | тем же плюс непустое число просмотренных файлов | зелёным гейтом, который ничего не видел |
| `measure` | строкой, содержащей **число** в названных единицах | словами «проверил», «в CI работает», «выглядит нормально» |
| `human` | значением, которое поставил человек | значением, которое агент поставил за человека |

**Числа нет — строка не PASS.** Законный исход тогда — `не измерено (<причина>)` плюс
blocker, и это **честный результат приёмки**, а не её провал: например, «числа
выполненных шагов нет, потому что репозиторий без хостинга и лога прогона не
существует». Чего нельзя — закрывать такую строку как пройденную. Слабое место было
не в том, что четыре строки остались без числа, а в том, что «без числа, потому что
негде взять» ничем не отличалось от «без числа, потому что не стал мерить».

**Самооценка исполнителя закрытием не является нигде.** Единственная строка, которая
так закрывалась (`ci-oracles`), с `cqg@1.34` проверяется гейтом: `check_ci_status.sh`
сверяет заявленное значение со своим наблюдением, и заявление сильнее факта — ошибка.
Отсутствие CI при этом остаётся предупреждением: наказывается ложь, а не бедность.

**Метка обязательна у каждой строки, и это правило, а не оформление.** Три
независимых развёртывания дали 33 места «пришлось догадываться», и двенадцать
строк этого чеклиста были **без метки вовсе** — то есть по ним нельзя было понять,
проверяются они командой или на глаз. Строка без метки закрывается самооценкой, а
самооценка в приёмке контура — это то, против чего контур и построен. Добавляешь
строку — ставь `auto`, `tune`, `measure` или `human`; не можешь выбрать — строка
сформулирована слишком общо, чтобы быть инвариантом.

| Инвариант | Кто исполняет | Проверка |
|---|---|---|
| Spec до широкого кода (M/L) | `delivery_check` (error) · auto | [ ] |
| Human OK на spec / plan | `delivery_check` (error) · auto — но значение пишет человек; нет человека → `deferred` (Delivery §2.2b) | [ ] |
| Форма кода (lint/types/слои) | pre-commit + `pre-commit run --all-files` в CI · **tune** (пути §6 CQG) | [ ] |
| Baseline тает, не растёт | `check_baseline_ratchet.sh` в CI · auto | [ ] |
| Нет секретов в git | detect-secrets/gitleaks (soft-режим запрещён) · auto | [ ] |
| Product oracles на M/L | `delivery_check` (error) + smoke-джоба · auto | [ ] |
| Формат knowledge-bundle | `okf_validate.py` в CI · auto | [ ] |
| Канон не разъехался с кодом | `okf_sync_gate.py` + `implementation:` у concept'ов · auto | [ ] |
| Freshness канона | `okf_sync_gate.py --check-stale` (weekly) · auto | [ ] |
| Circuit breakers (объём поставки) | `delivery_check --diff-base` в CI · auto | [ ] |
| Метрики harness снимаются | `delivery_metrics.py` + гейт на блок в verify-report · auto | [ ] |
| Builder ≠ Verifier | `builder:`/`Verifier:` + `merge_guard.sh` · auto · **human** для review, если тариф даёт | [ ] |
| **Красный не попадает в main** | `merge_guard.sh` (гейты на слитом состоянии) · auto · усиление: required checks, если тариф даёт | [ ] |
| **Красное не уезжает в прод** | `needs: [gates, delivery]` у deploy-джобы · auto (тариф не нужен) | [ ] |
| **Красное на main не остаётся тихим** | workflow `main-guard` открывает issue · auto | [ ] |
| Force-push в main / обход админом | **human** — на бесплатном тарифе закрыть нечем; локально `protect-main` поднимает цену, но не запрещает. Остаток пишется в STATUS, а не замалчивается | [ ] |
| Diff-coverage ≥70% на изменённом | `check_diff_coverage.sh` в CI · **tune** (раннер/пакет §6 CQG) — гейт мерит **только Python**; на диффе без Python он с `cqg@1.32` называет непокрытые файлы вместо зелёного (F9), и покрытие фронта — отдельный шаг | [ ] |
| **Все гейты подключены, а не только исправны** | `check_gate_coverage.sh` (CQG §3) · auto | [ ] |
| **Гейты фактически исполняются** (не за упавшим шагом) | `if: always()` на независимых шагах + `contour_doctor.py` шагом CI (CQG §8.3) · auto — доктор сверяет разложенный workflow со снимком канона и называет расхождение; **до `cqg@2.03` строка закрывалась ручным замером, то есть держалась человеком, читающим лог**, и полевой замер показал цену: развёрнутый `quality.yml` не нёс `if: always()` НИ РАЗУ, две недели, при зелёной приёмке. Названный предел: сверка требует снимка (§7.1, вариант C) и даёт `WEAK`, а не отказ — она отвечает «разложенное разошлось с каноном», а не «шаг сегодня выполнился» | [ ] |
| **`ci-oracles` в STATUS соответствует реальности** | `check_ci_status.sh` в `merge_guard.sh` — **auto только когда прогоны есть**. Нет `gh`, нет авторизации, нет ни одного прогона, репозиторий не на хостинге → гейт выходит 0 с WARNING, и строка закрывается **самооценкой агента**, а не оракулом. Тогда честное значение — `weak`, и оно пишется в STATUS. Проверено на трёх независимых развёртываниях: все три пометили `weak` сами и завели blocker, то есть правило работает — но держится оно на честности исполнителя, а не на гейте | [ ] |
| **Конфиги проекта парсятся** (YAML/JSON/TOML) | `check-yaml` / `check-json` / `check-toml` в pre-commit · auto | [ ] |
| **Сложность функций под ратчетом** | `check_complexity_gate.sh` · auto — иначе правила отключат целиком на легаси | [ ] |
| **Ошибки локализуемы** (сообщения с контекстом) | `check_grep_gate.sh --rule blind-error` · auto; логи и correlation id — ревью (CQG §2.4a) | [ ] |
| **Зависимости без известных уязвимостей** | `check_deps_audit.sh` · auto **+ human, когда фикс semver-major**: гейт красный честно, но обновление с ломающими изменениями — решение человека. Нет человека → не waiver, а `blockers:` + `escalation.md` (Delivery §12.3). Проверено приёмочным прогоном: лестница обновлений дошла до мажорной версии, которая ломает плагин, и pre-push не пустил наружу **собственный bootstrap контура** | [ ] |
| **Список-эндпоинт имеет границу** | `check_ast_gate.py --rule unbounded-list` (CQG §3) · auto — 22-й гейт, лимит поднят решением с записью (CQG §3). Класс T4 lab-12: обе независимые арки написали почти дословно одинаковый `list_all()`, при том что сосед с лимитом лежал в том же приложении. Правило требует не только объявить границу, но и **использовать** её в теле: `limit` в подписи без передачи вниз — самый дешёвый обход, и он проверен подстановкой | [ ] |
| **Тесты что-то утверждают** | `check_mutation_gate.sh` на PR с бюджетом (diff-coverage этого не мерит) · auto **с `cqg@1.54`, и на дефолтной раскладке тоже**: замерено в обе стороны — утверждения на месте → 90% убито, exit 0; все вырезаны → 0%, exit 1, при зелёном pytest (CQG §3.7). До 1.54 на раскладке `backend/` гейт не судил вообще, и эта строка врала `auto`. **Две предпосылки, без них снова `weak`:** `[tool.mutmut] source_paths` в `backend/pyproject.toml` (без неё области нет и гейт молчит) и `brew install coreutils` на macOS — иначе гейт честно пропускается, а до `cqg@1.32` был на ней молчаливым no-op с диагнозом «нет тестов» (F14) | [ ] |
| **Багфикс приносит регрессионный тест** | `repro_test:` в STATUS + `delivery_check` (Delivery §3.1c) · auto | [ ] |
| **История bisect-able** | `MERGE_MODE=squash` в `merge_guard.sh` (CQG §8.8) · auto | [ ] |
| **Предупреждения рантайма = падение теста** | `filterwarnings = ["error"]` + `PYTHONDEVMODE=1` (CQG Прил. B) · auto | [ ] |
| **Квота CI не кончится через две недели** | **measure**: посчитать цену прогона (CQG §8.7) — множитель раннера × округление × число триггеров, и привести минуты/месяц; `timeout-minutes` на каждой джобе | [ ] |
| **Хуки в бюджете** (pre-commit ≤5 c, pre-push ≤10 c) | **measure**: привести секунды замера (CQG §8.6); вышли за бюджет → их начнут обходить | [ ] |
| **`delivery_metrics.py` хоть раз запускался** | **measure**: привести **число метрик** в приложенном блоке вывода (не «запускал» и не сам факт наличия скрипта) — он обязателен на handoff, и сломанный вскроется в худший момент | [ ] |
| **Гейты не только вписаны, но и СУДЯТ** | `contour_doctor.py` (CQG Приложение A) · **measure**: привести **числа** итога — сколько AUTO / WEAK / ABSENT / SKIP / DEAD (строка `contour-doctor:` в конце вывода). `DEAD` обязан быть 0. Инструмент подсовывает каждому гейту канарейку с нарушением его класса и требует красного; `DEAD` = вписан и молчит, то есть контур сообщает о защите, которой нет. Ловит и то, чего не ловит никто: конфиг есть, а `pre-commit install` не сделан | [ ] |
| Payload самих канонов не сломан | `stack_selftest.py` (§7) в шаге `Canon payload selftest` workflow'а (CQG §8.3) · auto — разворачивается CQG §5 шаг 11; каноны вне репо → §7.1, вариант B проверяется по строке `stack-selftest:` в STATUS | [ ] |
| Приёмка и её провалы записаны | `delivery/STACK-ACCEPTANCE.md` (Delivery A.12) · **human** — запись делает исполнитель, гейта нет | [ ] |

Осталось **соглашением, а не гейтом** (сознательно — механически не проверяется):
worktree по умолчанию (§5.1), одна active-поставка (§2.3), `max_implement_retries`
и `max_session_minutes` (§3.4), «ошибка → усиление harness» (§5.4 — метрика
`harness_hardened` его лишь показывает, но не требует).

Быстрая самопроверка одной командой (из корня репо):

```bash
python scripts/delivery_check.py --diff-base origin/main   # фазы + breakers
pre-commit run --all-files                                 # форма кода
bash scripts/lint/check_baseline_ratchet.sh                # снимки только вниз
python scripts/okf_validate.py knowledge/                  # формат канона
python scripts/okf_sync_gate.py --base origin/main         # code <-> canon
bash delivery/evals/smoke/run.sh                           # product oracles
python stack_selftest.py                                   # payload канонов (§7)
BRANCH=feat/your-branch
DRY_RUN=1 bash scripts/merge_guard.sh "$BRANCH"            # гейт мержа (CQG §8.5)
```

Если какая-то строка падает «нет такого файла» — этот слой не развёрнут; отметь
соответствующий `*-oracles: weak` в `STATUS.md`, чтобы дыра была видна, а не забыта.

---

## 7. Самопроверка канонов

Каноны требуют oracles от чужого кода — а его собственный payload (51 скрипт и
конфиги внутри markdown-фенсов) до этого не проверялся ничем. Скрипт ниже
извлекает **все** код-блоки из четырёх канонов и проверяет их: python через
`ast`, bash через `bash -n`, YAML — парсером (или tab-проверкой, если PyYAML нет).
Плюс сверяет версии в шапках канонов с таблицей §1 — расхождение ревизий ловится
механически.

Гоняй **при любой правке канонов** (и в CI репозитория, где каноны живут как
источник):

```bash
python stack_selftest.py            # каноны в текущем каталоге
python stack_selftest.py path/to/canons
```

### Приложение A — `stack_selftest.py`

```python
#!/usr/bin/env python3
"""Self-test for the canon stack: every fenced code block must parse.

Usage:
  python stack_selftest.py [dir]

Проверяет:
  * python-блоки  -> ast.parse
  * bash-блоки    -> bash -n
  * yaml-блоки    -> yaml.safe_load (или запрет табов, если PyYAML нет)
  * json/toml     -> json.loads / tomllib.loads
  * сбалансированность ``` в каждом файле
  * версии в шапках канонов == таблица §1 в AGENT_STACK.md
  * объявленная стоимость чтения == измеренная (selftest_sizes, допуск ±10%)

Exit 0 = всё чисто. Exit 1 = хоть один блок не парсится / версии разошлись.
«Чисто» = чисто ПРОВЕРЕННОЕ: языки без парсера в stdlib (javascript, ini) и проза
(markdown, text) пропускаются — но пропуск ИМЕНУЕТСЯ числом с разбивкой в той же
итоговой строке. Измерено: 159 фенсов, 102 проверялось, 57 уходило МОЛЧА, и «Exit 0
= всё чисто» врало не неумением, а молчанием о нём.
"""

from __future__ import annotations

import ast
import json
import re
import subprocess
import sys
import tomllib
from collections import Counter
from pathlib import Path

import selftest_sizes

CANONS = (
    "AGENT_STACK.md",
    "AGENT_DELIVERY_HARNESS.md",
    "CODE_QUALITY_GATES.md",
    "OKF_KNOWLEDGE_BUNDLE.md",
)
# json/toml добавлены сюда парсерами из stdlib. javascript и ini НЕ добавлены
# намеренно: парсера в stdlib нет, а `node --check` сделал бы сьют зависимым от
# машины. Они уходят в счётчик пропущенного — названы, а не спрятаны.
CHECKED_LANGS = {"python", "bash", "sh", "yaml", "yml", "json", "toml"}


def blocks(text: str) -> tuple[list[tuple[int, str, str]], int]:
    """[(line_no, lang, code)] + число незакрытых фенсов.

    Две правки после приёмочного развёртывания — обе про то, что эта функция была
    ровно тем «инструментом проверки, который не видит своей области», против
    которого написан весь CQG §6:

    * **Отступ.** `line.startswith("```")` не видел фенсы с отступом, а их четыре:
      два в CQG (процедура §5 шаги 4–5) и два в OKF. То есть исполняемые блоки
      процедуры развёртывания не проверялись НИКОГДА. Теперь фенс ищется по
      `lstrip()`, а тело блока сохраняется как есть.
    * **Глубина.** Функция была ТУМБЛЕРОМ, хотя Delivery §7.2 велит извлекать
      «считая глубину, как `blocks()` в stack_selftest.py» — то есть канон указывал
      на контрпример. Измерено: на шаблоне A.1 тумблер даёт 46 строк и **теряет**
      блок `agent-permissions`, счёт глубины — 79 строк и сохраняет его. Ровно тот
      дефект, от которого §7.2 предупреждает своих читателей.

    Вложенное открытие — фенс с info-строкой; закрытие — голой. Незакрытый блок
    остаётся ошибкой: несбалансированность мы хотим видеть, а не угадывать.
    """
    out: list[tuple[int, str, str]] = []
    lang: str | None = None
    depth = 0
    start = 0
    buf: list[str] = []
    for i, raw in enumerate(text.splitlines(), 1):
        line = raw.lstrip()
        if not line.startswith("```"):
            if lang is not None:
                # ⚠ Тело ВЫРАВНИВАЕТСЯ по отступу открывающего фенса, как это
                # делает `block_after` в извлекателе (`cqg@2.10`). До этого
                # `blocks()` хранил строки как есть, а извлекатель — тоже как
                # есть, но фенс с отступом вообще не видел: два поставляемых
                # парсера отвечали по-разному на вопрос «что здесь блок».
                # Выравнивание несущее: heredoc с отступом у терминатора не
                # закрывается, то есть непрогретый блок неисполним.
                buf.append(raw[pad:] if raw[:pad].isspace() else raw)
            continue
        info = line[3:].strip().lower()
        if lang is None:                       # открытие верхнего уровня
            lang, start, buf, depth = info, i, [], 1
            pad = len(raw) - len(line)
            continue
        if info:                               # вложенное открытие
            depth += 1
            buf.append(raw[pad:] if raw[:pad].isspace() else raw)
            continue
        depth -= 1                             # закрытие
        if depth == 0:
            out.append((start, lang, "\n".join(buf)))
            lang = None
        else:
            buf.append(raw[pad:] if raw[:pad].isspace() else raw)
    return out, (1 if lang is not None else 0)


#: Открытие heredoc: `<<TAG`, `<<'TAG'`, `<<"TAG"`, `<<-TAG`.
HEREDOC = re.compile(r"<<(-?)\s*(['\"]?)([A-Za-z_][A-Za-z0-9_]*)\2")


def unclosed_heredoc(code: str) -> str | None:
    """None = чисто. Терминатор heredoc, до которого не дойдёт shell.

    **`bash -n` этот класс НЕ ловит, и это замерено:** блок, где терминатор
    написан с отступом, синтаксически валиден — heredoc просто «доедает» файл
    до конца. То есть проверка печатала «исполняемый блок проверен» про блок,
    который исполниться не может.

    Найдено на собственной правке `cqg@2.09`: генератор провенанса в §5 шага 11
    лежал в нумерованном списке, терминатор `PY` шёл с отступом, `bash -n`
    молчал, и неисполнимость обнаружилась только прогоном блока оракулом.

    Терминатор обязан стоять в НУЛЕВОЙ колонке (для `<<-` допустимы табы —
    так определён сам оператор). Тело блока к этому моменту уже выровнено
    `blocks()` по отступу фенса, поэтому проверка судит то же, что исполнится.
    """
    # ⚠ Комментарии срезаются ДО поиска, и это не аккуратность. Первый же
    # прогон дал ложное срабатывание на строке канона `# пайп в `python -
    # <<heredoc` не работает` — то есть проверка обвинила блок за РАССКАЗ о
    # heredoc. Ложное срабатывание здесь дороже пропуска (§4.3b): такие
    # проверки чинят снятием, а эта только что завелась.
    bare = "\n".join(re.sub(r"(^|\s)#.*$", "", l) for l in code.splitlines())
    for dash, _q, tag in HEREDOC.findall(bare):
        allowed = rf"(?m)^\t*{tag}\s*$" if dash else rf"(?m)^{tag}\s*$"
        if not re.search(allowed, code):
            where = "с отступом" if re.search(rf"(?m)^\s+{tag}\s*$", code) else "отсутствует"
            return (f"heredoc <<{tag}: терминатор {where} — блок не закроется. "
                    "`bash -n` такое принимает, поэтому проверка отдельная")
    return None


try:
    import yaml  # type: ignore

    HAVE_YAML = True
except ImportError:  # pragma: no cover - зависит от машины
    HAVE_YAML = False

# Сколько yaml-блоков прошло УРЕЗАННУЮ проверку (без парсера). Считается, чтобы
# итоговая строка не могла прочитаться как «всё проверено».
yaml_unparsed = 0


def check_yaml(code: str) -> str | None:
    """None = чисто. Без PyYAML проверка УРЕЗАНА, и это обязано быть видно.

    Стоимость молчания измерена на живом прогоне: `entry:` pre-commit-хука получил
    в текст «Установка: pip install …», последовательность `": "` в plain-скаляре
    YAML запрещена, и не парсился ВЕСЬ конфиг — pre-commit не стартовал ни одним
    хуком. Самопроверка при этом печатала «0 failures», потому что PyYAML на машине
    не стоял и ветка молча деградировала до поиска табов. Дефект нашло приёмочное
    развёртывание, а не этот скрипт — то есть скрипт был именно тем «гейтом,
    который ничего не видит», против которого написан весь CQG §6.
    """
    global yaml_unparsed
    if not HAVE_YAML:
        yaml_unparsed += 1
        # Без парсера ловим самое частое: таб в отступе YAML запрещён спекой.
        for n, line in enumerate(code.splitlines(), 1):
            if line[:1] == "\t" or (line.strip() and "\t" in line[: len(line) - len(line.lstrip())]):
                return f"tab indentation at line {n} (YAML forbids tabs)"
        return None
    try:
        # safe_load_all, а не safe_load: блоки-шаблоны frontmatter обрамлены `---`,
        # то есть являются потоком из нескольких YAML-документов, и одиночный
        # safe_load ронял их с ComposerError (ложное срабатывание).
        list(yaml.safe_load_all(code))  # noqa: F821 - есть при HAVE_YAML
    except Exception as exc:  # noqa: BLE001 -- любая ошибка парсера = провал блока
        return f"{type(exc).__name__}: {str(exc).splitlines()[0]}"
    return None


def _sec_key(num: str) -> tuple[int, str] | None:
    """'2.2a' -> (2, 'a'); 'A' -> None. None = не числовая секция, не сравниваем."""
    m = re.fullmatch(r"(\d+)([a-z]*)", num)
    return (int(m.group(1)), m.group(2)) if m else None


def section_order(text: str) -> list[str]:
    """Секции, стоящие НЕ по порядку среди своих соседей.

    Зачем: anchor-правка markdown не знает про нумерацию, и новая секция ставится
    физически не туда — §8.6 перед §8.5.3, §3.1d перед §3.1c, §2.2b перед §2.2a,
    §7.2 перед §7.1. Случалось шесть раз, каждый ловился только глазами.

    Сравниваются ТОЛЬКО числовые соседи с общим родителем: буквенные метки секций
    (`### A. Первый заход`, `### B.`) в одном документе перемешаны с числовыми
    намеренно, и требовать от них порядка — ложное срабатывание.

    Заголовки внутри фенсов игнорируются: шаблоны содержат свои `## …`.
    Фенсы считаются ГЛУБИНОЙ и по `lstrip()` — как в blocks() выше. lab-11:
    blocks() починили по обеим осям (v1.33), а эта функция в том же файле
    осталась тумблером без lstrip — фенс с отступом сдвигал чётность, и
    дальше «внутри»/«снаружи» менялись местами: настоящие секции пропадали
    из проверки, заголовки шаблонов в неё попадали. Брат — не отдельный
    случай: у свойства «видит ли парсер фенс» два потребителя в этом файле,
    и правка обязана накрывать обоих.
    """
    problems: list[str] = []
    last: dict[str, tuple[int, str]] = {}
    depth = 0
    for line in text.splitlines():
        stripped = line.lstrip()
        if stripped.startswith("```"):
            info = stripped[3:].strip()
            if depth == 0:
                depth = 1                      # открытие верхнего уровня
            elif info:
                depth += 1                     # вложенное открытие — с info-строкой
            else:
                depth -= 1                     # закрытие — голым фенсом
            continue
        if depth or not line.startswith("#"):
            continue
        m = re.match(r"#{2,4}\s+(?:⚠️?\s*)?([0-9A-Za-z.]+)\.\s", line)
        if not m:
            continue
        parts = m.group(1).split(".")
        key = _sec_key(parts[-1])
        if key is None:
            continue
        parent = ".".join(parts[:-1])
        prev = last.get(parent)
        if prev is not None and key < prev:
            problems.append(
                f"§{m.group(1)} стоит после §{parent + '.' if parent else ''}"
                f"{prev[0]}{prev[1]}"
            )
        if prev is None or key > prev:
            last[parent] = key
    return problems


def broken_tables(text: str) -> list[str]:
    """Строки таблицы, которые таблицей НЕ отрендерятся.

    Зачем: врезка `> …`, вставленная В СЕРЕДИНУ таблицы, съедает всё, что стоит
    после неё. Строка на `|` сразу за строкой на `>` — ленивое продолжение абзаца
    цитаты (CommonMark), то есть уезжает ВНУТРЬ врезки текстом с трубами. А если
    пустая строка есть, ряды образуют новую таблицу без заголовка — и это тоже не
    таблица. Найдено рендером: врезка про mutation стояла внутри каталога гейтов
    §3, и четыре гейта из шести (baseline-ratchet, new-dependency, ci-status,
    diff-coverage) не были строками таблицы, хотя §5 велит читать каталог
    построчно. Обе половины проверены: до переноса правило срабатывало ровно один
    раз на четыре канона, после — ни разу.

    Фенсы считаются глубиной и по `lstrip()` — как в blocks() и section_order():
    у свойства «видит ли парсер фенс» в этом файле уже три потребителя.
    """
    problems: list[str] = []
    depth = 0
    run: list[tuple[int, str]] = []
    prev = ""

    def flush() -> None:
        # Валидная таблица GFM: строка-заголовок, затем строка-разделитель.
        if len(run) >= 2 and not run[1][1].lstrip().lstrip("|").lstrip().startswith("--"):
            problems.append(f"строка {run[0][0]}: ряды без строки-разделителя")

    for i, line in enumerate(text.splitlines(), 1):
        stripped = line.lstrip()
        if stripped.startswith("```"):
            info = stripped[3:].strip()
            if depth == 0:
                depth = 1
            elif info:
                depth += 1
            else:
                depth -= 1
            prev = line
            continue
        if depth:
            prev = line
            continue
        if stripped.startswith("|"):
            if not run and prev.lstrip().startswith(">"):
                problems.append(f"строка {i}: ряд затянут во врезку `>` (нет пустой строки)")
            run.append((i, line))
        else:
            flush()
            run = []
        prev = line
    flush()
    return problems


def declared_versions(root: Path) -> tuple[dict[str, str], dict[str, str]]:
    """(версии из шапок канонов, версии из таблицы §1 карты)."""
    heads: dict[str, str] = {}
    for name in CANONS:
        p = root / name
        if not p.is_file():
            continue
        # ⚠ ОБЕ формы шапки и дефис в имени (`cqg@2.12`). Здесь стояла только
        # форма `Canon version` и класс `[a-z]+`, то есть карта не читалась
        # дважды: ни по форме шапки, ни по имени `stack-map`. Цена этой слепоты
        # была названа в `field/fleet/build_versions.py` пределом «stack-map в
        # индекс НЕ попадает» — то есть версия карты не мерилась у флота вовсе.
        # Из четырёх реализаций этого понятия расходилась одна; остальные три
        # (`doctor_versions.py`, `tests/extract.py`, реестр классов) обе формы
        # читают. Согласие держит дифференциальный оракул.
        text = p.read_text(encoding="utf-8")
        m = (re.search(r"\*\*Canon version:\*\*\s*`([a-z-]+)@([\d.]+)`", text)
             or re.search(r"\*\*Эта карта:\*\*\s*`([a-z-]+)@([\d.]+)`", text))
        if m:
            heads[m.group(1)] = m.group(2)
    table: dict[str, str] = {}
    smap = root / CANONS[0]
    if smap.is_file():
        for m in re.finditer(r"\|\s*`([a-z]+)@([\d.]+)`\s*\|", smap.read_text(encoding="utf-8")):
            table[m.group(1)] = m.group(2)
    return heads, table


def main() -> int:
    root = Path(sys.argv[1] if len(sys.argv) > 1 else ".").resolve()
    failures: list[str] = []
    total = 0
    skipped: Counter[str] = Counter()

    for name in CANONS:
        path = root / name
        if not path.is_file():
            failures.append(f"{name}: MISSING")
            continue
        text = path.read_text(encoding="utf-8")
        found, unbalanced = blocks(text)
        if unbalanced:
            failures.append(f"{name}: unbalanced ``` fence (unterminated block)")
        checked = 0
        gone: Counter[str] = Counter()
        for line_no, lang, code in found:
            if lang not in CHECKED_LANGS or not code.strip():
                # Здесь стоял голый `continue`, и это была вся находка: 57 фенсов
                # из 159 уходили без следа, а итог печатал только проверенные.
                # Теперь пропуск считается по языку — знаменатель обязан сходиться.
                gone["<empty>" if lang in CHECKED_LANGS else (lang or "<none>")] += 1
                continue
            checked += 1
            total += 1
            err: str | None = None
            if lang == "python":
                try:
                    ast.parse(code)
                except SyntaxError as exc:
                    err = f"SyntaxError: {exc.msg} (block line {exc.lineno})"
            elif lang in {"bash", "sh"}:
                proc = subprocess.run(
                    ["bash", "-n"], input=code, text=True, capture_output=True, check=False
                )
                err = unclosed_heredoc(code)
                if err:
                    pass
                elif proc.returncode:
                    err = proc.stderr.strip().splitlines()[-1] if proc.stderr.strip() else "bash -n failed"
            elif lang in {"json", "toml"}:
                # Оба декодера наследуют ValueError (JSONDecodeError, TOMLDecodeError).
                try:
                    (json.loads if lang == "json" else tomllib.loads)(code)
                except ValueError as exc:
                    err = f"{type(exc).__name__}: {str(exc).splitlines()[0]}"
            else:
                err = check_yaml(code)
            if err:
                failures.append(f"{name}:{line_no} [{lang}] {err}")
        skipped += gone
        for problem in section_order(text):
            failures.append(f"{name}: порядок секций — {problem}")
        for problem in broken_tables(text):
            failures.append(f"{name}: таблица не отрендерится — {problem}")
        print(f"{name}: {checked} executable block(s) checked, {sum(gone.values())} skipped")

    # Объявленная стоимость чтения против измеренной. Числа, по которым агент
    # решает, что НЕ открывать, до stack-map@1.43 расходились с фактом в 4.6 раза
    # и противоречили друг другу — механики, которая бы это ловила, не было.
    sizes: dict[str, tuple[int, int, int]] = {}
    last_sections: dict[str, str] = {}
    for name in CANONS:
        path = root / name
        if not path.is_file():
            continue
        text = path.read_text(encoding="utf-8")
        sizes[name] = selftest_sizes.measure(text)
        last = selftest_sizes.last_section(text)
        if last:
            last_sections[name] = last
        failures += [f"стоимость чтения — {p}" for p in
                     selftest_sizes.check_file(name, text)]
    smap = root / CANONS[0]
    if smap.is_file():
        failures += [f"стоимость чтения — {p}" for p in selftest_sizes.check_map(
            smap.read_text(encoding="utf-8"), sizes, last_sections)]

    heads, table = declared_versions(root)
    for layer, ver in heads.items():
        if layer in table and table[layer] != ver:
            failures.append(
                f"version skew: {layer}@{ver} in canon header vs {layer}@{table[layer]} "
                f"in {CANONS[0]} §1 table"
            )
    missing = sorted(set(table) - set(heads))
    if missing:
        print(f"note: no Canon version header for {', '.join(missing)}")

    # Непокрытость называется в ИТОГОВОЙ строке, а не только в предупреждении выше:
    # именно итоговую строку копируют в отчёт и читают как «всё проверено».
    covered = f"{total} block(s) checked"
    if yaml_unparsed:
        covered += f" (из них {yaml_unparsed} yaml БЕЗ парсера — PyYAML не установлен)"
        print(
            "\n⚠ PyYAML не установлен: yaml-блоки проверены только на табы. "
            "Синтаксис YAML НЕ проверен — поставь PyYAML (pip install PyYAML), "
            "иначе битый конфиг проходит самопроверку молча.",
            file=sys.stderr,
        )
    # Пропущенное стоит в ТОЙ ЖЕ строке, что и проверенное, а не отдельной: в отчёт
    # копируют итоговую строку, и отдельную можно не заметить. Разбивка по языкам
    # называет, ЧЕГО мы не умеем; сумма с проверенным = все верхнеуровневые фенсы.
    lost = ", ".join(f"{lang} {n}" for lang, n
                     in sorted(skipped.items(), key=lambda kv: (-kv[1], kv[0])))
    covered += f", {sum(skipped.values())} skipped" + (f" ({lost})" if lost else "")
    print(f"\nstack_selftest: {covered}, {len(failures)} failure(s)")
    for f in failures:
        print(f"FAIL {f}", file=sys.stderr)
    return 1 if failures else 0


if __name__ == "__main__":
    sys.exit(main())
```

### Приложение A1 — `selftest_sizes.py`

Часть `stack_selftest.py` (`stack-map@1.43`): сверка объявленной стоимости чтения
с измеренной. **Разворачивается вместе с ним** — без этого файла самопроверка не
стартует вовсе (`ModuleNotFoundError`), и это ровно тот класс, который сегодня
ловили у доктора: скрипт разрезали, а копировали поштучно.

```python
#!/usr/bin/env python3
"""Объявленная стоимость чтения сверяется с ИЗМЕРЕННОЙ (`stack-map@1.43`).

**Собственный класс контура, сработавший на самом контуре.** Канон объявляет, во
что обходится чтение — «канон ≈710 строк, payload ≈1320, 65% файла» в CQG §0 и
таблица размеров в карте. На этих числах стоит вся навигация: агент решает по ним,
что читать, а что не открывать. Замер 2026-08-07 (внешняя рецензия + перепроверка):

    объявлено ≈2030 строк · факт 9275 (×4.6)
    баннер payload говорит о себе «строки ~710–2029» · стоит на 2597 (×3.3)
    таблица карты: CQG 3700 · факт 9275 (×2.5); Delivery 3010 · факт 5506 (×1.8)
    OKF 1410 · факт 1467 — единственное точное объявление

Два вывода, и оба неприятные: про CQG канон утверждал ДВЕ разные цифры, и обе
неверны; а рядом с таблицей стоял html-комментарий «при правке канона пересчитывай
здесь же» — то есть правило было записано прозой и ручное. Это ровно то, что канон
говорит про любое правило без гейта: живёт, пока его читают.

Границу «канон / payload» берём у баннера `⬇ BOOTSTRAP PAYLOAD` — он и так
единственный признак раздела, никакого второго источника правды не заводим.

Допуск ±10%: объявление живёт в прозе и округляется, «≈710» против 730 — не ложь.
Расхождение в разы — ложь, и её ловим.
"""

from __future__ import annotations

import re

#: Насколько объявление может отстать от факта, оставаясь честным округлением.
TOLERANCE = 0.10

BANNER = re.compile(r"^>?\s*#*\s*⬇[^\n]*BOOTSTRAP PAYLOAD([^\n]*)", re.M)

#: `(строки ~710–2029, 65% файла)` — баннер описывает САМ СЕБЯ, и это объявление
#: тоже гниёт: до `stack-map@1.43` он говорил «~710», стоя на строке 2597.
BANNER_CLAIM = re.compile(r"строк[аи]?\s*~?(\d+)\s*[–—-]\s*~?(\d+)")
PERCENT = re.compile(r"(\d+)\s*%")

#: Проза §0: «Канон — §1–§8 (≈710 строк)» + «payload (≈1320 строк, 65% файла)».
#:
#: ⚠ Первая редакция требовала у payload'а ФОРМУ CQG — «строк» и процент внутри
#: скобок. Замер: из трёх канонов её держит один. Delivery пишет «payload
#: (≈3350)», OKF — «payload (≈670 строк)», и **оба объявления оракул не находил
#: вовсе**, а оба к тому же врали (3350 против 3750, 670 против 774 — сверх
#: допуска). Проверка молчала не потому, что расхождения нет, а потому что искала
#: одну запись из трёх: класс «правка на форму вместо правки на свойство».
#: Свойство — «в скобках после слова payload стоит число», всё остальное вольно.
PROSE_CANON = re.compile(r"Канон[^\n]*?\(≈?~?(\d+)\s*строк")
PROSE_PAYLOAD = re.compile(r"payload\*{0,2}\s*\(≈?~?(\d+)")

#: Абзац объявления существует. Если он есть и payload есть, а числа нет —
#: сверять нечего, и это тише протухшего объявления: следующая форма записи
#: снова уедет из-под проверки.
PROSE_MARK = re.compile(r"\*\*Стоимость чтения")


def cost_paragraph(text: str) -> str:
    """Абзац «Стоимость чтения» — и только он. Нет абзаца → пустая строка.

    ⚠ Без этой границы вольная регулярка читает ЛЮБУЮ прозу про payload как
    объявление файла о себе: в карте есть фраза «его собственный payload (49
    скрипт и …)», и оракул объявил её протухшим объявлением ×49 — при том что
    карта размеры объявляет таблицей, а не абзацем. Ложное срабатывание тут
    дороже пропуска (§4.3b): по нему снимают проверку целиком.
    """
    m = PROSE_MARK.search(text)
    if not m:
        return ""
    tail = text[m.start():]
    end = re.search(r"\n[ \t]*\n", tail)
    return tail[:end.start()] if end else tail

#: Строка таблицы карты: `| CQG | §0–§8, ~1410 строк | Приложения A–B, ~2290 |`.
TABLE_ROW = re.compile(r"^\|\s*(Delivery|CQG|OKF)\s*\|[^|]*?~?(\d+)\s*строк[^|]*\|"
                       r"[^|]*?~?(\d+)\s*\|", re.M)

#: Ярлык слоёв в той же строке: `§0–§12` обязан покрывать последнюю секцию файла.
TABLE_LABEL = re.compile(r"^\|\s*(Delivery|CQG|OKF)\s*\|\s*§\d+[–—-]§(\d+)", re.M)

LAYER_FILE = {"Delivery": "AGENT_DELIVERY_HARNESS.md",
              "CQG": "CODE_QUALITY_GATES.md",
              "OKF": "OKF_KNOWLEDGE_BUNDLE.md"}


def outside_fences(text: str) -> str:
    """Текст без содержимого блоков; строки сохраняются, чтобы номера не поехали.

    ⚠ Иначе оракул читает ПРИМЕР из блока и наказывает за документирование
    собственного правила: этот файл поставляется payload'ом карты, а в его
    докстринге стоят числа находки («≈710 строк, ≈1320, 65%») — и проверка
    объявила их объявлением самой карты. Класс, ради которого в сьюте живёт
    `extract.code_only()`, и третий его рецидив за день (`cqg@1.74`, `1.80`).
    """
    out, depth = [], 0
    for line in text.splitlines():
        if line.lstrip().startswith("```"):
            info = line.lstrip()[3:].strip()
            depth += 1 if (info and depth == 0) else (-1 if not info else 0)
            depth = max(depth, 0)
            out.append("")
            continue
        out.append("" if depth else line)
    return "\n".join(out)


def last_section(text: str) -> str | None:
    """Номер последней секции документа — ТОЛЬКО вне фенсов.

    Наивная регулярка читает `## 99.` из шаблона внутри блока и объявляет, что
    ярлык «§0–§13» не покрывает §99. Это третий потребитель свойства «видит ли
    парсер фенс» в этом файле — два других (`blocks`, `section_order`) уже чинили
    по обеим осям (lab-11). Поймано собственным тестом сьюта, а не полем.
    """
    depth, best = 0, None
    for line in text.splitlines():
        if line.lstrip().startswith("```"):
            info = line.lstrip()[3:].strip()
            depth += 1 if (info and depth == 0) else (-1 if not info else 0)
            depth = max(depth, 0)
            continue
        if depth:
            continue
        m = re.match(r"#{2,3}\s+(\d+)\.", line)
        if m and (best is None or int(m.group(1)) > int(best)):
            best = m.group(1)
    return best


def measure(text: str) -> tuple[int, int, int]:
    """→ (всего строк, строк канона, строк payload). Нет баннера → payload 0."""
    lines = text.splitlines()
    m = BANNER.search(text)
    if not m:
        return len(lines), len(lines), 0
    at = text[:m.start()].count("\n") + 1
    return len(lines), at - 1, len(lines) - at + 1


def _off(declared: int, actual: int) -> bool:
    """Разошлось ли объявление с фактом больше допуска."""
    if actual <= 0:
        return declared > 0
    return abs(declared - actual) / actual > TOLERANCE


def _say(where: str, what: str, declared, actual) -> str:
    ratio = f", ×{max(declared, actual) / max(1, min(declared, actual)):.1f}"
    return (f"{where}: объявлено {what} {declared}, измерено {actual}{ratio} — "
            "стоимость чтения врёт, а по ней решают, что не открывать")


def check_file(name: str, text: str) -> list[str]:
    """Объявления ФАЙЛА о себе: баннер и проза «Стоимость чтения»."""
    out: list[str] = []
    total, canon, payload = measure(text)
    text = outside_fences(text)   # примеры внутри блоков — не объявления файла
    m = BANNER.search(text)
    if m:
        at = text[:m.start()].count("\n") + 1
        claim = BANNER_CLAIM.search(m.group(1))
        if claim and _off(int(claim.group(1)), at):
            out.append(_say(f"{name} баннер payload", "начало payload",
                            int(claim.group(1)), at))
        pct = PERCENT.search(m.group(1))
        if pct and total and _off(int(pct.group(1)), round(payload * 100 / total)):
            out.append(_say(f"{name} баннер payload", "долю payload, %",
                            int(pct.group(1)), round(payload * 100 / total)))
    para = cost_paragraph(text)   # объявления файла о себе живут ЗДЕСЬ
    prose_c = PROSE_CANON.search(para)
    if prose_c and _off(int(prose_c.group(1)), canon):
        out.append(_say(f"{name} §0 «Стоимость чтения»", "канон",
                        int(prose_c.group(1)), canon))
    prose_p = PROSE_PAYLOAD.search(para)
    if prose_p and _off(int(prose_p.group(1)), payload):
        out.append(_say(f"{name} §0 «Стоимость чтения»", "payload",
                        int(prose_p.group(1)), payload))
    elif prose_p is None and payload and para:
        out.append(f"{name} §0 «Стоимость чтения»: размер payload не объявлен "
                   "числом при живом payload — сверять нечего, и молчание тише "
                   "протухшего объявления")
    return out


def check_map(map_text: str, sizes: dict[str, tuple[int, int, int]],
              last_sections: dict[str, str]) -> list[str]:
    """Таблица размеров в карте: числа против факта, ярлык против последней секции."""
    out: list[str] = []
    for layer, canon_s, payload_s in TABLE_ROW.findall(map_text):
        name = LAYER_FILE.get(layer)
        if name not in sizes:
            continue
        _, canon, payload = sizes[name]
        if _off(int(canon_s), canon):
            out.append(_say(f"карта, строка {layer}", "канон", int(canon_s), canon))
        if _off(int(payload_s), payload):
            out.append(_say(f"карта, строка {layer}", "payload",
                            int(payload_s), payload))
    for layer, upto in TABLE_LABEL.findall(map_text):
        name = LAYER_FILE.get(layer)
        last = last_sections.get(name)
        if last and int(upto) < int(last):
            out.append(f"карта, строка {layer}: ярлык §…–§{upto} не покрывает §{last} — "
                       "раздел есть, а в бюджет чтения не входит")
    return out
```

Не заменяет функциональные прогоны гейтов — ловит то, что дешевле всего сломать
при правке markdown: сломанный синтаксис в фенсе, оборванный блок, разъехавшиеся
версии, **порядок секций**, **таблицы, которые не отрендерятся**.

**Про таблицы.** Врезка `> …`, вставленная в СЕРЕДИНУ таблицы, съедает всё, что
стоит после неё: строка на `|` сразу за строкой на `>` — ленивое продолжение абзаца
цитаты, то есть уезжает внутрь врезки текстом с трубами. Найдено рендером в CQG §3:
врезка про `mutation` стояла посреди каталога гейтов, и **четыре гейта из шести**
(`baseline-ratchet`, `new-dependency`, `ci-status`, `diff-coverage`) строками
таблицы не были — при том что §5 CQG велит читать этот каталог построчно.
Проверяются обе формы, и вторая неочевидна: если пустую строку поставить, ленивого
продолжения нет, но ряды образуют таблицу **без заголовка** и таблицей всё равно не
рендерятся. Иначе «починка» переносом на пустую строку выглядела бы починкой.

**Про порядок секций.** Anchor-правка markdown не знает про нумерацию, и новая
секция встаёт физически не туда: §8.6 перед §8.5.3, §3.1d перед §3.1c, §2.2b перед
§2.2a, §7.2 перед §7.1 — **шесть случаев**, каждый находился только глазами и
только если посмотреть. Теперь это провал самопроверки. Сравниваются лишь числовые
соседи с общим родителем: буквенные метки (`### A. Первый заход`, `### B.`)
намеренно перемешаны с числовыми, и требовать от них порядка было бы ложным
срабатыванием. Заголовки внутри фенсов игнорируются — шаблоны содержат свои `## …`.

### 7.1. ⚠ Если каноны лежат ВНЕ репозитория

§4 по умолчанию исходит из того, что четыре `*.md` лежат в корне проекта и
коммитятся. Бывает иначе — человек держит их в отдельном каталоге (`~/Prepare/`)
и не коммитит в проект. Тогда **в CI их физически нет, и проверять нечего**: этот
инвариант остаётся непокрытым, и приёмка §6 об этом не узнает.

Так тоже можно, но это надо **назвать вслух** (§2.A′), а не оставить молча:

| Вариант | Что делать | Строка в STATUS |
|---|---|---|
| **A. Каноны в репо** (дефолт) | Коммитить в корень; `stack_selftest.py` — шаг CI | `stack-selftest: ci` |
| **B. Каноны вне репо** | Гонять `stack_selftest.py` **там, где они лежат**, вручную при каждой правке; в `delivery/STACK-ACCEPTANCE.md` записать путь и то, что CI их не видит | `stack-selftest: external (<путь>)` |
| **C. Vendored-копия** | Коммитить снимок канонов в `docs/canon/` + версии в `stack:`; CI проверяет копию | `stack-selftest: ci (vendored)` |

Выбор **B без записи в STATUS** — это то же самое, что неподключённый гейт:
инвариант выглядит покрытым, а его нет.

**Дефолт — C, и выбирать его агент обязан сам, не спрашивая.** Все три арки пятого
развёртывания споткнулись на этой развилке: канон перечислял варианты и не говорил,
какой брать, поэтому каждая выбирала заново и одна из них через это же пробила
breaker (снимок канонов — 10 000 строк в дифф bootstrap'а). Почему именно C: при
A каноны попадают в продуктовый репозиторий целиком и живут двумя копиями с
upstream; при B инвариант непокрыт по построению — CI не видит файлов. C даёт
покрытие в CI и не делает проект владельцем канонов.

Один нюанс, который стоил находки: снимок `docs/canon/` — это **механика контура**,
а не продуктовый код, поэтому он обязан быть в `BREAKER_EXCLUDE` (Delivery §3.4):
без этого снимок даёт 10 743 строки из 13 743 пробитого лимита breaker'а.
Гейт длины трогать не нужно — его дефолтная маска (`LINT_LENGTH_GLOBS="*.py *.ts
*.tsx"`) `*.md` не видит; расширил маску на документацию — тогда `docs/canon/` идёт
в `exclude_contour()` (CQG `check_file_length.sh`). До `stack-map@1.35` здесь стояло
«обязан быть в исключениях breaker'а **и гейта длины**» без этой развилки, и
инструкция была невыполнима: в `exclude_contour()` такого пути нет и по дефолту не
нужен.

**Минус варианта C, названный вслух.** У A минус назван («каноны живут двумя копиями
с upstream»), и у C он **тот же** — снимок расходится с источником ровно так же. До
`stack-map@1.36` здесь этого не было, и получалось, что дефолт продаётся без своей
цены. Хуже: `stack_selftest.py`, запущенный по снимку, ловит **ошибки правки
канонов**, а правятся каноны upstream, куда CI проекта не смотрит никогда. Внутри
проекта шаг краснеет ровно в одном случае — если снимок испортили руками.

Полевой прогон lab-9 проверил пять механизмов на «а этот заметит расхождение?» — не
заметит ни один: шаг читает только `docs/canon/`; `declared_versions()` сверяет шапки
внутри снимка с таблицей §1 **того же снимка**, то есть сам с собой; `okf_sync_gate`
смотрит `knowledge/`; мета-гейт снимок за гейт не считает. Поэтому в шаг §8.3
добавлена **сверка `stack:` из STATUS с шапками снимка**: она не заменяет upstream‑CI,
но ловит расхождение записи и снимка — единственное, что видно изнутри проекта.

Практическое правило до конца остаётся дисциплиной: **обновил каноны — пере-вендори
снимок и обнови `stack:`**. Механически проверяется вторая половина, не первая.

**Снимок обязан называть, ОТКУДА он** (`stack-map@1.48`). Рядом со снимком —
`docs/canon/VENDORED.json`: `upstream` (адрес источника), `commit`, `vendored_at`,
`versions`, `sha256` по каждому файлу снимка.

Хеши сами по себе доказывают не то, что кажется: «копию не правили ПОСЛЕ
вендоринга» — и молчат о том, **из чего вендорили**. Снимок, снятый с грязной
рабочей копии, от снятого с коммита неотличим. А без `upstream` вопрос «отстал ли
я» не имеет адресата вовсе: замер lab-9 выше показал, что расхождение снимка с
источником не видит ни один из пяти механизмов, и все пять читают только снимок.
Запись даёт точку истории, с которой есть что сравнивать.

⚠ **Названный предел:** она делает расхождение АДРЕСУЕМЫМ, а не обнаруживаемым.
Сравнить может тот, у кого есть доступ к источнику — обход флота или машина
владельца; CI проекта без токена к приватному upstream не ходит и после этой
правки. Это меньше, чем хотелось бы, и больше, чем было.

**Кто исполняет этот выбор.** Вариант C — не рекомендация в прозе, а два конкретных
шага: **CQG §5 шаг 11** (снимок + `stack_selftest.py` рядом с четырьмя `*.md` +
`BREAKER_EXCLUDE` + строка в STATUS) и **шаг `Canon payload selftest` в workflow
§8.3** — он и закрывает строку приёмки §6. Вариант B этот же шаг проверяет по
наличию `stack-selftest:` в STATUS и без неё падает, поэтому «молча выбрать B»
больше нельзя.

Рядом с канонами едут **три** файла, и все три отвечают на разные вопросы:
`stack_selftest.py` — «payload канонов не сломан», `selftest_sizes.py` —
«объявленная стоимость чтения равна измеренной», `extract_payload.py`
(`cqg@2.08`) — «достать payload и сказать, ВСЁ ли достал» (`--manifest`).
Третий появился после полевого замера: чужой извлекатель нашёл 73 файла из 74.

До `cqg@1.31` ни одного из этих шагов не было: §7.1 объявляла дефолт, §6 помечала
строку `auto`, а вызывался скрипт только из чеклиста самой приёмки — то есть
исполнителем был человек, который её читает. Восьмое развёртывание назвало это F7.
Урок общий: **`auto` — это про постоянного исполнителя (шаг CI, хук), а не про
существование команды.** Проверяется тестом `test_acceptance_checklist.py`.

---

*Карта канонов. Детали — только в связанных файлах выше.*
### 7.2. Второй слой: регрессия на ПОВЕДЕНИЕ (`tests/`)

`stack_selftest.py` отвечает на вопрос «payload **парсится**». На вопрос «payload
**работает**» он не отвечает, и это оказалось дорого: за одну сессию доработок
**шесть дефектов было внесено самими правками канона**, и каждый нашёлся не первым
прогоном («сломал → упало»), а обратным («сделал верно → обязано пройти»). Прогоны
при этом были одноразовыми — повторить их было нельзя, значит следующая правка
ломала молча.

Второй слой — фикстурный сьют в `tests/` репозитория канонов:

```bash
python3 tests/run.py          # локально
```

| Файл | Что держит |
|---|---|
| `tests/extract.py` | Извлечение payload'а **с учётом вложенных фенсов** + полный инвентарь: 51 скрипт, 15 шаблонов и 9 конфигов. Наивная регулярка теряла блок `agent-permissions` из A.1 |
| `tests/harness.py` | Одноразовый git-полигон: положить скрипты, собрать состояние, прогнать, сверить exit-код и подстроку |
| `tests/test_delivery_check.py` | Фазовый гейт: idle-состояние, `deferred`, журналы §12, наблюдение §13, права §4.5/§4.6, индекс архива |
| `tests/test_cqg_gates.py` | Ратчет и мета-гейты: снимок только вниз, подключение **правил**, новая зависимость, область и маска гейта длины |
| `tests/test_remaining_gates.py` | AST-ратчеты, гейт code↔canon (вкл. waiver из STATUS), гейт мержа (squash по результату), `deps-audit` на чужом стеке |
| `tests/test_selftest.py` | Сама самопроверка: порядок секций, version skew, оборванный фенс, битый python/bash, сверка payload'а с файлом в корне |
| `tests/test_uncovered_scripts.py` | Семь скриптов, которых сьют не держал: метрики (интервал по `**жирной**` разметке STATUS, MANUAL-поля не выдумываются, шов с `delivery_check` на handoff), `okf_validate`, `ci-status` (зелёный прогон ДРУГОГО SHA — красный), `gate_value`, и поведение mutation/jscpd/eslint **без инструмента** — через PATH из одних системных каталогов и подделки `gh`/`jscpd`/`eslint` |
| `tests/test_gate_class_invariants.py` | **Оракул на класс, а не на случай:** гоняет всех path-зависимых гейтов сразу — никто не роняет сырую ошибку шелла, отсутствие цели названо словами, успешный путь печатает число просмотренных файлов. Написан потому, что один и тот же класс чинился у одного гейта и не переносился на братьев — четыре раза |
| `tests/test_breaker_scope.py` | §3.4: breaker меряет продукт, а не контур — развёртывание (1527 строк механики) проходит, 900 строк продуктового кода пробивают |
| `tests/test_deployment_procedure.py` | **Сама процедура развёртывания:** таблица шага 1 Delivery §7.2 сверяется с деревом §2.3, каждая её строка разворачивается в одноразовый репозиторий, дальше приёмка — `delivery_check`, `.gitkeep` под git, доктор. До неё процедуру проверял только живой полигон, а полигоны одноразовые: следующий заход повторял уже закрытые находки. Первая же редакция нашла три файла дерева, выпавших из инвентаря |
| `tests/test_payload_clean_for_scanners.py` | Канон не поставляет payload, который метит его же secrets-гейт: настоящий `detect-secrets` по четырём `*.md` и всему извлечённому payload'у, порог ноль (CQG §2.7) |

**Дисциплина, зашитая в сьют — три прогона на проверку:** сломано → падает,
**незаполненный шаблон → падает**, верно → проходит. Третий обязателен: именно им
находились мёртвые гейты, которые на первом прогоне выглядели рабочими.

Сьют **прибит в CI** отдельным шагом рядом с самопроверкой. Первый же его прогон
нашёл дыру, которую четыре полевых развёртывания пропустили: при **пустом снимке в
базе** `check_baseline_ratchet` был полным no-op — идиома `NR == FNR` в awk истинна
для каждой строки второго файла, если первый пуст, поэтому все новые записи уходили
в массив вместо сравнения. Проект, стартовавший чистым, мог легализовать любое
число нарушений молча.

**Третий случай, и он про сам этот скрипт.** `check_yaml` без PyYAML молча
деградировал до поиска табов и возвращал «чисто», а итоговая строка печатала
`58 block(s), 0 failure(s)` — то есть самопроверка канонов была ровно тем «гейтом,
который ничего не видит», против которого написан весь CQG §6. Цена измерена: правка
`entry:` в pre-commit-конфиге сделала весь файл непарсящимся (`": "` в plain-скаляре),
контур не запускался ни одним хуком, локальный прогон печатал «0 failures», и дефект
нашло приёмочное развёртывание, поднявшее venv с настоящим парсером. Теперь
непокрытость стоит **в итоговой строке**: `58 block(s) (из них 11 yaml БЕЗ парсера —
PyYAML не установлен)`. CI репозитория PyYAML ставит, поэтому там проверка полная —
дыра была именно в локальном прогоне, который я предъявлял как доказательство.

Так вышло и во второй раз, когда сьют дописали до **всех двадцати** скриптов:
`okf_validate.py` принимал **пустой `type:`**, забирая значением следующую строку
frontmatter'а (`\s` в регулярке включает перевод строки), и печатал `0 error(s)`
там, где SPEC §11 требует непустой тип. Пять полевых развёртываний прошли мимо —
валидный bundle такого файла просто не содержит, а тест содержит. Правило из этого:
**полигон проверяет то, что случается; регрессия — то, что может случиться.**

## `stack-map@1.43` — объявленная стоимость чтения сверяется с измеренной

**Собственный класс контура, сработавший на самом контуре.** По числам «стоимость
чтения» агент решает, что НЕ открывать. Замер 2026-08-07 (внешняя рецензия +
перепроверка): объявлено ≈2030 строк — факт 9275 (**×4.6**); баннер payload
говорил о себе «строки ~710–2029», стоя на 2597; таблица выше утверждала третье
(3700). Про CQG канон держал **две разные цифры сразу**, и обе неверны. Точным
оказалось одно объявление — OKF, — значит дело не в подходе, а в отсутствии
проверки: рядом с таблицей стояла просьба «при правке пересчитывай здесь же», то
есть правило прозой. Ровно то, что канон говорит про любое правило без гейта.

Теперь это механика: `selftest_sizes.py` меряет файлы по баннеру
`⬇ BOOTSTRAP PAYLOAD`, сверяет с объявленным в трёх местах (проза §0, сам баннер,
таблица выше) и роняет самопроверку при расхождении свыше **10%**. Допуск не
украшение: «≈710» против 730 — честное округление, а оракул, краснеющий на нём,
снимают первым (§4.3b). Заодно ловится ярлык, не покрывающий последнюю секцию:
`§0–§12` при живой §13 — раздел есть, а в бюджет чтения не входит.

**Три дефекта самой правки, все пойманы сьютом, а не чтением.** ① Стенд копировал
`stack_selftest.py` поштучно, и разрезанный скрипт падал `ModuleNotFoundError` —
тот же класс, что у доктора в `cqg@1.83`. ② Замер последней секции читал заголовки
внутри фенсов; это **третий** потребитель свойства «видит ли парсер фенс» в одном
файле, и два других чинили ещё в lab-11. ③ Сам оракул прочитал ПРИМЕР из своего же
докстринга (числа находки в блоке payload) и объявил его объявлением карты — класс
`extract.code_only()`, третий рецидив за день.

## `stack-map@1.44` — маршрут чтения один, и оглавление сверяется

Наряд (внешняя рецензия) требовал слить `§0.3`, `§0.3a` и `§3`: «три
перекрывающихся указателя расходятся первыми». **Замер поправил посылку:** они
отвечают на разные вопросы и уже не дублируются — оглавление, маршрут по `kind`
и чеклист сессии, причём последний ссылается на маршрут, а не повторяет его.
Оглавление тоже оказалось полным: все тринадцать секций на месте.

Поэтому сделано не слияние, а **удержание**: `tests/test_reading_routes.py`
роняет сьют, если в карте появится вторая таблица по `kind`, если чеклист
перестанет ссылаться на §0.3a или если оглавление разойдётся с секциями.

⚠ **Ловушка, найденная по дороге:** `extract.code_only()` срезает строки,
начинающиеся с `#`, — на markdown это ВСЕ заголовки. Первый заход насчитал ноль
секций и объявил бы оглавление полным при любом его содержании. Хелпер написан
для кода; для разметки нужен `selftest_sizes.outside_fences`, убирающий только
тела блоков. Класс тот же, что у «проверки, которая молчит, потому что не видит»,
и это третий его случай за два дня.

## `stack-map@1.45` — объявление payload'а сверяется в любой форме записи

**Собственная проверка была слепа на двух файлах из трёх, и оба в этой слепой
зоне врали.** `stack-map@1.43` научил оракул сверять объявленную стоимость чтения
с измеренной — но регулярка требовала ФОРМУ CQG: слово «строк» и процент внутри
скобок. Замер 2026-08-10: Delivery пишет `payload (≈3350)`, OKF —
`payload (≈670 строк)`, и **ни одно из этих объявлений оракул не находил вовсе**.
Факт при этом был 3750 и 774 — расхождение сверх допуска в обоих случаях.

Это класс «правка на форму вместо правки на свойство», и он в контуре уже
назван — только теперь сработал на самом механизме, который вводили против
протухших объявлений. Правило прежнее: пока признак взят под одну запись, вторая
запись живёт без присмотра.

Сделано:

- **свойство вместо формы:** число берётся из скобок после слова `payload`, всё
  остальное вольно — «строк», процент, порядок слов;
- **молчание названо:** абзац «Стоимость чтения» есть, payload есть, а числа про
  payload нет — это отдельное замечание, потому что тишина тише протухшего
  объявления: следующая форма записи снова уехала бы из-под проверки;
- **граница поиска — абзац объявления, а не весь файл.** Первая редакция читала
  ЛЮБУЮ прозу про payload и обвинила саму карту: фраза «его собственный payload
  (N скрипт и …)» дала расхождение ×49 при том, что карта объявляет размеры
  таблицей. Ложное срабатывание тут дороже пропуска (§4.3b) — по нему снимают
  проверку целиком, вместе с защитой от расхождения в разы;
- **два объявления исправлены числом по факту** (Delivery, OKF), таблица карты
  обновлена по замеру.

Три прогона на правку: верно → проходит; строгая регулярка → падают ровно пробы
на формы и на живые каноны; проза вне абзаца → молчит. **И обратный прогон нашёл
слабость в самой пробе:** утверждение «в сообщении есть слово payload» проходило
через ветку «не объявлен» — то есть проба зеленела, не найдя ни одного
объявления. Ужесточено до сверки «объявлено payload N, измерено M». Класс
«оракул проверяет имя, а не свойство», третий его случай в контуре.

## `stack-map@1.46` — пятая ось встала в маршрут чтения

Delivery §14 (поверхность поведения модели) — новая несущая секция, и всё, что о
документе объявлено, обязано это знать: ярлык слоёв `§0–§13` стал `§0–§14`,
оглавление §0.3 получило строку про §14, размер канона в таблице пересчитан
(≈2230 → ≈2420), объявление §0 и баннер payload — по замеру.

**Проверку сделал оракул, а не внимательность.** Ровно та механика, которую
починили в `stack-map@1.45`, назвала уехавший баннер сама: «объявлено начало
payload 2150, измерено 2418». Это первый случай, когда объявление стоимости чтения
поймало собственную правку канона в момент внесения, — то есть механизм работает
не только на исторических расхождениях. Ярлык слоёв держит отдельная проба
(`§0–§12` при живой §13 — уже была), поэтому забыть про него тоже нельзя.

## `stack-map@1.47` — показываемый срез и его проверяемость

Наряд II.8 просил артефакт на английском: полный контур на интервью не читают —
и вывод делают не «мощно», а «не умеет сжимать», то есть прямо против собственного
принципа бюджета чтения. Срез — `CONTOUR-EN.md`, одна страница: проблема →
принцип → пять осей, таблица enforced vs advisory, одна история отказа целиком,
кривая по лабам, бюджет гейтов и правило про единицу счёта.

**Главное в нём не текст, а то, что его числа сверяются механикой**
(`tests/test_showable_slice.py`, восемь проб). Показываемая страница — первый
кандидат на протухание: её числа перестают пересчитывать в день написания, а
расходятся они молча, потому что документ не читает ни один гейт. Этот класс на
контуре срабатывал дважды (`stack-map@1.43` и `1.45`), поэтому здесь он закрыт
сразу: раскладка правил, бюджет гейтов, вес контура, кривая и классы, порог DoD с
текущим положением, число тестов и число исполняемых блоков — всё берётся **из тех
же источников, что у остального сьюта**, а не из второй копии чисел.

⚠ **Два дефекта собственной пробы, оба поймал прогон, а не глаз.** ① Раскладку
enforced/advisory я сначала считал по сырым строкам таблицы вместо
классификатора — вышло `[0, 0, 0]` против `[16, 1, 6]`, то есть проба зеленела бы
на любой странице, если бы утверждала «числа есть». ② Блоки посчитал по ВСЕМ
фенсам — 153 против 96: прогон судит только исполняемые языки, и мой счёт был
вторым источником истины, из-за которого страница стала бы честной по отношению к
выдуманному числу. Оба лечения — переиспользовать существующий предикат, а не
писать свой.

Число тестов на странице тоже под пробой, и первым же прогоном она поймала мою
ошибку на единицу: я поставил 645, ожидая, что новая проверка добавит тест, — а
она добавилась внутрь существующего.

## `stack-map@1.48` — снимок называет, откуда он

Вариант C держался на дисциплине наполовину: «пере-вендори снимок» проверялось
только изнутри проекта, а изнутри снимок сверяется сам с собой — замер lab-9 выше
назвал это прямо, и пять механизмов из пяти расхождение с источником не видят.

Дыру нашёл обход флота: у одного развёрнутого проекта `VENDORED.md` записывал
upstream как **«рабочая копия человека, не коммитится»**, то есть вне одной машины
вопрос «отстал ли я» не имел адресата вообще. Второй проект не имел даже
`SHA256SUMS` — целостность его снимка не проверяется ничем. При этом отставание
там оказалось **−18 ревизий `delivery`**, и не заметил этого никто.

Введён `docs/canon/VENDORED.json` с пятью полями. Существенны два: `upstream` даёт
адрес источника, `commit` — точку истории. Хеши без них доказывают только «копию не
правили после вендоринга» и молчат о том, из чего вендорили: снимок с грязной
рабочей копии неотличим от снимка с коммита.

Предел назван в тексте и не спрятан: запись делает расхождение **адресуемым, а не
обнаруживаемым** — сравнить может тот, у кого есть доступ к источнику. Проверяет
запись не сам проект, а обход флота: канон объявляет, флот сверяет. Оракул —
`tests/test_fleet_preflight.py`, ветка «провенанс не объявлен».

**веса эта правка не добавила** — тронута проза §7.1, а ратчет веса считает
`scripts/**`; названо, чтобы отсутствие цены не читалось как забытая запись.
**объём: +42 строк, за что** — требование к снимку с названным пределом и эта запись.

## `stack-map@1.51` — извлекатель научился называть владельца

Скрининг состава (`cqg@2.25`) спрашивает у извлекателя, что вообще должно лежать в
проекте. Ответ «список путей» для этого недостаточен: снимок в проекте несёт ВСЕ
четыре канона и там, где развёрнуто три (вариант A/B), поэтому судящий полноту
обязан выбросить payload канона, объявленного `@absent`. Иначе он обвинит законную
раскладку, а ложное срабатывание снимают вместе с проверкой (§4.3b).

`--manifest --by-canon` печатает `канон<TAB>путь`. **Отдельный флаг, а не смена
формата `--manifest`:** его вывод стоит в рецепте сверки в шапке извлекателя и в
§5.0, и менять его значило бы сломать чужие однострочники ради своего потребителя.

Заодно **владелец шаблонов сведён в одну константу** `TEMPLATES_CANON`: и
`template()`, и новый флаг отвечают на «из какого канона они читаются», а два
литерала разъехались бы молча — `one-notion-one-place` из реестра классов.

⚠ **Датированный замер в прозе перестал читаться как действующее утверждение.**
Сверка чисел прозы с payload'ом не отличает историю от заявления: запись «замер на
2026-08-07» совпадала с фактом ровно пока то число было текущим, и покраснела на
первом же новом скрипте. Число переставлено ЗА слово; первая редакция врезки про
это процитировала запрещённую форму дословно и покраснела сама.

**вес: считается в `cqg@2.25`** — извлекатель едет рядом с канонами и попадает в
ратчет веса контура (§9.1a п.5, ре-базировка `delivery@1.73`).
**объём: считается в `cqg@2.25`** — общий бюджет прозы на четыре канона один.
**класс:** none reason=правка вводит механику для скрининга состава (`cqg@2.25`), а
дефекта не чинит: перестановка числа в датированном замере — следствие СВОЕГО же
роста, форма совпадала с фактом ровно пока то число было текущим, и никого до
сегодня не обманула. Закрытый класс записан там, где закрыт, и второй раз не
считается.

## `stack-map@1.50` — самопроверка молчала о трети своих блоков

`Exit 0 = всё чисто` было неправдой не потому, что проверка чего-то не умеет, а
потому, что она об этом **молчала**. Замер: верхнеуровневых фенсов **159**,
проверялось 102, пропускалось **57 голым `continue`** — без счётчика, без строки в
отчёте. Среди пропущенных девять машинночитаемых, и два из них — поставляемые в
проекты конфиги `<frontend>/eslint.config.js` и `.dependency-cruiser.cjs`:
сломанная запятая уезжала в чужой проект незамеченной, и **проверяльщика для этих
блоков в репозитории не было ни одного**.

Стало: **108 проверено, 51 пропущено**, оба числа в ОДНОЙ итоговой строке с
разбивкой по языкам (`markdown 35, text 11, <none> 2, javascript 2, ini 1`).
Знаменатель сходится: 108 + 51 = 159, и это отдельный прогон — счётчик, не
покрывающий популяцию, врал бы ровно так же, как молчание.

Научилась `json` и `toml` (обе стандартной библиотекой). `javascript` и `ini`
осознанно НЕ парсятся: в stdlib парсера нет, а `node --check` сделал бы сьют
зависимым от того, что установлено на машине. Они названы в счётчике — это и есть
честный ответ: чего не умеем, то названо, а не спрятано.

⚠ Класс — `list-without-its-principle`: `CHECKED_LANGS` был списком без принципа,
и язык, не похожий на прежние записи, проваливался молча. Обратный прогон: **5 из
5 новых прогонов краснеют** на прежнем коде. Чётность тела `stack_selftest.py` с
приложением A проверена дословным прогоном шага CI.

**вес: см. `cqg@2.17`** — цена этой ревизии названа там одной строкой на обе:
из +130 строк тридцать дал `stack_selftest.py`.
**класс:** list-without-its-principle @ AGENT_STACK.md stack_selftest.py::CHECKED_LANGS

## `stack-map@1.49` — приложение A синхронизировано с файлом

`blocks()` научился выравнивать тело блока по отступу открывающего фенса, чтобы
совпасть с извлекателем: подробности и цена — в записи `cqg@2.10`, правка одна на
два канона. Здесь — обязательная половина: **payload приложения A пересобран из
файла**, иначе шаг CI «Payload and file must not drift» краснеет, и краснеет
правильно — контур возит копию, а проверяет оригинал.
