# Agent Delivery Harness — каркас предсказуемой AI-разработки

> **Карта канонов (вход для агента):** [AGENT_STACK.md](AGENT_STACK.md)  
> **Слой контура:** ① процесс (фундамент) → затем
> [CODE_QUALITY_GATES.md](CODE_QUALITY_GATES.md) → затем
> [OKF_KNOWLEDGE_BUNDLE.md](OKF_KNOWLEDGE_BUNDLE.md).  
> Не разворачивай CQG/OKF раньше этого файла, если человек явно не сказал иначе.

**Canon version:** `delivery@1.54` · 2026-08-07 (Changelog — в конце файла).
При развёртывании впиши версию в `delivery/CONSTITUTION.md` и `STATUS.md`
(поле `stack:`) — иначе проект не знает, какую ревизию каркаса он получил.

**Самодостаточный универсальный документ.** Кладёшь этот один файл в **новый или
существующий** проект — агент разворачивает **delivery-контур**: фазы
(specify → plan → tasks → implement → verify → converge → handoff), Definition of Done,
stop-conditions, изоляция изменений, thin behavioral contract для coding-агента,
smoke-evals и hook в `AGENTS.md` / Cursor rule.

> **Язык:** проза — русский, идентификаторы / пути / команды — английские.
>
> **Что это НЕ делает:** не дублирует правила чистого кода и lint-гейты
> (`CODE_QUALITY_GATES.md`) и не дублирует формат knowledge-bundle
> (`OKF_KNOWLEDGE_BUNDLE.md`). Этот файл — **каркас процесса**. Те два — **слои
> усиления**, которые подключаются **после** этого файла (порядок — §0 и
> [AGENT_STACK.md](AGENT_STACK.md)).
> «Рядом в репо» допустимо, но **разворачивать** их раньше delivery нельзя:
> сначала каркас процесса, затем CQG, затем OKF.

---

## 0. Порядок развёртывания контура (обязательно читать первым)

### 0.1. Этот файл — фундамент

В любом проекте (greenfield или brownfield) порядок такой:

| Шаг | Артефакт | Зачем |
|---:|---|---|
| **1** | **Этот файл** → контур `delivery/` + hook | Процесс: что строить, как резать, когда «готово», как не врать себе |
| **2** *(рекомендуется)* | `CODE_QUALITY_GATES.md` | Механические oracles на **форму** кода (lint/types/architecture) |
| **3** *(рекомендуется)* | `OKF_KNOWLEDGE_BUNDLE.md` | Канон **смысла** домена (политики, ADR, метрики) вместо RAG-угадывания |
| **4** *(по нужде)* | Agent Behavioral Contracts (ABC) для **продуктовых** агентов | Runtime-контракты ботов/outreach/judges — не путать с coding-delivery |

**Инвариант:** без шага 1 агент умеет писать код; **не умеет** доказуемо
заканчивать работу. Шаги 2–3 без шага 1 дают «чистый код без цели» и «wiki
без поставки».

Если в проекте уже есть CQG и/или OKF — **не сносить**. Разверни `delivery/`
поверх, затем в `AGENTS.md` пропиши порядок чтения: Delivery → Quality Gates →
OKF (см. A.5).

### 0.1a. CI — сквозной слой ④, не часть одного канона

Слои ①–③ отвечают на «что делать». **CI отвечает на «кто это проверит, когда
никто не смотрит»** — и по построению принадлежит всем трём: одна джоба гоняет
`delivery_check` (①), `pre-commit`/ратчеты (②) и `okf_validate`/`okf_sync_gate` (③).

Поэтому:

- **Контракт** («что обязано гоняться, что значит красный») — здесь, §10.4.
- **Реализация** (workflow, скрипты) — [CODE_QUALITY_GATES.md](CODE_QUALITY_GATES.md) §8,
  потому что там живут исходники всех гейт-скриптов и там же их настройка путей.
- **Разворачивается** — вместе с ② (шаг 2 ниже), но обслуживает все три слоя.

Это сознательное исключение из «CQG не ведёт поставку»: workflow — единственный
файл в контуре, который по определению сквозной. Не пытайся расщепить его на три.

### 0.2. Границы: что где живёт

| Вопрос | Этот harness | CQG | OKF | CI ④ |
|---|---|---|---|---|
| «Что и зачем строим?» | **Да** (`spec`) | Нет | Может хранить канон после принятия | Нет |
| «Как режем работу?» | **Да** (`plan`/`tasks`) | Нет | Нет | Нет |
| «Код достаточно чистый?» | Ссылается на CQG | **Да** (правила) | Нет | **Да** (прогон) |
| «Как у нас принято считать X?» | Ссылается на OKF | Нет | **Да** | Нет |
| «Канон не разъехался с кодом?» | Гейт в DoD §3.2.4 | Нет | **Да** (`implementation:`) | **Да** (`okf_sync_gate`) |
| «Можно ли закрыть задачу?» | **Да** (DoD + verify) | Часть DoD (гейты зелёные) | Часть DoD (канон обновлён) | **Да** (единственный неподделываемый прогон) |
| «Агент не ушёл в drift mid-session?» | **Да** (thin ABC §4) | Косвенно | Косвенно | breakers §3.4 |

### 0.3. Как пользоваться документом

| Секция | Содержание |
|---|---|
| §1 | Философия harness (без повторения CQG/OKF) |
| §2 | Фазовый конвейер и артефакты + **§2.2a** уроки архива перед implement |
| §3 | Oracles, DoD, stop-gates, circuit breakers |
| §4 | Thin ABC для **coding**-агента + **§4.5 права на действия** (что агенту можно делать, а не только что сдать) |
| §5 | Изоляция (worktree default), Builder≠Verifier, anti-oneshot |
| §6 | Eval suite / smoke (product oracles) |
| §7 | Развёртывание (агент / вручную) |
| §8 | Brownfield / стыковка с уже лежащими CQG+OKF |
| §9 | Observability / метрики harness |
| §10 | Agent hooks (enforcement stop-gates) + **§10.4 CI** (необходимый слой) |
| §11 | Skills / prompts catalog (указатель) |
| §12 | **Три журнала поведения**: как искал (`diagnosis`), что выбрал (`decisions`), чего просит (`escalation`) |
| §12.6 | **Путь, проверяемый только исполнением**: объявленные `runtime_paths`, доказательство прогона, breaker по поверхностям |
| §13 | **Post-merge**: контур не заканчивается на мерже — наблюдение как обязательство, лестница наблюдаемости |
| Приложение A | Шаблоны файлов |
| Приложение B | `delivery_check.py` — фазовый гейт |
| Приложение C | `delivery_metrics.py` — автосбор метрик §9 |

**Стоимость чтения.** Канон — §0–§12 (≈1380 строк). Приложения — **bootstrap
payload** (≈1630): читаются при развёртывании и при правке конкретного шаблона,
но **не** в обычной работе над фичей. Не тяни весь файл в контекст без нужды — таблица ниже
говорит, что читать под твою задачу.

### 0.3a. Маршрутизация: что читать под эту задачу

Таблица §0.3 отвечает «что в какой секции». Эта отвечает на настоящий вопрос
агента: **у меня `kind: X`, класс `Y` — что мне нужно.**

**База по классу** — §2.2 (S: `tasks`; M: `spec`+`plan`+`tasks`+`human_ok_spec`;
L: то же + `human_ok_plan`). Здесь — что `kind` добавляет **сверх** базы, чтобы
не дублировать §2.2 и не разъезжаться с ней:

| `kind` | Обязательно сверх базы класса | Читать в первую очередь | Что упадёт, если не сделать |
|---|---|---|---|
| `feature` | Acceptance-примеры с id в `spec.md` | §3.1d, §6.2 | `delivery_check` (нет блока примеров / id не в тестах) |
| `bugfix` | `repro_test:` + `diagnosis.md` с вердиктами | §3.1c, §12.1 | `delivery_check` (нет `repro_test` / `diagnosis`) |
| `refactor` | `behavior-oracles` ≠ `weak` (характеризационные / differential / waiver) | §3.1b, §6.5 | `delivery_check` (stop-gate по `kind=refactor`) |
| `hotfix` | То же, что `bugfix`, + post-factum `spec` в archive | §2.2, §3.1c, §12.1 | `delivery_check`; спека постфактум — на ревью |

**Всегда, независимо от `kind` и класса:**

| Что | Где правило |
|---|---|
| `STATUS.md` заполнен без плейсхолдеров | §2.3, A.2 |
| `verify-report.md` + зелёный CI-прогон **ссылкой** | DoD §3.2.2–3.2.3, §10.4 |
| `decisions.md` — строка на каждый выбор | §12.2 |
| Прочитаны уроки архива по затронутым путям | §2.2a |
| Дифф в пределах circuit breaker | §3.4 |

**Что читать НЕ надо:** приложения A–C в обычной работе; секции по другим `kind`;
CQG целиком — из него нужен только §3 (каталог гейтов), когда гейт покраснел.

⚠ **У этой таблицы нет гейта, и она может разойтись с реальностью.** Она —
указатель в прозу, а не источник истины: истина механическая — это
`delivery_check.py` (Приложение B). **Если таблица и скрипт расходятся, прав
скрипт**, а расхождение — дефект таблицы, который надо починить здесь же. То же
относится к §0.3 и к чеклисту [AGENT_STACK.md](AGENT_STACK.md) §3.

---

## 1. Философия

### 1.1. Agent = Model + Harness

Модель рассуждает. Harness делает рассуждение **проверяемым и останавливаемым**.
Ошибка агента → чиним **harness** (новый oracle, лимит, фаза, hook), а не только
промпт «в следующий раз будь внимательнее».

### 1.2. Пять слоёв (этот документ закрывает процессные)

| Слой | Кто закрывает |
|---|---|
| Tool orchestration | IDE/agent runtime (Cursor / Claude Code / …) |
| **Verification loops** | §3 + CQG (код) + §6 (продукт) |
| **Context & memory** | OKF (канон) + `delivery/` (текущая поставка) |
| **Guardrails** | §4–§5 + hooks |
| **Observability** | §9 |

### 1.3. Спека executable, код — следствие

Пока нет принятого `spec` (и для нетривиального — `plan`/`tasks`), агент
**не начинает** широкую реализацию. One-shot «сделай фичу целиком» запрещён
по умолчанию (§5.3).

### 1.4. Done = oracles, не самооценка

Агент **не имеет права** объявить задачу выполненной, пока не зелёны stop-conditions
§3. Victory declaration bias — известный failure mode; harness его режет.

---

## 2. Фазовый конвейер

Вдохновлено Spec-Driven Development ([GitHub Spec Kit](https://github.com/github/spec-kit)),
но **самодостаточно**: CLI Spec Kit не обязателен. Достаточно дерева `delivery/`
и дисциплины ниже.

### 2.1. Фазы

```text
constitution (раз на проект)
    → specify
    → clarify*          (*если дыры)
    → plan
    → tasks
    → analyze*          (*consistency check)
    → implement         (по tasks, slice-ами)
    → verify            (oracles)
    → converge          (дыры → новые tasks → implement…)
    → handoff           (PR / OKF update / log)
```

| Фаза | Артефакт | Вопрос | Human gate? |
|---|---|---|---|
| **constitution** | `delivery/CONSTITUTION.md` | Нерушимые принципы процесса/продукта (не lint-правила) | Да, при создании |
| **specify** | `delivery/active/spec.md` | *Что* и *зачем* (без выбора технологий) | **Да** перед plan |
| **clarify** | правки в `spec.md` + список Q→A | Убрать двусмысленности | По необходимости |
| **plan** | `delivery/active/plan.md` | *Как*: технологии, границы, риски | **Да** перед tasks на risky work |
| **tasks** | `delivery/active/tasks.md` | Атомарные шаги + зависимости | Нет (ревью по желанию) |
| **implement** | код + отмеченные tasks | Выполнение одного slice | Нет |
| **verify** | `delivery/active/verify-report.md` | Oracles зелёные? | Нет (факт машинный) |
| **converge** | доп. tasks или `CONVERGED` | Spec выполнен? | Да, если scope creep |
| **handoff** | PR + (если есть OKF) concept update | Передано людям | Да (merge) |

`*` — **подфазы**, не отдельные значения `phase:` в `STATUS.md`:
`clarify` идёт внутри/сразу после `specify`; `analyze` — после `tasks`,
перед `implement`. В STATUS остаются только фазы из колонки выше
(`specify` … `handoff`).

**Нельзя** схлопнуть `verify`. На мелких задачах `clarify`/`analyze` можно
пропустить.

### 2.2. Когда полный конвейер, когда короткий

| Класс работы | Минимум фаз |
|---|---|
| **S** — багфикс ≤1 файла, ясен repro | mini-spec (5–10 строк в `tasks.md`) → implement → verify |
| **M** — фича 1–2 дня | specify → plan → tasks → implement → verify → handoff |
| **L** — эпик / новый модуль / greenfield | полный конвейер + constitution + converge |
| **Hotfix** | mini-spec + implement → verify; post-factum `spec` в archive |

Класс **L без human OK на spec** — запрещён.

Перед фазой **implement** обязательно:

| Класс | Минимум артефактов |
|---|---|
| **S** | `tasks.md` (с mini-spec) |
| **M** | `spec.md` + `plan.md` + `tasks.md` + `human_ok_spec: yes` (или `deferred` — §2.2b) |
| **L** | как M + `human_ok_plan: yes` (risky/L) |

### 2.2a. Перед implement — прочитать уроки по этому модулю

`archive/` копит разобранные поставки, но **неиндексированный архив никем не
читается**: это тот самый класс «артефакт есть, никто не смотрит», который контур
ловит у чужого кода и три поставки держал внутри себя. §9.2 уже требовал «раз в
несколько поставок просмотри archive» — ритуал без адресации, поэтому не
выполнялся ни разу.

**Правило.** Каждая архивная поставка оставляет в `delivery/archive/INDEX.md`
(шаблон A.13) одну строку: пути, которые она трогала, и **урок с id** (`L1`,
`L2`…). Перед implement агент читает строки, чьи пути пересекаются с диффом, и
**ссылается на их id** в `plan.md` (класс M/L) или `tasks.md` (класс S) — либо
одной строкой пишет, почему урок не применим.

Механика — в `delivery_check.py` (Приложение B), три проверки разной силы:

| Проверка | Severity | Почему так |
|---|---|---|
| `archive/` непуст, а `INDEX.md` нет | **error** | Индекса нет ⇒ правило нечем исполнить |
| Архивный каталог не упомянут в `INDEX.md` | **error** | Индекс отстал ⇒ артефакт врёт о полноте, а это хуже отсутствия (§4.3b) |
| Урок применимого модуля не упомянут в `plan.md`/`tasks.md` | **warning** | Сопоставление «путь → модуль» приблизительно; ложное срабатывание тут дороже пропуска (§4.3b) |

Уроки — id по образцу acceptance-примеров (§3.1d): id связывает **обещание с
проверкой**, и ровно так же связывает **прошлую ошибку с текущей поставкой**.

### 2.2b. Агент работает, человека рядом нет

Самая опасная дыра из найденных полем, и нашло её **независимое развёртывание**:
агент, которому не с кем согласовать спеку, поставлен перед выбором «встать» или
«объявить класс S». Он объявил S — и был формально прав, потому что канон про эту
ситуацию **молчал**. Опасность не в том, что гейт сломался: гейт цел, а канон
**учит его обходить законным с виду способом**. Это хуже любого ложного
срабатывания.

Правило: **класс определяется работой, а не доступностью человека.** Занижать
класс, чтобы пройти гейт, запрещено. Вместо этого у подписи появляется третье
значение:

| `human_ok_spec` | Что означает | До какой фазы пускает |
|---|---|---|
| `yes (by=… at=…)` | Человек подтвердил примеры до кода | До конца |
| `deferred (reason=… at=…)` | Спека с примерами написана, человек **недоступен**; работа идёт под запись долга | До `verify`/`converge` включительно |
| `no` / пусто | Не написано и не согласовано | Не пускает в implement |

**`deferred` не ослабляет проверку, а переносит её туда, где человек всё равно
есть — на мерж.** Автономный прогон получает свои фазы, а подпись под ожиданиями
остаётся обязательной: `delivery_check` на `phase: handoff` требует именно `yes`.
Долг виден в диффе PR, живёт одну поставку и умирает вместе с ней — та же форма,
что у waiver'ов (§4.3a) и у обязательства наблюдения (§13.1).

**Занижение класса теперь пахнет механически.** Класс `S` с диффом шире
S-масштаба — не ошибка (бывает большой механический багфикс), но warning:

> `class S, а тронуто 14 файлов / 430 строк — класс занижен? (§2.2b)`

Гейтом это делать нельзя: законные исключения есть, а ложное срабатывание на них
дороже пропуска (§4.3b). Но молчать тоже нельзя — молчание и создало дыру.

#### Правило распространяется на ВСЕ поля-подписи, а не на `human_ok_spec`

Дыру закрыли один раз — и она вернулась. Пятое развёртывание (три независимые арки
на настоящем проекте) нашло тот же тупик в `human_ok_plan` на классе L: там стояла
отдельная проверка с той же логикой минус `deferred`, поэтому агент без человека
опять выбирал между «встать» и «занизить класс». Пока смысл один, а кода два,
третье поле повторит историю. Теперь развилка **одна на класс полей**
(`signature_verdict` в Приложении B), и она обслуживает `human_ok_spec`,
`human_ok_plan`, `asserts_reviewed_by` и всякое будущее поле-подпись.

Честная оговорка: **это утверждение уже было записано преждевременно.** В v1.30 тут стояло «все поля-подписи»,
а развилку получили два поля из трёх — третье (`asserts_reviewed_by`) нашло приёмочное развёртывание. Мораль
не про внимательность: пока класс не перечислен списком в оракуле, «закрыт класс» означает «закрыт случай».
Список живёт в `tests/test_signature_and_wrapping.py`; добавляешь поле-подпись — добавляй в него строку.

#### Waiver — не подпись, и `deferred` для него не существует

`waivers:` устроен иначе, и три арки наткнулись на это независимо: канон требовал
«только человек» (§3.4), а парсер принимал `max_loc_diff=99999` **без причины и без
подписи**. Одна арка честно записала, что подписалась `by=agent:` за неимением
человека — то есть механика позволяла исполнителю поднять себе лимит.

Теперь:

| Строка в STATUS | Что это | Что требуется |
|---|---|---|
| `circuit_breakers: max_loc_diff=2000` | **настройка проекта** — монорепо, миграция, генерённый код | ничего, это конфигурация |
| `waivers: max_loc_diff=2000 reason=… by=human:<имя>` | **human waiver** на одну поставку | `reason=` И `by=human:` — иначе error |
| `waivers: max_loc_diff=2000` | попытка обойти лимит молча | error |

**Человека нет — waiver'а нет.** Отложить лимит нельзя: дифф уже широкий, и
«потом подпишем» означает «прошло без проверки». Законных выходов два, оба уже в
каноне: разрезать поставку **или** записать `blockers:` и `escalation.md` (§12.3 —
там как раз требуются два варианта с ценой). Разница с `deferred` принципиальная:
подпись под ожиданиями можно догнать на мерже, а пробитый breaker к моменту мержа
уже случился.

**Чего это не даёт.** Агент может написать `deferred` и не иметь спеки вовсе —
поле проверяет форму, не добросовестность. Против этого работает то, что на
handoff требуется `yes`, а получить его можно только предъявив человеку спеку.

### 2.3. Дерево `delivery/`

```text
delivery/
  CONSTITUTION.md                 # принципы процесса (не CQG)
  README.md                       # как читать контур (коротко)
  STACK-ACCEPTANCE.md             # приёмка контура + разбор слепых пятен (A.12)
  active/                         # текущая поставка (одна)
    .gitkeep                      # ОБЯЗАТЕЛЕН: пустой active/ штатен (§2.3a),
                                  # а git пустые каталоги не хранит
    STATUS.md                     # фаза, класс S/M/L, blockers
    spec.md
    plan.md
    tasks.md
    verify-report.md
    eval-smoke.md                 # product oracles для этой поставки
    decisions.md                  # одна строка на выбор (§12.2)
    diagnosis.md                  # ТОЛЬКО kind=bugfix: гипотезы с вердиктами (§12.1)
    escalation.md                 # ТОЛЬКО когда агент встал: 2 варианта с ценой (§12.3)
  archive/
    INDEX.md                      # модуль → поставки → уроки с id (A.13, §2.2a)
    YYYY-MM-DD-<slug>/            # завершённые поставки
      observed.md                 # что показал прод (A.17, §13.1) — по observe_until
  evals/
    smoke/                        # постоянные smoke-сценарии репо
      README.md
```

Одновременно **одна** `active/` поставка. Новая тема → archive старой или
отдельная ветка/worktree со своей `delivery/active`.

### 2.3a. Пустой `active/` — штатное состояние, а не поломка

Между поставками `active/` пуст: предыдущая ушла в `archive/`, следующая не
начата. Это **не** ошибка, и гейт не имеет права быть красным в этом состоянии —
иначе CI между поставками врёт, а §13.1 к тому же **сам** приводит проект сюда,
требуя архивации на handoff вместе с обязательством наблюдения.

⚠ **`delivery/active/.gitkeep` обязателен именно из-за этого правила.** Git не
хранит пустые каталоги: как только последняя поставка уходит в `archive/`,
`active/` исчезает из дерева, и в свежем клоне `delivery_check` падает с
`missing delivery/active/` — жёстко, до всех остальных проверок. То есть канон
объявил штатным состояние, которое git не умеет представить. Найдено
**независимым развёртыванием** (агент, не участвовавший в разработке канона):
воспроизвелось на первом же fresh checkout после архивации.

`delivery_check` в idle: `warnings`, не `errors`, и **проверки уровня архива
продолжают работать** — полнота `INDEX.md` (§2.2a) и просроченные наблюдения
(§13.1). Именно в idle они и важнее всего: активной поставки нет, значит ничто не
отвлекает от долга по прошлой.

Заглушка вида `phase: idle` в STATUS **не** вводится сознательно: это ритуал
заполнения ради зелёности, а такие ритуалы канон уже разбирал (§9.2 до §2.2a).
Найдено развёртыванием, а не чтением: до первой архивации состояние не возникает.

### 2.4. Что писать в constitution (и чего не писать)

**Писать:** «сначала spec», «vertical slice», «не пушить в main», «HITL на
миграции/прод», «Builder≠Verifier», «ошибка → усили harness».

**Не писать:** лимиты длины функций, select ruff, import-linter слои — это
**CQG**. Не писать YAML frontmatter OKF — это **OKF**.

Если CQG/OKF уже развёрнуты, constitution содержит **одну строку-указатель**:

```markdown
- Code shape oracles: see `CODE_QUALITY_GATES.md` (deployed).
- Domain canon: see `knowledge/` per `OKF_KNOWLEDGE_BUNDLE.md` (deployed).
```

---

## 3. Oracles, DoD, stop-gates, circuit breakers

### 3.1. Три класса oracle

| Класс | Примеры | Где живёт |
|---|---|---|
| **A. Shape** | lint, types, import direction, file-length | CQG / pre-commit |
| **B. Behavior** | unit/integration tests, diff-coverage | тестовый раннер проекта |
| **C. Product / delivery** | smoke evals §6, «spec acceptance bullets зелёные», фаза verify | этот harness |

Для класса M/L harness **требует** все *доступные* классы oracle:

- **A:** если CQG/pre-commit есть — они; иначе временно `compile` /
  `tsc --noEmit` / «что есть», STATUS: `shape-oracles: weak`, backlog шаг 2 §0.1.
- **B:** если в репо есть тестовый раннер — прогон затронутого; иначе
  `behavior-oracles: weak` + обязательный класс **C**.
- **C:** acceptance / smoke §6 — для M/L обязателен всегда (даже при weak A/B).

**Где они должны запускаться.** Локальный прогон (pre-commit, hooks) — быстрая
петля для агента; **источник истины для DoD — CI** (§10.4). Любой oracle класса
A/B, который есть только локально, обходится одной командой (`git commit -n`) и
потому не является oracle в смысле §1.4.

### 3.1a. Три вопроса вместо одного (класс oracle, которого не хватало)

**Артефакт, который никто не исполняет и не парсит, неотличим от проходящего.**
Спрашивать «зелено ли?» недостаточно — вопросов три, и они закрываются разными
гейтами:

| # | Вопрос | Чем закрыт | Чем НЕ закрыт |
|---|---|---|---|
| 1 | **Всё ли на месте?** (подключено) | `gate-coverage` — каждый скрипт упомянут в конфиге; `delivery_check` — артефакты фазы есть | Зелёным прогоном: он молчит о недостающем |
| 2 | **Исполнилось ли оно?** | `if: always()` на независимых шагах CI; чтение лога, а не факта «шаг в файле есть» | Пунктом 1: подключённый гейт **за упавшим шагом** не запускается вовсе |
| 3 | **Зелено ли оно?** | `pre-commit run --all-files`, тесты, smoke | Локальным прогоном — см. `ci-status` ниже |

Дополнительно к трём: **где именно** оно зелено. Всё, что гоняется локально,
проверяет локальный интерпретатор и локальный venv; расхождение с CI такой набор
не видит в принципе. Закрывает `check_ci_status.sh` (CQG §3), подключённый
последним в `merge_guard.sh`.

### 3.1b. `weak` — не константа: цена зависит от того, что ты только что сделал

`behavior-oracles: weak` честно означает «поведение не проверяется». Но эта
строка стоит **разного** в разных поставках:

| Поставка | Чем рискуешь при `weak` |
|---|---|
| Новый модуль с нуля | Ошибка в новом коде — заметна на первом же использовании |
| **Рефакторинг существующего слоя** | Тихая регрессия в работавшем поведении. «Компилируется» ничего не доказывает: типы сошлись, семантика уехала |

Поэтому в STATUS есть `kind:`. Правило: **`kind: refactor` + `behavior-oracles:
weak` = stop-gate** (§3.3). Закрыть можно одним из трёх способов, и все три
записываются в STATUS:

1. **Differential-тест против старой реализации** — лучший вариант (§6.5): ожидание
   берётся из работающего кода, а не придумывается заново. Держи `_legacy.py` рядом
   на время поставки, сравнивай обе версии на одних входах, удали в converge.
2. **Наблюдаемая проверка вместо теста** — smoke-сценарий, кадр, лог, снимок
   ответа API: что-то, что показывает «до» и «после» (Delivery §6).
3. **Human waiver** — человек берёт риск на себя, строкой с причиной.

Характеризационные тесты (снять поведение вручную, потом двигать код) — тоже
законно, но слабее differential: их ожидания снова придумывает исполнитель.

Реальный случай (2026-07-28, третий проект): перетасован весь AR-слой, тестов нет,
единственное подтверждение — «компилируется». Формулировка исполнителя точная:
*после рефакторинга слово «weak» стоит дороже, чем вчера*.

### 3.1c. Багфикс без регрессионного теста не закрывается

Повторный баг — самый дорогой сорт: его уже находили, уже понимали и уже
исправляли, а платить пришлось дважды. Единственная защита — тест, который
**падал до фикса**.

Правило: **`kind: bugfix` + фаза verify и дальше ⇒ поле `repro_test:` обязательно**
(проверяет `delivery_check`). В поле — путь и имя теста: `tests/test_feed.py::test_empty_robots`.

Порядок работы, а не порядок записи:

1. Воспроизвести баг тестом — **сначала**. Тест красный.
2. Убедиться, что он красный **по правильной причине** (сообщение об ошибке
   совпадает с симптомом, а не «упал где-то раньше»).
3. Починить код. Тест зелёный.
4. Записать путь к тесту в `repro_test:`.

Если баг механически не воспроизводится тестом (гонка, окружение, внешний сервис) —
`repro_test: n/a reason=… by=human:…`. Причина обязательна: «не воспроизводится»
без объяснения обычно значит «не пробовал».

**`repro_test` доказывает, что баг найден; он ничего не говорит о том, как искали.**
Вторую половину закрывает `diagnosis.md` (§12.1) — журнал гипотез с вердиктами, поле
`diagnosis:` в STATUS. Без него агент проверяет одну гипотезу трижды, а человек,
подключаясь к разбору, начинает поиск с нуля.

**Порядок важен: сначала «кто это читает?», потом «зелено ли оно».** Формулировка
из второго реального развёртывания, где четыре разбора подряд оказались одним
классом ошибки: скрипт лежал в репозитории — никто не вызывал; workflow-файл был
добавлен — YAML сломан и никто его не парсил; значение `ci-oracles: tooling`
стояло в STATUS — никто не сверял с прогонами; гейты были подключены — но стояли
за шагом, который крашился и обрывал джобу.

Правило: вводя новый oracle, сразу отвечай на три вопроса — **кто его вызывает**,
**что будет, если шаг выше упадёт**, и **где он гоняется**. Если хоть один ответ
звучит как «ничего, будет тихо зелено» — нужен ещё один гейт (§5.4: ошибка →
усиление harness).

Разобранные случаи:

| Дата | Что было | Почему прошло мимо |
|---|---|---|
| 2026-07-27 | Скрипт jscpd извлечён и адаптирован, но не вписан в `.pre-commit-config.yaml` | Приёмка проверяла «работает ли подключённое», а не «подключено ли всё» → `gate-coverage` |
| 2026-07-28 | `ci-oracles: tooling` в STATUS при 6 красных прогонах подряд; две поставки закрыты как done | Значение самозаявлено: DoD требовал «ссылку на зелёный прогон» прозой, без oracle → `check_ci_status.sh` |
| 2026-07-28 | mypy крашился (INTERNAL ERROR) и обрывал джобу — 4 гейта не выполнялись ни разу | В логе их просто нет; «подключён» не значит «исполнен» → `if: always()` на независимых шагах |
| 2026-07-28 | Сломанный YAML в `main-guard`: хостинг показывал workflow путём вместо имени | Конфиги проекта никто не парсил → `check-yaml`/`check-json` |

### 3.1d. Откуда берётся ОЖИДАЕМОЕ значение (главный потолок контура)

Все гейты проверяют соответствие **объявленному**. Тест утверждает `f(x) == y`;
механически можно убедиться, что тест есть, исполняется, что-то утверждает
(mutation) и покрывает дифф. **Никакая проверка не знает, верно ли `y`.**

Отсюда потолок: агент, написавший правдоподобную спеку и тесты, закрепляющие
багованное поведение, проходит контур насквозь — и контур ему **помогает выглядеть
убедительно**. Причина не в тестах, а в **круге**: один и тот же исполнитель
породил и код, и ожидание от него.

Круг разрывается только одним способом — **ожидание должно появиться раньше кода
и из другого источника**. Три уровня, по силе:

| Уровень | Механизм | Что даёт |
|---|---|---|
| **1. Примеры в спеке** | `## Acceptance examples` в `spec.md`, подтверждённые человеком **до** implement (`human_ok_spec`) | Ожидание пришло от человека, а не от агента. Самый сильный доступный рычаг |
| **2. Реляционные оракулы** | Свойства и метаморфные отношения вместо конкретных значений (§6.5) | Не требуют знать правильный ответ вообще — в них нельзя спрятать неверное ожидание |
| **3. Ревью утверждений** | `assert_digest.sh` (CQG §3): человеку показывают **только изменённые утверждения**, а не весь дифф | Делает человеческий потолок дёшевым: 12 строк вместо 400 |

#### Уровень 1: примеры вместо прозы

`spec.md` для класса M/L обязан содержать блок с **конкретными** примерами
«вход → ожидаемый выход». Не «система должна корректно обрабатывать пустой
robots.txt», а:

```markdown
## Acceptance examples

| # | Вход | Ожидаемый выход |
|---|---|---|
| A1 | robots.txt пустой (0 байт) | `allows(any_url) == True`, в лог `robots_empty` |
| A2 | robots.txt = `Disallow: /` | `allows("/x") == False` |
| A3 | robots.txt недоступен (500) | `allows(any) == True`, warning `robots_unavailable` |
```

Правила:

- Примеры пишутся **на фазе specify**, до `plan` и до кода. Порядок принципиален:
  ожидание, придуманное после реализации, — это описание реализации.
- Человек подтверждает их вместе со спекой (`human_ok_spec`). Именно подпись под
  **числами и значениями** (а не под абзацем «работает корректно») делает подпись
  содержательной.
- Каждый пример получает id (`A1`, `A2`…), и **тест или smoke-сценарий ссылается
  на этот id** — так видно, что проверено именно обещанное:
  `def test_empty_robots_allows_all():  # A1`
- `delivery_check` требует: класс M/L, фаза implement и дальше ⇒ в `spec.md` есть
  блок примеров, и все их id встречаются в тестах или `eval-smoke.md`.

Чего это **не** даёт: человек может подтвердить неверный пример. Но ошибка
становится **явной и датированной** — она лежит строкой в спеке с подписью, а не
растворена в 400 строках кода. Это перевод неявного риска в явный, а не его
устранение.

#### ⛔ Чего здесь НЕ будет: оценки решений моделью

Напрашивается очевидное: раз механика не судит, «пусть судит модель» — оценка
архитектуры, скоринг вариантов, балл за качество. **Решено не делать, и вот почему,
чтобы к этому не возвращались каждые полгода.**

**Ломает определение.** Гейт в этом контуре — «механическая проверка: exit 0/1, без
участия модели» (AGENT_STACK §1.1, там же прямо: «не ревью и не „спроси LLM"»).
Судья-модель делает вердикт невоспроизводимым: тот же дифф в другой день получает
другой балл, и спорить с ним нечем.

**Это самооценка с лишними шагами.** Агент, писавший код, и агент, оценивающий код,
делят слепые зоны. Замер сессии 2026-07-31, на своей же работе: разведка по F7
**дважды** воспроизвела слепую зону, которую искала, и печатала «0 непокрытых» на
непокрытом; тест на форму `filterwarnings` пропускал ровно ту форму, из которой
находка выросла; тест на порядок окна проходил при сломанной сортировке; проверка
цены решения считала ценой **дату записи**. Четыре случая за день, и ни один не
поймало рассуждение — все поймал прогон.

**Балл станет целью.** Оценка, которая не может провалить, — декорация; оценка,
которая может, превращается в число для оптимизации, и оптимизировать будут число.
Это тот же запрет, что в §9.1a про единицу счёта гейтов: подгонка метрики под
результат — не уборка.

**Что делать вместо.** Не судить качество, а гарантировать, что случаи, где качество
решает, попадут к человеку, и что развилка описана так, чтобы он проверил её за
секунды: цена вместо предпочтения (§12.2a), ревью только изменённых утверждений
(уровень 3 ниже), свойства вместо значений (§6.5). Модель полезна как **автор**
вариантов и как читатель, но не как оракул.

#### Уровень 3: ревью пропорционально непроверяемой части

Наивная форма правила — «на M/L человек подписывает дайджест утверждений» —
требует участия человека там, где он **уже подписался**. Если утверждение
ссылается на пример `A3`, а `A3` человек подтвердил на фазе specify до кода
(`human_ok_spec`), то повторная подпись под тем же числом не добавляет ничего:
она проверяет исполнителя на согласие с уже одобренным ожиданием, а это
механическая работа.

Ревью обязательно ровно там, где **ожидание придумал агент**:

| Что показал дайджест | Что требуется | Почему |
|---|---|---|
| Все утверждения привязаны к id примеров | `asserts_reviewed_by: n/a (все утверждения ведут к одобренным примерам)` | Подпись человека стоит раньше и на числах |
| Есть непривязанные (`-` в первой колонке) | `asserts_reviewed_by: human:… at=…` | Эти ожидания не подписывал никто |

`assert_digest.sh` (CQG §3) классифицирует **каждое** утверждение по тест-блоку и
печатает машинно-читаемую строку `asserts_without_example: N`. `n/a` законен
только при `N = 0`; `delivery_check` требует, чтобы дайджест был вставлен в
`verify-report.md` — заявить `n/a` без него нельзя.

Стоимость ревью становится пропорциональной: поставка, где спека проработана,
не требует человека вообще; поставка, где агент насочинял ожиданий, требует его
ровно на этих строках. Это **не ослабление** проверки — то же требование,
адресованное точнее (см. оговорку 1 в `BACKLOG.md`: автономия без нового оракула
= снятая проверка; здесь оракулом стал подписанный пример).

**Остаточная щель.** Утверждение может ссылаться на `A3` и при этом проверять не
то, что в `A3` написано — id ставит тот же агент. Дайджест печатает id рядом с
текстом утверждения именно поэтому: расхождение видно при чтении, но механически
не ловится. Уровень 3 сужает щель до «id проставлен неверно», а не закрывает её.

### 3.2. Definition of Done (закрытие tasks / поставки)

Задачу/поставку **нельзя** считать done, пока:

1. Все релевантные items в `tasks.md` checked **или** явно `wontfix` с причиной.
2. `verify-report.md` существует и все обязательные oracles = PASS.
3. Зелены **и** локальные гейты (pre-commit / CQG), **и** CI-прогон на ветке
   поставки (§10.4). Локальный обход (`git commit -n`, `STRICT=0`, отключённый
   hook) прохождением **не считается**: DoD закрывает только зелёный CI-run,
   **ссылка на который вписана в `verify-report.md`** — URL прогона, а не слова
   «CI зелёный». Механически это проверяет `check_ci_status.sh` (CQG §3);
   «у меня локально прошло» закрытием не является. Если CI ещё нет —
   `ci-oracles: weak` в STATUS + пункт backlog; на класс L — human waiver.
4. Если развёрнут OKF и поставка меняла инвариант — обновлён concept +
   `knowledge/**/log.md` (правила — в OKF-каноне, здесь только гейт «сделано»).
5. `STATUS.md` → `phase: handoff` (после успешного converge) **или** явный
   verdict `CONVERGED` в verify-report при `phase: converge` с нулём открытых
   gaps (затем сразу handoff). Значения `phase: converged` **нет** — только
   `converge` / `handoff` из enum STATUS.
6. Diff в рамках circuit breaker (§3.4) **или** human waiver в STATUS.
7. **Класс M/L:** ревью утверждений закрыто (§3.1d уровень 3) — в
   `verify-report.md` стоит `asserts_reviewed_by: human:… at=…` **либо**
   `n/a`, заслуженный нулём непривязанных утверждений в дайджесте. Проверяет
   `delivery_check`. До v1.19 это требование существовало только комментарием
   в шаблоне A.8 и не проверялось ничем — то есть последний рубеж против
   неверного ожидания держался на добросовестности.
8. **Журналы поведения (§12), каждый по своему условию:** `kind: bugfix` ⇒
   `diagnosis.md` с вердиктами **или** `diagnosis: n/a reason=…`; непустые
   `blockers:` ⇒ `escalation.md` с двумя вариантами и ценой; `decisions.md` —
   строки формата «выбрал X вместо Y, потому что Z» (класс L: минимум одна).
   Проверяет `delivery_check` — **формат, не содержание** (§12.5).
9. **На handoff заполнены `observe_signal:` и `observe_until:`** (§13.1) —
   поставка уходит в archive вместе с обязательством проверить её в проде.
   Без этого «готово» проверено во всех средах, кроме единственной значимой.
10. **Задет объявленный `runtime_paths` (§12.6)** ⇒ в `verify-report.md` блок
   «## Исполнение рисковых путей»: на каждый задетый путь — чем исполнял, что
   увидел, `at=<дата>`. Сборка оракулом не является; статические проверки этот
   класс не видят, потому что платформа отвечает падением, а не кодом ошибки.
   Проверяет `delivery_check` (предупреждение на verify, отказ на handoff).
11. **Класс L:** в `plan.md` есть `## Rejected alternatives` с причиной отказа
   (§12.2a), и причина перенесена в `knowledge/**` как `type: ADR`. Секцию
   проверяет `delivery_check`; перенос в ADR — правило без гейта, живёт пока его
   читают.

### 3.3. Stop-gates (агент останавливается)

| Условие | Действие агента |
|---|---|
| Нет `spec.md` на классе M/L | Не писать prod-код; сначала specify |
| Oracle FAIL | Чинить или эскалировать; **не** «закрыть задачу» |
| Нет human OK на spec (M/L) | Ждать; не переходить к широкому implement |
| Circuit breaker сработал | Стоп; `escalation.md` (§12.3) + blocker в STATUS; ждать человека |
| Противоречие spec ↔ plan | Clarify/analyze; не импровизировать в коде |
| Нужен секрет / prod mutate / force-push | HITL; не обходить |

**Останавливаясь, агент обязан оформить остановку** — `delivery/active/escalation.md`
по §12.3: что пробовал, какое решение нужно, **два варианта с ценой**. Стоп без
этого перекладывает восстановление контекста на человека, то есть съедает ровно то
время, которое контур экономит.

### 3.4. Circuit breakers (дефолты; правятся в STATUS)

| Breaker | Дефолт | Смысл |
|---|---|---|
| `max_files_touched` | 25 за одну implement-сессию | Анти-oneshot |
| `max_loc_diff` | ±800 строк net | Разбиение PR |
| `max_implement_retries` | 3 на одну task | Потом эскалация |
| `max_verify_retries` | 3 | Потом эскалация / сужение scope |
| `max_runtime_paths` | 1 | **§12.6**: сколько объявленных `runtime_paths` задевает одна поставка. Объёмные breaker'ы выше этого не видят: три правки в три подсистемы на десяток строк проходят их не заметив |
| `max_session_minutes` | 90 (мягкий) | Handoff / новый slice |

Waiver: только человек, одной строкой в `STATUS.md`:
`waiver: max_files_touched=40 reason=… by=human:…`.

Сработавший breaker — это остановка, а не повод сразу просить waiver: сначала
`escalation.md` (§12.3), где «поднять лимит» — один из двух вариантов с ценой, а
не единственный предложенный выход.

**Кто проверяет.** `max_files_touched` и `max_loc_diff` — механически:
`python scripts/delivery_check.py --diff-base origin/main` (шаг в CI, §10.4).
Считается дифф `merge-base..HEAD`; **не считаются** процессные артефакты и
механика контура: `delivery/**`, `knowledge/**`, `scripts/lint/**`,
`scripts/merge_guard.sh`, `.pre-commit-config.yaml`, `.github/workflows/**`,
`.claude/**`. Число исключённых файлов печатается — исключение видно, а не
растворено.

**Почему механика контура исключена** (уточнено в v1.25 по полевой находке):
breaker меряет **blast radius продукта**, а развёртывание контура или поставка
`kind: refactor` по контуру (§9.1a их предусматривает) трогают 15+ файлов при
нулевом изменении продукта. Такая поставка пробивала breaker **всегда**, и waiver
становился рутиной — а рутинный waiver обесценивает механизм ровно так же, как
env-обход (§4.3a). Обратная сторона честная: плохой гейт-скрипт может сломать
работу всем, и его объём теперь не ограничен. Против этого работает не breaker, а
то, что дифф читает человек, и мета-гейт §3. Waiver-строка выше
парсится оттуда же: `max_*=N` в STATUS поднимает лимит без правки скрипта.
`max_implement_retries` / `max_verify_retries` / `max_session_minutes` остаются
самонаблюдением агента — счётчика сессии harness не имеет (см. §9.1,
`implement_retries` — поле MANUAL).

### 3.5. Долг во времени: снимок обязан таять, а не стоять

Все гейты смотрят на **один коммит**, а вырождение — это **тренд**, и один коммит
его не показывает. Конкретно: ратчет запрещает снимку **расти** и ничего не говорит
о том, что снимок стоит на месте. Двадцать поставок с неподвижным снимком зелёные
все двадцать, и формально никто ничего не нарушил.

Платить по долгу заставляло только соглашение «тронул легаси — почисти часть», то
есть честность исполнителя. Не заметил или схалтурил — запись живёт вечно, и об этом
никто не узнает, потому что каждый отдельный коммит честный.

**Проверяется на handoff, и только там.** На каждом коммите это блокировало бы
работу, никак не связанную со старым долгом (человек правит опечатку — ему говорят
«легаси не чистится»), и такой гейт отключают через неделю. Предупреждением —
бесполезно: предупреждение, которое ничего не останавливает, читают трижды. Поэтому:
работать не мешаем, **объявить сделанным** не даём.

**Памятью служит история git, а не файл.** Своя «тетрадка» валидатора правится тем
же коммитом, что и нарушение: снаружи всё сойдётся, и подделывать даже не надо —
достаточно пересчитать. Историю так не поправишь, нужно переписать прошлое, а это
видно (и это уже названный остаток — force-push, AGENT_STACK §6). Точки замера —
коммиты, добавившие каталоги в `delivery/archive/`.

Две метрики, потому что одной мало:

| Метрика | Что ловит | Почему одной недостаточно |
|---|---|---|
| **сумма по снимкам за окно** | долг не тает вообще | сумму можно уронить, растопив лёгкие записи, а тяжёлые оставив гнить |
| **запись, прожившая всё окно без движения** | вечного жильца | сама по себе долгая жизнь записи нарушением не является: её могут честно чистить по частям |

Вторая **обязана** различать «гниёт» и «чистят медленно»: признак — вес не
уменьшился ни разу. Запись `40 → 34 → 28 → 22 → 16` живёт всё окно и нарушением не
является; запись `80 → 80 → 80 → 80 → 80` — является.

**Три состояния, и проверка узнаёт их сама, без разрешения от агента.** Иначе это
был бы гейт, который нельзя пройти в конфигурации, разрешённой каноном, — тот же
класс, что F4/F6/F13, и первое, что с ним сделают, — отключат:

1. **долга нет** (снимки пусты или отсутствуют) → проход, у нового проекта чистить нечего;
2. **данных мало** (закрытых поставок меньше окна) → проход, и печатается «накоплено
   K из N, включится на N-й» — чтобы включение не выглядело внезапной поломкой;
3. **накопилось** → судим.

Окно — `DEBT_WINDOW` (дефолт 5 поставок). Долг стоит по делу (переписывание модуля
целиком отложено) — это `blockers:` со сроком, а не waiver: waiver снимает проверку,
а blocker переносит срок и остаётся видимым.

⚠ **Единственный механизм контура, который нельзя проверить полигоном.** Полигон —
одно развёртывание, а здесь нужны двадцать поставок. Логика закреплена регрессией на
**синтетической** истории архива; работает ли она на настоящей — покажет только месяц
работы на живом проекте. Это ограничение названо здесь, а не замолчано.

---

## 4. Thin ABC для coding-агента

Не путать с полным фреймворком Agent Behavioral Contracts — *«Agent Behavioral
Contracts: Formal Specification and Runtime Enforcement for Reliable Autonomous
AI Agents»*, V. P. Bhardwaj,
[arXiv:2602.22302](https://arxiv.org/abs/2602.22302) (сверено 2026-07-27) —
для автономных product-агентов. Здесь — **минимальный** контракт сессии
кодирования: \(C = (P, I, G, R)\).

Хранится в `delivery/CONSTITUTION.md` §Coding-agent contract и дублируется
кратко в `AGENTS.md`.

### 4.1. Preconditions \(P\)

- Известен класс работы S/M/L; прочитан `STATUS.md`.
- Рабочая копия: ветка/feature или worktree (§5.1), не «грязный main» без
  явного разрешения.
- **Перед implement** — артефакты из таблицы §2.2 (S: tasks; M: spec+plan+tasks
  + human_ok_spec; L: то же + human_ok_plan на risky/L).
- До implement на фазах specify/plan/tasks достаточно артефактов *текущей*
  фазы (не требовать tasks на этапе specify).

### 4.2. Invariants \(I\)

- Не коммитить секреты; не писать в `.env` с прод-значениями.
- Не `push --force` в `main`/`master`.
- Не объявлять done при красных oracles.
- Не расширять scope за пределы `spec.md` без записи в spec + human note.
- Одна active-поставка.

### 4.3. Governance \(G\)

- Human OK на spec (M/L) и на plan (L / risky).
- HITL: прод-деплой, миграции данных, удаление больших деревьев, платежные/
  security-инварианты (§10.3).
- Секреты в diff → stop (механический скан — CQG §2.7; процесс — §10).
- Builder не принимает свою работу (§5.2).

### 4.3a. Waiver живёт там, где идёт ревью

У любого гейта есть законные исключения — иначе его начнут обходить целиком. Но
**место** обхода определяет, работает ли гейт дальше:

| Где waiver | Виден ревьюеру | Срок жизни | Годится? |
|---|---|---|---|
| **env-переменная** (`ALLOW_*=1`) | ❌ нет: следа в диффе не остаётся | В CI — только правкой workflow, то есть **навсегда** | Аварийно, локально |
| **Файл конфига** (`STRICT=0` в `.pre-commit-config.yaml`) | ✅ да | **Навсегда** — худший вариант: гейт выключен и об этом забыли | Нет |
| **Строка в `STATUS.md`** | ✅ да, попадает в дифф PR | **Одна поставка** — уходит в archive вместе с ней | **Да, основной** |

Формат — как у circuit breakers (§3.4): `<имя>_waiver: reason=… by=human:…`.
Гейт **печатает причину на каждом прогоне**, чтобы устаревшее исключение бросалось
в глаза (тот же приём, что `not_wired_reason()` в CQG).

Реальный случай (2026-07-28, третий проект): гейт code↔canon уронил PR, где
типизация тронула модули под `implementation:`, а инварианты не менялись. Правка
concept'а была бы **ложным «обновлено»** — свежий `generated.at` создаёт вид
пересмотра канона, которого не было. Единственным обходом был
`ALLOW_CANON_DRIFT=1`: в CI не задать, локально невидим. Теперь оба гейта
(`okf_sync_gate.py`, `check_baseline_ratchet.sh`) читают строку из STATUS.

**Правило для нового гейта:** прежде чем добавить env-обход, ответь — *увидит ли
его ревьюер* и *умрёт ли он вместе с поставкой*. Если нет на любой из вопросов —
waiver должен быть строкой в STATUS.

### 4.3b. Ложное срабатывание — дефект гейта, а не повод его снять

Гейт, стабильно красный на **заведомо безопасных** изменениях, — сломанный гейт.
И он опасен вдвойне: сначала бесит, потом его отключают, а потом никто не помнит,
что он был.

Лечение строго в этом порядке:

| # | Способ | Когда |
|---|---|---|
| 1 | **Сузить область** | Гейт смотрит шире, чем должен: `implementation:` объявлен на каталог вместо файлов; `paths:` в CI отсутствует; область гейта и область измерения разошлись |
| 2 | **Видимый waiver** (§4.3a) | Область верна, но конкретный случай законен: смысл не менялся, рост снимка от переименования |
| 3 | ~~Отключить / `STRICT=0` в конфиг~~ | **Никогда.** Это не решение, а потеря гейта с сохранением видимости |

**Диагностический вопрос:** гейт ругается на то, что он *должен* был проверять?
Если нет — у него неверная область (случай 1), и правка «чтобы позеленело»
(тронуть concept, добавить фиктивный тест) делает хуже: гейт остаётся сломанным,
а его сигнал теперь ещё и врёт.

Два разобранных случая, оба — дефекты гейтов, не кода (2026-07-28, третий проект):

| Гейт | Ложное срабатывание | Корень | Фикс |
|---|---|---|---|
| `okf_sync_gate` | Требовал обновить concept'ы, которых типизация не меняла | Гейт видит «файл тронут», а не «инвариант изменён» — по-другому и не может | Waiver строкой в STATUS (§4.3a) |
| `check_diff_coverage` | Ронял PR на harness-скрипте с «0%» | Файл **вне** измеряемого пакета отсутствует в `coverage.json` — это не «не покрыт», а «не мерили». Область гейта (`LINT_PY_SRC`) разошлась с областью измерения (`--cov`) | Классификация «вне пакета» вместо провала, с печатью списка |

### 4.4. Recovery \(R\)

| Нарушение | Recovery |
|---|---|
| Oracle red | Fix ≤ `max_verify_retries`, иначе эскалация |
| Превышен breaker | Revert/разбить PR; STATUS |
| Scope creep | Вернуть в spec как open question; откатить лишний код или вынести в новую поставку |
| Потеря контекста / compact | Перечитать STATUS + spec + tasks; не продолжать «по памяти» |
| Опасное действие | Abort; описать в STATUS |

### 4.5. Права на действия — крупнейшее слепое пятно контура

Всё выше контролирует **результат**: артефакты фазы, форму кода, утверждения
тестов, зелёный CI. Про **действия** агента контур не говорил ничего: какие
команды он запускает без спроса, куда пишет за пределами репозитория, ходит ли в
сеть. §4.3 называет опасные классы прозой — но проза живёт в контексте модели и
конкурирует с задачей, а права живут в настройках инструмента и не конкурируют
ни с чем.

**Механическая половина существовала всегда, канон про неё молчал.** В Claude Code
это `.claude/settings.json`, ключ `permissions`: списки `allow` / `ask` / `deny`
плюс `additionalDirectories` (куда можно писать вне репозитория). Имена ключей —
из Claude Code; в другом инструменте они другие, но роль та же, и правило ниже от
имён не зависит.

**Полевые данные (2026-07-28, машина, на которой этот контур и писался):**
в пользовательском `settings.json` — **3236 правил в `allow`, ни одного в `deny`
и `ask`**, плюс 11 путей в `additionalDirectories`. Это не политика, а **осадок
от кликов «yes»**: каждое правило выдано однажды под конкретную команду, ни одно
не отозвано, запретов не объявлено вообще. Права ползли **ратчетом вверх** — тем
самым движением, которое контур запрещает снимкам baseline (CQG §8.2). Симметрии
не было, потому что никто не смотрел: у прав не было ни владельца, ни гейта.

Три ведра, и различие между вторым и третьим — не строгость, а **кто платит за
ошибку**:

| Ведро | Что туда попадает | Критерий |
|---|---|---|
| **автономно** (`allow`) | Чтение, локальные прогоны, гейты, git в пределах ветки | Ошибка стоит одного прогона и откатывается сама |
| **с подтверждением** (`ask`) | Push, мерж, установка зависимостей, запись вне репозитория, сеть | Ошибка обратима, но платит за неё **человек своим временем** |
| **никогда** (`deny`) | Прод-мутации, секреты в открытый вывод, `force-push` в main, `rm -rf` вне worktree, история git | Ошибка **необратима** — подтверждение тут не защита, а соучастие |

**Правило: объявлено ↔ подключено.** Источник истины — `delivery/CONSTITUTION.md`,
блок `agent-permissions` (шаблон A.1): в нём построчно перечислены **запреты**
(`deny:`) и **подтверждения** (`ask:`) — теми же строками, что в настройках.
`delivery_check` сверяет блок с `.claude/settings.json` **в обе стороны**:

| Расхождение | Что это значит |
|---|---|
| Правило в каноне, нет в настройках | Запрет **объявлен и не работает** — худший случай: выглядит как контроль |
| Правило в настройках, нет в каноне | Границу двигали молча, без ревью — то же, что переснять baseline вверх |

**`allow` не сверяется** — и это осознанно. Это длинный накопительный список
конкретных команд; байт-в-байт сверка давала бы постоянное ложное срабатывание, а
по §4.3b такой гейт обречён (сначала бесит, потом его снимают). Вместо сверки —
**один вопрос к форме списка**: если `allow` длинный, а `deny` пуст, права росли
только вверх. Это ровно случай выше, и `delivery_check` печатает его как warning.

**Где оседают клики.** Разрешение, выданное на ходу, попадает не в коммит:
`.claude/settings.local.json` (не коммитится) или пользовательский
`~/.claude/settings.json`. Отсюда практическое следствие: **политика — в
коммитимом `.claude/settings.json` и в CONSTITUTION, накопление — в локальных**.
Сверять имеет смысл только коммитимую половину; локальную — считать и показывать.

**Чего это не даёт.** Права ограничивают **инструмент**, а не намерение: агент,
которому запрещён `Bash(psql …)`, может написать скрипт, который делает то же.
Настройки — это забор от случайного, а не от изобретательного. Против второго
работает только то, что дифф читает человек (§5.2, §3.1d уровень 3).

### 4.6. Опасную операцию — в песочницу, а не в HITL

Список HITL (§4.3) не сакрален. Пункт попадает в него не потому, что операция
опасна сама по себе, а потому что **у неё нет оракула**: проверить результат без
человека нечем. Значит пункт можно сдвинуть вправо — но **только добавив оракул**,
не сняв проверку.

Это ровно та торговля, о которой оговорка 1 в `BACKLOG.md`: «больше
самостоятельности» без нового оракула = «меньше проверок», то есть ухудшение под
видом улучшения. Поэтому правило одно:

> **Перевод из HITL в автономию обязан назвать оракул, который заменил человека.**
> Нет оракула — нет перевода. Формулировка «агент аккуратный» оракулом не является.

| Было в HITL | Оракул-песочница | Что остаётся человеку |
|---|---|---|
| Миграция данных | Дамп → эфемерная БД → `up`, затем `down`, затем `up`; сверка схемы и счётчиков | Миграция на **проде**: объём, время блокировки, окно |
| Деструктивная команда (`rm -rf`, `DROP`, `TRUNCATE`) | Обёртка с **dry-run по умолчанию**: печатает список того, что удалит; реальный проход — только с явным флагом | Удаление **вне** worktree и вне эфемерной среды |
| Массовая правка файлов | worktree (§5.1) + `git`: revert бесплатный, blast radius измерен breaker'ом | Ничего — этот пункт закрыт полностью |
| Вызов внешнего API | Запись фикстур и прогон против записи (VCR-подход) | **Первый живой** прогон и смена контракта апстрима |
| Прод-деплой | Staging с теми же гейтами + smoke-evals §6.1 | Сам прод-деплой |

**Как оформляется перевод.** Строкой в блоке `agent-permissions` (§4.5, шаблон
A.1) — там же, где живут запреты, потому что перевод и есть изменение границы:

```text
sandbox: Bash(alembic upgrade:*) oracle=scripts/db_ephemeral.sh reason=миграция гоняется на дампе, схема сверяется
```

`delivery_check` проверяет **существование файла в `oracle=`**. Это не формальность:
объявленная песочница без скрипта — это снятая проверка с видом усиления, и
класс ошибки тот же, что «скрипт лежал в репозитории, никто не вызывал» (§3.1a).
Что скрипт действительно изолирует — гейт не знает и знать не может.

**Что не переводится никогда.** Песочница доказывает **обратимость**. Там, где
ошибка необратима или стоит не времени, а доверия, обратимости нет по определению:

- секреты в открытый вывод (лог, PR, issue) — утечка не отзывается;
- платёжные и security-инварианты — цена ошибки внешняя;
- `force-push` в main, удаление истории или окружений;
- всё, что видит пользователь под нашим именем (письма, публикации).

**Честный остаток.** Песочница проверяет, что команда делает **заявленное** в
изолированной среде. Она не проверяет, что заявленное — то, что нужно (тот же
потолок, §3.1d). И эфемерная копия — **не** прод: объём данных, блокировки и
гонки там другие, поэтому «прогналось на копии» не равно «пройдёт на проде».
Именно из-за этого прод-строки в таблице выше остались за человеком.

---

## 5. Изоляция, роли, anti-oneshot

### 5.1. Изоляция изменений (worktree — default)

**Дефолт для класса M/L:** отдельный git worktree (или эквивалентный sandbox),
чтобы провал агента не портил `main` и параллельные задачи не мешали друг другу.

```bash
# из корня репо (один раз на поставку)
slug="<short-slug>"          # из STATUS.md
git fetch origin
git worktree add "../$(basename "$PWD")-wt-${slug}" -b "feat/${slug}" origin/main
cd "../$(basename "$PWD")-wt-${slug}"
# дальше: delivery/active живёт на этой ветке; oracles гонять здесь
```

Закрытие worktree после merge:

```bash
cd /path/to/main-clone
git worktree remove "../$(basename "$PWD")-wt-${slug}"
git branch -d "feat/${slug}"   # если уже слита
```

| Режим | Когда |
|---|---|
| **Worktree (default M/L)** | Новая фича, рефакторинг, неизвестный blast radius |
| Feature-branch в том же clone | Класс S; или человек явно сказал «без worktree» |
| Прямо на `main` | **Запрещено** по умолчанию |

Опционально усилить: Docker/devcontainer для evals с БД — не обязательно для
bootstrap; достаточно worktree + локальные oracles.

В `STATUS.md` фиксируй: `worktree: ../repo-wt-<slug>` или `worktree: none (S)`.

### 5.2. Builder ≠ Verifier

| Роль | Делает | Не делает |
|---|---|---|
| **Builder** | Пишет код по tasks | Не ставит финальный PASS на verify |
| **Verifier** | Гоняет oracles, читает diff против spec, пишет verify-report | Не «доделывает фичу молча» в том же проходе без новой task |

На практике Verifier = (1) детерминированные команды + (2) отдельный агентский
проход / другая модель / человек. Self-verify alone на классе M/L — **недостаточно**.

**Кто проверяет.** Полностью механически независимость не проверить — но
самопринятие ловится: в `STATUS.md` есть `builder:`, в `verify-report.md` —
`Verifier:`; `delivery_check` падает, если Verifier не заполнен, и если на M/L
он совпадает с builder. Заявление в markdown — аудит-след.

Принуждение зависит от того, что доступно:

| Есть | Механизм |
|---|---|
| Платный тариф / публичный репо | **required review** в branch protection — PR не мержится без второго участника (строго лучше) |
| Бесплатный тариф | `merge_guard.sh` (CQG §8.5) не мержит, пока `delivery_check` красный, а тот требует заполненного и **отличного от builder** Verifier. Плюс дисциплина: verify-проход делает другая модель/сессия/человек |

Не подменяй Verifier на `process:ci`, если оракулов класса C нет: тогда «CI» не
проверил обещание spec, а только форму кода.

### 5.3. Anti-oneshot

- Implement идёт **task-by-task** (или vertical slice из tasks), с verify soft
  после slice.
- Запрещено в одном ходе: «реализуй весь plan».
- Новый файл в prod без строки в `tasks.md` — сначала дописать task.

### 5.4. Ошибка → усиление harness

После каждого провала, который «агент мог бы не повторить»:

1. Запись в `delivery/active/STATUS.md` (или archive lessons).
2. Если паттерн повторится — добавить oracle / breaker / строку в constitution /
   hook. **Не** оставлять только устное «учти».

**Критерий «усилил, а не извинился»:** воспроизведи исходную ошибку и покажи, что
новый oracle её ловит. Пример из первого реального развёртывания контура: гейт был извлечён,
но не подключён к `pre-commit` → добавлен мета-гейт `gate-coverage` (CQG §3) →
проверка: временно отключить гейт, убедиться, что мета-гейт падает с точным
указанием. Закрыт **класс** ошибки, а не один случай.

Итог приёмки контура и разбор таких случаев живут в `delivery/STACK-ACCEPTANCE.md`
(шаблон A.12) — чтобы следующий агент видел не только «что развёрнуто», но и где
процедура уже однажды подвела.

---

## 6. Eval suite / smoke-evals (product oracles)

Это не unit-тесты и не CQG. Это короткий набор **наблюдаемых** проверок:
«система делает то, что обещал spec». Eval harness ≠ agent harness: здесь
фиксируем *успех задачи*, а не стиль кода.

### 6.1. Постоянные (`delivery/evals/smoke/`)

Держи **3–10** сценариев на весь репо. Минимум после bootstrap:

| ID | Пример команды | Expected |
|---|---|---|
| S1 | health/curl или `GET /health` | HTTP 200 |
| S2 | `pytest -q` / `npm test` (CI-эквивалент) | exit 0 |
| S3 | (если есть) migrate на пустой БД | exit 0 |

Формат каталога:

```text
delivery/evals/smoke/
  README.md          # таблица ID → command → expected → timeout
  run.sh             # опц.: прогон всех S* подряд, exit 1 при любом fail
```

Каждый сценарий: **id, command, expected, timeout_sec**. Без «проверить
глазами», если можно автоматизировать.

### 6.2. На поставку (`delivery/active/eval-smoke.md`)

Acceptance из `spec.md` → **3–7** проверяемых пуль (класс M/L). Пример:

```markdown
- [ ] POST /items with valid body → 201 + id
- [ ] duplicate key → 409
- [ ] CMS list shows created item
```

**Verify** = прогон постоянных smoke **плюс** acceptance bullets.
Результат — в `verify-report.md` (класс C oracles).

### 6.3. Когда писать / обновлять evals

| Момент | Действие |
|---|---|
| После plan (M/L) | Набросать `eval-smoke.md` из acceptance |
| Перед implement | Зафиксировать; по возможности 1 failing/red сценарий = цель slice |
| После багфикса «агент снова сломал X» | Добавить **постоянный** S* в `evals/smoke/` (ошибка → oracle) |
| Класс S | Хватает существующих tests/smoke; строка в verify-report |

### 6.4. Как гонять

```bash
# постоянные
bash delivery/evals/smoke/run.sh    # если есть
# или построчно из README

# затем acceptance из active/eval-smoke.md (ручные/скриптовые чекбоксы)
python scripts/delivery_check.py --require-verify   # когда phase>=verify
```

Не дублируй сюда весь pytest — smoke = узкий «система жива + обещание spec».

### 6.5. Реляционные оракулы: проверка без знания правильного ответа

Обычный тест утверждает **значение**: `f(2) == 4`. Если ожидание неверно, тест
закрепит баг — и mutation-гейт его не поймает, потому что мутант будет честно убит
неправильным утверждением. Это дно потолка §3.1d.

Выход: оракулы, которые утверждают **отношения**, а не значения. В них неверное
ожидание спрятать нельзя, потому что ожидаемого значения в них нет.

| Вид | Что утверждает | Пример |
|---|---|---|
| **Инвариант** | Свойство, верное всегда | итог никогда не отрицателен; список отсортирован; сумма частей = целому |
| **Round-trip** | Обратимость | `decode(encode(x)) == x` для любого `x` |
| **Метаморфное отношение** | Как меняется выход при известном изменении входа | добавили один элемент → `count` вырос ровно на 1; удвоили вход → цена удвоилась |
| **Differential** | Две реализации согласны | старая и новая версия на одних входах дают одно и то же |
| **Идемпотентность** | Повтор ничего не меняет | `sync(); sync()` == `sync()` |

Практика:

- **Класс M/L: минимум один реляционный оракул** на поставку — обычно инвариант
  или round-trip. Пишется через property-based раннер (`hypothesis` в Python,
  `fast-check` в TS): он сам ищет контрпример, и это ищущая сторона, а не
  подтверждающая.
- **Рефакторинг: differential — правильный инструмент.** Не «напиши тесты на
  старое поведение» (это снова агент придумывает ожидания), а *прогони обе
  реализации на одних входах и сравни*. Ожидание берётся из работающего кода:

```python
# tests/test_refactor_differential.py — живёт ОДНУ поставку, потом удаляется
@given(payloads=st.lists(st.text(), max_size=50))
def test_new_matches_old(payloads):
    assert new_impl.process(payloads) == old_impl.process(payloads)
```

  Старую реализацию на время поставки держи рядом (`_legacy.py`), удали в converge.
  Это закрывает §3.1b (`refactor` + `weak`) содержательно, а не waiver'ом.

- **Смысл, а не покрытие.** Один инвариант обычно ловит больше, чем десять
  тестов-значений, потому что раннер перебирает входы, о которых автор не подумал.

Чего это не даёт: инвариант можно сформулировать **слишком слабо**
(`assert result is not None`, `len(sort(xs)) == len(xs)` — верно и для функции,
которая не сортирует; `decrypt(encrypt(x)) == x` — верно и для «шифрования»,
возвращающего вход как есть). Формулировку выбирает автор, и это его лазейка.

**Но отличить слабое свойство от работающего гейтом МОЖНО, и это исправление
прежней формулировки** («гейтом не отличить» стояло здесь до `delivery@1.37`).
Работает мутационный гейт: слабое свойство не реагирует на порчу кода, поэтому
мутанты остаются живыми, и доля убитых падает ниже порога. Это видно **числом**, а
не на глаз. Проверка складывается в четыре слоя, и каждый закрывает лазейку
предыдущего:

| Слой | Что закрывает | Чем |
|---|---|---|
| Свойство вместо значения | нельзя списать ответ с кода | §6.5 |
| Генератор входов | нельзя выбрать удобные случаи | `hypothesis` / `fast-check` |
| Мутационный гейт | нельзя заявить свойство, ни на что не реагирующее | CQG `check_mutation_gate.sh` |
| Один якорный пример снаружи | прибивает, **какую именно** функцию построили | §3.1d уровень 1 |

Отсюда практическое следствие: **свойства без работающего мутационного гейта
неполны.** На macOS он молча пропускался до `cqg@1.32`, а нужен `timeout` из
coreutils — то есть слой 3 отсутствовал, и «есть свойство» было ритуалом. Ставить
требование свойств раньше, чем починен mutation, значит вводить проверку, проверять
которую нечем.

Что остаётся человеку: свойство может быть **верным, сильным и не тем**. Это ревью
утверждений (§3.1d уровень 3), и оно дёшево — `assert_digest.sh` показывает только
изменённые утверждения, 12 строк вместо 400.

### 6.5a. Оракул на АРТЕФАКТ: между исходником и продуктом

Каталог §3 CQG судит **форму исходника**: длину, сложность, направление
импортов, дубли, ключи. Тесты судят **поведение**. А между ними у собирающего
проекта лежит третье — **собранный артефакт**, и его до `delivery@1.54` не читал
никто.

**Замер приёмки восьмого развёртывания (Astro/TS).** Пятнадцать ролей CQG
развёрнуты и зелены, CI зелёный, `astro check` проверяет типы, zod — frontmatter.
`dist/` не читает ни одна проверка. Сборка останется зелёной при уехавшей
раскладке, и «зелёный CI» читается как «продукт правилен» — хотя не проверялось
ровно то, ради чего продукт существует.

**Правило.** Проект объявляет шаг сборки (`scripts.build`, цель `build:`) →
у артефакта обязан быть машинный инвариант, объявленный строкой
`artifact_oracle:` в STATUS. Форма та же, что у `runtime_paths` и `repro_test`:
объявление + проверка по ФАКТУ файла, потому что объявленный оракул без скрипта —
снятая проверка с видом усиления (§4.6). `n/a reason=…` законно: «не делали» и
«нечем» обязаны различаться в тексте. На `handoff` класса M/L отсутствие
объявления — ошибка, до него — предупреждение.

⚠ **«Не развёрнуто» и «непроверяемо» — разные вещи, и `weak` их смешивает.**
Тот же замер: слой behavior пуст, но большая часть непокрытого проверяется
**дёшево и без новых зависимостей** — разбор собранного html (мета, hreflang,
JSON-LD), состав `dist/`, контраст как арифметика над токенами палитры,
переключатель языков как функция пути. Дорого стоит только то, чему нужен
layout engine. Поэтому `behavior-oracles: weak` обязан называть, что проверяемо
машиной СЕГОДНЯ и не сделано: `weak — дёшево: <что именно>`. Правило то же, что
§9.1a п.2 даёт гейтам («ни разу не сработал → удалить или написать, почему
держим»): молчащая непокрытость либо закрывается, либо объясняется.

**Чего сюда НЕ брать по умолчанию — визуальные снапшоты.** У них фирменный отказ
ровно нашего класса: «зелёный, потому что снимок перезаписали». Если брать, то с
дисциплиной журнала веса (§9.1a п.5): пересъём — решение с записью и точным
числом, иначе снимок легализует что угодно.


---

## 7. Развёртывание

### 7.1. Способ 1 — через агента (рекомендуется)

Положи `AGENT_DELIVERY_HARNESS.md` в корень проекта и дай команду:

> «Разверни Agent Delivery Harness по `AGENT_DELIVERY_HARNESS.md`: создай
> дерево `delivery/` из Приложения A, заполни `CONSTITUTION.md` под этот
> проект (без дублирования lint-правил CQG и без OKF-спеки), вставь hook
> из A.5 в `AGENTS.md` или `.cursor/rules/`, положи `scripts/delivery_check.py`
> из Приложения B. Впиши версии канонов (`stack: delivery@1.18, …`) в constitution и
> `STATUS.md`; проставь `ci-oracles:` по факту (§10.4). Если в корне уже есть
> `CODE_QUALITY_GATES.md` / `OKF_KNOWLEDGE_BUNDLE.md` — не копируй их содержимое;
> только проставь указатели в constitution и соблюди порядок §0.1. CQG и OKF **не**
> разворачивай в этом шаге, если человек отдельно не попросил — сначала
> каркас delivery.»

Greenfield «с нуля под полный контур» — отдельная команда человека, **после**
успешного шага 1:

> «Теперь по порядку §0.1 разверни CODE_QUALITY_GATES, затем OKF_KNOWLEDGE_BUNDLE.»

### 7.2. Способ 2 — вручную

1. Создай **все файлы дерева §2.3** — оно источник истины, а не номера приложений:

   | Файл | Шаблон | Когда обязателен |
   |---|---|---|
   | `delivery/CONSTITUTION.md` | A.1 | Всегда |
   | `delivery/README.md` | A.6 | Всегда |
   | `delivery/STACK-ACCEPTANCE.md` | A.12 | Всегда (заполняется на приёмке §6) |
   | `delivery/active/.gitkeep` | — | **Всегда** (иначе пустой `active/` исчезнет из git, §2.3a) |
   | `delivery/active/STATUS.md` | A.2 | Всегда |
   | `delivery/active/spec.md` | A.3 | Класс M/L |
   | `delivery/active/plan.md` | A.7 | Класс M/L |
   | `delivery/active/tasks.md` | A.4 | Всегда |
   | `delivery/active/verify-report.md` | A.8 | С фазы verify |
   | `delivery/active/eval-smoke.md` | A.9 | Класс M/L с verify |
   | `delivery/active/decisions.md` | A.14 | Всегда (§12.2) |
   | `delivery/active/diagnosis.md` | A.15 | Только `kind: bugfix` (§12.1) |
   | `delivery/active/escalation.md` | A.16 | Только при непустых `blockers:` (§12.3) |
   | `delivery/archive/INDEX.md` | A.13 | Всегда (§2.2a) |
   | `delivery/archive/<slug>/observed.md` | A.17 | На каждую архивную поставку (§13.1) |
   | `delivery/evals/smoke/README.md` | A.9 | Всегда |

   ⚠ **Эта таблица под регрессией** (`tests/test_deployment_procedure.py` репозитория
   канонов): она сверяется с деревом §2.3 в обе стороны, каждая строка
   разворачивается в одноразовый репозиторий, дальше гоняется приёмка. Правишь
   таблицу или дерево — держи их согласованными, иначе тест назовёт расхождение
   строкой. До этого процедуру проверял только живой полигон, а полигоны
   одноразовые: следующее развёртывание начинало с нуля и повторяло уже закрытые
   находки. Первый же прогон нашёл три файла дерева, выпавших из инвентаря
   извлечения, — тот же класс, что описан ниже, просто на уровень глубже.

   **Почему списком файлов, а не «A.1–A.11»:** так и было до v1.25, и первое же
   реальное развёртывание это поймало. За двенадцать доработок приложений стало
   семнадцать, а число в процедуре осталось прежним — шесть шаблонов выпали из
   развёртывания молча. Номер не говорит, что внутри, поэтому пропуск не виден ни
   при развёртывании, ни при чтении DoD. Дерево §2.3 растёт вместе с артефактами;
   число в прозе — нет.

2. Заполни constitution под продукт (10–20 пуль).
3. **Создай `.claude/settings.json` из блока `agent-permissions`** шаблона A.1
   (§4.5) — те же строки `deny`/`ask`. Шаблон приезжает с **заполненным** блоком,
   поэтому без этого шага гейт прав, а развёртывание нет: свежий контур падал на
   «объявляет `agent-permissions`, а настроек нет» в каждом новом проекте.
4. Вставь A.5 в agent instructions.
5. Скопируй Приложение B → `scripts/delivery_check.py`, Приложение C →
   `scripts/delivery_metrics.py`, `chmod +x`.
6. **Сам bootstrap оформляется как `kind: bootstrap`** — его объём breaker'ом не
   мерится (§3.4): развёртывание трогает всё дерево контура и не режется на части,
   а его DoD — §7.3 плюс приёмка §6. Приёмочное развёртывание намерило 30 файлов и
   2437 строк **после** исключения механики контура: без этого правила единственным
   выходом был рутинный waiver, то есть гейт учил себя обходить.

   **Финал bootstrap'а — мерж в main, и он идёт ДО архивации и ДО следующей
   поставки:** `merge_guard.sh <ветка> main`, ПОКА bootstrap — активная поставка.
   Исключение breaker'а привязано к `kind:` активной поставки и архивацию не
   переживает: у следующей поставки класса S весь незамерженный bootstrap ложится
   в её дифф — 34 файла / 12 585 строк при собственных 3 / 190, и совет гейта
   «split the PR» невыполним, потому что резать нечего (lab-11 F12, обе арки
   независимо). Пять правил — «kind: bootstrap», «одна active-поставка», «архивация
   на handoff», «нет человека → deferred», «merge_guard судит против main» — по
   отдельности верны, а их композиция без этого порядка даёт тупик. После
   squash-мержа следующая ветка отводится от СВЕЖЕГО main: ветка от старой базы
   несёт уже слитое повторно, и мерж красен конфликтом по построению — выход
   `git rebase --onto main <старая-база> <ветка>` (цена squash, CQG §8.8;
   lab-11 F13). Непустой стартовый дифф ловит `delivery_check` на
   specify/plan/tasks — дедлок на входе стоит минуту, на handoff — полный цикл.

   **Дефект, найденный развёртыванием в СУЩЕСТВУЮЩЕМ коде, bootstrap не
   останавливает** (lab-11 F8: строгий рантайм нашёл чужую утечку соединений →
   merge_guard честно красен → main отстаёт → следующая поставка мерит чужой
   дифф → waiver агенту запрещён §12.3 — дедлок, в котором каждый шаг прав).
   DoD bootstrap'а — «существующий сьют зелен ПОД контуром», и к этому два
   законных пути: починить в объёме bootstrap'а, если правка точечная, или
   отложить ТОЧЕЧНО — трёхполевой `filterwarnings`-ignore на конкретное
   предупреждение конкретного модуля (форма — CQG §6, там же почему двухполевая
   не гасит) с `TODO` и первой строкой в spec следующей поставки. Глобальное
   ослабление строгого рантайма — снятая проверка, так нельзя; стоять — хуже
   всего: стоящий bootstrap блокирует все последующие поставки.
7. Для первой поставки продукта: `STATUS.md` (`phase: specify`, `class: …`) + черновик
   `spec.md` или пустые скелеты plan/tasks.
8. Commit: `chore(delivery): bootstrap agent delivery harness`.

⚠ **Извлекаешь шаблоны скриптом — считай глубину фенсов.** Шаблон A.1 содержит
вложенный ```` ```text ```` (блок `agent-permissions`), и наивная регулярка вида
`` ```markdown(.*?)``` `` обрывается на первом внутреннем фенсе, теряя ровно тот
блок, который требует §4.5, — а дальше гейт ругается так, будто виноват
исполнитель. Сканируй построчно со счётчиком глубины, как `blocks()` в
`stack_selftest.py` (AGENT_STACK §7).

### 7.3. Definition of Done для bootstrap harness

- [ ] `delivery/CONSTITUTION.md` существует и не копирует CQG/OKF дословно
- [ ] `delivery/active/STATUS.md` с `phase:` и `class:`
- [ ] Hook A.5 в agent instructions
- [ ] `scripts/delivery_check.py` запускается
- [ ] `delivery/evals/smoke/README.md` (+ опц. `run.sh`) созданы
- [ ] `delivery/active/decisions.md` и `delivery/archive/INDEX.md` созданы
      (§12.2, §2.2a) — оба обязательны в любой поставке
- [ ] `delivery/active/.gitkeep` закоммичен — проверь `git ls-files delivery/active/`;
      без него первая же архивация уносит каталог из дерева (§2.3a)
- [ ] `.claude/settings.json` создан и совпадает с блоком `agent-permissions`
      в constitution (§4.5) — сверяет `delivery_check`
- [ ] В constitution есть указатели на CQG/OKF **если** они уже в репо; иначе
      явный backlog «шаг 2/3 §0.1»
- [ ] В constitution и `STATUS.md` записаны версии канонов
      (`stack: delivery@<version>, cqg@…, okf@…`) — какую ревизию канона развернули
- [ ] CI-джоба (§10.4) настроена **или** в STATUS стоит `ci-oracles: weak`
      с явным пунктом backlog
- [ ] ⚠ **Branch protection подтверждён человеком** (CQG §8.5) — ручной шаг вне
      репозитория, который никакой скрипт не проверит. Не подтверждён →
      `ci-oracles: weak`, а не «deployed»
- [ ] ⚠ **Раскладка путей настроена** (CQG §6, «Первый прогон»): каждый гейт
      показывает непустое число просканированных файлов. Зелёный гейт, который
      ничего не видит, — хуже красного
- [ ] (Опц.) hooks §10 / A.11 подключены к агенту — `hooks:` в STATUS/constitution

---

## 8. Brownfield и стыковка

### 8.1. Уже есть хаотичные `docs/` и агент «просто кодит»

1. Развернуть этот harness (§7).
2. Новую работу — только через `delivery/active`.
3. Потом CQG (ratchet, не big-bang).
4. Потом OKF (канон + ratchet из плоских docs — по OKF-канону).

### 8.2. Уже есть CQG и/или OKF

1. Delivery поверх (§7.1).
2. В DoD verify включить: `pre-commit` / CQG-скрипты; OKF update при смене
   инвариантов.
3. Не переносить lint-правила в constitution.

### 8.3. Уже есть product-agent contracts (ABC)

Оставить их в коде/доках продукта. Coding thin-ABC (§4) — отдельно, в
`delivery/`. Не сливать в один файл.

---

## 9. Observability / метрики (чтобы harness улучшался)

Без цифр harness не улучшается — остаётся «папка с md». Снимается **раз на
поставку**, автоматически:

```bash
python scripts/delivery_metrics.py --base origin/main --write
```

Скрипт (Приложение C) считает из git всё, что считается, и вставляет блок A.10 в
`verify-report.md`; поля, которые git не знает, помечает `MANUAL` — их дозаполняет
агент/человек. `delivery_check` **не даёт закрыть handoff** без этого блока, поэтому
ритуал §9.2 перестал быть добровольным.

### 9.1. Базовый набор

| Метрика | Как считать | Зачем |
|---|---|---|
| `time_to_accepted_spec` | часы/дни от первой draft spec → `human_ok_spec: yes` | Узкое место в clarify? |
| `implement_retries` | число «починил после fail oracle» на task | Слабый plan/tasks |
| `verify_fails_before_green` | сколько красных прогонов до PASS | Слабые evals / oneshot |
| `files_touched` / `loc_diff` | `git diff --stat` | Ломаются ли breakers §3.4 |
| `rework_after_done` | коммиты/PR после объявленного done до merge | Не было Verifier |
| `harness_hardened` | yes/no: добавлен oracle/hook/breaker после ошибки | Растёт ли система |
| `est_token_or_cost` | грубо: сессии × модель (если видно) | Дорогие oneshot-петли |

**Что считается само** (`delivery_metrics.py`): `files_touched`/`loc_diff`,
`commits`, `time_to_accepted_spec` (от появления `spec.md` до `human_ok_spec: yes`
в истории STATUS), `rework_after_done` (коммиты после первого `phase: handoff`),
`harness_hardened` (тронуты ли `scripts/lint/**`, `delivery/evals/**`, hooks, CI).
**MANUAL:** `implement_retries`, `verify_fails_before_green`, `est_token_or_cost` —
их знает только сессия/биллинг, и выдумывать их скрипт не будет.

Опционально на уровне команды (не обязательно в md): cost per merged PR,
time-to-merge — из Git hosting / биллинга IDE.

### 9.1a. Ратчет на сам контур: гейты должны уметь уходить

Контур умеет затвердевать и не умеет худеть. За одну сессию доработок гейтов стало
10 → 17, и каждый добавлялся по разбору настоящей ошибки, то есть обоснованно.
Асимметрия всё равно опасна: процессные системы умирают не от слабости, а от веса.

Ратчет на код мы прибили («снимок только вниз»). Симметричное правило на контур:

**1. Бюджет.** Лимит — **число, а не «сколько понадобится»**. Пока гейтов меньше
лимита, добавляются свободно; следующий сверх лимита — только вместе с удалением
одного **или письменным обоснованием**. Число не сакральное; сакрально то, что оно
есть. Текущий счёт **и текущий лимит** держатся в **одном** месте — заголовок
каталога CQG §3; здесь чисел сознательно нет, чтобы счётчики не расходились.

⚠ **Бюджет исчерпан и был исчерпан всё время с 2026-07-29.** Это не авария, а
рабочее состояние, ради которого правило и вводилось: дальше контур растёт только
через обмен или через **названную цену**.

**Единица счёта не пересматривается под результат.** Пять правил
`check_grep_gate.sh` лежат в одном файле, но это пять гейтов, потому что у каждого
свой снимок, порог и сообщение. Пересчитать «по файлам» и получить свободные слоты
технически можно; это будет подгонка метрики, а не уборка. Правило проверено
работой: 2026-08-02 разбор lab-12 предлагал новое AST-правило как «слот: 0» —
именно этот абзац показал, что оно стоит слота, и лимит пришлось поднимать
**решением с записью**, а не считать правку бесплатной.

**Поднятие лимита — законный ход, и он обязан выглядеть как решение.** В записи
(CQG §3, при развёртывании — `delivery/STACK-ACCEPTANCE.md`) обязаны быть четыре
вещи, иначе это размытие правила, а не его применение: **кто** занял слот; **за
что заплатили** — какой класс дефектов и чем доказано, что оракула на него не
было; **замер**, что новый гейт ловит этот класс и не шумит на легаси; **почему не
обмен** — то есть чем плохи кандидаты на выбытие прямо сейчас.

**2. Гейт обязан платить за себя.** Раз в 10 поставок — сверка по данным
(`check_gate_value.sh`, CQG §3):

| Сколько раз сработал | Что делать |
|---|---|
| Ни разу за 10 поставок | **Кандидат на удаление.** Либо удалить, либо написать `gate_kept_reason:` — почему держим (например «страховка от класса, который дорог в проде») |
| Только на форме, ни одного настоящего бага | Понизить: перевести в warning или в CI-only, снять с коммита |
| Ловил настоящие дефекты | Оставить как есть |

**3. Причина хранения печатается.** Если гейт держат без данных, `gate_kept_reason:`
выводится на каждом прогоне — как `not_wired_reason()` в мета-гейте. Тихо
хранящийся бесполезный гейт хуже отсутствующего: он тратит время и учит обходам.

**4. Удаление — обычная поставка.** `kind: refactor` по контуру: удалил гейт →
записал в `STACK-ACCEPTANCE.md`, что удалено и на каких данных. Без записи это не
уборка, а потеря памяти.

**Почему это не отговорка от новых гейтов:** правило требует не «не добавляй», а
«знай цену». Гейт, который ловит реальные дефекты, проходит бюджет всегда.

**5. Вес контура — тот же ратчет, что и на число.** Бюджет на количество гейтов
не держит вес: число стоит на 22 с 2026-07-29, а механика всё это время росла
внутри существующих файлов. Замер на 2026-08-07: **48 скрипт(ов), 8820 строк**,
шесть свыше 300, рекорд — `delivery_check.py` (1948). Правило симметрично коду:

- **потолок на скрипт = размер на день съёмки**, снимок только вниз;
- **новый скрипт контура — не выше 300 строк**, то есть по §2.1, который контур
  требует от чужого кода;
- ужатие законно всегда и разрешения не требует;
- **рост потолка — законный ход, но он обязан выглядеть как решение**: пересъём
  виден диффом снимка и называется строкой changelog «вес: +N строк, за что» —
  ровно как поднятие лимита гейтов в п.1. Найдено первым же применением правила:
  оракул покраснел на правке, которая его и вводила (комментарий в
  `check_file_length.sh` вырос на две строки), а что делать с законным ростом,
  первая редакция не говорила — снимок «только вниз» без описанного хода вверх
  либо остановит работу, либо будет переснят молча.

⚠ **Правило зашито механикой, иначе оно покупается одной командой** (`cqg@1.84`).
Ратчет, который переснимается `--generate`, держит ровно до первого раза, когда
мешает. Поэтому:

1. **Снимок — журнал, а не потолок.** Он обязан совпадать с деревом ТОЧНО: и
   рост, и ужатие без пересъёма красные. Потолок выше живого размера — это
   молчаливый запас, в который скрипт отрастает обратно, и никто не замечает.
2. **Планка нового судится по ИСТОРИИ** (снимок из `HEAD`), а не по рабочему
   файлу, который правит тот же коммит. Иначе новый скрипт на 400 строк
   легализуется пересъёмом в ту же секунду, когда планку впервые применяют.
3. **Число в записи сверяется с фактом.** Прирост против истории требует строки
   «вес: +N строк, за что», где N — настоящая дельта: «немного подросли» прячет
   цену так же, как отсутствие записи. Первым, кого поймала эта сверка, был абзац
   журнала о ней же — стояло +113 при фактических +119.
4. **Новых монолитов не заводим совсем.** Функция сверх §2.1, которой не было в
   истории, — красный без обхода. Перенос при разрезе монолитом не считается,
   если имя и обе величины не изменились; выросшая при переезде — считается,
   потому что рост прячут именно так.

**И вторая величина, без которой первая покупается перекладыванием: ФУНКЦИЯ.**
§2.1 говорит прямо — файл на 300 строк из одной функции хуже файла на 500 из
десяти, значит скрипт можно ужать, не тронув ни одного монолита. Поэтому второй
снимок — по функциям, с hard-порогами §2.1 (длина 80, сложность 10) и той же
дисциплиной «только вниз». Замер 2026-08-07: **136 функций, 21 сверх порога**;
рекорд — `delivery_check.py::main`, **965 строк и cx=211**, то есть половина файла
в одной функции. Девять из двадцати одной — в `contour_doctor.py`, и это опять
проверяющий слой. Отсюда порядок резки: `main` режется по фазам, а не по размеру.
Сложность считается прокси на stdlib-ast, а не ruff `C901` — оракул, который молча
не запускается без внешнего инструмента, был бы ровно тем классом, который контур
и ловит; равенства с `C901` числа не обещают, сравнимости между прогонами хватает.

⚠ **Быстрее всего растёт проверяющий слой, и это надо знать, а не удивляться.**
`contour_doctor.py` за одно развёртывание прошёл 591 → 940 → 1082 строки, три
ревизии из тринадцати чинили самого доктора, и три дефекта правки `cqg@1.80` были
дефектами проверки, а не проверяемого. Слой, который судит, имеет собственную
частоту дефектов — ратчет веса на него распространяется в первую очередь.

**Замер живёт в репозитории канона** (`tests/test_contour_size.py`, снимок
`tests/contour_size_baseline.txt`), а не в развёрнутом проекте: размер payload —
свойство канона, в проекте эти файлы не меняются, а если меняются — это адаптация,
и её объявляет `adapted.json` (CQG §5.5). До этого правила `check_file_length.sh`
исключал контур из гейта длины со ссылкой на «отдельный замер», которого не
существовало нигде: обещание без механики — тот же класс, который контур ловит у
чужого кода.

### 9.2. Ритуал

1. На handoff — прогони `delivery_metrics.py --write`, дозаполни MANUAL-поля
   (без блока метрик `delivery_check` не пропустит handoff).
2. Если `harness_hardened: no` при `verify_fails_before_green ≥ 2` — **в том же
   PR/следующей поставке** добавь smoke S* или breaker/hook (§5.4).
3. На handoff — впиши строку поставки в `delivery/archive/INDEX.md` (A.13) при
   переносе в `archive/`. «Раз в несколько поставок просмотри archive» было
   ритуалом без адреса и не выполнялось ни разу; теперь чтение адресовано диффом
   (§2.2a), а полнота индекса проверяется механически.
4. Повторяющиеся fails по индексу — сигнал усилить §3/§6/§10, а не добавить ещё
   одну строку урока.

---

## 10. Agent hooks (enforcement stop-gates)

Текст в `AGENTS.md` агент иногда игнорирует. **Hook** — срабатывает на событии
инструмента (до/после edit, stop) и может **заблокировать** ход.

Полный runtime зависит от IDE (Cursor hooks, Claude Code `hooks`, и т.д.).
Ниже — **контракт поведения**; конкретный JSON/shell — Приложение A.11
(адаптируй под свой агент).

### 10.1. Минимальный набор hooks

| Hook | Когда | Поведение |
|---|---|---|
| **stop-on-red** | Перед объявлением done / Stop | Если `pre-commit` / `delivery_check` / smoke fail → **не** завершать успешно; вернуть stderr агенту |
| **protect-main** | PreToolUse shell/git | Блок `push --force` в main/master; блок commit прямо на main без waiver |
| **secrets-scan** | PreCommit / перед push | Делегировать CQG / gitleaks / detect-secrets (§ CQG 2.7) |
| **circuit-breaker** | Post-edit или stop | Если `git diff --stat` > `max_files_touched` / LOC — предупреждение или блок до human waiver в STATUS |

### 10.2. Правила

1. Hook = детерминированная команда (exit 0/1), не «спроси LLM».
2. `STRICT=0` / soft — только с явного human waiver (как в CQG).
3. Не дублируй в hooks весь CQG — вызывай `pre-commit run` / уже существующие скрипты.
4. После установки hooks — строка в constitution: `hooks: deployed (cursor|claude|…)`.

### 10.3. HITL / security (процессная часть)

Механический «секрет в git» — **CQG §2.7**. Здесь — **когда остановиться**:

- Prod deploy, irreversible data migration, удаление больших деревьев, платежи,
  ротация ключей → **HITL**, не «агент сам».
- Обнаружен секрет в diff → stop + человек; не «почистим потом».

### 10.4. CI — необходимый слой (локальные гейты обходятся)

Hooks и pre-commit живут **на машине агента** и обходятся тремя способами:
`git commit -n`, `STRICT=0`, «hook не установлен в этом клоне». Пока те же
проверки не гоняются **вне** этой машины, «Done = oracles» (§1.4) держится на
добросовестности исполнителя — то есть на самооценке, которую harness как раз
и запрещает.

**Инвариант:** каждый oracle, упомянутый в DoD, должен иметь **CI-эквивалент**
на ветке поставки. CI — не «дубль для красоты», а единственный неподделываемый
прогон.

| Слой | Локально | В CI | Комментарий |
|---|---|---|---|
| **A. Shape** | pre-commit (быстро, на staged) | `pre-commit run --all-files` | CI видит всё дерево, а не staged-файлы |
| **B. Behavior** | затронутые тесты | полный сьют + `check_diff_coverage.sh` | diff-coverage — ручной локально, обязательный в CI |
| **C. Product** | `delivery/evals/smoke/run.sh` | тот же `run.sh` | если нужен БД/сервис — services в CI-джобе |
| **Delivery-фазы** | `delivery_check.py` | `delivery_check.py --require-spec` | ловит «implement без spec» до ревью |
| **Secrets** | secrets-хук | тот же хук + scan истории ветки | soft-режим в CI запрещён (CQG §2.7) |

**Правила**

1. **`STRICT=0` в CI запрещён.** Soft-режим — только локальная аварийная мера,
   и она обязана быть видна в PR. CI гоняет строго.
2. **CI обязателен для класса M/L.** Для S достаточно локальных гейтов + CI на
   PR, если он уже настроен.
3. **Красный CI = stop-gate** (§3.3): не «домержим и починим», не rerun «а вдруг
   позеленеет» без разбора причины.
3a. **Гейт мержа не зависит от тарифа хостинга.** Серверная защита ветки платная
   (GitHub: rulesets — Team+, branch protection приватного репо — Pro+), поэтому
   база принуждения лежит **в репозитории**: мерж только через `merge_guard.sh`
   (гейты на слитом состоянии), тяжёлые проверки на `pre-push`, downstream-джобы
   через `needs:`. Детали и скрипт — CQG §8.5. Защита ветки — усиление сверху,
   не предпосылка.
4. **Нет CI (нет hosting / офлайн-репо)?** Тогда в `STATUS.md`
   `ci-oracles: weak`, и Verifier (§5.2) обязан прогнать `pre-commit run
   --all-files` + полный сьют + smoke **в чистом клоне** и вложить вывод в
   `verify-report.md`. Это компенсация, а не эквивалент.

   **Форма компенсации — проверяемая, три части сразу** (`delivery@1.35`):

   | Часть | Где | Зачем |
   |---|---|---|
   | `clean_clone_run: <что прогнал> at=<дата>` | `verify-report.md` | заявление превращается в артефакт |
   | непустые `blockers:` | `STATUS.md` | отсутствие CI названо блокером, а не привычкой |
   | `active/escalation.md` | дерево §2.3 | §12.3 требует там два варианта с ценой |

   **Почему это отдельное правило, а не «гейт стал мягче».** До `delivery@1.35`
   компенсация жила прозой и не проверялась ничем, а `delivery_check` на классе L
   знал единственный выход — human waiver. Автономное развёртывание в проект без
   хостинга **не имело честного способа** сделать гейт зелёным: waiver требует
   человека, занижать класс запрещено §2.2b, а `blockers:` + `escalation.md` —
   выход, который §2.2b прямо называет законным, — гейт не принимал. Рядом лежали
   две дешёвые лазейки: занизить класс до M или написать `waivers: ci`. То есть
   канон **учил обходить себя законным с виду способом** — ровно то, против чего
   написана §2.2b, и асимметрия была видна в собственном выводе: для `human_ok_*`
   третье значение сделали, для `ci-oracles` нет. Нашло восьмое развёртывание,
   пройдя всеми законными путями и получив красный гейт.

   Компенсация **не делает `ci-oracles` зелёным**: значение остаётся `weak`,
   предупреждение печатается громко, а строка приёмки §6 закрывается как `weak`, а
   не как `auto`. Разница с `deferred` (§2.2b) принципиальна: подпись человека можно
   догнать на мерже, а отсутствие хостинга агент закрыть не может вообще — поэтому
   это не долг с датой, а названный остаток.
5. Конкретный workflow (GitHub Actions) — **CQG §8**; здесь только контракт
   «что обязано гоняться».

**Три состояния `ci-oracles:` в STATUS** (не два — иначе бесплатный тариф вечно
числится «weak» и waiver превращается в формальность):

| Значение | Когда | Класс L |
|---|---|---|
| `deployed` | CI + **серверный** гейт мержа (required checks / protected branch) | ок |
| `tooling` | CI есть; гейт мержа — в репозитории (`merge_guard.sh` + pre-push + `needs:`), серверного нет по тарифу | ок; в STATUS одной строкой отметить остаток: force-push и админ-обход не закрыты |
| `weak` | CI нет вообще | нужен human waiver |

⚠ **Это значение — заявление, и оно ОБЯЗАНО быть проверяемым.** `deployed` и
`tooling` означают «CI существует **и зелёный**», а не «workflow-файл добавлен».
Реальный случай (2026-07-28): в STATUS стояло `tooling`, CI был красным шесть
прогонов подряд, две поставки закрылись как done — потому что значение писал агент,
а сверять его было нечем.

Механическая проверка — `check_ci_status.sh` (CQG §3), подключён последним в
`merge_guard.sh`: последний прогон на ветке обязан быть `success` **и относиться к
текущему коммиту**. Нет `gh`, нет прогонов — гейт даёт честный WARNING, а не тишину.

Правило для агента: **прежде чем писать `ci-oracles: tooling`, посмотри на прогон**
(`gh run list --workflow quality --branch <ветка>`). Если он красный, правильное
значение — `weak` плюс blocker в STATUS, и поставку закрывать нельзя.

Строка в constitution после настройки:
`ci: deployed (github-actions) merge-gate: server|tooling`.

⚠ **Первый прогон: круг, который канон замыкал сам** (`delivery@1.52`). Правило
выше велит писать `deployed` **по факту зелёного прогона**, а `delivery_check
--require-ci` стоит внутри того самого прогона (§8.3, джоба `delivery`). Пока
значение честное (`weak`), прогон красный; пока прогон красный, значение не может
стать честным. Замерено на восьмом развёртывании: агент вышел из круга флипом в
том же коммите — законно, но канон об этом молчал, и оба видимых выхода выглядели
как обход собственного правила.

**Развязка — по смыслу флага:** он защищает не «каждый прогон», а ЗАКРЫТИЕ
поставки. До `handoff` `weak` при `--require-ci` даёт **warning** с единственным
названным ходом: поставь `deployed` по факту ЭТОГО прогона, в том же коммите. На
`handoff` это по-прежнему **ошибка** — иначе развязка была бы снятием требования.
Реальность (прогон исполняется) старше записи, и спорить с ней значит требовать
вранья в одну или другую сторону.

---

## 11. Skills / prompts catalog (указатель)

Промпты и agent skills **не живут внутри этого канона** (и не раздувают OKF
эпиками). Каталог в репо:

```text
skills/                    # или .cursor/skills / prompts/
  README.md                # индекс: имя → путь → когда звать
  <skill-name>/
    SKILL.md               # инструкция
    scripts/…              # опц. детерминированные шаги
```

| Правило | Где enforced |
|---|---|
| LLM-промпт >5–10 строк не inline в `.py` | **CQG** `inline-prompt` |
| Индекс skills + «когда звать» | `skills/README.md`; указатель в [AGENT_STACK.md](AGENT_STACK.md) |
| Стабильный skill как сущность домена | Опц. OKF `type: Reference` → путь в `skills/` |

Агент: не копируй промпты в `delivery/active/`; ссылайся на skill по имени.

---

## 12. Три журнала: как искал, что выбрал, чего просит

Всё до этой секции фиксирует **результат**: артефакты фазы, форму кода, зелёный
прогон, права на действия. Из результата не видно **поведения** — как агент шёл к
нему. Практические следствия, каждое стоит времени человека:

| Не видно | Чем это оборачивается |
|---|---|
| Как искали баг | Агент ходит по кругу; человек, подключаясь, начинает поиск с нуля |
| Почему выбран этот подход | Через месяц никто не знает, был ли выбор; отброшенные варианты предлагаются снова |
| Чего именно агент просит, встав | «Разбирайся, что тут произошло» вместо решения за минуту |

Три журнала закрывают ровно эти три. Формат у всех одинаково жёсткий в одном:
**строка обязана содержать вердикт или цену**, иначе это не журнал, а список
намерений.

### 12.0. Сначала о весе — правило §9.1a применяется и к артефактам

Бюджет гейтов (20) придуман потому, что «процессные системы умирают не от
слабости, а от веса». К артефактам это относится ровно так же, а обязательных
файлов в поставке уже восемь. Поэтому:

| Журнал | Когда существует | Всегда ли |
|---|---|---|
| `diagnosis.md` | Только `kind: bugfix` (или `hotfix`) | **Нет** — условный |
| `escalation.md` | Только когда агент **встал**: blocker, сработавший breaker, stop-gate | **Нет** — условный |
| `decisions.md` | Одна строка на выбор, по ходу поставки | Да, но пустой файл законен для S |

Два из трёх в нормальной поставке **не появляются вообще**. Это не оговорка, а
условие, на котором они добавлены: журнал, обязательный всегда, превратился бы в
ритуал заполнения — как §9.2 «раз в несколько поставок просмотри archive» до
§2.2a. Если в проекте `diagnosis.md` появляется в каждой поставке — это данные о
проекте (слишком много багфиксов), а не о канонe.

### 12.1. `diagnosis.md` — журнал гипотез (kind: bugfix)

§3.1c требует `repro_test` — доказательство, что баг найден и не вернётся. Он
ничего не говорит о том, **как искали**, а именно там теряется время: агент
проверяет одну и ту же гипотезу трижды, потому что не помнит, что уже
опроверг.

Формат — одна строка на гипотезу, обязательно с вердиктом:

```text
- H1: кэш возвращает stale-значение → проверил: TTL=0 в тесте → ОПРОВЕРГНУТА
- H2: два воркера пишут в один ключ → проверил: лог показывает два PID → ПОДТВЕРЖДЕНА
```

**Вердикт обязателен механически.** Журнал гипотез без вердиктов — это список
подозрений, он не экономит ничего: следующий читатель обязан перепроверить всё.
`delivery_check` требует хотя бы одну строку со словом `ОПРОВЕРГНУТА` /
`ПОДТВЕРЖДЕНА` (или `refuted` / `confirmed`).

Не нужен, когда баг найден сразу: `diagnosis: n/a reason=…` в STATUS — та же
форма, что у `repro_test` (§3.1c), и по той же причине: «не искал» и «нашёл
мгновенно» должны различаться в тексте, а не в тишине.

### 12.2. `decisions.md` — одна строка на выбор

Не проза, а greppable-лента. Требование к строке: **что выбрано, вместо чего, и
почему** — три части, иначе строка не считается решением:

```text
- 2026-07-29 · выбрал polling вместо webhook — потому что у провайдера нет
  подписи запросов, проверка подлинности встала бы дороже задержки в 60с
- 2026-07-29 · выбрал денормализацию счётчика вместо COUNT(*) — потому что
  запрос в горячем пути, 40мс против 3мс на 200k строк
```

Проверка формата, а не наличия смысла: строка обязана содержать «вместо» и
«потому что» (или `instead of` / `because`). Строка «выбрал polling» —
**ошибка**: без альтернативы это не выбор, а сообщение о том, что сделано, и
аудит по нему невозможен.

**Связь с индексом архива (§2.2a).** `decisions.md` — сырой след, он умирает с
поставкой. На handoff то, что оказалось **уроком о системе**, переносится строкой
в `archive/INDEX.md`. Разница: решение — «я выбрал X»; урок — «мы узнали, что
система ведёт себя так». Второе переживает поставку, первое нет.

### 12.2a. Отклонённые варианты — след в `plan.md`

`plan.md` фиксирует выбранный подход и молчит про отброшенные. Следствие: агент
через месяц предлагает **тот же тупик**, потому что причина отказа нигде не
записана, а сам подход выглядит разумным — он и был разумным, пока не выяснилось,
почему нет.

Секция `## Rejected alternatives` в `plan.md` (шаблон A.7). Одна запись: **что
рассматривали, почему отказались.**

Граница с `decisions.md` (§12.2) — не в размере, а в **моменте и сроке жизни**:

| | `decisions.md` | `## Rejected alternatives` |
|---|---|---|
| Когда | По ходу implement | На фазе plan, до кода |
| Что | Выбор, который сделал | Подход, который **не** взял |
| Срок | Умирает с поставкой | Класс L: причина уходит в ADR и переживает |

**Класс L: отказ от подхода обязан стать ADR.** Эпик — единственный класс, где
отброшенный подход дорог: его предложат снова, и снова придётся считать. Поэтому
на L причина переносится строкой в `knowledge/**` как `type: ADR` (OKF §3.2 —
ADR по определению «принято/отклонено»). Секция в плане умрёт вместе с поставкой,
ADR — нет.

**Что механически, а что нет.** `delivery_check` проверяет **секцию в плане**:
класс L на implement и дальше ⇒ `## Rejected alternatives` существует и содержит
хотя бы одну заполненную запись с причиной (класс M — warning). Перенос в ADR
гейтом **не проверяется**: связать «этот отказ» с «этим ADR» механически можно
только по id, а вводить ещё одну систему id ради одного правила — плата выше
пользы. Это правило живёт как §2.2a до v1.19: работает, пока его читают.

Законная запись — и та, где альтернатив не было: «рассматривали только X —
потому что Y исключён требованием Z». Это тоже информация, и её отсутствие
неотличимо от «не думал».

### 12.3. `escalation.md` — контракт остановки

Stop-gate (§3.3) и circuit breaker (§3.4) останавливают агента — и всё. Дальше
человек получает «я застрял» и тратит первые двадцать минут на восстановление
контекста, который у агента был.

Обязательные четыре части:

| Часть | Зачем |
|---|---|
| **Что произошло** | Один абзац: какой gate/breaker, на чём |
| **Что уже пробовал** | Список с результатами — чтобы человек не повторил |
| **Какое решение нужно** | Один конкретный вопрос, а не «что делать?» |
| **Два варианта с ценой** | По каждому: что делаем, чего это стоит, что теряем |

**Два варианта — не украшение.** Один вариант — это не выбор, а просьба
согласиться; человек в этой позиции либо штампует, либо начинает разбираться сам,
то есть эскалация не сработала. `delivery_check` требует ровно этого: минимум две
строки-варианта с ценой. Если агент видит только один путь — второй вариант
всегда существует и называется «не делать / отложить», и его цену тоже надо
назвать.

### 12.4. Чего три журнала не дают

- **Журнал не делает рассуждение лучше.** Он делает его **видимым**. Агент,
  который проверяет гипотезы бессистемно, запишет бессистемные гипотезы.
- **Формат проверяем, содержание — нет.** `delivery_check` видит, что вердикт
  стоит и что вариантов два. Верен ли вердикт и честна ли цена — вопрос к
  человеку; это тот же потолок, что §3.1d.
- **Заполнение постфактум неотличимо от ведения по ходу.** Журнал, написанный
  одним движением перед handoff, пройдёт проверки. Против этого работает только
  то, что он **бесполезен для самого агента**, если писать его в конце — а
  бесполезная работа отмирает.

### 12.5. Маячок на рисковый дифф: маршрут к читателю, а не оценка

Есть класс дефектов, у которых **нет оракула**: архитектура выбрана неудачно,
функция делает не то, что нужно. §3.1d называет потолок и отказывается от оценки
решений моделью — с причинами. Здесь механизм для того же класса, но другой формы:
не судить качество, а **гарантировать, что рисковый дифф кто-то прочитал**.

**Почему не «подтверди, что смотрел».** У такого заявления нет наблюдения для
сверки — по критерию §12.2a «кто здесь может поймать ложь» ответ был бы «никто».
Это анкета, и она хуже отсутствия проверки, потому что выглядит контролем.

**Что проверяется вместо.** Не «смотрел ли», а **что нашёл**: блок
`## Ревью рисковых мест` в `verify-report.md`, в котором назван класс риска и
процитировано ≥2 идентификатора **из диффа** — имена функций, полей, файлов. Такое
нельзя написать, не открыв код. Тот же приём, что `repro_test:` (§3.1c) и признак
цены решения (§12.2a): проверяем не утверждение о работе, а её след.

| Не проходит | Проходит |
|---|---|
| «проверил транзакцию, всё ок» | «в `transfer()` между `debit` и `credit` возможно исключение — обе внутри одной `session.begin()`» |

**Классы риска** (дифф задел хотя бы один → маячок): деньги · безопасность ·
транзакция БД · цепочка гейтов · интеграция · новый модуль. Список — тот же, что
CQG §7 называет для ревью глазами, только теперь он **вычисляется по диффу**, а не
держится в голове.

⚠ **`RISK_CLASSES` — стартовый список, а не константа.** Он подстраивается под
домен проекта так же, как `LINT_PY_SRC` под раскладку. Причина конкретна: слово может
быть многозначным. На проекте, где «токен» — единица стрима LLM, голый `token` в
паттерне безопасности совпадает в `test_..._one_token`, и маячок начинает требовать
ревью безопасности от правки текстового чанкера. Такой маячок снимают через неделю —
поэтому многозначные слова берутся только в **составных** формах (`access_token`,
`auth_token`), а корни вроде `доступ`, совпадающие внутри `недоступен`, не берутся
вовсе. Оба случая найдены на первом же прогоне маячка.

**Законный выход, и он не waiver.** Риска правда нет — так и напиши: «риска нет,
потому что …», с теми же цитатами. Это тоже требует чтения, поэтому выход есть
всегда, а галочки нет. Так закрыт класс F4/F6/F13: у проверки обязан быть путь,
которым её можно пройти честно.

**Лестница как у `deferred` (§2.2b):** на `verify` — предупреждение, на `handoff` —
отказ. Блокировать на каждом коммите нельзя: правка опечатки получала бы требование
ревью, и маячок сняли бы через неделю — вместе с пользой.

⚠ **Чего это НЕ даёт, прямым текстом.** Доказательства, что смотрел **человек**,
здесь нет: есть доказательство, что дифф **прочитан**. Для конфигурации «один
владелец + агенты» это достижимый потолок, и он всё равно ценен — чтение вынуждено,
а запись владелец просматривает за секунды. Обойти можно, выгрепав идентификаторы,
но это дороже, чем прочитать маленький дифф, и видно при просмотре. Строка приёмки
для «человек посмотрел» остаётся `human` с названным остатком, а не превращается в
`auto`.

---

### 12.6. Путь, который проверяется только исполнением

**Есть класс отказов, который не видит ни один статический оракул и который
нельзя обработать в рантайме: платформа отвечает на нарушение политики смертью
процесса, а не кодом ошибки.** Замер — четыре падения за сутки на одном iOS-проекте,
все четыре нашёл человек на телефоне, все четыре при зелёной сборке:

| Отказ | Что было зелёным | Почему не поймалось |
|---|---|---|
| нет ключа в `Info.plist` | сборка | ключ лежал в сборочной настройке, которая при `GENERATE_INFOPLIST_FILE = NO` игнорируется **молча** |
| запрос снимка сверх потолка выхода | сборка, типы | NSException в рантайме; тракт съёмки не исполнялся ни разу |
| чтение библиотеки под правом «только добавлять» | сборка, права запрошены | поиск альбома — операция чтения; TCC убивает процесс |
| панель уехала за экран | сборка, типы | `ZStack` внутри `GeometryReader` сжался по содержимому |

Ни одно из первых трёх не возвращает ошибку, которую можно поймать `try`. Их
можно только **не допустить**, а значит — исполнить путь заранее.

#### Почему это отдельно от §12.5

§12.5 доказывает, что дифф **прочитан**, и для «архитектура неудачна» этого
достаточно. Здесь чтение не помогает вовсе: весь этот код автор читал и считал
верным. Полевой разбор называет причину прямо — «**я рассуждал о правах вместо
того, чтобы их проверить**»: мысль «просим минимальное право» была верной, а вот
что это право *позволяет*, выяснилось падением на устройстве.

#### Механика: объявить поверхности, исполнить, записать

1. **`runtime_paths:` в STATUS** — пути, чей отказ не виден ни сборке, ни тестам.
   Список настраивается под проект, как `RISK_CLASSES` под домен и `LINT_PY_SRC`
   под раскладку. Типовые: экран, права и приватность, работа с железом, тракт
   ввода-вывода, платёжный SDK, миграции на живой базе.
   **`none` без причины не считается объявлением** — то же правило, что у
   `diagnosis: n/a reason=…`: «не думали» и «подумали и нет» обязаны различаться
   в тексте, а не в тишине.
2. **Дифф задел объявленный путь ⇒ в `verify-report.md` блок
   «## Исполнение рисковых путей»**, и в нём на КАЖДЫЙ задетый путь строка:
   чем исполнял (в backticks), что увидел, `at=<дата>`.
3. **Лестница как у §12.5 и `deferred`:** предупреждение на `verify`, отказ на
   `handoff`. Блокировать на каждом коммите нельзя — правку строки в README это
   тоже остановило бы, и механизм сняли бы за неделю (§4.3b).

```markdown
## Исполнение рисковых путей

- `ios/…/Camera/` — прогнал `-scanShot 1 -scanTarget frame.png` без `--console` и
  без убийства процесса по таймауту; кадр дошёл до Mac, отношение сторон вырезки
  1.50 против 1.50 у рамки, новых отчётов в `systemCrashLogs` нет. at=2026-08-06
```

#### Breaker по поверхностям, а не по объёму

`max_runtime_paths` (дефолт **1**) — сколько объявленных путей может задеть одна
поставка. Существующие breaker'ы §3.4 считают файлы и строки, и три правки в три
подсистемы на десяток строк проходят их не заметив; именно так и выглядел
составной отказ, который потом разбирали перебором. Поднимается той же формой,
что остальные: `max_runtime_paths=2 reason=… by=human:…`.

**Почему breaker, а не правило.** Правило «одна правка — одна установка» в поле
было **написано и нарушено тем же автором через несколько часов**. Это не про
дисциплину: канон сам говорит о себе то же самое в DoD §3.2 п.10 — *«правило без
гейта, живёт пока его читают»*.

#### Чего это НЕ даёт, прямым текстом

**Доказательства, что прогон был, здесь нет и быть не может.** Есть доказательство,
что про каждую задетую поверхность сделано конкретное утверждение с датой и названа
команда. Это ловит не ложь, а **пропуск** — и пропуск ровно то, что случилось все
четыре раза: путь не был исполнен НИ РАЗУ, а не исполнен плохо.

Отсюда же требование назвать команду в backticks, и оно не косметическое. В поле
инструмент для такой проверки **существовал и был выброшен**: дев-аргумент
подвесил камеру (не сам по себе — процесс убивали по таймауту посреди отправки),
и вместо починки способа запуска проверку перестали делать вовсе — слабую заменили
на никакую. Записанная команда делает следующий раз воспроизводимым: чинить
инструмент дешевле, чем изобретать его заново, а выбросить молча уже нельзя.

⚠ **Сборка — не оракул.** Шаг «проект собрался» отвечает на вопрос о компиляции и
ни на один вопрос о поведении. Если он стоит первым в smoke-сьюте, сьют начинается
с утверждения, которое ничего не утверждает. Проверять надо **артефакт, который
поедет**: для ключей — `plutil -p` по собранному бандлу, для прав — `codesign -d
--entitlements`, для формата снимка — параметры пришедшего кадра. Часть этого
статична и стоит копейки: два падения из четырёх выше ловились **на хосте, без
устройства**.

---

## 13. Post-merge: контур не заканчивается на мерже

Из трёх исходных целей третья — «быстро локализовать и решить» — была закрыта
наполовину, и это признавалось прямым текстом. Сильная половина есть: найти,
**какое изменение** сломало (bisect-able история §8.8 CQG, мелкие поставки,
зелёный main, ошибки с контекстом §2.4a). Слабая половина — в том, что
**локализация начинается после мержа**, а контур на мерже заканчивался.

Три вещи отсутствовали, и каждая стоит дорого:

| Чего не было | Чем это оборачивается |
|---|---|
| Сигнала, что изменение **работает в проде** | «Готово» проверено во всех средах, кроме той единственной, которая имеет значение |
| Сигнала, что стало **хуже** | Деградацию замечает пользователь, а не контур. Агент не может сделать хуже «незаметно» — он может сделать хуже **ненаблюдаемо** |
| Инструкции, **что делать**, когда сигнал сработал | Алерт без runbook'а будит человека, чтобы он начал разбираться с нуля |

### 13.1. Наблюдение — обязательство, а не фаза

Соблазн — добавить фазу `observe` после `handoff`. **Так делать нельзя:** фаза
живёт в `delivery/active/`, а `active/` по §2.3 одна, и поставка, ждущая
наблюдения, заблокировала бы следующую. Наблюдение по природе **асинхронно
и переживает поставку**, значит и жить обязано не в ней.

Поэтому конструкция такая:

1. На handoff в `STATUS.md` заполняются два поля — `observe_signal:` (что именно
   будет проверено в проде) и `observe_until:` (до какой даты). Поставка уходит
   в `archive/` **вместе с обязательством**.
2. Наблюдение записывается в `archive/<slug>/observed.md` (шаблон A.17).
3. `delivery_check` на **следующей** поставке проверяет: если у архивной поставки
   `observe_until` в прошлом, а `observed.md` нет — **ошибка**.

Лаг в одну поставку — тот же приём, что у индекса архива (§2.2a), и он уже
показал, что работает: проверка срабатывает ровно тогда, когда человек всё равно
смотрит в PR. Дыра честная и называется прямо: **если следующей поставки не
будет, никто не проверит.** Закрывается тем же периодическим прогоном, что
`okf_sync_gate --check-stale` (OKF §7.2) — `delivery_check` в ту же cron-джобу.

### 13.2. Что наблюдать: примеры спеки против прода

Здесь замыкается круг §3.1d. Ожидание пришло от человека **до** кода — строкой
`A1`/`A2` в `## Acceptance examples`, подписанной `human_ok_spec`. Тесты
проверили эти id против реализации. **Post-merge та же строка проверяется против
реальности:**

```text
observe_signal: A1, A3 подтверждены в проде + p95 латентности /feed не выше 180мс
```

Это сильнее, чем «ошибок нет», по двум причинам. Во-первых, «ошибок нет» —
отсутствие сигнала, а не сигнал: молчание одинаково выглядит при работающей
фиче и при не вызываемом коде (тот же класс, что §3.1a «зелёность ≠ покрытие»).
Во-вторых, id уже связан с человеческой подписью, и проверять надо **не мнение
агента о своей работе**, а конкретное значение, которое человек утвердил заранее.

**Про «стало хуже» — нужна одна метрика с замером ДО.** Иначе сравнивать не с чем,
а «кажется, так же» деградацией не является. Форма — реляционный оракул (§6.5),
только не между двумя реализациями, а между двумя моментами времени: одна
метрика, замер до мержа, порог допустимого сдвига. Одна, а не десять: десять
никто не смотрит, а одна названная в STATUS попадает в дифф PR.

### 13.3. Лестница наблюдаемости: чем платят за каждую ступень

Уровни по силе, как в §3.1d. Проект не обязан быть на верхнем — обязан **знать,
на каком он**, и не называть нижний верхним:

| Уровень | Что развёрнуто | Что даёт | Чего не даёт |
|---|---|---|---|
| **1. Структурные логи** | Логгер по модулю, контекст ключ-значением, correlation id (§2.4a правило 2) | Можно собрать историю одного случая постфактум | Никто не смотрит, пока не спросили. Это архив, не сигнал |
| **2. + ошибки и метрики** | Error tracking (Sentry и подобное) + метрика поставки с замером до | Появляется **сигнал**: ошибка находит человека сама, деградация видна числом | Приходит после того, как пользователь уже столкнулся |
| **3. + алерты и runbook'и** | Порог → алерт → `type: Runbook` в OKF (§3.2) | Реакция становится процедурой, а не импровизацией в три часа ночи | Алерт без runbook'а хуже отсутствия: будит и не помогает |

Состояние пишется в STATUS одной строкой `observability: <1|2|3>` — и это
значение, как `ci-oracles`, **обязано быть проверяемым**: уровень 2 означает, что
`observe_signal` содержит метрику с замером до, а не что «Sentry вроде
подключён». Правило из §10.4, добытое дорого: значение, которое пишет агент и не
сверяет никто, через две поставки становится ложным.

**Алерт без runbook'а — незакрытый пункт, а не мелочь.** Каждый алерт уровня 3
обязан иметь concept `type: Runbook`: симптом → как проверить → как откатить →
кого звать. Правило живёт в OKF; гейта на «у каждого алерта есть runbook» нет,
потому что список алертов лежит вне репозитория (в мониторинге), и связать его
механически нечем. Сказано прямо, чтобы не выглядело покрытым.

### 13.4. Что механически, а что нет

| Механически | Чем |
|---|---|
| На handoff заполнены `observe_signal:` и `observe_until:` | `delivery_check` (ошибка на handoff, если пусто) |
| Просроченное наблюдение не забыто | `delivery_check` по `archive/**/STATUS.md` — лаг в одну поставку |
| `observability: 2+` не заявлен без метрики в сигнале | `delivery_check` сверяет заявленный уровень с содержимым `observe_signal` |
| Логи структурные (§2.4a правило 2) | **`check_grep_gate.sh --rule unstructured-log`** (CQG §3) — пятое правило в файле существующего скрипта, но **двадцатая строка каталога**: бюджет §9.1a теперь **20 из 20, исчерпан** |
| Что прод действительно шлёт логи и метрики | **Ничем.** Канон не имеет доступа к проду |

### 13.5. Чего post-merge половина не даёт

- **Наблюдение можно подделать** — как и всё остальное в контуре: `observed.md`
  пишет тот же агент. Единственное, что не подделывается, — конкретное значение
  рядом с id примера, который человек подписал до кода. Поэтому §13.2 требует
  именно id и число, а не формулировку «работает штатно».
- **«Сигнал не сработал» ≠ «всё хорошо».** Ненастроенный алерт молчит так же
  убедительно, как настроенный. Это ровно §3.1a на уровне прода, и лечится оно
  так же — проверкой, что сигнал **исполнялся** (искусственное срабатывание при
  настройке), а не тем, что он «есть».
- **Порог наблюдения — компромисс, а не истина.** `observe_until` +3 дня поймает
  быструю регрессию и пропустит сезонную; ловить обе — значит держать наблюдение
  открытым вечно, а вечное обязательство перестают выполнять.
- **Runbook'и стареют быстрее кода.** Процедура откатa, написанная под прошлую
  архитектуру, вредна активно. Против этого — только `stale_after` (OKF §7.2),
  то есть перечитывание по сроку.

---

---

> ## ⬇ Ниже — BOOTSTRAP PAYLOAD (строки ~706–1613)
>
> Шаблоны файлов и исходники скриптов. **В обычной работе (фича/баг) не читай** —
> канон закончился выше. Читай отсюда только когда **разворачиваешь** контур или
> правишь конкретный шаблон/скрипт: тогда бери нужное приложение целиком.
>
> | Что | Где |
> |---|---|
> | Шаблоны `delivery/**` | Приложение A (A.1–A.17) |
> | `scripts/delivery_check.py` | Приложение B |
> | `scripts/delivery_metrics.py` | Приложение C |

---

# Приложение A — шаблоны

### A.1. `delivery/CONSTITUTION.md`

```markdown
# Delivery constitution

**Version:** 1.0
**Ratified:** YYYY-MM-DD
**Canon stack:** delivery@<version> · cqg@<version|absent> · okf@<version|absent>
<!-- версии из шапок соответствующих *.md; absent = слой не развёрнут -->
**CI:** not-deployed | deployed (github-actions|gitlab|…)   <!-- §10.4 -->

## Non-negotiables

- Done закрывается **зелёным CI-прогоном** (§10.4), не локальным «у меня прошло».
- `STRICT=0` / `git commit -n` — аварийная локальная мера, видимая в PR; в CI запрещены.

## Process principles

1. Spec before broad implementation (class M/L).
2. Vertical slices; no oneshot of the whole plan.
3. Done = oracles (shape + behavior + product), never self-declaration alone.
4. Builder ≠ Verifier.
5. Agent mistake → strengthen harness (oracle / breaker / hook), not only prompts.
6. One `delivery/active` at a time.

## Coding-agent contract (thin ABC)

### Preconditions
- STATUS.md read; class S/M/L known; branch/worktree set.
- Before implement: artifacts per harness §2.2 (S: tasks; M/L: spec+plan+tasks + human_ok_spec).

### Invariants
- No secrets in git; no force-push to main; no done-on-red; no silent scope creep.

### Governance
- Human OK on spec (M/L). Human OK on plan (L / risky). HITL on prod deploy, data migrations, security-sensitive changes.

### Recovery
- On oracle red: fix ≤ retry budget, else escalate in STATUS.md.

## Agent permissions (§4.5)

Источник истины по **действиям**. Строки ниже обязаны совпадать с
`.claude/settings.json` (`permissions.deny` / `permissions.ask`) — сверяет
`delivery_check` в обе стороны. Правило здесь и не в настройках = запрет,
который не работает; в настройках и не здесь = граница, сдвинутая без ревью.

`allow` тут не перечисляется: это накопительный список конкретных команд,
его место — только в настройках (§4.5, почему).

```text
agent-permissions
# Необратимое: подтверждение здесь не защита, а соучастие.
deny: Bash(git push --force:*)
deny: Bash(git push:* --force-with-lease)
deny: Bash(rm -rf /:*)
deny: Bash(psql:*)
deny: Bash(mysql:*)
deny: Read(./.env)
deny: Read(./**/.env)
# Обратимо, но платит человек своим временем.
ask: Bash(git push:*)
ask: Bash(gh pr merge:*)
ask: Bash(pip install:*)
ask: Bash(npm install:*)
ask: WebFetch
# Переведено из HITL в автономию (§4.6) — только вместе с оракулом.
# `delivery_check` проверяет, что файл в oracle= существует.
# sandbox: Bash(alembic upgrade:*) oracle=scripts/db_ephemeral.sh reason=гоняется на дампе, схема сверяется
```

<!-- Правь под проект: список выше — рабочий минимум, а не догма. Добавляя
     строку в настройки, добавь её и здесь одним коммитом; иначе гейт упадёт,
     и это правильное поведение (граница двигается только через ревью). -->

## Pointers to sibling layers (fill if deployed)

- Code shape oracles: `CODE_QUALITY_GATES.md` — [ ] not deployed / [ ] deployed
- Domain canon: `OKF_KNOWLEDGE_BUNDLE.md` / `knowledge/` — [ ] not deployed / [ ] deployed
- Agent hooks (§10): [ ] not deployed / [ ] deployed
- CI oracles (§10.4, workflow per CQG §8): [ ] not deployed / [ ] deployed
- Skills catalog: `skills/README.md` — [ ] absent / [ ] present
```

### A.2. `delivery/active/STATUS.md`

**Важно:** значения-плейсхолдеры записаны как `<a|b|c>` и **не проходят**
`delivery_check.py` — незаполненный STATUS падает с ошибкой, а не молча
трактуется как `class: S`. Заполняй **одним** значением без вертикальной черты.

```markdown
# Active delivery status

- **slug:** <short-slug>
- **stack:** delivery@<version>, cqg@<version|absent>, okf@<version|absent>
- **class:** <S|M|L>
- **kind:** <feature|bugfix|refactor|hotfix|bootstrap>   <!-- §3.1b: refactor меняет цену weak; bootstrap = развёртывание контура, breaker'ом не мерится (§3.4) -->
- **repro_test:** <path::test_name>   <!-- обязателен при kind=bugfix (§3.1c):
     тест, который ПАДАЛ до фикса. Нет теста — баг вернётся, и это самый обидный сорт -->
- **diagnosis:** <active/diagnosis.md|n/a reason=…>   <!-- при kind=bugfix (§12.1):
     журнал гипотез с вердиктами. n/a только с причиной — «нашёл сразу» и «не искал»
     обязаны различаться в тексте, а не в тишине -->
- **phase:** <specify|plan|tasks|implement|verify|converge|handoff>
  (подфазы clarify/analyze в STATUS не выносятся — см. §2.1)
- **builder:** <agent:name|human:name>     <!-- кто пишет код; §5.2 -->
- **verifier:** <agent:name|human:name|process:ci>  <!-- обязан отличаться на M/L -->
- **human_ok_spec:** no
  <!-- yes (by=human:…, at=…) когда человек подтвердил примеры ДО кода.
       `deferred (reason=человек недоступен, at=…)` — §2.2b: спека с примерами есть,
       согласовать не с кем, работа идёт под запись долга. Пускает до verify/converge;
       на handoff требуется именно `yes`. Занижать класс вместо этого ЗАПРЕЩЕНО. -->
- **human_ok_plan:** no       <!-- yes (by=…, at=…) | n/a для класса S/M -->
- **shape-oracles:** <weak|cqg-deployed>
- **behavior-oracles:** <weak|tests-present>  <!-- weak — назови, что дёшево и не сделано (§6.5a) -->
- **artifact_oracle:** <путь к проверке собранного|n/a reason=…>  <!-- §6.5a; только если проект собирает -->
- **ci-oracles:** <weak|tooling|deployed>  <!-- §10.4; weak на классе L = нужен waiver -->
  <!-- tooling = CI есть, гейт мержа в репо (merge_guard.sh), серверного нет по тарифу -->
- **worktree:** <../repo-wt-slug|none (S)>
- **hooks:** <not-deployed|cursor|claude|other>
- **blockers:** …
  <!-- Непустое значение ⇒ обязателен active/escalation.md (§12.3): что пробовал,
       какое решение нужно, ДВА варианта с ценой. «Я застрял» решением не является -->
- **waivers:** …
  <!-- Waiver = строка ЗДЕСЬ, не env-переменная (§4.3a): видна в диффе PR и
       умирает вместе с поставкой. Формат: reason=… by=human:… -->
- **new_dependency:** …
  <!-- Одна строка на КАЖДЫЙ новый прямой пакет (CQG §3 `new-dependency`):
       new_dependency: httpx reason=нужен HTTP/2, requests не умеет by=agent:claude
       `reason=` и `by=` обязательны — упоминание пакета решением не является.
       Самое долгоживущее решение поставки: пакет остаётся на годы, тянет
       транзитивные зависимости и свою лицензию. Bump версии объявлять не надо. -->
- **runtime_paths:** <glob[,glob…]|none reason=…>
  <!-- §12.6: пути, чей отказ НЕ виден ни сборке, ни тестам — платформа отвечает
       падением, а не кодом ошибки. Экран, права/приватность, железо, тракт
       ввода-вывода, платёжный SDK, миграции на живой базе.
       Дифф задел такой путь ⇒ в verify-report нужен блок «## Исполнение
       рисковых путей» со строкой на каждый: чем исполнял, что увидел, at=<дата>.
       `none` БЕЗ причины объявлением не считается: «не думали» и «подумали и
       нет» обязаны различаться в тексте, а не в тишине. -->
- **canon_drift_waiver:** no    <!-- код тронут без смены инвариантов (OKF Прил. C) -->
- **baseline_growth_waiver:** no  <!-- законный рост снимка, напр. массовое переименование -->
- **observability:** <1|2|3>
  <!-- §13.3: 1 = структурные логи; 2 = + error tracking и метрика с замером ДО;
       3 = + алерты с runbook'ами. Значение проверяемое, не декларативное:
       уровень 2+ требует метрику в observe_signal ниже -->
- **observe_signal:** <что будет проверено в проде>
  <!-- §13.2. Заполняется на handoff. Форма: id примеров спеки + одна метрика
       с порогом, напр. «A1, A3 подтверждены в проде + p95 /feed не выше 180мс».
       «Ошибок нет» сигналом НЕ является: молчание одинаково выглядит при
       работающей фиче и при не вызываемом коде -->
- **observe_until:** <YYYY-MM-DD>
  <!-- До какой даты наблюдаем. Просрочка без archive/<slug>/observed.md —
       ошибка delivery_check на следующей поставке (§13.1) -->
- **circuit_breakers:** defaults from AGENT_DELIVERY_HARNESS.md §3.4
```

Заполненный пример:

```markdown
- **slug:** inbound-dedup
- **stack:** delivery@1.28, cqg@1.23, okf@absent
- **class:** M
- **phase:** implement
- **builder:** agent:claude-code
- **verifier:** human:anthony
- **human_ok_spec:** yes (by=human:anthony, at=2026-07-27T10:00:00Z)
- **human_ok_plan:** n/a
- **shape-oracles:** cqg-deployed
- **behavior-oracles:** tests-present
- **ci-oracles:** deployed
- **worktree:** ../crm-wt-inbound-dedup
- **hooks:** claude
```

### A.3. `delivery/active/spec.md` (скелет)

```markdown
# Spec: <title>

## Problem
…

## Users / stakeholders
…

## In scope
- …

## Out of scope
- …

## Acceptance (observable)
- [ ] …
- [ ] …

## Acceptance examples
<!-- ОБЯЗАТЕЛЬНО для класса M/L (§3.1d). Пишется НА ФАЗЕ specify, до кода, и
     подтверждается человеком вместе со спекой. Смысл: ожидаемое значение должно
     прийти от человека, а не от того же исполнителя, который потом напишет код.
     Каждый пример получает id; тест помечается этим id в комментарии. -->

| # | Вход | Ожидаемый выход |
|---|---|---|
| A1 | … | … |
| A2 | … (граничный случай) | … |
| A3 | … (ошибочный вход) | … |

<!-- Формулировка «система должна корректно обрабатывать X» примером НЕ является:
     под ней нечего подписывать. Пример — это конкретные значения. -->

## Non-functional
- …

## Open questions
- …
```

### A.4. `delivery/active/tasks.md` (скелет)

```markdown
# Tasks

## Slice 1 — <vertical>
- [ ] T1: …
- [ ] T2: …
- [ ] T3: tests/smoke for slice 1

## Slice 2 — …
- [ ] …
```

### A.5. Hook для `AGENTS.md` / Cursor rule

```markdown
## Canon stack / Agent Delivery Harness

- **Start here:** `AGENT_STACK.md` (order: Delivery → CQG → OKF).
- Process canon: `AGENT_DELIVERY_HARNESS.md`.
- Active delivery: `delivery/active/STATUS.md` — read before coding.
- **Order:** follow delivery phases. Do not oneshot large work.
- **Done:** only when verify oracles pass; never declare done on red.
- Prefer git worktree for class M/L (Delivery §5.1).
- Smoke/evals: `delivery/evals/smoke/` + `active/eval-smoke.md` (Delivery §6).
- Metrics on handoff: Delivery §9 / A.10.
- Hooks if deployed: Delivery §10.
- Skills/prompts: `skills/README.md` (Delivery §11); no inline prompts (CQG).
- Do **not** duplicate code-quality rules here — use `CODE_QUALITY_GATES.md` if present.
- Do **not** invent domain canon — use `knowledge/` / OKF if present.
- Deploy order for missing layers: Delivery → CQG → OKF (see `AGENT_STACK.md`).
```

### A.6. `delivery/README.md`

```markdown
# Delivery contour

Phased AI delivery harness. See root `AGENT_DELIVERY_HARNESS.md`.

- `CONSTITUTION.md` — process principles + thin coding-agent contract
- `active/` — current shipment only
- `archive/` — finished shipments
- `evals/smoke/` — durable product smoke oracles
```

### A.7. `delivery/active/plan.md` (скелет)

```markdown
# Plan: <title>

## Approach
…

## Stack / boundaries
…

## Rejected alternatives
<!-- §12.2a: что рассматривали и почему отказались. Класс L — обязательно, и
     причина отказа переносится в knowledge/** как type: ADR (OKF §3.2), иначе
     тот же тупик предложат снова. Причина обязательна в каждой записи.
     Законная запись и такая: «рассматривали только X — потому что Y исключён
     требованием Z». Отсутствие альтернатив неотличимо от «не думал». -->
- <подход> — отклонён, потому что <причина>

## Risks
…

## Rollout / migration
…

## Test / eval strategy
- Link to eval-smoke.md
```

### A.8. `delivery/active/verify-report.md` (скелет)

```markdown
# Verify report

**Date:** YYYY-MM-DD
**Verifier:** <process:ci|agent:NAME|human:NAME>
**asserts_reviewed_by:** <human:NAME at=… | n/a (все утверждения ведут к одобренным примерам)>
<!-- Обязательно для M/L (§3.1d уровень 3, DoD §3.2.7). Порядок такой:
     1) `bash scripts/lint/assert_digest.sh >> этот файл` — дайджест обязан быть
        вставлен, в нём есть строка `asserts_without_example: N`;
     2) N = 0 → все ожидания ведут к примерам, подписанным человеком ДО кода;
        законно писать `n/a (…)`, повторная подпись ничего не добавляет;
     3) N > 0 → читать нужно только строки с `-` в первой колонке: эти ожидания
        не подписывал никто. Подпись `human:NAME at=…` обязательна.
     `n/a` без вставленного дайджеста или при N > 0 — ошибка delivery_check. -->
**CI run:** <URL зелёного прогона>
<!-- URL, а не слова «CI зелёный»: без ссылки DoD §3.2.3 не закрыт.
     Взять: gh run list --workflow quality --branch <ветка> --limit 1 --json url -->
**Commit:** <sha, на котором прогон зелёный>
<!-- Заполнить ОДНИМ значением. Должен отличаться от `builder:` в STATUS (§5.2);
     на M/L совпадение = ошибка delivery_check. -->

## Shape oracles
- [ ] PASS/FAIL — (pre-commit / CQG / tsc / …)

## Behavior oracles
- [ ] PASS/FAIL — tests …

## Product oracles
- [ ] PASS/FAIL — delivery/evals/smoke
- [ ] PASS/FAIL — active/eval-smoke acceptance

## Spec coverage gaps
- …

## Verdict
- [ ] READY FOR HANDOFF
- [ ] NEED CONVERGE (new tasks)
- [ ] BLOCKED
```

### A.9. `delivery/active/eval-smoke.md` + `delivery/evals/smoke/README.md`

`delivery/active/eval-smoke.md`:

```markdown
# Eval smoke — this shipment

Derived from spec acceptance. Run during verify.

- [ ] …
- [ ] …
```

`delivery/evals/smoke/README.md`:

```markdown
# Durable smoke oracles

List 3–10 repo-level checks (command + expected). Example:

| ID | Command | Expected | Timeout |
|---|---|---|---|
| S1 | `curl -sf localhost:8000/health` | HTTP 200 | 10s |
| S2 | `pytest -q` | exit 0 | 120s |

Optional: `run.sh` loops IDs and exits 1 on first failure.
```

`delivery/evals/smoke/run.sh` (скелет):

```bash
#!/usr/bin/env bash
set -euo pipefail
ROOT="$(git rev-parse --show-toplevel)"
cd "$ROOT"
# Добавь реальные команды проекта:
# curl -sf "http://127.0.0.1:8000/health" >/dev/null
# (cd backend && .venv/bin/pytest -q)
echo "TODO: wire smoke commands in delivery/evals/smoke/run.sh" >&2
exit 0   # смени на реальные проверки; bootstrap may keep exit 0 until wired
```

### A.10. Блок метрик (в конец verify-report или archive STATUS)

```markdown
## Harness metrics (this shipment)

| Metric | Value |
|---|---|
| time_to_accepted_spec | … |
| implement_retries | … |
| verify_fails_before_green | … |
| files_touched / loc_diff | … / … |
| rework_after_done | … |
| harness_hardened | yes/no — what changed: … |
| est_token_or_cost | … / n/a |
```

### A.11. Примеры agent hooks (адаптируй под IDE)

Концепт **stop-on-red** (shell, вызывать из Stop / before-finish hook):

```bash
#!/usr/bin/env bash
set -euo pipefail
ROOT="$(git rev-parse --show-toplevel)"
cd "$ROOT"
if [[ -f scripts/delivery_check.py ]]; then
  python scripts/delivery_check.py || {
    echo "delivery_check failed — cannot declare done" >&2
    exit 1
  }
fi
if [[ -x delivery/evals/smoke/run.sh ]]; then
  bash delivery/evals/smoke/run.sh || {
    echo "smoke evals failed — cannot declare done" >&2
    exit 1
  }
fi
# если CQG развёрнут:
# pre-commit run --all-files || exit 1
exit 0
```

Концепт **protect-main** (проверка аргументов shell перед git push):

```bash
#!/usr/bin/env bash
# stdin or $1 = command line; exit 1 to block
cmd="${1:-}"
if echo "$cmd" | grep -Eiq 'push[[:space:]].*(--force|-f).*[[:space:]]+(origin[[:space:]]+)?(main|master)\b'; then
  echo "blocked: force-push to main/master" >&2
  exit 1
fi
exit 0
```

Повесь эти скрипты через механизм hooks своего агента (Cursor / Claude Code /
иной). Детали JSON-схемы — в доке инструмента; контракт поведения — §10.

### A.12. `delivery/STACK-ACCEPTANCE.md`

Результат приёмки контура (AGENT_STACK §6) + разбор случаев, где процедура подвела.
Живёт в `delivery/` (не в `active/` — это про репозиторий, не про поставку).

```markdown
# Stack acceptance

**Date:** YYYY-MM-DD
**Stack:** delivery@… · cqg@… · okf@… (или absent)
**Где лежат каноны:** repo root (коммитятся) | вне репо: <путь> (см. «Остатки»)

## Что развёрнуто

| Слой | Состояние | Примечание |
|---|---|---|
| ① delivery/ | deployed | |
| ② гейты | deployed | хуков: N; адаптация под <язык>: … |
| ③ knowledge/ | deployed / absent | concept'ов с `implementation:`: N |
| ④ CI + гейт мержа | deployed / tooling / weak | merge_guard: да/нет; branch protection: да/нет |

## Карта ролей гейтов

Заполняется по CQG «Применимость → Карта ролей». Инвариант — **роль**, не имя файла:
если роль занял другой скрипт (другой язык / технологический стек), это фиксируется здесь, иначе
таблица CQG §3 и репозиторий расходятся молча.

| Роль | Канонический скрипт | В этом проекте | Статус |
|---|---|---|---|
| … | … | … | как есть / адаптирован / n/a + причина |

## Полнота, не только зелёность

- [ ] `check_gate_coverage.sh` — OK (все гейты подключены)
- [ ] `pre-commit run --all-files` — зелено
- [ ] `stack_selftest.py` — OK (или причина, почему неприменим)
- [ ] Каждый гейт в выводе показал непустое число просканированных файлов Одно законное исключение: у правила, выбирающего файлы по имени нарушителя (`no-grab-bag-module`), пустая выборка — норма, и строка закрывается как `n/a (выборка законно пуста: <правило>)` — CQG §6.

## Остатки (не закрыто — и почему)

| Инвариант | Почему не закрыт | Компенсация |
|---|---|---|
| force-push / обход админом | бесплатный тариф хостинга | правило «мерж только merge_guard.sh» |
| … | | |

## Разбор: где эта процедура подвела

Записывать КАЖДЫЙ случай, когда приёмка показала зелено, а дыра была.
Формат: симптом → корневая причина → что добавлено в harness → как проверено.

### YYYY-MM-DD — <короткое имя>

- **Симптом:** …
- **Причина:** … (не «невнимательность», а механизм: что именно позволило пройти)
- **Усиление:** … (oracle / гейт / шаг процедуры)
- **Проверено воспроизведением:** … (сломал обратно → упало с таким сообщением)
```

### A.13. `delivery/archive/INDEX.md`

Одна таблица, не проза: её читает и человек, и `delivery_check` (§2.2a). Формат
жёсткий в двух местах — **пути в backticks** (по ним считается пересечение с
диффом) и **id урока `L<N>`** (по нему считается ссылка из `plan.md`/`tasks.md`).
Остальное — свободный текст.

```markdown
# Archive index

Что это: адресный указатель по `archive/`. Перед implement найди строки, чьи пути
пересекаются с твоим диффом, прочитай их уроки и сошлись на id в `plan.md`
(M/L) или `tasks.md` (S). Не применимо — напиши одной строкой почему.

Правила ведения:
- Строка добавляется на handoff, вместе с переносом поставки в `archive/`.
- Пути — префиксы от repo-root, в backticks. Только те, где жил урок.
- Урок — что мы **узнали о системе**, а не что сделали. «Добавил кэш» — не урок.
  «Пустой robots.txt и недоступный robots.txt — разные случаи, мы их слили» — урок.
- Урок без id не считается: id — то, чем на него ссылаются.
- Поставка без урока — строка с `—` в колонке. Это законно и означает «ничего
  нового не узнали»; частое `—` подряд означает, что разборы стали формальными.

| Дата | Поставка (каталог) | Пути | Урок |
|---|---|---|---|
| 2026-07-20 | `2026-07-20-robots-cache` | `backend/features/robots/` | **L1:** пустой robots.txt и недоступный (500) — разные случаи; слитые в один дают «запретить всё» на аварии апстрима. Тест A3 в той поставке |
| 2026-07-24 | `2026-07-24-billing-retry` | `backend/features/billing/`, `frontend/src/billing/` | **L2:** ретрай по таймауту без идемпотентного ключа удваивает списание; ключ обязан приходить от вызывающего, а не генерироваться внутри |
| 2026-07-26 | `2026-07-26-typing-pass` | `backend/features/` | — |
```

**Чего этот индекс не даёт.** Он заставляет урок быть записанным, адресуемым и
упомянутым — но не заставляет его быть **понятым**. Ссылка `L1` в плане
доказывает, что строку прочитали, а не что вывод из неё сделали верный. Это
перевод «никто не смотрит» в «прочитано под подпись», а не гарантия обучения.

### A.14. `delivery/active/decisions.md` (§12.2)

```markdown
# Decisions

Одна строка на выбор. Три части обязательны: **что выбрано**, **вместо чего**,
**потому что**. Строка без альтернативы — не решение, а отчёт о сделанном, и
`delivery_check` её отклонит.

На handoff перечитай: то, что оказалось уроком **о системе** (а не о твоём
выборе), перенеси строкой в `archive/INDEX.md` (§2.2a). Решение умирает с
поставкой, урок — нет.

- YYYY-MM-DD · выбрал <X> вместо <Y> — потому что <причина с числом или риском>
```

### A.15. `delivery/active/diagnosis.md` (§12.1, только kind=bugfix)

```markdown
# Diagnosis journal

Одна строка на гипотезу. **Вердикт обязателен** — журнал без вердиктов не
экономит ничего, следующий читатель перепроверяет всё заново.
Вердикты: `ОПРОВЕРГНУТА` / `ПОДТВЕРЖДЕНА` (или `refuted` / `confirmed`).

**Симптом:** <что наблюдаем, с конкретикой: вход, окружение, сообщение>
**Repro:** <команда или тест, которым воспроизводится>

- H1: <гипотеза> → проверил: <как, чем> → ОПРОВЕРГНУТА
- H2: <гипотеза> → проверил: <как, чем> → ПОДТВЕРЖДЕНА

**Корневая причина:** <механизм, а не «невнимательность»>
**Почему прошло мимо гейтов:** <какой оракул должен был поймать и не поймал>
<!-- Последняя строка — вход в §5.4: ошибка обязана усилить harness, а не
     закончиться фиксом. Если ни один гейт не должен был поймать — так и напиши,
     это кандидат на новый оракул. -->
```

### A.16. `delivery/active/escalation.md` (§12.3, только когда агент встал)

```markdown
# Escalation

**Что произошло:** <какой stop-gate / breaker / oracle, на чём именно>
**Фаза / класс:** <phase> / <S|M|L>

## Что уже пробовал

- <попытка> → <результат>
- <попытка> → <результат>

## Какое решение нужно

<Один конкретный вопрос. Не «что делать?», а «поднимать ли лимит файлов до 40
или разбивать поставку на две?».>

## Варианты

**Вариант A: <название>**
- Что делаем: <шаги>
- Цена: <время, риск, что теряем>

**Вариант B: <название>**
- Что делаем: <шаги>
- Цена: <время, риск, что теряем>

<!-- Двух вариантов требует §12.3, и это не формальность: один вариант — просьба
     согласиться, а не выбор. Если путь видится один, второй всегда существует и
     называется «не делать / отложить» — назови и его цену. -->
```

### A.17. `delivery/archive/<slug>/observed.md` (§13, post-merge)

```markdown
# Observed in production

**Поставка:** <YYYY-MM-DD-slug>
**Наблюдали:** <с даты мержа> → <дата закрытия наблюдения>
**Сигнал из STATUS:** <observe_signal дословно — что обещали проверить>

## Примеры спеки против прода

| id | Обещано в спеке | Что в проде | Вердикт |
|---|---|---|---|
| A1 | <ожидаемый выход> | <фактическое значение или запрос, которым посмотрели> | ПОДТВЕРЖДЁН / РАСХОЖДЕНИЕ |
| A3 | … | … | … |

<!-- Проверяются те же id, что человек подписал ДО кода (§3.1d уровень 1).
     «Работает штатно» вердиктом не является: нужно значение или запрос. -->

## Метрика: стало ли хуже

| Метрика | До мержа | После | Порог | Вердикт |
|---|---|---|---|---|
| <p95 /feed> | <150мс> | <168мс> | <не выше 180мс> | В пределах |

<!-- Замер ДО обязателен, иначе сравнивать не с чем и «кажется, так же»
     деградацией не является (§13.2). Одна метрика, не десять. -->

## Что сделано по результату

- [ ] Расхождений нет — наблюдение закрыто
- [ ] Расхождение → новая поставка `kind: bugfix` с `repro_test` (§3.1c), ссылка: <…>
- [ ] Деградация выше порога → откат, ссылка на revert: <…>

**Урок для `archive/INDEX.md`:** <строка, если узнали что-то о системе; иначе «—»>
<!-- §2.2a: то, что прод показал неожиданно, — самый дорогой сорт урока.
     Именно он должен попасть в индекс, а не «поставка прошла успешно». -->
```

---

# Приложение B — `scripts/delivery_check.py`

Фазовый гейт: проверяет наличие каркаса и согласованность STATUS с артефактами.
**Не** заменяет pytest/CQG.

Severity — по документу, а не «всё warning»: то, что §3.3 называет stop-gate
(нет spec/plan/tasks по классу, нет `human_ok_*`, нет verify-report на
converge/handoff, нет `eval-smoke.md` на M/L), даёт **exit 1**. Warning остаётся
для незаполненных мета-полей и режимов `weak`. Незаполненный шаблон STATUS
(`class: <S|M|L>`) — **ошибка**, а не «класс S по умолчанию».

```python
#!/usr/bin/env python3
"""Delivery harness checker.

Usage:
  python scripts/delivery_check.py
  python scripts/delivery_check.py --require-spec

Exit 0 = OK (warnings allowed). Exit 1 = errors.
"""


from __future__ import annotations

import argparse
import re
import sys
from datetime import date

from delivery_base import (ACTIVE, ARCHIVE, DELIVERY, ROOT,
                           field,
                           is_placeholder, read, read_json)
from delivery_decisions import (permission_block)
from delivery_evidence import check_evidence
from delivery_journals import check_journals
from delivery_limits import check_limits
from delivery_status import check_status_shape

def main() -> int:
    ap = argparse.ArgumentParser()
    ap.add_argument("--require-spec", action="store_true")
    ap.add_argument("--require-verify", action="store_true")
    ap.add_argument(
        "--require-ci",
        action="store_true",
        help="fail unless ci-oracles: deployed (§10.4)",
    )
    ap.add_argument(
        "--diff-base",
        metavar="REF",
        help="check circuit breakers §3.4 against REF (e.g. origin/main)",
    )
    args = ap.parse_args()

    errors: list[str] = []
    warnings: list[str] = []

    if not (DELIVERY / "CONSTITUTION.md").is_file():
        errors.append("missing delivery/CONSTITUTION.md")
    if not ACTIVE.is_dir():
        errors.append("missing delivery/active/")
        for w in warnings:
            print(f"WARNING: {w}")
        for e in errors:
            print(f"ERROR: {e}", file=sys.stderr)
        print(f"delivery_check: {len(errors)} error(s), {len(warnings)} warning(s)")
        return 1

    status = read(ACTIVE / "STATUS.md")
    if not status:
        # Пустой active/ — ШТАТНОЕ состояние (§2.3a): предыдущая поставка
        # заархивирована, следующая не начата. Ошибкой это быть не может, иначе
        # контур красный между поставками — причём §13.1 сам загоняет проект в
        # это состояние, требуя архивации на handoff (полевая находка F12).
        # Проверки уровня архива ниже продолжают работать: именно в idle всплывает
        # просроченное наблюдение.
        warnings.append(
            "idle: нет delivery/active/STATUS.md — активной поставки нет (§2.3a). "
            "Начинаешь работу? Создай STATUS по шаблону A.2"
        )
    else:
        ctx = check_status_shape(status, args, errors, warnings)
        check_evidence(status, args, errors, warnings, ctx)
        check_journals(status, args, errors, warnings, ctx)
        check_limits(status, args, errors, warnings, ctx)

    # --- §2.2a: архив без индекса не читается, а отставший индекс врёт о полноте.
    # Обе проверки не зависят от фазы и от наличия diff-base: они про сам архив.
    if ARCHIVE.is_dir():
        shipments = sorted(d.name for d in ARCHIVE.iterdir() if d.is_dir())
        idx_text = read(ARCHIVE / "INDEX.md")
        if shipments and not idx_text:
            errors.append(
                f"delivery/archive/ содержит {len(shipments)} поставок, а "
                "INDEX.md нет (§2.2a / A.13) — неиндексированный архив никем "
                "не читается"
            )
        elif idx_text:
            unlisted = [s for s in shipments if s not in idx_text]
            if unlisted:
                errors.append(
                    "archive/INDEX.md отстал от архива: не упомянуты "
                    f"{', '.join(unlisted)} (§2.2a) — индекс, который врёт о "
                    "своей полноте, хуже отсутствующего"
                )

        # --- §13.1: просроченное наблюдение. Лаг в одну поставку — проверка
        # срабатывает тогда, когда человек всё равно смотрит в PR. Дыра названа
        # в каноне: если следующей поставки не будет, никто не проверит; на это
        # тот же периодический прогон, что okf_sync_gate --check-stale.
        today = date.today().isoformat()
        for name in shipments:
            arch_status = read(ARCHIVE / name / "STATUS.md")
            if not arch_status:
                continue
            until = field(arch_status, "observe_until")
            if is_placeholder(until):
                continue
            due = re.match(r"(\d{4}-\d{2}-\d{2})", until)
            if not due or due.group(1) > today:
                continue
            if not (ARCHIVE / name / "observed.md").is_file():
                errors.append(
                    f"наблюдение просрочено: {name} — observe_until={due.group(1)} "
                    f"(сегодня {today}), а observed.md нет (§13.1 / A.17). "
                    "Проверь сигнал в проде и закрой наблюдение, либо сдвинь срок "
                    "с причиной"
                )

    if args.require_spec and not (ACTIVE / "spec.md").is_file():
        errors.append("--require-spec: active/spec.md missing")
    if args.require_verify and not (ACTIVE / "verify-report.md").is_file():
        errors.append("--require-verify: verify-report.md missing")

    # --- §4.5: права на действия. Объявлено (CONSTITUTION) ↔ подключено (настройки).
    settings_path = ROOT / ".claude" / "settings.json"
    declared = permission_block(read(DELIVERY / "CONSTITUTION.md"))
    settings, s_err = read_json(settings_path)
    if s_err:
        errors.append(
            f".claude/settings.json: не парсится ({s_err}) — права агента "
            "не проверяемы (§4.5)"
        )
    perms = settings.get("permissions") or {}
    wired = {k: list(perms.get(k) or []) for k in ("deny", "ask")}

    if declared is None:
        msg = (
            "CONSTITUTION.md: нет блока `agent-permissions` (§4.5 / A.1) — контур "
            "контролирует выход и молчит про действия агента"
        )
        if any(wired.values()):
            errors.append(
                msg + "; при этом deny/ask в настройках заданы, то есть границы "
                "действий живут без ревью"
            )
        else:
            warnings.append(msg)
    elif not settings_path.is_file():
        errors.append(
            "CONSTITUTION.md объявляет agent-permissions, а .claude/settings.json "
            "нет (§4.5) — объявленный неработающий запрет хуже отсутствующего: "
            "он выглядит как контроль"
        )
    elif not s_err:
        for bucket in ("deny", "ask"):
            only_canon = [r for r in declared[bucket] if r not in wired[bucket]]
            only_wired = [r for r in wired[bucket] if r not in declared[bucket]]
            if only_canon:
                errors.append(
                    f"permissions.{bucket}: объявлено в CONSTITUTION, нет в "
                    f".claude/settings.json: {', '.join(only_canon)} (§4.5)"
                )
            if only_wired:
                errors.append(
                    f"permissions.{bucket}: есть в .claude/settings.json, нет в "
                    f"CONSTITUTION: {', '.join(only_wired)} (§4.5) — границу "
                    "действий двигали молча"
                )

    # --- §4.6: перевод из HITL в автономию обязан назвать существующий оракул.
    # Объявленная песочница без скрипта — снятая проверка с видом усиления;
    # класс тот же, что «скрипт лежал в репозитории, никто не вызывал» (§3.1a).
    for line in (declared or {}).get("sandbox", []):
        m_or = re.search(r"oracle=([^\s]+)", line)
        if not m_or:
            errors.append(
                f"sandbox без oracle=: {line[:60]} (§4.6) — перевод из HITL "
                "в автономию обязан назвать оракул, заменивший человека"
            )
        elif not (ROOT / m_or.group(1)).exists():
            errors.append(
                f"sandbox: oracle={m_or.group(1)} не существует (§4.6) — "
                "объявленная песочница без скрипта это снятая проверка "
                "с видом усиления"
            )

    # Ратчет прав: длинный allow при пустом deny = права росли только вверх.
    # Считаем и локальные настройки — именно там оседают клики «yes».
    local, _ = read_json(ROOT / ".claude" / "settings.local.json")
    n_allow = len(perms.get("allow") or []) + len(
        (local.get("permissions") or {}).get("allow") or []
    )
    if n_allow >= 50 and not wired["deny"]:
        warnings.append(
            f"permissions: {n_allow} правил в allow и ни одного в deny (§4.5) — "
            "права накапливались кликами «yes» и ни разу не сужались"
        )

    if (ROOT / "CODE_QUALITY_GATES.md").is_file():
        cons = read(DELIVERY / "CONSTITUTION.md")
        if "CODE_QUALITY_GATES" not in cons:
            warnings.append("CQG present but CONSTITUTION.md has no pointer")
    if (ROOT / "OKF_KNOWLEDGE_BUNDLE.md").is_file() or (ROOT / "knowledge").is_dir():
        cons = read(DELIVERY / "CONSTITUTION.md")
        if "OKF" not in cons and "knowledge/" not in cons:
            warnings.append("OKF/knowledge present but CONSTITUTION.md has no pointer")

    for w in warnings:
        print(f"WARNING: {w}")
    for e in errors:
        print(f"ERROR: {e}", file=sys.stderr)
    print(f"delivery_check: {len(errors)} error(s), {len(warnings)} warning(s)")
    return 1 if errors else 0


if __name__ == "__main__":
    sys.exit(main())
```

# Приложение B1 — `scripts/delivery_base.py`

```python
#!/usr/bin/env python3
"""Общее для всех частей фазового гейта: пути, чтение, разбор полей STATUS.

`delivery_check.py` разрезан на модули (`delivery@1.53`): 1969 строк при планке
300 (§9.1a п.5), из них 986 — одна функция `main`. Здесь то, что нужно ВСЕМ
частям, и только оно; соседей этот модуль не импортирует, поэтому цикла быть не
может по построению.
"""

from __future__ import annotations

import json
import re
import subprocess
from dataclasses import dataclass
from pathlib import Path

ROOT = Path(__file__).resolve().parents[1]
DELIVERY = ROOT / "delivery"
ACTIVE = DELIVERY / "active"
ARCHIVE = DELIVERY / "archive"

# §3.4; переопределяются строкой в STATUS: "max_files_touched=40 reason=… by=human:…"
DEFAULT_BREAKERS = {"max_files_touched": 25, "max_loc_diff": 800,
                    # §12.6: breaker по ПОВЕРХНОСТЯМ, а не по объёму. Три
                    # правки в три подсистемы на десяток строк проходят два
                    # порога выше не заметив — и именно так выглядел
                    # составной отказ, который потом разбирали перебором.
                    "max_runtime_paths": 1}

# Процессные артефакты не считаются в breaker'ах: spec/plan/tasks и concept'ы —
# это не blast radius кода, а его описание. Механика самого контура — тоже:
# гейт-скрипты, их снимки, конфиги и workflow. Развёртывание контура и поставки
# `kind: refactor` по контуру (§9.1a прямо их предусматривает) трогают 15+ файлов
# и пробивали breaker всегда, превращая waiver в рутину — а рутинный waiver
# обесценивает механизм (§4.3a). Найдено развёртыванием, на handoff.
# Сами скрипты контура — тоже его механика, а не blast radius продукта. Их не было
# в списке, и три независимых развёртывания на настоящем проекте пробили breaker
# развёртыванием контура: `delivery_check.py` (946) + `delivery_metrics.py` (171) +
# `okf_validate.py` (113) + `okf_sync_gate.py` (297) = 1527 строк против лимита 800.
# «Разбей поставку» тут невыполним: один `delivery_check.py` больше лимита. Тот же
# класс уже был закрыт у гейта длины (его grep исключает эти четыре пути) и не
# перенесён сюда — правка была на СЛУЧАЙ, а не на класс.
BREAKER_EXCLUDE = (
    "delivery/",
    "knowledge/",
    "scripts/lint/",
    "scripts/merge_guard.sh",
    "scripts/delivery_check.py",
    "scripts/delivery_metrics.py",
    "scripts/okf_validate.py",
    "scripts/okf_sync_gate.py",
    # `.claude/` — механика контура (права агента §4.5), а не продуктовый код.
    # Без исключения маячок §12.5 срабатывал на классе «безопасность» из-за
    # слова `permission` в собственном файле настроек прав: контур ругался на
    # свою же машинерию. Найдено на первом прогоне маячка (арена lab-10).
    ".claude/",
    # Снимок канонов варианта C (§7.1 AGENT_STACK) — механика контура, а не код
    # продукта: 10 743 строки из 13 743 пробитого лимита давал именно он. §7.1 велит
    # «проверь, что путь снимка в BREAKER_EXCLUDE» — а его тут не было: инструкцию
    # добавили, путь забыли. Нашло приёмочное развёртывание.
    "docs/canon/",
    ".pre-commit-config.yaml",
    ".github/workflows/",
    ".claude/",
)


# Текстовые расширения для поиска id примеров в тестах. Маска `*test*` ловит и
# `tests/__pycache__/test_x.cpython-314.pyc` — бинарник, на котором чтение падало
# и роняло весь гейт (полевая находка F9: воспроизводится в любом проекте, где
# хоть раз запускали pytest).
TEST_TEXT_SUFFIXES = (
    ".py", ".ts", ".tsx", ".js", ".jsx", ".go", ".rs", ".swift", ".kt", ".java",
    ".rb", ".php", ".cs", ".md",
)
SKIP_DIR_PARTS = ("__pycache__", ".venv", "node_modules", ".mypy_cache", ".pytest_cache")


def read(path: Path) -> str:
    """Текст файла или "".

    `errors="replace"` — не косметика: фазовый гейт не имеет права падать
    трейсбеком про кодек на файле, который он читает попутно. Отказ гейта хуже
    его ошибки: в CI шаг красный по причине, не связанной с поставкой.
    """
    if not path.is_file():
        return ""
    return path.read_text(encoding="utf-8", errors="replace")


def git(*args: str) -> str:
    """git с подавлением ошибок: пустая строка = не смог."""
    try:
        out = subprocess.run(["git", *args], capture_output=True, text=True, check=False)
    except FileNotFoundError:
        return ""
    return out.stdout if out.returncode == 0 else ""



# Незаполненный шаблон не должен проходить проверку — тот же урок, что с STATUS
# в v1.5 («class: <S|M|L>» читался как class=S). Плейсхолдер здесь — угловые
# скобки со СЛОВОМ внутри: `<гипотеза>`, `<название>`, `<X>`. Сужение до кириллицы
# или одиночной заглавной сделано нарочно, чтобы не считать плейсхолдерами
# законные `List<int>` и `<100ms` в тексте решения.
PLACEHOLDER_RE = re.compile(r"<[^<>]*(?:[А-Яа-яЁё][^<>]*|[A-Z])>")


def unfilled(line: str) -> bool:
    """Строка — ещё шаблон, а не содержание."""
    return bool(PLACEHOLDER_RE.search(line))


def list_entries(text: str) -> list[str]:
    """Логические записи списка: продолжения переноса склеены с их пунктом.

    Разбор по ФИЗИЧЕСКИМ строкам делал вердикт гейта зависимым от вёрстки: одно и
    то же решение проходило при переносе после «потому что» и краснело при переносе
    перед ним. Канонному примеру §12.2 повезло, записи исполнителя — нет. Тот же
    класс независимо нашли два развёртывания, в разных файлах (`decisions.md` и
    `plan.md`), поэтому склейка живёт здесь одна на всех потребителей.

    Продолжение — строка, которая не начинает новый пункт и не пуста; отступ не
    требуем: markdown его прощает, а люди и агенты пишут по-разному.
    """
    entries: list[str] = []
    for raw in text.splitlines():
        line = raw.strip()
        if re.match(r"^[-*]\s+\S", line):
            entries.append(line)
        elif line and entries and not line.startswith("#") and not line.startswith("```"):
            entries[-1] += " " + line
    return entries



def read_json(path: Path) -> tuple[dict, str]:
    """(данные, ошибка). Битый файл настроек — сама по себе находка, а не пустой dict."""
    if not path.is_file():
        return {}, ""
    try:
        data = json.loads(path.read_text(encoding="utf-8"))
    except (OSError, ValueError) as exc:
        return {}, f"{type(exc).__name__}: {str(exc).splitlines()[0]}"
    return (data if isinstance(data, dict) else {}), ""


def field(status: str, name: str) -> str:
    """Значение поля STATUS: "- **class:** M" -> "M". Inline-комментарий срезается."""
    m = re.search(rf"(?im)^[ \t]*[-*]?[ \t]*\**{name}\**[ \t]*:\**[ \t]*(.*)$", status)
    if not m:
        return ""
    return re.sub(r"<!--.*?-->", "", m.group(1)).strip()


# Голый список коротких альтернатив: ВСЯ строка — токены ≤16 символов через `|`.
# Это форма необработанного шаблона ("S | M | L"), а не выбранного значения.
BARE_CHOICES_RE = re.compile(r"^[\w/.\-]{1,16}(?:\s*\|\s*[\w/.\-]{1,16})+$")


def is_placeholder(value: str) -> bool:
    """Незаполненный шаблон: "<S|M|L>", "S | M | L", "…", пусто.

    Вертикальная черта — признак шаблона ТОЛЬКО в форме голого списка коротких
    альтернатив: "S | M | L" не может быть выбранным значением, и без этой
    проверки класс L проезжал бы мимо требований spec/plan (§2.2).

    Просто «есть |» признаком НЕ является: is_placeholder зовут 14 полей, и
    часть из них свободнотекстовые. lab-11 F7: точный observe_signal с
    jq-конвейером (`docker logs app | jq …`) объявлялся «пустым» — гейт
    наказывал точность, а сообщение врало направлением. Сегменты конвейера
    многословны, поэтому голый список коротких альтернатив их не ловит.
    Область применимости признака — решение, а не деталь реализации; обе
    стороны закреплены тестами (тот же класс, что фильтр блочных скаляров
    в check_gate_coverage.sh, cqg@1.40).
    """
    if not value or value in {"…", "...", "-", "TBD", "tbd"}:
        return True
    return value.startswith("<") or bool(BARE_CHOICES_RE.match(value))


VERDICT_RE = re.compile(r"(?i)ОПРОВЕРГНУТА|ПОДТВЕРЖДЕНА|refuted|confirmed")
# Начало структурного блока: пункт списка, строка таблицы, заголовок.
# `\|\s*` а не `\|`: в таблице после вертикальной черты стоит пробел, и
# требование непробела сразу за ней отвергало ровно тот формат, ради
# которого правка и делалась.
BLOCK_START = re.compile(r"^\s*(?:[-*+]\s+|\|\s*|#{1,6}\s+)\S")


def verdict_blocks(text: str) -> list[str]:
    """Блоки диагноза, содержащие заполненный вердикт (§12.1).

    Считаются БЛОКИ, а не строки, и это не косметика. Прежняя версия требовала
    вердикт на той же физической строке, что маркер списка, — из-за чего отвергала
    и таблицу «гипотеза | вердикт», и пункт с переносом строки. На dogfooding'е
    (lab-10) я споткнулся об это дважды в одном файле, и оба раза сообщение
    говорило «вердиктов нет», хотя вердикты в файле были: диагноз про содержание,
    когда причина в форме.

    Ограничение, которое СОХРАНЯЕТСЯ: проза о формате вердиктом не считается. В
    шаблоне A.15 есть строка «Вердикты: `ОПРОВЕРГНУТА` / `ПОДТВЕРЖДЕНА`» — она не
    начинается структурным маркером, поэтому в блок не попадает. Ровно от неё
    ограничение и защищало.
    """
    blocks: list[str] = []
    cur: list[str] | None = None
    for raw in text.splitlines():
        if BLOCK_START.match(raw):
            if cur:
                blocks.append("\n".join(cur))
            cur = [raw.strip()]
        elif cur is not None and raw.strip() and raw.startswith((" ", "\t")):
            cur.append(raw.strip())  # продолжение пункта
        else:
            if cur:
                blocks.append("\n".join(cur))
            cur = None
    if cur:
        blocks.append("\n".join(cur))
    return [b for b in blocks if VERDICT_RE.search(b) and not unfilled(b)]




@dataclass(frozen=True)
class ActiveCtx:
    """Что первый раздел вычислил, а следующие читают (`delivery@1.53`).

    Восемь значений — вся связь между разделами активной поставки; внутри `main`
    их роль играли полсотни локальных переменных, и именно поэтому функция не
    резалась. Список получен замером: пересечение «присвоено раньше — прочитано
    позже» по AST, а не на глаз.
    """

    phase: str
    klass: str
    spec: Path
    plan: Path
    tasks: Path
    verify: Path
    eval_smoke: Path
    implement_like: set[str]
```

# Приложение B2 — `scripts/delivery_diff.py`

```python
#!/usr/bin/env python3
"""Что изменилось: дифф, идентификаторы правки, уроки из архива.

Часть `delivery_check.py` (`delivery@1.53`). Ответ на вопрос «на что вообще
смотреть»: объём диффа, какие имена в нём появились и какие строки архива к нему
применимы. Суждения здесь нет — оно в разделах, которые этим пользуются.
"""

from __future__ import annotations

import re

from delivery_base import BREAKER_EXCLUDE, git
from delivery_decisions import DECISION_STOPWORDS

def diff_stats(base: str) -> tuple[int, int, int, int, list[str]] | None:
    """(files, added, deleted, excluded, paths) для base..HEAD; None если ref недоступен.

    `paths` — только код: процессные артефакты отфильтрованы тем же
    BREAKER_EXCLUDE, потому что оба потребителя (breaker'ы §3.4 и сопоставление
    уроков §2.2a) спрашивают про изменения в коде, а не в его описании.
    """
    if not git("rev-parse", "--verify", "--quiet", base).strip():
        return None
    merge_base = git("merge-base", base, "HEAD").strip() or base
    files = added = deleted = excluded = 0
    paths: list[str] = []
    for line in git("diff", "--numstat", f"{merge_base}..HEAD").splitlines():
        parts = line.split("\t")
        if len(parts) != 3:
            continue
        a, d, path = parts
        if path.startswith(BREAKER_EXCLUDE):
            excluded += 1
            continue
        files += 1
        paths.append(path)
        added += int(a) if a.isdigit() else 0      # "-" у бинарников
        deleted += int(d) if d.isdigit() else 0
    return files, added, deleted, excluded, paths


def archive_index_rows(text: str) -> list[tuple[list[str], list[str]]]:
    """[(префиксы путей, id уроков)] из таблицы A.13.

    Разбор построчный по markdown-таблице: строка шапки и разделитель отсеиваются
    сами — в них нет backtick'ов, а значит и путей. Формат жёсток ровно в двух
    колонках (пути в backticks, id вида L<N>), чтобы правило §2.2a было
    исполнимым, а не пожеланием.
    """
    rows: list[tuple[list[str], list[str]]] = []
    for line in text.splitlines():
        if not line.lstrip().startswith("|"):
            continue
        cells = [c.strip() for c in line.strip().strip("|").split("|")]
        if len(cells) < 4:
            continue
        prefixes = [p for p in re.findall(r"`([^`]+)`", cells[2]) if "/" in p]
        if prefixes:
            rows.append((prefixes, re.findall(r"\bL\d+\b", cells[3])))
    return rows


def applicable_lessons(index_text: str, changed: list[str]) -> list[str]:
    """id уроков из строк, чьи пути пересекаются с диффом (порядок сохранён)."""
    out: list[str] = []
    for prefixes, ids in archive_index_rows(index_text):
        if any(c.startswith(p) for p in prefixes for c in changed):
            out.extend(ids)
    return list(dict.fromkeys(out))



def diff_identifiers(paths: list[str]) -> set[str]:
    """Токены, которые есть в диффе: базовые имена файлов + идентификаторы кода.

    Берём и пути, и содержимое изменённых строк: решение может ссылаться и на файл
    («вынес в sentences.py»), и на функцию («`_cut_points` считает границы»).
    """
    ids: set[str] = set()
    for path in paths:
        for part in re.split(r"[/\\.]", path):
            if len(part) >= 4 and part.lower() not in DECISION_STOPWORDS:
                ids.add(part.lower())
    body = git("diff", "--unified=0", "HEAD~1..HEAD", "--", *paths) if paths else ""
    for tok in re.findall(r"[A-Za-z_][A-Za-z0-9_]{3,}", body):
        low = tok.lower()
        if low not in DECISION_STOPWORDS:
            ids.add(low)
    return ids


```

# Приложение B3 — `scripts/delivery_decisions.py`

```python
#!/usr/bin/env python3
"""Решения и подписи: кто подтвердил, чем заплатили, что объявлено в правах.

Часть `delivery_check.py` (`delivery@1.53`). Собрано по вопросу «что человек
решил и записал», а не по типу кода: подпись под спекой, блок прав в
CONSTITUTION и строки `decisions.md` — одна тема, и правила у них общие
(альтернатива и причина обязательны, шаблон решением не считается).
"""

from __future__ import annotations

import re

from delivery_base import list_entries, unfilled

def signature_verdict(name: str, raw: str, klass: str, phase: str) -> tuple[list[str], list[str]]:
    """§2.2b для ЛЮБОГО поля-подписи: `yes` / `deferred (reason=…)` / нет.

    Правило одно на класс полей, а не на одно имя. Дыру «человека нет, и канон про
    это молчит» закрыли для `human_ok_spec` — и она вернулась в `human_ok_plan`,
    потому что там стояла отдельная ветка с той же логикой минус `deferred`. Пока
    смысл один, а кода два, третье поле повторит историю.

    `deferred` не ослабляет проверку, а переносит её на мерж: до converge включительно
    работа идёт под запись долга, на handoff требуется `yes`.
    """
    value = raw.lower()
    errs: list[str] = []
    warns: list[str] = []
    if value.startswith("deferred"):
        if phase == "handoff":
            errs.append(
                f"class {klass} at handoff: {name} всё ещё deferred (§2.2b) — долг по "
                "подписи закрывается ДО мержа: покажи человеку и поставь yes (by=… at=…)"
            )
        elif "reason=" not in value:
            errs.append(
                f"{name}: deferred без reason= (§2.2b) — «человек недоступен» надо "
                "назвать, иначе это тихий обход"
            )
        else:
            warns.append(
                f"class {klass}: {name} deferred — ещё никем не подписано (§2.2b); "
                "долг закрывается до handoff"
            )
    elif not (value.startswith("yes") or "human:" in value):
        # Подпись — это `yes (by=human:…)` ИЛИ прямое `human:<имя> at=…`: у полей
        # разная форма (`human_ok_spec` против `asserts_reviewed_by`), а смысл один.
        # Проверять только префикс `yes` значило бы краснеть на канонной форме
        # второго поля — ложное срабатывание, которое §4.3b называет дефектом гейта.
        errs.append(
            f"class {klass} at phase={phase}: {name} без подписи человека "
            "(stop-gate §3.3; `yes (by=human:…)` или `human:<имя> at=…`; "
            "человека нет — deferred (reason=…), §2.2b)"
        )
    return errs, warns



def permission_block(constitution: str) -> dict[str, list[str]] | None:
    """Блок `agent-permissions` из CONSTITUTION (§4.5); None = блока нет.

    `allow` не разбираем сознательно: сверять накопительный список конкретных
    команд = постоянное ложное срабатывание (§4.3b, случай 1). `sandbox` (§4.6) —
    переводы из HITL в автономию, они не сверяются с настройками, а проверяются на
    существование оракула.
    """
    m = re.search(r"```(?:text)?\n[ \t]*agent-permissions[ \t]*\n(.*?)```", constitution, re.S)
    if not m:
        return None
    out: dict[str, list[str]] = {"deny": [], "ask": [], "sandbox": []}
    for raw in m.group(1).splitlines():
        line = raw.strip()
        if not line or line.startswith("#"):
            continue
        # partition по ПЕРВОМУ двоеточию: сами правила его содержат
        # ("deny: Bash(git push --force:*)").
        key, _, rule = line.partition(":")
        if key.strip() in out and rule.strip():
            out[key.strip()].append(rule.strip())
    return out


def decision_lines(text: str) -> tuple[list[str], list[str]]:
    """(валидные, битые) решения §12.2.

    Кандидат — только list-item: проза и заголовки в файле не проверяются.
    Валидная запись называет альтернативу И причину; без альтернативы это отчёт о
    сделанном, по которому аудит провести нельзя. Записи шаблона не попадают ни в
    один список: они не решения, но и ругаться на них как на битый формат нельзя.
    """
    ok: list[str] = []
    bad: list[str] = []
    for line in list_entries(text):
        if unfilled(line):
            continue
        low = line.lower()
        has_alt = "вместо" in low or "instead of" in low
        has_why = "потому что" in low or "because" in low
        (ok if (has_alt and has_why) else bad).append(line)
    return ok, bad


# Цена решения, а не предпочтение. «Выбрал A вместо B — потому что дешевле» формат
# §12.2 проходит, но **неопровержимо**: проверить это нельзя ни человеком, ни машиной,
# значит запись не даёт аудита, ради которого она и ведётся. «B потребовал бы миграции
# 40k строк» человек проверяет за секунды.
#
# ⚠ Признак — НЕ список глаголов. Первая версия (delivery@1.38) держала список
# «требует / ломает / зависит / переписать / миграц…», и он ошибался в обе стороны:
# обходился дописыванием слова, а хорошую запись отвергал. На dogfooding'е (lab-10)
# он пометил «без цены» решение, где следствие названо предельно конкретно — «длина
# съедала легитимное `First one. Second one?`», — просто глагола «съедала» в списке
# не было. Ложное срабатывание на своей же проверке в тот же день, когда она введена.
#
# Признак — СОЮЗ трёх, а не замена. Первая попытка правки была «только цитата из
# диффа», и она неверна по построению: **дифф содержит то, что выбрали, а не то, что
# отвергли**. Цена отвергнутого варианта естественно ссылается на то, чего в коде
# НЕТ («B требует внешнего сервиса, которого у нас нет», «B потребовал бы миграции»),
# и такая запись — образцовая, а проверка её отвергала. Поймал существующий тест.
#
# Поэтому: число ИЛИ цитата из диффа ИЛИ маркер следствия. Союз даёт наименьшую долю
# ложных срабатываний, и «дешевле» им всё равно не удовлетворить. По нашему критерию
# «кто здесь может поймать ложь» ответ — человек за секунды, значит задача проверки
# не принуждать, а придать записи форму, в которой ложь видна. Отсюда предупреждение,
# не ошибка, и широкий признак вместо узкого.
COST_MARKERS = (
    "требует", "требовал", "ломает", "сломал", "съедал", "съест", "зависит",
    "недоступ", "не поддерж", "переписать", "миграц", "потерял", "нет доступа",
    "запрещ", "нет в", "пришлось бы", "потребовал",
)

DECISION_STOPWORDS = frozenset(
    "test tests main init app src lib code file data self none true false "
    "type name path text line list dict json yaml toml".split()
)



def decisions_without_cost(text: str, diff_ids: set[str] | None = None) -> list[str]:
    """Решения, где цена не названа ни числом, ни ссылкой в код.

    `diff_ids=None` — дифф недоступен (нет ref'а, не git-репозиторий). Тогда
    судить нечем, и проверка молчит: предупреждение по неверной причине хуже его
    отсутствия — оно учит игнорировать этот гейт.
    """
    if diff_ids is None:
        return []
    out = []
    for line in decision_lines(text)[0]:
        # ⚠ Дата записи выкидывается ДО поиска числа. Формат §12.2 — «- <дата> ·
        # решение», поэтому цифры есть в КАЖДОЙ валидной записи, и наивный поиск
        # числа не срабатывал бы никогда. Третий случай этого класса за сессию:
        # хвост версии читался как количество, а цифра из id примера `A1`
        # закрывала проверку `observability: 2+`. Число надо искать там, где оно
        # что-то значит.
        body = re.sub(r"^\s*[-*]?\s*\d{4}-\d{2}-\d{2}\s*[·:—-]?\s*", "", line)
        if re.search(r"(?<![A-Za-z])\d", body):
            continue  # число — самая проверяемая форма цены
        low = body.lower()
        if any(m in low for m in COST_MARKERS):
            continue  # названо следствие
        words = {w.lower() for w in re.findall(r"[A-Za-zА-Яа-яЁё_][\w-]{3,}", body)}
        if words & diff_ids:
            continue  # цитирует код — такое не напишешь, не открыв дифф
        out.append(line)
    return out


```

# Приложение B4 — `scripts/delivery_risk.py`

```python
#!/usr/bin/env python3
"""Маячки на рисковый дифф (§12.5): что обязано быть прочитано человеком.

Часть `delivery_check.py` (`delivery@1.53`).
"""

from __future__ import annotations

import re
from pathlib import Path

from delivery_base import git

# --- Маячки на рисковый дифф (§12.5) ----------------------------------------
# Есть класс дефектов, у которых НЕТ оракула: архитектура выбрана неудачно, функция
# делает не то, что нужно. Механика такое не судит, и притворяться, что судит, —
# худшее из решений (см. §3.1d, врезка про отказ от оценки решений моделью).
#
# Что здесь делается вместо: не оценка качества, а **маршрутизация** — гарантия, что
# рисковый дифф кто-то ПРОЧИТАЛ, и след этого прочтения остался.
#
# ⚠ «Человек посмотрел» проверить нельзя: у этого заявления нет наблюдения для
# сверки, и требовать его — значит завести анкету. А вот «кто-то прочитал ИМЕННО
# ЭТОТ дифф» — можно: запись обязана цитировать идентификаторы, которые в диффе
# реально есть. Тот же приём, что `repro_test:` (не «ты тестировал?», а «тест
# существует и падает без фикса») и что признак цены решения (§12.2a).
#
# Чего это НЕ даёт, и это надо говорить прямо: доказательства, что смотрел ЧЕЛОВЕК,
# здесь нет. Есть доказательство, что дифф прочитан. Для конфигурации «один
# владелец + агенты» это достижимый потолок, и он всё равно ценен: чтение вынуждено,
# а запись владелец просматривает за секунды. Обойти можно, выгрепав идентификаторы,
# но это дороже, чем прочитать маленький дифф, и видно при просмотре.
RISK_CLASSES = {
    "деньги": r"(?i)\b(price|pricing|payment|invoice|balance|refund|billing|amount|"
              r"цена|платеж|платёж|баланс|счёт)\w*",
    # ⚠ Русские корни — только однозначные. `доступ` пришлось убрать: он совпадал
    # внутри слова `недоступен` в обычном комментарии, и маячок требовал ревью
    # безопасности от правки текстового чанкера. Ложное срабатывание от ФРАГМЕНТА,
    # а не от смысла; поймано на первом же прогоне маячка (арена lab-10).
    # Только СОСТАВНЫЕ формы там, где слово многозначно: `token` в этом проекте —
    # токен стрима LLM, и голый `\btoken` совпадал в `test_..._one_token`, требуя
    # ревью безопасности от правки текстового чанкера. На проекте, чей домен —
    # токены, такой маячок сработал бы на каждом диффе и был бы снят через неделю.
    "безопасность": r"(?i)(\bauth|access_token|auth_token|api_token|refresh_token|"
                    r"\bbearer|\bsecret|\bpassword|\bcredential|\bpermission|"
                    r"\bjwt\b|session_key|парол|авториз)",
    "транзакция БД": r"(?i)\b(commit|rollback|transaction|begin\(\)|savepoint|"
                     r"session\.begin|atomic)\w*",
    # lab-12: единственный класс, подтверждённый как slipped ОБЕИМИ арками, —
    # CPU-bound работа по всей выборке в async-хендлере. Маячок его тогда поднял,
    # но ПО ЧУЖОМУ ПОВОДУ («новый модуль» + `commit` у соседа в репозитории):
    # замер показал «classes matching perf/limit/scan: []». Та же правка в
    # СУЩЕСТВУЮЩЕМ модуле не подняла бы маячок вовсе.
    #
    # Признак — не слово «медленно», а ФОРМА: выборка без границы (`list_all`,
    # `.all()`, `fetchall`, `select(...)` без `limit`) и разбор/поиск в цикле.
    # Слова однозначные и составные, по правилу выше: голый `all` или `loop`
    # совпадал бы всюду и снял бы маячок за неделю.
    "производительность": r"(?i)(\blist_all\b|\bfetchall\b|\.all\(\)|"
                          r"scan_all|full_scan|\bselectinload\b|"
                          r"json\.loads|json\.dumps|\bre\.(search|compile|finditer)\b)",
    "цепочка гейтов": r"scripts/lint/|merge_guard|pre-commit-config",
    "интеграция": r"(?i)\b(httpx|requests\.|aiohttp|urllib|webhook|grpc|boto3|"
                  r"openai|anthropic)\w*",
}


def risky_classes(paths: list[str], base: str) -> dict[str, set[str]]:
    """Классы риска, которых коснулся дифф → чем именно они подняты.

    Возвращается не список имён, а КЛАСС → ТОКЕНЫ, которые его подняли
    (`производительность` → `{list_all, json.loads}`). Токены нужны проверке
    ниже: назвать класс можно и его именем, и тем, что его вызвало, — второе
    честнее, потому что показывает, что читатель видел конкретное место.
    """
    if not paths:
        return {}
    body = git("diff", "--unified=0", f"{base}..HEAD", "--", *paths)
    haystack = "\n".join(paths) + "\n" + body
    hits: dict[str, set[str]] = {}
    for name, pat in RISK_CLASSES.items():
        found = {m.group(0).lower() for m in re.finditer(pat, haystack)}
        if found:
            hits[name] = found
    # Новый модуль — отдельный класс: у него ещё нет ни одного читателя.
    # Токены — имена самих новых файлов: сказать «`transcripts.py` — чистые
    # функции, состояния нет» и есть разбор этого класса.
    added = {
        Path(p).stem.lower()
        for p in paths
        if p.endswith((".py", ".ts", ".tsx"))
        and git("log", "--diff-filter=A", "--format=%H", f"{base}..HEAD", "--", p).strip()
    }
    if added:
        hits["новый модуль"] = added
    return hits


def risk_review_gaps(
    report: str, classes: dict[str, set[str]], diff_ids: set[str]
) -> list[str]:
    """Чего не хватает в записи ревью рисковых мест.

    Требуется НЕ подтверждение, а содержание: назван класс и процитировано то, что
    в диффе есть. Законный выход — «риска нет, потому что …» с теми же цитатами:
    он тоже требует чтения, поэтому это не waiver и не галочка.
    """
    if not classes:
        return []
    block = re.search(
        r"(?ims)^#{1,4}\s*(?:ревью\s+рисковых|risk\s+review)\b.*?(?=^#{1,4}\s|\Z)",
        report,
    )
    if not block:
        return [
            "нет блока «## Ревью рисковых мест» (§12.5) — дифф задел: "
            + ", ".join(classes)
            + ". Гейта на это нет и быть не может: у «архитектура неудачна» нет "
              "оракула. Нужно НЕ подтверждение, что смотрел, а содержание: что "
              "именно может сломаться, с именами из диффа. Риска нет — так и "
              "напиши «риска нет, потому что …», тоже с именами"
        ]
    body = block.group(0)
    low = body.lower()
    gaps = []
    words_of = lambda t: {w.lower() for w in re.findall(r"[A-Za-z_][A-Za-z0-9_]{3,}", t)}

    # КАЖДЫЙ задетый класс обязан быть разобран, а не «хотя бы один».
    #
    # Было «один класс где угодно в блоке + ≥2 цитаты где угодно» — это покупало
    # заголовок, а не содержание: lab-12 показала пробой, что проходят и
    # трёхстрочный блок с одним классом, и настоящий блок, из которого удалили
    # ровно перф-строку. Отписка по частям — тот же класс, что отписка целиком.
    #
    # ⚠ Класс считается разобранным ДВУМЯ способами: назвали его имя ИЛИ назвали
    # тот токен, который его поднял (`list_all`, `session.begin`, имя нового
    # модуля). Второе честнее имени: оно показывает, что читатель видел
    # конкретное место, а не переписал ярлык. И это снимает хрупкость: требовать
    # дословного русского ярлыка в каждой строке значило бы краснить осмысленную
    # запись — ровно та ловушка §4.3b, из-за которой первая версия этой правки
    # покраснела на собственном тесте «законный выход» (класс «новый модуль»
    # структурный, его никто не называет словами).
    # «новый модуль» из требования исключён СОЗНАТЕЛЬНО: он структурный — поднимает
    # маячок (заставляет написать блок), но своей строки не требует, потому что
    # разбор любого места нового файла и есть его разбор. Требование отдельной
    # строки под него краснило бы осмысленные записи — поймано собственным тестом
    # «законный выход» на первой версии правки.
    STRUCTURAL = {"новый модуль"}
    undiscussed = [
        c for c, tokens in (classes or {}).items()
        if c not in STRUCTURAL
        and c.lower() not in low
        and not any(t in low for t in tokens)
    ]
    if undiscussed:
        gaps.append(
            "в блоке ревью не разобран(ы) задетый(е) класс(ы) риска: "
            + ", ".join(undiscussed)
            + " (§12.5) — на каждый нужна строка: либо чем он опасен здесь, либо "
            "«риска нет, потому что …». Достаточно назвать то, что его подняло "
            "(функцию, поле, файл из диффа) — ярлык переписывать не обязательно"
        )

    cited = words_of(body) & diff_ids
    if len(cited) < 2:
        gaps.append(
            f"в блоке ревью {len(cited)} цитат(ы) из диффа, нужно ≥2 (§12.5) — "
            "«проверил, всё ок» проверить нельзя ничем; назови функции, поля или "
            "файлы, которые смотрел: этого не написать, не открыв дифф"
        )
    return gaps


```

# Приложение B5 — `scripts/delivery_runtime.py`

```python
#!/usr/bin/env python3
"""§12.6: путь, который проверяется только исполнением.

Часть `delivery_check.py` (`delivery@1.53`). Класс отказов, невидимый всем
статическим оракулам: платформа отвечает падением процесса, а не кодом ошибки.
"""

from __future__ import annotations

import fnmatch
import re

from delivery_base import DEFAULT_BREAKERS, field, is_placeholder

# --- §12.6: путь, который проверяется только исполнением ---------------------
# Есть класс отказов, невидимый ВСЕМ статическим оракулам и необрабатываемый в
# рантайме: платформа отвечает на нарушение политики смертью процесса, а не кодом
# ошибки. Замер (iOS, четыре падения за сутки, разбор в поле): нет ключа в
# Info.plist — падение; запрос снимка сверх потолка выхода — NSException; чтение
# библиотеки под правом «только добавлять» — падение от TCC. Ни одно не возвращает
# ошибку, которую можно поймать. Сборка при этом ЗЕЛЁНАЯ во всех трёх случаях.
#
# Отсюда правило: такой путь обязан быть ИСПОЛНЕН до handoff. Не «покрыт тестом» —
# исполнен на целевой среде, потому что именно среда и выносит вердикт.
#
# Почему это отдельно от §12.5, хотя механика похожа. §12.5 доказывает, что дифф
# ПРОЧИТАН, и этого достаточно для «архитектура неудачна». Здесь чтение не
# помогает вовсе: все четыре отказа были в коде, который автор читал и считал
# верным. Рассуждение о правах — не проверка прав; замер поля прямо это назвал
# («я рассуждал о правах вместо того, чтобы их проверить»).
def runtime_surfaces(status: str) -> tuple[set[str], bool]:
    """(объявленные пути, объявлено ли осознанно) из строки `runtime_paths:`.

    Второе значение отличает «объявили `none reason=…`» от «поля нет вовсе».
    Разница ровно та же, что у `n/a` без причины в остальном каноне: не думали и
    подумали-и-нет — разные состояния, и молчание не должно выглядеть как ответ.
    """
    raw = field(status, "runtime_paths")
    if is_placeholder(raw):
        return set(), False
    if re.match(r"(?i)^\s*none\b", raw):
        # `none` без причины — то же, что его отсутствие: см. `n/a reason=` у
        # `diagnosis:`. Слово «none» само по себе ничего не сообщает.
        return set(), bool(re.search(r"(?i)reason\s*=\s*\S", raw))
    return {p.strip() for p in raw.split(",") if p.strip()}, True


def breaker_value(status: str, name: str) -> int:
    """Порог breaker'а с учётом ЗАКОННОГО override, без дублирования принуждения.

    Override принимается по тому же правилу, что у остальных breaker'ов §3.4:
    либо строка `circuit_breakers:`, либо подписанный waiver (`reason=` +
    `by=human:`). Ругается на неподписанный override НЕ здесь, а тот же парсер,
    что и для `max_files_touched` — ключ лежит в `DEFAULT_BREAKERS`, поэтому
    ветка ошибки достаётся ему. Две реализации одного правила разъехались бы
    при первой же правке, а тут это значило бы «лимит поднят молча».
    """
    val = DEFAULT_BREAKERS.get(name, 1)
    for m in re.finditer(rf"({re.escape(name)})[ \t]*=[ \t]*(\d+)", status):
        bol = status.rfind("\n", 0, m.start()) + 1
        eol = status.find("\n", m.end())
        line = status[bol: eol if eol != -1 else len(status)]
        low = line.lower()
        if "circuit_breakers" in low or ("reason=" in low and "by=human:" in low):
            val = int(m.group(2))
    return val


def runtime_touched(paths: list[str], surfaces: set[str]) -> set[str]:
    """Какие объявленные поверхности задел дифф.

    Совпадение по префиксу И по glob: проект объявляет либо каталог
    (`ios/LashTryOn/Camera/`), либо маску (`**/Capture*.swift`). Требовать одну
    форму значило бы заставить подгонять раскладку под гейт.
    """
    hit: set[str] = set()
    for s in surfaces:
        pat = s.strip().rstrip("/")
        for p in paths:
            if p.startswith(pat + "/") or p == pat or fnmatch.fnmatch(p, s):
                hit.add(s)
                break
    return hit


def runtime_proof_gaps(report: str, touched: set[str]) -> list[str]:
    """Чего не хватает в записи об исполнении.

    ⚠ Прямо о пределе, как это делает §12.5 про своё: **доказательства, что
    прогон был, здесь нет и быть не может** — есть доказательство, что про
    КАЖДУЮ задетую поверхность сделано конкретное утверждение с датой и названа
    команда. Это ловит не ложь, а пропуск, и пропуск — ровно то, что случилось
    все четыре раза: путь не был исполнен НИ РАЗУ, а не исполнен плохо.
    """
    if not touched:
        return []
    block = re.search(
        r"(?ims)^#{1,4}\s*(?:исполнение\s+рисковых|runtime\s+run)\b.*?(?=^#{1,4}\s|\Z)",
        report,
    )
    if not block:
        return [
            "нет блока «## Исполнение рисковых путей» (§12.6) — дифф задел: "
            + ", ".join(sorted(touched))
            + ". Это пути, чей отказ не виден ни сборке, ни тестам — падением "
              "процесса (права, платформа) или молча (раскладка экрана). Нужен "
              "не вывод, что всё должно работать, а строка «прогнал <что>, "
              "увидел <что> at=<дата>»"
        ]
    body = block.group(0)
    low = body.lower()
    gaps: list[str] = []

    # Каждая задетая поверхность — своя строка. «Прогнал, всё ок» одной строкой
    # на три поверхности это отписка по частям, и §12.5 уже платила за то, чтобы
    # это понять: там пробой прошёл блоком с одним классом из трёх.
    # Поверхность считается названной ДВУМЯ способами: написали её саму
    # (`ios/UI/`) или назвали её содержательный сегмент (`Camera`, `Capture`).
    #
    # ⚠ Одних сегментов НЕ хватает, и это поймал собственный тест: у `ios/UI/`
    # нет ни одного куска длиннее трёх символов, поэтому удовлетворить проверку
    # было нечем ВООБЩЕ — гейт, который не пройти. Ложное срабатывание тут
    # дороже пропуска: §4.3b, такую проверку снимают с проекта за неделю вместе
    # с пользой. Сначала ищем саму строку пути, сегменты — только запасной путь
    # для глобов вида `**/Capture*.swift`.
    def named(s: str) -> bool:
        if s.strip().rstrip("/").lower() in low:
            return True
        return any(tok and tok.lower() in low
                   for tok in re.split(r"[/*]", s) if len(tok) > 3)

    silent = [s for s in sorted(touched) if not named(s)]
    if silent:
        gaps.append(
            "в блоке исполнения не названы задетые пути: " + ", ".join(silent)
            + " (§12.6) — на каждый нужна своя строка: исполнять их надо по "
              "одному, иначе отказ приходит составным и разбирается перебором"
        )
    if not re.search(r"(?i)\bat\s*=\s*\d", body):
        gaps.append(
            "в блоке исполнения нет отметки `at=<дата>` (§12.6) — прогон без "
            "даты неотличим от прогона до правки, а именно так и выглядел бы "
            "самый вероятный обход"
        )
    if "`" not in body:
        gaps.append(
            "в блоке исполнения не названа команда (§12.6) — впиши в backticks "
            "то, ЧЕМ исполнял (`-scanShot 1`, `plutil -p …`): инструмент, "
            "которого нельзя повторить, следующий раз будет выброшен, а не починен"
        )
    return gaps


```

# Приложение B6 — `scripts/delivery_history.py`

```python
#!/usr/bin/env python3
"""История: откуда взялось ожидание (§3.1d) и тает ли долг во времени (§3.5).

Часть `delivery_check.py` (`delivery@1.53`). Обе проверки смотрят не в текущее
дерево, а в git-историю — поэтому и живут вместе.
"""

from __future__ import annotations

import functools
import os
import re

from delivery_base import ARCHIVE, ROOT, git

# --- Происхождение ожидания (§3.1d) -----------------------------------------
# §3.1d говорит: «примеры пишутся на фазе specify, ДО plan и до кода. Порядок
# принципиален: ожидание, придуманное после реализации, — это описание реализации».
# До delivery@1.38 это принуждалось НИЧЕМ: проверялось наличие блока, наличие id и
# ссылки на них в тестах — то есть три признака, ни один из которых не про порядок.
#
# Память — история git, как в §3.5. Ищем `git log -S<id>`: коммиты, где менялось
# число вхождений строки. Самый старый — тот, где id появился.
#
# ⚠ ТРИ исхода, а не два, и это не мягкость. Агент часто кладёт спеку, код и тесты
# ОДНИМ коммитом; требовать «спека строго старше» значило бы краснеть рутинно, и
# проверку сняли бы (класс F4/F6/F13). Когда всё в одном коммите, порядок
# установить НЕЛЬЗЯ — честный ответ «не проверить», а не «нарушение». Считается и
# печатается числом: это подсказывает коммитить спеку отдельно, ничего не ломая.
def _first_commit_with(text: str, *paths: str) -> str:
    """Самый старый коммит, где появился ТОКЕН `text` в указанных путях.

    Токен, а не подстрока, и это не педантизм. lab-12: id примеров короткие
    (`A2`), а `-S` считает вхождения подстроки — `A2` совпадал внутри
    `DATA2_FIXTURE` в legacy-тесте, закоммиченном задолго до поставки. Итог:
    ложное «пример появился ПОЗЖЕ теста», то есть обвинение в худшем
    процессном грехе (ожидание придумано после кода) на честной работе.
    Исполнителю его не снять ничем, кроме переименования id, — что одна арка и
    сделала, порвав связь с id, подписанными заказчиком. **Канон учил себя
    обходить**, а обход стоил ровно того, ради чего проверка и написана.

    ⚠ Граница — явный класс символов, а НЕ `\\b`. Замерено: `-S'\\bA2\\b'
    --pickaxe-regex` в git не находит НИЧЕГО, то есть очевидная правка сделала
    бы проверку МЁРТВОЙ — хуже исходного дефекта, и обратный прогон «сломано →
    падает» этого бы не показал (падало бы и там, и там). Тот же класс, что
    `\\b` в awk и `^\\+` в ugrep.
    """
    if not paths:
        return ""
    token = rf"(^|[^A-Za-z0-9_]){re.escape(text)}([^A-Za-z0-9_]|$)"
    out = git("-C", str(ROOT), "log", "--format=%H", f"-S{token}",
              "--pickaxe-regex", "--", *paths)
    revs = [l.strip() for l in out.split("\n") if l.strip()]
    return revs[-1] if revs else ""


def expectation_predates_tests(ex_ids: list[str], test_paths: list[str]) -> list[str]:
    """[(предупреждения)] — порядок «пример раньше теста» по истории.

    Возвращает предупреждения, а не ошибки: до первого живого проекта у нас нет
    данных, как часто «не проверить» встречается в норме. Ужесточать — после
    замера, а не из симметрии.
    """
    if not ex_ids or not test_paths:
        return []
    order = {rev: i for i, rev in enumerate(
        git("-C", str(ROOT), "rev-list", "--topo-order", "HEAD").split())}
    if not order:
        return []
    spec_rel = "delivery/active/spec.md"
    after, unknown = [], []
    for ex in dict.fromkeys(ex_ids):
        s_rev = _first_commit_with(ex, spec_rel)
        t_rev = _first_commit_with(ex, *test_paths)
        if not s_rev or not t_rev:
            continue  # id ещё не в истории — поставка не закоммичена, не наше дело
        if s_rev == t_rev:
            unknown.append(ex)
        elif order.get(s_rev, 0) < order.get(t_rev, 0):
            # Меньший индекс = НОВЕЕ. Пример новее теста — ожидание дописали после.
            after.append(ex)
    msgs = []
    if after:
        msgs.append(
            "§3.1d: примеры появились в истории ПОЗЖЕ ссылающихся на них тестов: "
            f"{', '.join(after)}. Ожидание, придуманное после реализации, — это "
            "описание реализации, и все гейты ниже проверяют его согласие с самим "
            "собой. Такой пример надо переписать от задачи, а не от кода"
        )
    if unknown:
        msgs.append(
            f"§3.1d: у {len(unknown)} из {len(dict.fromkeys(ex_ids))} примеров "
            f"порядок не проверить — пример и тест приехали одним коммитом "
            f"({', '.join(unknown[:5])}). Это не нарушение, но и не доказательство: "
            "коммить спеку отдельным коммитом до кода, тогда порядок виден"
        )
    return msgs


# --- Долг во времени (§3.5) -------------------------------------------------
# Гейты смотрят на один коммит, а вырождение — это тренд. Ратчет запрещает снимку
# РАСТИ и ничего не говорит про то, что он стоит на месте: двадцать поставок с
# неподвижным снимком зелёные все двадцать, и формально никто не нарушил. Платить
# по долгу заставляет только соглашение «тронул легаси — почисти часть», то есть
# честность исполнителя: не заметил или схалтурил — запись живёт вечно.
#
# ПАМЯТЬЮ СЛУЖИТ ИСТОРИЯ GIT, а не отдельный файл. Свою «тетрадку» валидатора
# можно поправить тем же коммитом, что и нарушение, — снаружи всё сойдётся, и
# подделывать даже не надо, достаточно пересчитать. Историю так не поправишь:
# нужно переписать прошлое, а это видно. Точки замера — коммиты, добавившие
# каталоги в `delivery/archive/`: они и есть отметки «поставка закрыта».
DEBT_WINDOW = int(os.environ.get("DEBT_WINDOW", "5"))
BASELINE_GLOB = "scripts/lint/*_baseline.txt"
# Формат снимков всех ратчет-гейтов один: `<число>:<путь>`. Агрегатные строки
# (`py_total=0 js_critical=0`) записями долга не являются и не считаются.
DEBT_ENTRY = re.compile(r"^(\d+):(.+)$")


def debt_entries(blob: str) -> dict[str, int]:
    """{путь: вес} из текста одного снимка. Комментарии и секции пропускаются."""
    out: dict[str, int] = {}
    for line in blob.splitlines():
        m = DEBT_ENTRY.match(line.strip())
        if m:
            out[m.group(2)] = out.get(m.group(2), 0) + int(m.group(1))
    return out


@functools.lru_cache(maxsize=None)
def _debt_at_cached(rev: str) -> tuple[tuple[str, int], ...]:
    """Кэш на ревизию: одна и та же точка окна читается дважды (сумма и возраст),
    а каждое чтение — это `git ls-tree` плюс `git show` на снимок."""
    total: dict[str, int] = {}
    # `-C ROOT` обязателен: `ROOT` берётся от пути скрипта, а `git` без него
    # работал бы в ТЕКУЩЕМ каталоге. В продакшене они совпадают, но проверка,
    # правильность которой зависит от того, откуда её позвали, — не проверка.
    names = git("-C", str(ROOT), "ls-tree", "--name-only", "-r", rev,
                "scripts/lint/").split()
    for name in names:
        if not name.endswith("_baseline.txt"):
            continue
        for path, weight in debt_entries(
                git("-C", str(ROOT), "show", f"{rev}:{name}")).items():
            total[path] = total.get(path, 0) + weight
    return tuple(sorted(total.items()))


def debt_at(rev: str) -> dict[str, int]:
    """Долг на ревизии: объединение снимков всех гейтов."""
    return dict(_debt_at_cached(rev))


def archive_samples() -> list[str]:
    """Ревизии закрытия поставок — по одной на каталог в `delivery/archive/`.

    Берём коммит, ДОБАВИВШИЙ каталог (`--diff-filter=A`, последний в логе), в
    порядке от старых к новым. Число поставок — десятки, поэтому это дешёвле, чем
    обходить всю историю снимков.
    """
    if not ARCHIVE.is_dir():
        return []
    # Порядок берём из ИСТОРИИ, а не из времени коммита. Сортировка по `%ct`
    # разъезжается при совпадении секунды: в фикстуре все поставки попали в одну,
    # и началом окна оказалась САМАЯ НОВАЯ — проверка сравнивала «сейчас» с
    # «сейчас» и молчала. В настоящем репозитории это редкость, но оракул, чья
    # правильность зависит от скорости коммитов, оракулом не является.
    order = {rev: i for i, rev in enumerate(
        git("-C", str(ROOT), "rev-list", "--topo-order", "HEAD").split())}  # 0 = самый новый
    revs: list[str] = []
    for d in sorted(p for p in ARCHIVE.iterdir() if p.is_dir()):
        rel = f"delivery/archive/{d.name}"
        log = [l for l in git("-C", str(ROOT), "log", "--diff-filter=A",
                              "--format=%H", "--", rel)
               .split("\n") if l.strip()]
        if log:
            revs.append(log[-1].strip())
    # order даёт «насколько давно»: больший индекс = старше. Нужен возрастающий
    # возраст -> сортируем по убыванию индекса.
    return sorted(set(revs), key=lambda r: -order.get(r, 0))


def debt_is_not_frozen() -> list[str]:
    """Долг обязан таять, и у него не должно быть вечных жильцов.

    Три состояния, и проверка узнаёт их САМА, без разрешения от агента, — иначе
    получился бы гейт, который нельзя пройти в конфигурации, разрешённой каноном
    (тот же класс, что F4/F6/F13, и первое, что с ним сделают, — отключат):
      • долга нет (снимки пусты или их нет) -> проход;
      • данных мало (закрытых поставок меньше окна) -> проход + сколько накоплено;
      • накопилось -> судим.
    """
    now = debt_at("HEAD")
    if not now:
        return []  # долга нет — таять нечему, и это нормальное состояние
    samples = archive_samples()
    if len(samples) < DEBT_WINDOW:
        print(
            f"debt: накоплено {len(samples)} закрытых поставок из {DEBT_WINDOW} — "
            f"проверка тренда включится на {DEBT_WINDOW}-й "
            f"(сейчас в долге {len(now)} записей, вес {sum(now.values())})"
        )
        return []

    window = samples[-DEBT_WINDOW:]
    oldest = debt_at(window[0])
    if not oldest:
        return []  # на начале окна долга не было — мерить нечего

    errors: list[str] = []
    was, is_now = sum(oldest.values()), sum(now.values())
    if is_now >= was:
        errors.append(
            f"debt: за последние {DEBT_WINDOW} поставок долг не растаял "
            f"({was} -> {is_now}). Ратчет запрещает РОСТ и не требует таяния, "
            "поэтому неподвижный снимок проходит вечно — а значит правило "
            "«baseline тает» держится ни на чём. Почисти часть легаси в этой "
            "поставке либо назови причину в blockers и перенеси срок"
        )

    # Вечный жилец: запись, прожившая ВСЁ окно И НЕ СДВИНУВШАЯСЯ. Сумму можно
    # уронить, растопив лёгкие записи, а тяжёлые оставив гнить, — поэтому меряется
    # и возраст. Но «прожила окно» само по себе жильцом не делает: запись, которую
    # честно чистят по частям (40 -> 34 -> ... -> 10), тоже живёт всё окно, и
    # ругаться на неё — верный способ добиться того, чтобы проверку отключили.
    # Признак гниения — вес не уменьшился ни разу.
    samples_debt = [debt_at(r) for r in window]
    stuck = sorted(
        p for p in now
        if all(p in s for s in samples_debt) and now[p] >= samples_debt[0][p]
    )
    if stuck:
        errors.append(
            f"debt: {len(stuck)} записей живут все {DEBT_WINDOW} поставок подряд — "
            f"это уже не «известное нарушение», а вечный жилец: "
            + ", ".join(stuck[:5])
            + ("…" if len(stuck) > 5 else "")
        )
    return errors


```

# Приложение B7 — `scripts/delivery_status.py`

```python
#!/usr/bin/env python3
"""Форма поставки: фаза, класс, обязательные артефакты фазы.

Часть `delivery_check.py` (`delivery@1.53`) — гейт разрезан по планке 300 строк
(§9.1a п.5), из 1969 строк 986 приходились на одну функцию `main`. Здесь первый
раздел: он же вычисляет то, что читают следующие, и отдаёт это `ActiveCtx` —
восемь значений, а не полсотни локальных переменных, как было внутри `main`.
"""

from __future__ import annotations

import re

from delivery_base import (ACTIVE, ActiveCtx, field,
                           is_placeholder, read)
from delivery_decisions import signature_verdict
from delivery_diff import diff_identifiers, diff_stats
from delivery_risk import risk_review_gaps, risky_classes
from delivery_runtime import (breaker_value, runtime_proof_gaps,
                              runtime_surfaces, runtime_touched)

def check_status_shape(status: str, args, errors: list[str], warnings: list[str]) -> ActiveCtx:
    """Фаза, класс и артефакты фазы. → контекст для остальных разделов."""
    allowed = {
        "specify",
        "plan",
        "tasks",
        "implement",
        "verify",
        "converge",
        "handoff",
    }
    implement_like = {"implement", "verify", "converge", "handoff"}

    raw_phase = field(status, "phase")
    raw_class = field(status, "class")

    phase_m = (
        re.match(r"(?i)^([a-z_]+)", raw_phase)
        if not is_placeholder(raw_phase)
        else None
    )
    class_m = (
        re.match(r"(?i)^([SML])\b", raw_class)
        if not is_placeholder(raw_class)
        else None
    )
    phase = phase_m.group(1).lower() if phase_m else ""
    klass = class_m.group(1).upper() if class_m else ""

    if is_placeholder(raw_phase):
        errors.append(
            f"STATUS.md: phase not filled in (template placeholder {raw_phase!r})"
        )
    elif not phase:
        errors.append("STATUS.md: missing phase")
    elif phase not in allowed:
        # constitution is project-level file, not active phase
        errors.append(
            f"STATUS.md: unknown phase '{phase}' "
            f"(allowed: {', '.join(sorted(allowed))})"
        )

    if not klass:
        msg = (
            f"STATUS.md: class not resolved (value {raw_class!r}) — "
            "cannot apply §2.2 artifact gates; write exactly one of S|M|L"
        )
        # На specify класс ещё может уточняться; с plan и дальше это блокер:
        # неизвестный класс = молча отключённые требования spec/plan.
        if phase == "specify":
            warnings.append(msg)
        else:
            errors.append(msg)

    spec = ACTIVE / "spec.md"
    plan = ACTIVE / "plan.md"
    tasks = ACTIVE / "tasks.md"
    verify = ACTIVE / "verify-report.md"
    eval_smoke = ACTIVE / "eval-smoke.md"

    if klass in {"M", "L"} and phase in {
        "plan",
        "tasks",
        *implement_like,
    }:
        if not spec.is_file():
            errors.append(f"class {klass} at phase={phase}: missing active/spec.md")

    if klass in {"M", "L"} and phase in implement_like:
        if not plan.is_file():
            errors.append(f"class {klass} at phase={phase}: missing active/plan.md")
        if not tasks.is_file():
            errors.append(f"class {klass} at phase={phase}: missing active/tasks.md")
        # §3.3 называет это stop-gate'ом — значит error, не warning.
        # `deferred` (§2.2b) — законный третий вариант: человека нет, работа
        # идёт под запись долга. Он пускает до converge включительно, но НЕ на
        # handoff: подпись под ожиданиями обязательна там, где человек всё
        # равно есть — на мерже. Без этого значения агент без доступного
        # человека занижал класс до S, обходя требование спеки законным с виду
        # способом (найдено независимым развёртыванием).
        e, w = signature_verdict("human_ok_spec", field(status, "human_ok_spec"), klass, phase)
        errors += e
        warnings += w

    if klass == "L" and phase in implement_like:
        # §2.2b распространяется на ВСЕ поля-подписи, а не только на спеку:
        # `human_ok_plan` упирался в тот же тупик, и агент без человека выбирал
        # между «встать» и «занизить класс» — дыра, закрытая один раз, вернулась
        # во втором поле. Развилка теперь одна на класс полей (см. функцию).
        e, w = signature_verdict("human_ok_plan", field(status, "human_ok_plan"), klass, phase)
        errors += e
        warnings += w

    if klass == "S" and phase in implement_like and not tasks.is_file():
        errors.append("class S at implement+: missing active/tasks.md (mini-spec)")

    if not verify.is_file():
        if phase == "verify":
            # Фаза в процессе: отчёт ещё пишется — но выйти из неё без него нельзя.
            warnings.append(
                "phase=verify: active/verify-report.md not created yet"
            )
        elif phase in {"converge", "handoff"}:
            errors.append(
                f"phase={phase}: missing active/verify-report.md (DoD §3.2.2)"
            )

    if klass in {"M", "L"} and phase in {"verify", "converge", "handoff"}:
        if not eval_smoke.is_file():
            errors.append(
                "class M/L at verify+: missing active/eval-smoke.md "
                "(product oracles are mandatory, §6.2)"
            )

    # --- §12.5: маячок на рисковый дифф. Предупреждение на verify, ОТКАЗ на
    # handoff — та же лестница, что у `deferred` (§2.2b): работать не мешаем,
    # объявить сделанным не даём. Блокировать на каждом коммите нельзя: правка
    # опечатки получала бы требование ревью, и маячок сняли бы через неделю.
    if phase in {"verify", "converge", "handoff"}:
        rbase = args.diff_base or "HEAD~1"
        rstats = diff_stats(rbase)
        if rstats and rstats[4]:
            gaps = risk_review_gaps(
                read(verify),
                risky_classes(rstats[4], rbase),
                diff_identifiers(rstats[4]),
            )
            for g in gaps:
                (errors if phase == "handoff" else warnings).append(
                    f"рисковый дифф: {g}"
                )

            # --- §12.6: путь, проверяемый только исполнением. Та же лестница
            # (предупреждение на verify, отказ на handoff) и по той же причине.
            surfaces, declared = runtime_surfaces(status)
            if not declared:
                (errors if phase == "handoff" else warnings).append(
                    "нет строки `runtime_paths:` в STATUS (§12.6) — назови "
                    "пути, чей отказ не виден ни сборке, ни тестам (экран, "
                    "права, железо, тракт съёмки), либо `none reason=…`. "
                    "Молчание тут читается как «таких нет», а замер поля дал "
                    "четыре падения подряд ровно в них"
                )
            else:
                touched = runtime_touched(rstats[4], surfaces)
                for g in runtime_proof_gaps(read(verify), touched):
                    (errors if phase == "handoff" else warnings).append(
                        f"исполнение: {g}"
                    )
                # Breaker по ПОВЕРХНОСТЯМ, а не по объёму. §3.4 считает файлы
                # и строки, и три правки в три подсистемы на десяток строк
                # проходят его не заметив — а именно так и выглядел составной
                # отказ, который потом разбирали перебором.
                cap = breaker_value(status, "max_runtime_paths")
                if len(touched) > cap:
                    (errors if phase == "handoff" else warnings).append(
                        f"исполнение: одной поставкой задето {len(touched)} "
                        f"рисковых путей ({', '.join(sorted(touched))}) при "
                        f"пороге {cap} (§12.6) — ставь по одному: составной "
                        "отказ разбирается перебором, а перебор оплачивает "
                        "человек. Осознанно — строкой "
                        "`max_runtime_paths=<N> reason=… by=human:…` в STATUS"
                    )


    return ActiveCtx(phase=phase, klass=klass, spec=spec, plan=plan, tasks=tasks, verify=verify, eval_smoke=eval_smoke, implement_like=implement_like)
```

# Приложение B8 — `scripts/delivery_evidence.py`

```python
#!/usr/bin/env python3
"""Доказательства: происхождение ожидания, ревью утверждений, репро-тест.

Часть `delivery_check.py` (`delivery@1.53`). Раздел отвечает на вопрос «чем
подтверждено», а не «что заполнено»: подпись под примерами, дифф утверждений,
тест, падавший до фикса.
"""

from __future__ import annotations

import re

from delivery_base import (ACTIVE, ActiveCtx, field, is_placeholder,
                           read)
from delivery_base import SKIP_DIR_PARTS, TEST_TEXT_SUFFIXES
from delivery_decisions import signature_verdict
from delivery_history import debt_is_not_frozen, expectation_predates_tests

def check_evidence(status: str, args, errors: list[str], warnings: list[str], ctx: ActiveCtx) -> None:
    """Ожидание раньше кода, утверждения отревьюены, багфикс принёс тест."""
    phase, klass, spec, tasks, verify, eval_smoke, implement_like = (
        ctx.phase,
        ctx.klass,
        ctx.spec,
        ctx.tasks,
        ctx.verify,
        ctx.eval_smoke,
        ctx.implement_like)
    # --- Долг не должен болтаться вечно (§3.5). Проверяется ТОЛЬКО на handoff:
    # на каждом коммите это блокировало бы работу, не связанную со старым
    # долгом, и такой гейт отключают через неделю. Работать не мешаем —
    # не даём объявить сделанным.
    if phase == "handoff":
        errors.extend(debt_is_not_frozen())

    # --- §13.1: поставка уходит в archive вместе с обязательством проверить
    # её в проде. Без этих полей «готово» проверено во всех средах, кроме
    # единственной значимой.
    if phase == "handoff":
        sig = field(status, "observe_signal")
        until = field(status, "observe_until")
        if is_placeholder(sig):
            errors.append(
                "phase=handoff: пусто observe_signal (§13.2) — что именно "
                "будет проверено в проде; «ошибок нет» сигналом не является, "
                "молчание одинаково выглядит и при не вызываемом коде"
            )
        if is_placeholder(until):
            errors.append(
                "phase=handoff: пусто observe_until (§13.1) — до какой даты "
                "наблюдаем; обязательство без срока не выполняется"
            )
        # Уровень наблюдаемости — значение проверяемое, а не декларативное
        # (урок §10.4: самозаявленное `ci-oracles: tooling` при красном CI).
        level = field(status, "observability")
        if not is_placeholder(level) and level.strip().startswith(("2", "3")):
            # Цифра, которой НЕ предшествует латинская буква: так число порога
            # (`180мс`) отличается от id примера (`A1`) и от имени метрики
            # (`p95`). Наивный поиск любой цифры проходил на сигнале
            # «A1 подтверждён, ошибок нет» — то есть по неверной причине.
            if not re.search(r"(?<![A-Za-z])\d", sig):
                errors.append(
                    f"observability: {level.strip()} заявлен, но observe_signal "
                    "без метрики с числом (§13.3) — уровень 2+ означает замер "
                    "ДО и порог, а не «мониторинг вроде подключён»"
                )

    if phase == "handoff" and verify.is_file():
        vr = read(verify)
        if not re.search(r"READY FOR HANDOFF", vr, re.I):
            errors.append(
                "phase=handoff: verify-report.md lacks 'READY FOR HANDOFF' verdict"
            )
        # §9.2: метрики снимаются на handoff. Без гейта ритуал не выполняется.
        if "Harness metrics" not in vr:
            errors.append(
                "phase=handoff: verify-report.md lacks the 'Harness metrics' "
                "block (§9.2 / A.10) — run: python scripts/delivery_metrics.py "
                "--base origin/main --write"
            )

    # --- Builder ≠ Verifier (§5.2): не сама независимость, но явное заявление
    # о ней. Настоящее разделение обеспечивает required review в branch
    # protection (CQG §8.5); здесь ловим «сам построил, сам принял».
    if phase in {"verify", "converge", "handoff"} and verify.is_file():
        declared = field(read(verify), "Verifier") or field(status, "verifier")
        builder = field(status, "builder")
        if is_placeholder(declared):
            errors.append(
                "verify-report.md: Verifier not filled in — Builder must not "
                "accept own work (§5.2); write process:ci | agent:NAME | human:NAME"
            )
        elif (
            klass in {"M", "L"}
            and not is_placeholder(builder)
            and declared.strip().lower() == builder.strip().lower()
        ):
            errors.append(
                f"class {klass}: verifier == builder ('{declared}') — §5.2 "
                "requires a different agent/model/human, or process:ci"
            )

    # --- §3.1d уровень 3 / DoD §3.2.7: ревью утверждений пропорционально
    # непроверяемой части. `n/a` законен, только если КАЖДОЕ утверждение
    # ведёт к примеру, который человек подписал до кода; иначе ожидание
    # придумал исполнитель, и подпись обязана быть.
    if klass in {"M", "L"} and phase in {"verify", "converge", "handoff"} and verify.is_file():
        vr = read(verify)
        reviewed = field(vr, "asserts_reviewed_by")
        digest = re.search(r"(?im)^\s*asserts_without_example:\s*(\d+)\s*$", vr)
        if is_placeholder(reviewed):
            errors.append(
                f"class {klass}: verify-report.md без asserts_reviewed_by "
                "(§3.1d уровень 3, DoD §3.2.7) — последний рубеж против "
                "неверного ожидания; вставь дайджест: "
                "bash scripts/lint/assert_digest.sh >> verify-report.md"
            )
        elif reviewed.lower().startswith("n/a"):
            if not digest:
                errors.append(
                    "asserts_reviewed_by: n/a без вставленного дайджеста "
                    "(нет строки 'asserts_without_example: N') — n/a надо "
                    "заслужить, а не заявить (§3.1d уровень 3)"
                )
            elif int(digest.group(1)) > 0:
                errors.append(
                    f"asserts_reviewed_by: n/a, но {digest.group(1)} "
                    "утверждений не ссылаются на примеры спеки — их ожидание "
                    "придумал исполнитель; нужна подпись human:… at=… "
                    "(§3.1d уровень 3)"
                )
        else:
            # Третье появление той же дыры. §2.2b закрыла её для `human_ok_spec`,
            # v1.30 — для `human_ok_plan`, и оба раза правка была на ПОЛЕ, а не на
            # класс полей: здесь ветка принимала ЛЮБУЮ непустую строку, поэтому
            # `asserts_reviewed_by: посмотрел сам` и `by=agent:…` проходили, а
            # автономному агенту не оставалось законного значения вовсе. Теперь
            # поле идёт через ту же развилку: подпись человека, либо `deferred`
            # с причиной, либо заслуженное `n/a` выше.
            e, w = signature_verdict("asserts_reviewed_by", reviewed, klass, phase)
            errors += e
            warnings += w

    # --- §3.1d уровень 1: ожидание обязано появиться РАНЬШЕ кода.
    # Проверяем не качество примеров (это невозможно), а сам факт, что они
    # есть и на них ссылаются тесты: иначе ожидание придумал тот же, кто писал
    # код, и все гейты ниже проверяют его согласие с самим собой.
    if klass in {"M", "L"} and phase in implement_like and spec.is_file():
        spec_text = read(spec)
        ex_ids = re.findall(r"(?m)^\s*\|\s*([A-Z]\d+)\s*\|", spec_text)

        # Корпус «где искать ссылки»: smoke + tasks + verify + тексты тестов.
        # Строится БЕЗУСЛОВНО, потому что его читают ДВЕ независимые проверки —
        # ссылки на id ниже и реляционный оракул §6.5 дальше.
        #
        # lab-12: он строился внутри else-ветки, и обе ветки с ошибкой роняли
        # гейт `UnboundLocalError: haystack` ВМЕСТО того, чтобы напечатать уже
        # сформулированный диагноз. Класс: **проверка, обязанная поставить
        # диагноз, умирает вместо диагноза** — исполнитель видит поломку
        # инструмента там, где ему сообщали о его собственной ошибке. Это
        # зеркало «гейта, врущего зелёным»: тот молчит, когда должен говорить,
        # этот кричит не о том. Мера — не «инициализировать переменную», а
        # держать сбор данных отдельно от разбора причин.
        haystack = read(eval_smoke) + read(tasks) + read(verify)
        for root_dir in ("tests", "backend/tests", "src/tests"):
            d = ACTIVE.parent.parent / root_dir
            if not d.is_dir():
                continue
            for f in d.rglob("*test*"):
                # Только текстовые исходники и не служебные каталоги:
                # маска ловила .pyc и роняла гейт (F9).
                if (
                    f.is_file()
                    and f.suffix in TEST_TEXT_SUFFIXES
                    and not any(p in SKIP_DIR_PARTS for p in f.parts)
                ):
                    haystack += read(f)

        if not re.search(r"(?im)^#+\s*acceptance\s+examples", spec_text):
            errors.append(
                f"class {klass}: spec.md без блока '## Acceptance examples' "
                "(§3.1d) — конкретные вход→выход, подтверждённые человеком "
                "ДО кода; проза «работает корректно» подписью не является"
            )
        elif not ex_ids:
            # Грамматика id названа ЯВНО: она не очевидна, а её нарушение
            # раньше выглядело как «примеров нет» при полной таблице примеров.
            # lab-12: арка написала id вида `EX1` и получила краш; вторая арка
            # переименовала свои id, обходя соседнюю проблему, — то есть
            # неназванное правило заставляло подстраиваться вслепую.
            errors.append(
                "spec.md: блок Acceptance examples есть, но примеров с id в "
                "нём нет (§3.1d). id читается из первой колонки таблицы и "
                "обязан быть вида ОДНА заглавная латинская буква + цифры "
                "(`A1`, `E2`); `EX1`, `A.1`, `1` и `случай-1` не считаются"
            )
        else:
            # id должны встречаться в тестах или eval-smoke: связь примера с
            # проверкой — то, что отличает обещание от отчёта о реализации.
            missing = [i for i in dict.fromkeys(ex_ids) if i not in haystack]
            if missing:
                warnings.append(
                    "acceptance-примеры без ссылки в тестах/eval-smoke: "
                    f"{', '.join(missing)} (§3.1d) — пометь тест id примера"
                )

            # Порядок «пример раньше теста» — на verify и дальше: раньше
            # тестов может просто не быть, и проверка ругалась бы на штатное
            # состояние фазы implement.
            if phase in {"verify", "converge", "handoff"}:
                test_dirs = [d for d in ("tests", "backend/tests", "src/tests")
                             if (ACTIVE.parent.parent / d).is_dir()]
                warnings += expectation_predates_tests(ex_ids, test_dirs)

        # --- §6.5 уровень 2: хотя бы один реляционный оракул на M/L.
        # «Минимум один» стояло в §6.5 практикой и не принуждалось ничем —
        # та же форма, что F7. Пример со значением (`f(2) == 4`) закрепляет
        # баг, если ожидание списано с кода, и мутационный гейт его не поймает:
        # мутант будет честно убит НЕВЕРНЫМ утверждением. Свойство подделать
        # нельзя, потому что ожидаемого значения в нём нет.
        #
        # ⚠ Это проверка НАЛИЧИЯ, и она заполняется галочкой: `@given(...)` с
        # `assert True` её проходит. Ловит такое не она, а мутационный гейт —
        # декоративное свойство оставляет мутантов живыми. Поэтому требование
        # осмысленно ТОЛЬКО при работающем mutation (на macOS —
        # `brew install coreutils`, CQG §5 шаг 3), и об этом сказано в тексте
        # ошибки: иначе получим ритуал вместо оракула.
        if not re.search(r"@given|@hypothesis\.given|fc\.assert|fc\.property",
                         haystack):
            warnings.append(
                f"class {klass}: ни одного реляционного оракула (§6.5) — "
                "не найдено ни `@given` (hypothesis), ни `fc.property` "
                "(fast-check). Инвариант, round-trip, идемпотентность, "
                "метаморфное отношение или differential: в них нет ожидаемого "
                "значения, поэтому в них нельзя спрятать неверное ожидание. "
                "Один инвариант обычно ловит больше десяти тестов-значений, "
                "потому что раннер перебирает входы, о которых автор не думал. "
                "Проверь заодно, что mutation-гейт у тебя не пропускается: "
                "иначе слабое свойство (`assert result is not None`) пройдёт"
            )

```

# Приложение B9 — `scripts/delivery_journals.py`

```python
#!/usr/bin/env python3
"""Журналы поведения: диагноз, эскалация, отклонённые варианты, решения.

Часть `delivery_check.py` (`delivery@1.53`). §12.1–12.3: контур фиксировал
результат и не показывал, КАК агент к нему шёл. Проверяется форма записи —
альтернатива и причина обязательны, шаблон записью не считается.
"""

from __future__ import annotations

import re

from delivery_base import (ACTIVE, ActiveCtx, field, is_placeholder,
                           list_entries, read, unfilled, verdict_blocks)
from delivery_artifact import (check_artifact_oracle,
                               check_weak_names_the_cheap_gap)
from delivery_decisions import decision_lines, decisions_without_cost
from delivery_diff import diff_identifiers, diff_stats



def check_journals(status: str, args, errors: list[str], warnings: list[str], ctx: ActiveCtx) -> None:
    """Диагноз, эскалация, отклонённые варианты и лента решений."""
    phase, klass, plan, implement_like = (
        ctx.phase,
        ctx.klass,
        ctx.plan,
        ctx.implement_like)
    # kind/behavior читаем один раз — их используют обе проверки ниже.
    kind = field(status, "kind").lower()
    behavior = field(status, "behavior-oracles").lower()

    # --- §3.1c: багфикс обязан принести тест, который падал до фикса
    if kind.startswith("bugfix") and phase in {"verify", "converge", "handoff"}:
        repro = field(status, "repro_test")
        if is_placeholder(repro):
            errors.append(
                "kind=bugfix at verify+: missing repro_test (§3.1c) — тест, "
                "который падал до фикса; иначе баг вернётся"
            )
        elif repro.lower().startswith("n/a") and "reason=" not in repro.lower():
            errors.append(
                "repro_test: n/a без reason= (§3.1c) — «не воспроизводится» "
                "обычно значит «не пробовал»"
            )

    # --- §12.1: багфикс обязан показать, КАК искали, а не только что нашёл.
    # repro_test доказывает, что баг найден; журнал гипотез — что его не будут
    # искать заново с нуля.
    if kind.startswith(("bugfix", "hotfix")) and phase in {
        "verify",
        "converge",
        "handoff",
    }:
        diag = field(status, "diagnosis")
        diag_file = ACTIVE / "diagnosis.md"
        if is_placeholder(diag):
            errors.append(
                f"kind={kind.split()[0]} at verify+: missing diagnosis (§12.1) "
                "— журнал гипотез с вердиктами; n/a только с reason="
            )
        elif diag.lower().startswith("n/a"):
            if "reason=" not in diag.lower():
                errors.append(
                    "diagnosis: n/a без reason= (§12.1) — «нашёл сразу» и «не "
                    "искал» обязаны различаться в тексте, а не в тишине"
                )
        elif not diag_file.is_file():
            errors.append(
                "diagnosis указывает на файл, которого нет (§12.1) — "
                "ожидается delivery/active/diagnosis.md"
            )
        elif not verdict_blocks(read(diag_file)):
            errors.append(
                "diagnosis.md без заполненных вердиктов (§12.1) — гипотезы без "
                "ОПРОВЕРГНУТА/ПОДТВЕРЖДЕНА это список подозрений: следующий "
                "читатель обязан перепроверить всё заново. Ожидается вердикт "
                "внутри СТРУКТУРНОГО блока: пункт списка, строка таблицы или "
                "раздел (перенос строки внутри пункта допустим). Строки шаблона "
                "(с <…>) и проза о формате вердиктами не считаются"
            )

    # --- §12.3: остановка обязана быть оформлена, иначе восстановление
    # контекста перекладывается на человека — то самое время, которое контур
    # экономит.
    blockers = field(status, "blockers")
    if blockers and not is_placeholder(blockers) and blockers.lower() not in {
        "none",
        "нет",
        "n/a",
    }:
        esc = ACTIVE / "escalation.md"
        if not esc.is_file():
            errors.append(
                f"blockers непусты ({blockers[:50]}), а active/escalation.md "
                "нет (§12.3) — «я застрял» решением не является: нужны что "
                "пробовал, какой нужен ответ, два варианта с ценой"
            )
        else:
            # Строки шаблона не считаются: скопированный и незаполненный
            # escalation.md — это не оформленная остановка (ср. STATUS, v1.5).
            lines_e = [ln for ln in read(esc).splitlines() if not unfilled(ln)]
            # Разметка принимается ЛЮБАЯ разумная: жирная строка шаблона,
            # markdown-заголовок, list-item, строка таблицы. До delivery@1.39
            # принималась только жирная строка, и `## Вариант 1 — …` давало
            # «вариантов 0» — то есть диагноз про СОДЕРЖАНИЕ, когда причина
            # была в ФОРМЕ. На dogfooding'е (lab-10) я споткнулся об это трижды
            # в трёх артефактах; ни одна подстройка не была про смысл.
            opts = len(
                [
                    ln
                    for ln in lines_e
                    if re.match(
                        # `\s+` обязателен: с `\s*` заголовок раздела
                        # «## Варианты» считался вариантом («вариант» + «ы»),
                        # и одного варианта хватало на два. Поймал существующий
                        # тест, а не я.
                        r"(?i)^\s*(?:#{1,6}\s*|[-*]\s+|\|\s*)?\**\s*"
                        r"(?:вариант|option)\s+\w",
                        ln,
                    )
                ]
            )
            costs = len(
                [ln for ln in lines_e if re.search(r"(?i)\bцена\b|\bcost\b", ln)]
            )
            if opts < 2:
                errors.append(
                    f"escalation.md: заполненных вариантов {opts}, нужно ≥2 "
                    "(§12.3) — один вариант это просьба согласиться, а не выбор; "
                    "второй всегда есть и называется «не делать / отложить». "
                    "Ожидается строка, начинающаяся со слова «Вариант» (можно "
                    "как заголовок `## Вариант A`, жирным `**Вариант A:**`, "
                    "пунктом списка или строкой таблицы)"
                )
            elif costs < 2:
                errors.append(
                    f"escalation.md: вариантов {opts}, а названных цен {costs} "
                    "(§12.3) — вариант без цены выбрать нельзя"
                )

    # --- §12.2a: отклонённые варианты. Без следа тот же тупик предлагают
    # снова — и он снова выглядит разумным, потому что причина отказа нигде
    # не записана.
    if klass in {"M", "L"} and phase in implement_like and plan.is_file():
        plan_text = read(plan)
        sec = re.search(
            r"(?ims)^#{2,3}\s*rejected\s+alternatives\s*$(.*?)(?=^#{2,3}\s|\Z)",
            plan_text,
        )
        msg_alt = ""
        if not sec:
            msg_alt = (
                "plan.md без секции '## Rejected alternatives' (§12.2a) — "
                "отброшенный подход без записанной причины предлагается снова"
            )
        else:
            # Та же склейка переносов, что у журналов: причина часто уезжает на
            # вторую строку, и построчный разбор объявлял запись «без причины».
            entries = [
                ln
                for ln in list_entries(sec.group(1))
                if not unfilled(ln)
                and re.search(r"(?i)потому что|because|reason=", ln)
            ]
            if not entries:
                msg_alt = (
                    "plan.md: 'Rejected alternatives' без заполненных записей "
                    "с причиной (§12.2a) — законна и запись «рассматривали "
                    "только X, потому что Y исключён требованием Z»"
                )
        if msg_alt:
            (errors if klass == "L" else warnings).append(msg_alt)

    # --- §12.2: журнал решений. Проверяется ФОРМАТ: лента без альтернатив
    # неаудируема, а именно аудируемость — весь смысл файла.
    dec_file = ACTIVE / "decisions.md"
    late = phase in {"verify", "converge", "handoff"}
    if dec_file.is_file():
        good, bad = decision_lines(read(dec_file))
        if bad:
            errors.append(
                f"decisions.md: {len(bad)} строк(а) не в формате §12.2 "
                "«выбрал X вместо Y — потому что Z»: "
                + "; ".join(b[:55] for b in bad[:3])
            )
        if klass == "L" and late and not good:
            errors.append(
                "class L: decisions.md без ни одного решения (§12.2) — эпик "
                "или новый модуль без выбора не бывает"
            )
        # Цена, а не предпочтение: «потому что дешевле» неопровержимо, значит
        # аудита не даёт, ради которого файл и ведётся (§12.2a).
        # Идентификаторы диффа — то, с чем сверяется «цена». Диффа нет
        # (нет ref'а, не git) -> `None`, и проверка молчит: предупреждение по
        # неверной причине учит игнорировать гейт.
        # Без `--diff-base` берём `HEAD~1`: иначе проверка молчала бы на
        # каждом локальном прогоне, а в CI работала — то есть вела бы себя
        # по-разному там, где решение и принимается.
        dec_stats = diff_stats(args.diff_base or "HEAD~1")
        diff_ids = diff_identifiers(dec_stats[4]) if dec_stats else None
        no_cost = decisions_without_cost(read(dec_file), diff_ids)
        if no_cost and late:
            warnings.append(
                f"decisions.md: {len(no_cost)} решен(ий) без цены — причина не "
                "названа ни числом, ни ссылкой в изменённый код: "
                + "; ".join(n[:55] for n in no_cost[:3])
                + ". «Дешевле» и «проще» проверить нельзя ничем; назови число "
                "или процитируй то, что менял (файл, функцию, поле) — такое "
                "не напишешь, не открыв дифф (§12.2a)"
            )
    elif klass == "L" and late:
        errors.append(
            "class L at verify+: missing active/decisions.md (§12.2)"
        )
    elif klass == "M" and late:
        warnings.append(
            "class M at verify+: нет active/decisions.md (§12.2) — поведение "
            "агента неаудируемо без чтения всего диффа"
        )

    check_artifact_oracle(status, phase, klass, errors, warnings)
    check_weak_names_the_cheap_gap(status, errors, warnings)

    # --- §3.1b: рефакторинг без behavior-oracles = тихая регрессия
    if kind.startswith("refactor") and phase in {"verify", "converge", "handoff"}:
        covered = (
            not behavior.startswith("weak")
            or "characteriz" in status.lower()
            or "характеризац" in status.lower()
            or "refactor" in field(status, "waivers").lower()
        )
        if not covered:
            errors.append(
                "kind=refactor with behavior-oracles: weak — «компилируется» не "
                "доказывает сохранение поведения (§3.1b). Нужны характеризационные "
                "тесты, наблюдаемая проверка или human waiver со словом refactor"
            )

    # --- CI (§10.4): единственный слой, который агент не может обойти локально
```

# Приложение B11 — `scripts/delivery_artifact.py`

```python
#!/usr/bin/env python3
"""§6.5a: артефакт сборки судится, а `weak` называет дешёвое непокрытое.

Часть `delivery_check.py` (`delivery@1.54`). Оба правила — про одну границу:
между «исходник корректен» и «продукт правилен». Форму исходника судит CQG,
поведение — тесты, а собранный артефакт до сих пор не читал никто, и «зелёный
CI» читался как «продукт правилен».
"""

from __future__ import annotations

import re

from delivery_base import ROOT, field, is_placeholder

BUILD_MARKERS = (
    ("package.json", r'"scripts"\s*:\s*\{[^{}]*"build"\s*:'),
    ("Makefile", r"(?m)^build\s*:"),
    ("justfile", r"(?m)^build\s*:"),
)


def declares_build(root) -> str:
    """Чем объявлен шаг сборки; пусто — проект ничего не собирает.

    Ищем ОБЪЯВЛЕНИЕ, а не угадываем стек: `scripts.build` в `package.json`,
    цель `build:` в Makefile/justfile. Не найдено — правило §6.5a не применяется,
    и это честнее, чем требовать артефакт у библиотеки.
    """
    for name, pattern in BUILD_MARKERS:
        p = root / name
        if p.is_file() and re.search(pattern, p.read_text(encoding="utf-8", errors="replace")):
            return name
    return ""


def check_artifact_oracle(status, phase, klass, errors, warnings) -> None:
    """§6.5a: у артефакта сборки обязан быть машинный инвариант.

    **Замер восьмого развёртывания.** Развёрнуто пятнадцать ролей CQG, CI зелёный,
    и при этом ни одна проверка не читает `dist/`: между «исходник корректен» и
    «продукт правилен» лежит зона, которую не судит ничто. Это не дыра каталога,
    а его предмет — форма исходника и поведение продукта разные слои, — но пока
    зона не названа, «зелёный CI» читается как «продукт правилен».

    Объявление живёт в STATUS (`artifact_oracle:`), как `runtime_paths` и
    `repro_test`, и проверяется по ФАКТУ файла: объявленный оракул без скрипта —
    снятая проверка с видом усиления (§4.6, тот же класс).
    """
    built = declares_build(ROOT)
    if not built:
        return
    declared = field(status, "artifact_oracle")
    hard = phase == "handoff" and klass in {"M", "L"}
    if not declared or is_placeholder(declared):
        (errors if hard else warnings).append(
            f"artifact_oracle: проект собирает артефакт ({built}), а оракула на "
            "него в STATUS нет — между «исходник корректен» и «продукт правилен» "
            "не судит ничто (§6.5a). Объяви проверку собранного или `n/a reason=…`"
        )
        return
    low = declared.lower()
    if low.startswith(("n/a", "нет", "no ")):
        if "reason=" not in low:
            errors.append(
                "artifact_oracle: n/a без reason= — «не делали» и «нечем» это "
                "разные вещи, и различать их обязана запись (§6.5a)"
            )
        return
    path = declared.split()[0].strip("`\"',")
    if not (ROOT / path).exists():
        errors.append(
            f"artifact_oracle: {path} — файла нет. Объявленный оракул без скрипта "
            "это снятая проверка с видом усиления (§4.6)"
        )


def check_weak_names_the_cheap_gap(status, errors, warnings) -> None:
    """`weak` обязан называть, что проверяемо машиной и не сделано (§6.5a).

    «Не развёрнуто» и «непроверяемо» — разные вещи, а `weak` их смешивает и
    потому живёт вечно. Замер того же развёртывания: слой behavior пуст, но
    большая часть непокрытого проверяется дёшево и без новых зависимостей —
    разбор собранного html, контраст как арифметика над токенами, переключатель
    языков как функция пути. Пока это не названо, `weak` читается как «нельзя».

    Правило то же, что §9.1a п.2 даёт гейтам: молчащий механизм либо убирают,
    либо пишут, почему держат. Здесь — либо покрывают, либо называют дешёвое.
    """
    raw = field(status, "behavior-oracles")
    if not raw.lower().startswith("weak"):
        return
    tail = raw[len("weak"):].strip(" —-:;()")
    if tail and not is_placeholder(tail):
        return
    warnings.append(
        "behavior-oracles: weak без названного — «не развёрнуто» и «непроверяемо» "
        "разные вещи (§6.5a). Что из непокрытого проверяемо машиной СЕГОДНЯ? "
        "Форма: `behavior-oracles: weak — дёшево: <что именно>`"
    )
```

# Приложение B10 — `scripts/delivery_limits.py`

```python
#!/usr/bin/env python3
"""Пределы поставки: CI-слой, объёмные breaker'ы, рисковый дифф.

Часть `delivery_check.py` (`delivery@1.53`). Здесь то, что ограничивает поставку
снаружи: чем закрыт CI, не разрослась ли она объёмом и прочитано ли то, что
помечено рисковым.
"""

from __future__ import annotations

import re

from delivery_base import (ACTIVE, ARCHIVE, BREAKER_EXCLUDE, ActiveCtx,
                           DEFAULT_BREAKERS, field,
                           is_placeholder, read)
from delivery_diff import applicable_lessons, diff_stats

def check_limits(status: str, args, errors: list[str], warnings: list[str], ctx: ActiveCtx) -> None:
    """CI-слой, объёмные breaker'ы и рисковый дифф."""
    phase, klass, plan, tasks, verify = (
        ctx.phase,
        ctx.klass,
        ctx.plan,
        ctx.tasks,
        ctx.verify)
    ci = field(status, "ci-oracles").lower()
    waivers = field(status, "waivers").lower()
    if is_placeholder(ci):
        warnings.append(
            "STATUS.md: missing ci-oracles (weak|tooling|deployed) — §10.4"
        )
    elif ci.startswith("weak"):
        # Было `"ci" not in waivers`: строка `waivers: ci` снимала требование
        # ПОДСТРОКОЙ — без причины и без подписи. Форма waiver'а одна на весь
        # контур (§4.3a). Нашло приёмочное развёртывание.
        ci_waived = bool(re.search(r"ci\b.*reason=.*by=human:", waivers))
        # Компенсация §10.4 правило 4 — ТРЕТИЙ законный выход, наравне с waiver'ом.
        # Она была описана прозой и не проверялась ничем, поэтому автономное
        # развёртывание в проект без хостинга не имело **честного** способа сделать
        # гейт зелёным: waiver требует человека, занижать класс запрещено §2.2b, а
        # `blockers:` + `escalation.md` гейт не принимал. Рядом лежали две дешёвые
        # лазейки (занизить класс, написать `waivers: ci`), то есть канон учил
        # обходить себя законным с виду способом — ровно то, против чего §2.2b.
        # Асимметрия была видна в собственном выводе: для `human_ok_*` третье
        # значение сделали, для `ci-oracles` — нет.
        #
        # Компенсация проверяется по ФАКТУ артефакта, а не по заявлению: строка
        # `clean_clone_run:` в verify-report (что прогнали и когда) + непустые
        # `blockers:` + существующий `escalation.md` (§12.3 сам требует там два
        # варианта с ценой). Это не эквивалент CI и не снятие требования: остаётся
        # громкий warning, `ci-oracles` остаётся `weak`, и строка приёмки §6
        # закрывается как `weak`, а не как `auto`.
        compensated = (
            bool(re.search(r"(?im)^\s*\**clean_clone_run\**\s*:\s*\S", read(verify)))
            and bool(field(status, "blockers").lower() not in {"", "none", "-"})
            and (ACTIVE / "escalation.md").is_file()
        )
        if klass == "L" and not ci_waived and not compensated:
            errors.append(
                "class L with ci-oracles: weak — нужен либо human waiver "
                "(`waivers: ci reason=… by=human:…`), либо компенсация §10.4 п.4: "
                "строка `clean_clone_run: <что прогнал> at=<дата>` в "
                "verify-report.md + непустые blockers: + active/escalation.md. "
                "Занижать класс, чтобы гейт позеленел, запрещено (§2.2b)"
            )
        elif klass == "L" and compensated and not ci_waived:
            warnings.append(
                "class L, ci-oracles: weak — закрыто компенсацией §10.4 п.4 "
                "(clean_clone_run + blockers + escalation.md). Это НЕ эквивалент "
                "CI: строка приёмки §6 закрывается как weak, не как auto"
            )
        else:
            warnings.append(
                "ci-oracles: weak — local gates are bypassable (§10.4); "
                "Verifier must attach a clean-clone run to verify-report.md"
            )
    # 'tooling' — легитимный режим (гейт мержа в репо, серверного нет по тарифу),
    # а не поддавки: см. §10.4. Недопустим только 'weak'.
    #
    # ⚠ У флага ОБЯЗАН быть первый прогон, иначе он замкнут в круг (`delivery@1.52`).
    # §10.4 велит ставить `deployed` по факту ЗЕЛЁНОГО прогона, а флаг стоит в
    # том самом прогоне: честное `weak` держит его красным, а красный прогон не
    # даёт значению стать честным. Восьмое развёртывание вышло из круга флипом в
    # том же коммите — законно, но канон об этом не говорил, и агент имел ровно
    # два выхода, оба выглядящие как обход собственного правила.
    #
    # Развязка по СМЫСЛУ флага: он защищает не «каждый прогон», а ЗАКРЫТИЕ
    # поставки. На фазе `handoff` `weak` — ошибка, как и было. До неё — громкий
    # warning, который называет ровно один ход: обновить STATUS по факту этого
    # прогона, в том же коммите. Реальность (прогон исполняется) старше записи,
    # и спорить с ней значит требовать вранья в одну или другую сторону.
    if args.require_ci and not ci.startswith(("deployed", "tooling")):
        if phase == "handoff":
            errors.append(
                "--require-ci: ci-oracles is neither 'deployed' nor 'tooling' "
                "— поставку с weak CI не закрываем (§10.4)"
            )
        else:
            warnings.append(
                "--require-ci: ci-oracles = weak, а прогон уже идёт. Это первый "
                "прогон: поставь `ci-oracles: deployed` по факту ЭТОГО прогона "
                "в том же коммите (§10.4). На фазе handoff это станет ошибкой"
            )

    stack = field(status, "stack")
    if is_placeholder(stack) or "delivery@" not in stack:
        warnings.append(
            "STATUS.md: missing stack version "
            "(e.g. 'delivery@<version>, cqg@<version>, okf@absent')"
        )

    # --- Circuit breakers (§3.4): анти-oneshot по объёму поставки.
    #
    # `kind: bootstrap` breaker'ом не мерится, и это не поддавки. Bootstrap — не
    # поставка продукта: у него своя DoD (§7.3), он по построению трогает всё
    # дерево контура и не может быть «разрезан на части» — совет гейта «split the
    # PR» для него не выполним ни в каком виде. Приёмочное развёртывание показало
    # это числом: даже после исключения механики контура остаётся 30 файлов и
    # 2437 строк конфигов, снимков и workflow'ов против лимита 800. Единственным
    # выходом оставался рутинный waiver, который §4.3a сам называет
    # обесцениванием механизма — то есть гейт учил себя обходить.
    #
    # Объём при этом не замалчивается: он печатается строкой ниже, и §7.3 требует
    # приёмку §6 — bootstrap проверяется своей процедурой, а не breaker'ом.
    if field(status, "kind").lower().startswith("bootstrap"):
        print(
            "breakers: kind=bootstrap — объём не мерится (§3.4): развёртывание "
            "контура не режется на части, его DoD — §7.3 + приёмка §6"
        )
    elif args.diff_base:
        limits = dict(DEFAULT_BREAKERS)
        # Одна и та же форма покрывает и override в circuit_breakers,
        # и human waiver: "max_files_touched=40 reason=… by=human:…".
        for m in re.finditer(r"(max_[a-z_]+)[ \t]*=[ \t]*(\d+)", status):
            if m.group(1) not in limits:
                continue
            # Смотрим СТРОКУ, в которой стоит override, а не только совпадение.
            # §3.4 различает две вещи одной формы: настройка проекта (строка
            # `circuit_breakers:`) и **human waiver**. Про waiver канон говорил
            # «только человек», а парсер принимал `max_loc_diff=99999` без
            # причины и без подписи — то есть требование жило в прозе, а
            # механика позволяла агенту поднять себе лимит молча. Три
            # независимых развёртывания нашли это по отдельности.
            bol = status.rfind("\n", 0, m.start()) + 1
            eol = status.find("\n", m.end())
            line = status[bol: eol if eol != -1 else len(status)]
            if "circuit_breakers" in line.lower():
                limits[m.group(1)] = int(m.group(2))
                continue
            if "reason=" not in line or "by=human:" not in line:
                errors.append(
                    f"waiver {m.group(1)}={m.group(2)} без reason= и by=human: "
                    "(§3.4) — лимит поднимает человек, не исполнитель. Нет "
                    "человека — не waiver, а `blockers:` + escalation.md (§12.3) "
                    "или более узкая поставка; настройка проекта пишется строкой "
                    "circuit_breakers:"
                )
                continue
            limits[m.group(1)] = int(m.group(2))
        stats = diff_stats(args.diff_base)
        if stats is None:
            warnings.append(
                f"circuit breakers: ref '{args.diff_base}' unavailable "
                "(shallow clone? need full history)"
            )
        else:
            n_files, added, deleted, excluded, changed = stats
            # --- §2.2a: уроки по затронутым путям должны быть упомянуты.
            # Warning, не error: сопоставление «префикс пути → урок»
            # приблизительно, а по §4.3b ложное срабатывание дороже пропуска.
            lessons = applicable_lessons(read(ARCHIVE / "INDEX.md"), changed)
            if lessons:
                cited = read(plan) + read(tasks) + status
                unread = [
                    i for i in lessons if not re.search(rf"\b{i}\b", cited)
                ]
                if unread:
                    warnings.append(
                        "archive/INDEX.md: уроки по затронутым путям не "
                        f"упомянуты: {', '.join(unread)} (§2.2a) — прочти "
                        "строки и сошлись на id в plan.md/tasks.md либо "
                        "напиши, почему не применимо"
                    )
            net = abs(added - deleted)
            print(
                f"breakers: files={n_files} net_loc={net} (+{added}/-{deleted}), "
                f"excluded={excluded} ({'|'.join(BREAKER_EXCLUDE)}), "
                f"limits={limits}"
            )
            # --- Стартовый дифф (lab-11 F12/F13/F8). На specify/plan/tasks
            # поставка ещё не написала продуктового кода: всё, что дифф
            # показывает сверх BREAKER_EXCLUDE, принесено извне — незамерженная
            # предыдущая поставка (bootstrap, который мержится ПОКА активен,
            # §7.2 шаг 6) или ветка от старой базы после squash-мержа. Обе арки
            # lab-11 обнаружили это на handoff ценой полного цикла: breaker
            # мерил 33 файла при собственных 2, и совет «split the PR» резал
            # нечего. Здесь тот же тупик называется на входе и стоит минуту.
            if phase in ("specify", "plan", "tasks") and n_files:
                # Слито в локальный main, но не запушено — отдельный честный
                # диагноз: чинится push'ем, а не rebase'ом.
                alt_clean = False
                for cand in ("main", "master"):
                    alt = None if cand == args.diff_base else diff_stats(cand)
                    if alt is not None and alt[0] == 0:
                        alt_clean = True
                        break
                if alt_clean:
                    warnings.append(
                        f"стартовый дифф против {args.diff_base} непуст "
                        f"({n_files} файл(ов)), против локального main пуст: "
                        "предыдущая поставка слита, но не запушена — CI и "
                        "breakers меряют чужой дифф, пока push не ушёл (§3.4)"
                    )
                else:
                    errors.append(
                        f"стартовый дифф непуст: {n_files} файл(ов) кода в "
                        f"{args.diff_base}..HEAD уже на фазе {phase} — это "
                        "чужие изменения, поставка не стартует поверх них "
                        "(§7.2 шаг 6). Незамерженная предыдущая — слей через "
                        "merge_guard (bootstrap мержится ПОКА активен); ветка "
                        "от старой базы после squash-мержа — git rebase --onto "
                        f"{args.diff_base} <старая-база> (lab-11 F12/F8)"
                    )
            # §2.2b: класс S при S-нетипичном объёме — warning, не ошибка.
            # Большой механический багфикс бывает, но и занижение класса ради
            # обхода human_ok_spec выглядит именно так. Молчать нельзя:
            # молчание эту дыру и создало.
            if klass == "S" and (n_files > 5 or net > 200):
                warnings.append(
                    f"class S, а тронуто {n_files} файл(ов) / {net} строк — "
                    "класс занижен? (§2.2b) Класс определяется работой, а не "
                    "доступностью человека; нет человека — human_ok_spec: deferred"
                )
            if n_files > limits["max_files_touched"]:
                errors.append(
                    f"circuit breaker: files_touched {n_files} > "
                    f"{limits['max_files_touched']} — split the PR or add a "
                    "human waiver line to STATUS (§3.4)"
                )
            if net > limits["max_loc_diff"]:
                errors.append(
                    f"circuit breaker: net loc_diff {net} > "
                    f"{limits['max_loc_diff']} — split the PR or add a "
                    "human waiver line to STATUS (§3.4)"
                )
```

```bash
chmod +x scripts/delivery_check.py
python scripts/delivery_check.py
```

**Обязательно в CI** (§10.4) на ветках с активной поставкой — там же, где гоняются
CQG-гейты (workflow — CQG §8):

```bash
python scripts/delivery_check.py --require-spec              # после фазы specify
python scripts/delivery_check.py --require-ci                # в самой CI-джобе
python scripts/delivery_check.py --diff-base origin/main     # + circuit breakers §3.4
```

Локальный pre-push — та же команда без флагов.

---

# Приложение C — `scripts/delivery_metrics.py`

Считает метрики §9 из git и печатает блок A.10. Нужен потому, что ритуал §9.2
без автосбора не выполняется: заполнять семь полей руками на каждом handoff
никто не будет, а `delivery_check` теперь требует наличие блока (иначе handoff
не закрывается).

**Принцип:** считает то, что git знает; остальное помечает `MANUAL` и **не
выдумывает**. Метрика-догадка хуже отсутствующей — по ней принимают решения об
усилении harness.

```python
#!/usr/bin/env python3
"""Delivery metrics collector (§9 / A.10).

Usage:
  python scripts/delivery_metrics.py --base origin/main
  python scripts/delivery_metrics.py --base origin/main --write

--write вставляет/обновляет блок "## Harness metrics" в
delivery/active/verify-report.md. Exit 0 всегда: это отчёт, не гейт
(гейт на наличие блока — в delivery_check.py на фазе handoff).
"""

from __future__ import annotations

import argparse
import re
import subprocess
import sys
from datetime import datetime
from pathlib import Path

ROOT = Path(__file__).resolve().parents[1]
ACTIVE = ROOT / "delivery" / "active"
STATUS = ACTIVE / "STATUS.md"
VERIFY = ACTIVE / "verify-report.md"

# Правка любого из этих путей = harness усилен (§5.4): новый oracle / hook / eval.
HARDENING_PATHS = (
    "scripts/lint/",
    "delivery/evals/",
    ".pre-commit-config.yaml",
    ".github/workflows/",
    "scripts/delivery_check.py",
    "scripts/okf_sync_gate.py",
    # `.claude/` — механика контура (права агента §4.5), а не продуктовый код.
    # Без исключения маячок §12.5 срабатывал на классе «безопасность» из-за
    # слова `permission` в собственном файле настроек прав: контур ругался на
    # свою же машинерию. Найдено на первом прогоне маячка (арена lab-10).
    ".claude/",
)
# НОВЫЙ тест-файл — тоже усиление harness: по словарю §1.1 oracle это «гейты + тесты
# + smoke + фазовые артефакты», и §5.4 считает усилением «добавить oracle». Путей
# тестов в списке не было, поэтому поставка, добавившая два регрессионных оракула
# (включая мета-оракул «проверка, что защита подключена»), получала
# `harness_hardened: no` — а §9.2 по этой метрике советует «добавь oracle», то есть
# добавить уже добавленное. Нашло приёмочное развёртывание.
#
# Считаются только ДОБАВЛЕННЫЕ файлы (`--diff-filter=A`), а не любая правка тестов:
# иначе метрика стала бы всегда `yes` — тесты трогает каждая поставка — и перестала
# бы отвечать на свой вопрос.
TEST_PATH_MARKERS = ("test_", "_test.", "/tests/", "spec.")
DOC_PREFIXES = ("delivery/", "knowledge/")


def git(*args: str) -> str:
    try:
        out = subprocess.run(["git", *args], capture_output=True, text=True, check=False)
    except FileNotFoundError:
        return ""
    return out.stdout if out.returncode == 0 else ""


def first_commit(
    path: str, needle: str | None = None, rev_range: str | None = None
) -> tuple[str, datetime] | None:
    """Первый коммит, добавивший path (или впервые внёсший needle в path).

    needle — РЕГУЛЯРКА (pickaxe-regex), а не литерал: в STATUS.md поля размечены
    markdown-жирным (`**phase:** handoff`), поэтому поиск литерала "phase: handoff"
    не находил ничего и метрики молча выходили пустыми.

    ⚠ rev_range ОБЯЗАТЕЛЕН для путей внутри `delivery/active/`, и это не
    перестраховка. Путь **переиспользуется каждой поставкой**: `STATUS.md` и
    `spec.md` у всех одни и те же, поэтому «первый в истории» — это первая
    поставка репозитория, а не текущая. lab-12 (нашли ОБЕ арки независимо):
    `rework_after_done` печатал «3 commit(s) after first phase: handoff» на
    поставке, которая handoff не объявляла — счёт шёл от handoff'а bootstrap'а.
    Собственная проба нашла и брата, которого не увидел никто:
    `time_to_accepted_spec` выдавал `4.0d` там, где спека и подпись сделаны в
    один день, — число из ПРОШЛОЙ поставки, и оно выглядит правдоподобно, потому
    что это настоящее число, просто не про эту работу.

    Класс: **переиспользуемый путь + поиск по всей истории = чужой ответ с видом
    своего.** Родня `--diff-filter=A` тому же: «файл добавлен» тоже случилось один
    раз, в первой поставке.
    """
    args = ["log", "--reverse", "--format=%H %aI"]
    if rev_range:
        args.append(rev_range)
    if needle:
        args += [f"-S{needle}", "--pickaxe-regex"]
    else:
        args.append("--diff-filter=A")
    args += ["--", path]
    for line in git(*args).splitlines():
        sha, _, iso = line.partition(" ")
        if sha and iso:
            try:
                return sha, datetime.fromisoformat(iso.strip())
            except ValueError:
                return None
    return None


def hours_between(a: datetime, b: datetime) -> str:
    delta = (b - a).total_seconds() / 3600
    return f"{delta:.1f}h" if delta < 48 else f"{delta / 24:.1f}d"


def collect(base: str) -> dict[str, str]:
    m: dict[str, str] = {}

    if not git("rev-parse", "--verify", "--quiet", base).strip():
        m["_error"] = f"ref '{base}' unavailable (shallow clone?)"
        return m
    merge_base = git("merge-base", base, "HEAD").strip() or base

    code_files = doc_files = added = deleted = 0
    hardened: list[str] = []
    for line in git("diff", "--numstat", f"{merge_base}..HEAD").splitlines():
        parts = line.split("\t")
        if len(parts) != 3:
            continue
        a, d, path = parts
        if path.startswith(HARDENING_PATHS):
            hardened.append(path)
        if path.startswith(DOC_PREFIXES):
            doc_files += 1
            continue
        code_files += 1
        added += int(a) if a.isdigit() else 0
        deleted += int(d) if d.isdigit() else 0

    m["files_touched / loc_diff"] = (
        f"{code_files} code (+{doc_files} process docs) / "
        f"+{added}/-{deleted} (net {added - deleted:+d})"
    )
    m["commits"] = git("rev-list", "--count", f"{merge_base}..HEAD").strip() or "0"

    # Область — ТЕКУЩАЯ поставка, а не вся история: пути в `delivery/active/`
    # переиспользуются, и «первый в истории» отвечает про чужую работу (см.
    # docstring `first_commit`). §5.1 требует ветку на поставку, поэтому
    # `merge_base..HEAD` и есть её граница.
    span = f"{merge_base}..HEAD"
    spec_created = first_commit("delivery/active/spec.md", rev_range=span)
    spec_ok = first_commit(
        "delivery/active/STATUS.md", r"human_ok_spec:\**[ \t]*yes", rev_range=span
    )
    if spec_created and spec_ok:
        m["time_to_accepted_spec"] = hours_between(spec_created[1], spec_ok[1])
    elif spec_created:
        m["time_to_accepted_spec"] = "spec drafted, not yet accepted"
    else:
        m["time_to_accepted_spec"] = "n/a (no spec.md in history — class S?)"

    handoff = first_commit(
        "delivery/active/STATUS.md", r"phase:\**[ \t]*handoff", rev_range=span
    )
    if handoff:
        after = git("rev-list", "--count", f"{handoff[0]}..HEAD").strip() or "0"
        m["rework_after_done"] = f"{after} commit(s) after first phase: handoff"
    else:
        m["rework_after_done"] = "0 (handoff not declared yet)"

    # Новые тест-файлы — отдельный источник усиления (см. TEST_PATH_MARKERS).
    for line in git("diff", "--name-only", "--diff-filter=A",
                    f"{merge_base}..HEAD").splitlines():
        path = line.strip()
        if path and any(mark in path for mark in TEST_PATH_MARKERS):
            hardened.append(f"{path} (новый оракул)")

    m["harness_hardened"] = (
        f"yes — {', '.join(sorted(set(hardened))[:4])}" if hardened else "no"
    )
    m["implement_retries"] = "MANUAL — fills from session log"
    m["verify_fails_before_green"] = "MANUAL — count red verify runs (CI run list)"
    m["est_token_or_cost"] = "MANUAL / n/a"
    return m


def render(m: dict[str, str], base: str) -> str:
    if "_error" in m:
        rows = f"| (collection failed) | {m['_error']} |"
    else:
        rows = "\n".join(f"| {k} | {v} |" for k, v in m.items())
    return (
        "## Harness metrics (this shipment)\n\n"
        f"<!-- generated by scripts/delivery_metrics.py --base {base} -->\n\n"
        "| Metric | Value |\n|---|---|\n"
        f"{rows}\n\n"
        "MANUAL-поля заполняет агент/человек на handoff. Если "
        "`verify_fails_before_green >= 2` при `harness_hardened: no` — по §9.2 "
        "добавь oracle/breaker/hook в этой же поставке.\n"
    )


def main() -> int:
    ap = argparse.ArgumentParser()
    ap.add_argument("--base", default="origin/main")
    ap.add_argument("--write", action="store_true", help="upsert block into verify-report.md")
    args = ap.parse_args()

    block = render(collect(args.base), args.base)

    if not args.write:
        print(block)
        return 0

    if not ACTIVE.is_dir():
        print("no delivery/active/ — nothing to write", file=sys.stderr)
        return 0
    text = VERIFY.read_text(encoding="utf-8") if VERIFY.is_file() else "# Verify report\n\n"
    # Заменить существующий блок целиком, чтобы не копить дубли на повторных прогонах.
    pattern = re.compile(r"(?ms)^## Harness metrics.*?(?=^## |\Z)")
    text = pattern.sub("", text).rstrip() + "\n\n" + block
    VERIFY.write_text(text, encoding="utf-8")
    print(f"metrics written to {VERIFY.relative_to(ROOT)}")
    return 0


if __name__ == "__main__":
    sys.exit(main())
```

Активация:

```bash
chmod +x scripts/delivery_metrics.py
python scripts/delivery_metrics.py --base origin/main --write   # на handoff
```

---

## Changelog

| Дата | Изменение |
|---|---|
| 2026-07-26 | Initial: delivery harness as foundation layer; phases; DoD; thin ABC; evals; composition order before CQG/OKF |
| 2026-07-26 | Consistency pass: clarify/analyze as subphases; DoD phase enum; M/L artifact table; oracle weak modes; delivery_check aligned; A.9 eval-smoke |
| 2026-07-26 | Cross-link to `AGENT_STACK.md`; AGENTS hook mentions stack map |
| 2026-07-26 | Gaps 2026: worktree default; eval suite; observability §9; agent hooks §10; skills pointer §11; A.10–A.11 |
| 2026-07-27 | **v1.5** (review pass): §10.4 CI как необходимый слой (локальные гейты обходятся) + DoD §3.2.3 / §3.1 «где гоняются oracles»; canon version + `stack:` в constitution/STATUS; STATUS-шаблон переведён на плейсхолдеры `<a\|b>` (незаполненный STATUS больше не читается как `class: S`); `delivery_check.py` — stop-gates §3.3 подняты из warning в error, добавлены `ci-oracles` / `stack` / `--require-ci`; фазовые цепочки приведены к §2.1; ABC-ссылка сверена и подписана названием |
| 2026-07-27 | **v1.6**: закрыты последние незапринуждённые инварианты — circuit breakers §3.4 механически (`delivery_check --diff-base`, дифф `merge-base..HEAD`, исключены `delivery/**` и `knowledge/**`); Приложение C `delivery_metrics.py` (автосбор §9 из git; MANUAL-поля не выдумываются) + гейт на блок метрик при handoff; Builder≠Verifier через `builder:`/`Verifier:` + указатель на required review; §0.1a — CI объявлен сквозным слоем ④ (снята размытость границы CQG); шаблон verify-report переведён на плейсхолдер |
| 2026-07-27 | **v1.7**: баннер `⬇ BOOTSTRAP PAYLOAD` перед Приложением A + «стоимость чтения» в §0.3 (канон ~700 строк vs payload ~950; приложения не читаются в обычной работе); самопроверка канонов — `AGENT_STACK.md` §7 |
| 2026-07-27 | **v1.8**: в DoD bootstrap (§7.3) помечены два ручных/настраиваемых пункта — подтверждение branch protection и раскладка путей («зелёный гейт, который ничего не видит, хуже красного») |
| 2026-07-27 | **v1.9**: гейт мержа отвязан от платного тарифа — §10.4 правило 3a (база в репозитории: `merge_guard.sh`, pre-push, `needs:`; защита ветки — усиление) + три состояния `ci-oracles` (`deployed`/`tooling`/`weak`, `tooling` — легитимный режим, а не поддавки); §5.2 переписан под оба случая; `--require-ci` принимает `tooling` |
| 2026-07-27 | **v1.10** (уроки первого реального развёртывания): §3.1a — «зелёность ≠ покрытие», класс oracle, которого не хватало (невключённый oracle неотличим от проходящего); §5.4 — критерий «усилил, а не извинился» = воспроизвести ошибку и показать, что новый oracle её ловит; A.12 `STACK-ACCEPTANCE.md` — приёмка + разбор её слепых пятен; в дереве §2.3 |
| 2026-07-27 | **v1.11**: в A.12 `STACK-ACCEPTANCE.md` добавлена «Карта ролей гейтов» — фиксирует, какой скрипт занял какую роль в этом проекте (иначе таблица CQG §3, ключёванная именами файлов, расходится с репозиторием молча) |
| 2026-07-28 | **v1.12**: терминология разведена (см. словарь `AGENT_STACK.md` §1.1) — «каноны» = документы, «контур» = развёрнутое в проекте, «стек» только про технологии проекта; поле `stack:` сохранено как историческое имя |
| 2026-07-28 | **v1.13**: §3.1a переписан в три вопроса — «всё ли на месте» → «исполнилось ли» → «зелено ли, и где»; таблица четырёх разобранных случаев. `ci-oracles` объявлено проверяемым значением (было самозаявленным: `tooling` при шести красных прогонах), DoD §3.2.3 требует URL прогона в `verify-report.md`, шаблон A.8 получил поля `CI run` / `Commit` |
| 2026-07-28 | **v1.14**: §3.1b — `weak` не константа: `kind: refactor` + `behavior-oracles: weak` стал stop-gate (закрывается характеризационными тестами, наблюдаемой проверкой или human waiver), поле `kind:` в STATUS, проверка в `delivery_check`. Указатели на CQG §8.6 (бюджет хуков) и §8.7 (квота CI) |
| 2026-07-28 | **v1.15**: §4.3a «Waiver живёт там, где идёт ревью» — таблица env / конфиг / STATUS с оценкой видимости и срока жизни; правило для новых гейтов; поля `canon_drift_waiver:` и `baseline_growth_waiver:` в шаблоне STATUS |
| 2026-07-28 | **v1.16**: §4.3b «Ложное срабатывание — дефект гейта, а не повод его снять»: порядок лечения (сузить область → видимый waiver → никогда не отключать), диагностический вопрос «ругается ли гейт на то, что должен проверять», два разобранных случая |
| 2026-07-28 | **v1.17**: §3.1c — багфикс без регрессионного теста не закрывается (поле `repro_test:`, проверка в `delivery_check`, `n/a` только с причиной): повторный баг — самый дорогой сорт |
| 2026-08-07 | **v1.54**: **§6.5a — между «исходник корректен» и «продукт правилен» лежал артефакт, и его не читал никто.** Замер приёмки восьмого развёртывания: пятнадцать ролей CQG зелены, CI зелёный, `astro check` проверяет типы, zod — frontmatter, а `dist/` не читает ни одна проверка. Сборка останется зелёной при уехавшей раскладке, и «зелёный CI» читается как «продукт правилен». Правило: проект объявил шаг сборки (`scripts.build`, цель `build:`) → у артефакта обязан быть машинный инвариант, объявленный строкой `artifact_oracle:`. Форма как у `runtime_paths`: объявление + проверка по ФАКТУ файла (объявленный оракул без скрипта — снятая проверка с видом усиления, §4.6), `n/a reason=…` законно, на `handoff` M/L отсутствие — ошибка. Правило не применяется к проектам без сборки: требовать артефакт у библиотеки значило бы шуметь там, где предмета нет (§4.3b). **Второе, и оно важнее: `weak` смешивал «не развёрнуто» и «непроверяемо».** Тот же отчёт разделил их сам — слой behavior пуст, но большая часть непокрытого проверяется ДЁШЕВО и без новых зависимостей (разбор собранного html, состав `dist/`, контраст как арифметика над токенами, переключатель языков как функция пути), а дорого стоит только то, чему нужен layout engine. Теперь `behavior-oracles: weak` обязан называть, что проверяемо машиной СЕГОДНЯ и не сделано, — правило то же, что §9.1a п.2 даёт гейтам: молчащая непокрытость либо закрывается, либо объясняется. Шаблон `<что именно>` за название не считается (класс v1.5). **Названо и то, чего не брать:** визуальные снапшоты — у них фирменный отказ «зелёный, потому что снимок перезаписали», и брать их можно только с дисциплиной журнала веса. Планка §2.1 зажала правку в том же прогоне: `delivery_journals.py` перевалил за 300, и новое выделено в `delivery_artifact.py`. Шесть прогонов на артефакт (нет оракула → warning; на handoff → ошибка; проект без сборки → тишина; объявленный файл отсутствует → ошибка; лежит → тишина; `n/a` без reason → ошибка) и три на `weak`. **вес: +107 строк, за §6.5a и его модуль** |
| 2026-08-07 | **v1.53**: **`delivery_check.py` разрезан — последний файл контура сверх планки 300.** 1969 строк, из них **986 в одной функции `main`** (cx=212) — половина файла. Одиннадцать модулей: `delivery_base` (пути, чтение, поля STATUS — соседей не знает, цикла нет по построению), `delivery_diff`, `delivery_decisions`, `delivery_risk`, `delivery_runtime`, `delivery_history` (обе проверки смотрят в git-историю, поэтому живут вместе) и четыре раздела активной поставки: `delivery_status` (форма — фаза, класс, артефакты), `delivery_evidence` (чем подтверждено), `delivery_journals` (§12.1–12.3), `delivery_limits` (CI, breaker'ы, рисковый дифф). **Точки разреза найдены ЗАМЕРОМ, а не на глаз:** перебор по AST искал границы, где меньше всего переменных живёт через шов, — их оказалось восемь, и они собраны в `ActiveCtx`. Внутри `main` их роль играли полсотни локальных, и именно поэтому функция не резалась годами. **Сверка вывода до/после в трёх режимах** (`--require-spec`, `--require-ci`, без флагов) — идентично. **Правило поймало саму правку, и это оказалось важнее разреза:** четыре раздела по ~200 строк — «новые функции сверх §2.1», а запрет `cqg@1.84` не имел выхода вовсе. Запрет без выхода даёт ровно два исхода: работа стоит или оракул снимают. Дописан выход — строка changelog с ТОЧНЫМИ числами, как «вес: +N строк» у веса. Записи этой правки: **функция: scripts/delivery_status.py::check_status_shape 166/39, за разрез main на разделы**; **функция: scripts/delivery_evidence.py::check_evidence 222/39, за разрез main на разделы**; **функция: scripts/delivery_journals.py::check_journals 214/47, за разрез main на разделы**; **функция: scripts/delivery_limits.py::check_limits 228/37, за разрез main на разделы**. Долг назван: разделы всё ещё вчетверо над §2.1, следующий заход — резать их на проверки. `main` при этом ужался 986 → 186 строк (cx 212 → 54). **вес: +218 строк, за разрез последнего файла сверх планки.** Свыше 300 строк в контуре не осталось НИ ОДНОГО файла: 46 скриптов, 8586 строк |
| 2026-08-07 | **v1.52**: **у `--require-ci` не было первого прогона — канон замыкал круг сам.** §10.4 велит ставить `ci-oracles: deployed` по факту ЗЕЛЁНОГО прогона, а флаг стоит внутри того самого прогона (§8.3, джоба `delivery`): честное `weak` держит прогон красным, красный прогон не даёт значению стать честным. Замерено на восьмом развёртывании — агент вышел флипом в том же коммите, законно, но канон об этом молчал, и оба видимых выхода выглядели как обход собственного правила. **Развязка по смыслу флага:** он защищает не каждый прогон, а ЗАКРЫТИЕ поставки. До `handoff` `weak` даёт warning с единственным названным ходом («поставь `deployed` по факту ЭТОГО прогона, в том же коммите»), на `handoff` остаётся ошибкой — иначе развязка была бы снятием требования. Реальность (прогон исполняется) старше записи. Три прогона: первый прогон → warning и exit 0; закрытие с weak → ошибка; честное `deployed` → тишина. Обратный прогон вернул ошибку на всех фазах — упал ровно тест первого прогона |
| 2026-08-07 | **v1.51**: **§9.1a п.5 — ратчет на ВЕС контура, а не только на число гейтов.** Бюджет держал количество (22 с 2026-07-29), а механика всё это время росла внутри существующих файлов: замер дал **22 скрипта и 8023 строки**, семь свыше 300, рекорд `delivery_check.py` — 1948. Правило симметрично коду: потолок на скрипт = размер на день съёмки, снимок только вниз; новый скрипт контура — не выше 300 строк, то есть по §2.1, который контур требует от чужого кода; ужатие законно всегда. **Повод — обещание без механики:** `check_file_length.sh` исключает контур из гейта длины со ссылкой «размер скриптов контура мерится отдельно (§9.1a)», а такого замера не существовало нигде — ни в сьюте, ни в `delivery_metrics.py`, ни в докторе. Контур вывел себя из-под собственного правила и не завёл замену — тот же класс, который он ловит у чужого кода. Замер живёт в репозитории канона (`tests/test_contour_size.py` + снимок `tests/contour_size_baseline.txt`), потому что размер payload — свойство канона: в проекте эти файлы не меняются, а если меняются, это адаптация и её объявляет `adapted.json`. **Названо и то, что растёт быстрее всего:** проверяющий слой — `contour_doctor.py` прошёл 591 → 940 → 1082 строки, и три дефекта правки `cqg@1.80` были дефектами самой проверки. Обратные прогоны: рост живого скрипта на 40 строк роняет сьют с адресом и дельтой (`check_ci_status.sh: 176 при потолке 136, +40`), плюс три прогона на выключенных правилах (рост, планка, призрак в снимке) — каждый уронил ровно свой тест. **И находка первого применения:** оракул покраснел на правке, которая его вводила (комментарий в `check_file_length.sh` вырос на две строки), а что делать с ЗАКОННЫМ ростом, первая редакция правила не говорила — снимок «только вниз» без описанного хода вверх либо останавливает работу, либо переснимается молча. Ход дописан: рост — решение с записью, как поднятие лимита в п.1. **вес: +2 строки, за комментарий, который перестал обещать несуществующий замер.** **Вторая величина — функция:** снимок на длину и сложность с hard-порогами §2.1 (80/10), потому что ратчет только на файл покупается перекладыванием — скрипт ужимается, монолит остаётся. Замер: 136 функций, 21 сверх порога, рекорд `delivery_check.py::main` — 965 строк и cx=211 (половина файла в одной функции), девять из двадцати одной — в `contour_doctor.py`. Сложность считается прокси на stdlib-ast и названа прокси: оракул, молча не запускающийся без ruff, был бы тем самым классом, который контур ловит |
| 2026-08-06 | **v1.50**: **новая §12.6 — класс отказов, невидимый ВСЕМ статическим оракулам.** Полевой замер: четыре падения за сутки на одном iOS-проекте, все четыре нашёл человек на телефоне, все четыре **при зелёной сборке**. Общее свойство, которое канон не знал: **платформа отвечает на нарушение политики смертью процесса, а не кодом ошибки** — нет ключа в `Info.plist` (сборочная настройка игнорируется молча при `GENERATE_INFOPLIST_FILE = NO`), запрос снимка сверх потолка выхода (NSException), чтение библиотеки под правом «только добавлять» (TCC убивает процесс). Ни одно не ловится `try`, значит не допустить можно только исполнением пути. Прежняя таксономия (`shape` / `behavior` / `ci`) этот класс не покрывала, а исполнение на целевой среде жило только в `observe_signal` — то есть **после** handoff. Механика построена на образце §12.5, но отвечает на другой вопрос: маячок доказывает, что дифф ПРОЧИТАН, а здесь чтение не помогает вовсе — весь этот код автор читал и считал верным, и полевой разбор называет причину прямо: «я рассуждал о правах вместо того, чтобы их проверить». Три части: ① `runtime_paths:` в STATUS — проект объявляет поверхности, чей отказ не виден ни сборке, ни тестам (настраивается под домен, как `RISK_CLASSES`), и **`none` БЕЗ причины объявлением не считается** — то же правило, что `diagnosis: n/a reason=…`, иначе поле вырождается в галочку на первой поставке; ② дифф задел объявленное ⇒ в verify-report блок «## Исполнение рисковых путей» со строкой на КАЖДЫЙ путь: чем исполнял (в backticks), что увидел, `at=<дата>`; ③ breaker `max_runtime_paths` (дефолт 1) — «одна правка — одна установка» механически. Последнее принципиально: **это правило в поле было написано и нарушено тем же автором через несколько часов**, а объёмные breaker'ы §3.4 такого не видят — две правки в две подсистемы на десяток строк проходят их не заметив. Канон говорит о себе то же самое в DoD §3.2 п.10: «правило без гейта, живёт пока его читают». Лестница как у §12.5 и `deferred`: предупреждение на verify, отказ на handoff. **Требование назвать команду — не косметика:** в поле инструмент для такой проверки СУЩЕСТВОВАЛ и был выброшен (подвис не сам по себе — процесс убивали по таймауту посреди отправки), и слабую проверку заменили на никакую; записанная команда делает следующий раз воспроизводимым. **Прямо сказано, чего механизм не даёт:** доказательства, что прогон был, здесь нет — есть доказательство, что про каждую задетую поверхность сделано конкретное утверждение с датой; это ловит не ложь, а ПРОПУСК, и пропуск ровно то, что случилось все четыре раза (путь не исполнялся НИ РАЗУ, а не исполнялся плохо). Отдельной врезкой: **сборка оракулом не является** — шаг «проект собрался» отвечает про компиляцию и ни на один вопрос про поведение; проверять надо артефакт, который поедет (`plutil -p` по бандлу, `codesign -d --entitlements`, параметры пришедшего кадра), и **два падения из четырёх ловились на хосте, без устройства**. **Дефект в собственной правке поймал собственный тест:** проверка «поверхность названа» искала только сегменты пути длиннее трёх символов, и у `ios/UI/` таких нет вовсе — удовлетворить её было НЕЧЕМ, то есть гейт был непроходим (§4.3b, такое снимают за неделю вместе с пользой). Сьют: **458 тестов** (+11) |
| 2026-08-04 | **v1.49**: **процедура развёртывания §7.2 впервые под регрессией, а не под честным словом.** Дыра была названа в бэклоге прямым текстом: сьют держал поведение СКРИПТОВ, а саму процедуру проверял только живой полигон — полигоны одноразовые, поэтому следующее развёртывание начинало с нуля и повторяло уже закрытые находки (класс «файл выпал из процедуры молча» ловили четырежды). Теперь `tests/test_deployment_procedure.py`: таблица шага 1 сверяется с деревом §2.3 в ОБЕ стороны, каждая обязательная строка разворачивается в одноразовый git-репозиторий, шаг 3 переводит блок `agent-permissions` в `.claude/settings.json` разбором самого гейта, дальше приёмка — `delivery_check` без трейсбека, `.gitkeep` под git, доктор. Отдельно проверено, что пропуск `pre-commit install` доктор зовёт ложью (DEAD), а бедность стенда прогон не роняет. Три обратных прогона: убрать строку таблицы, подменить номер приложения, вписать несуществующий файл — каждый роняет свой тест. **Первый же прогон нашёл настоящее: три файла дерева** (`eval-smoke.md`, `evals/smoke/README.md`, `archive/<slug>/observed.md`) **не были в инвентаре извлечения**, потому что обход шёл по «### A.N», а A.9 отдаёт два файла под одним номером — ровно тот класс, про который §7.2 написал свою врезку |
| 2026-08-03 | **v1.48** (lab-12): **проверка происхождения ожидания обвиняла честную работу — и научила арку себя обходить.** `_first_commit_with` искал id примера через `-S`, то есть как ПОДСТРОКУ: короткий `A2` совпадал внутри `DATA2_FIXTURE` в legacy-тесте, закоммиченном задолго до поставки, и §3.1d печатала «примеры появились в истории ПОЗЖЕ ссылающихся на них тестов». Это обвинение в худшем процессном грехе (ожидание придумано после кода) на работе, где порядок был соблюдён. Снять его исполнитель не мог ничем, кроме переименования id — арка A так и сделала (A1–A6 → E1–E6), порвав связь с id, **подписанными заказчиком**, ради тишины гейта. Арка B оставила id и предсказала свой ложный warning заранее. Теперь id ищется как ТОКЕН: явный класс символов с якорями. ⚠ **Очевидная правка `\b` сделала бы проверку МЁРТВОЙ:** замерено — `-S'\bA2\b' --pickaxe-regex` в git не находит ничего, и прогон «сломано → падает» этого не показал бы, потому что падало бы в обоих случаях. Тот же класс, что `\b` в awk и `^\+` в ugrep. Поэтому тест проверяет ЖИВУЧЕСТЬ отдельно, и он ловит подстановку наивного `\b` — проверено. **Дефект в собственной фикстуре:** первая версия клала спеку заранее, `git add -A` уносил её тем же коммитом, что и тест, порядок становился «не проверить» — и тест на живучесть прошёл бы по неверной причине |
| 2026-08-03 | **v1.47** (lab-12, нашли ОБЕ арки независимо + собственная проба): **метрики §9 приписывали текущей поставке числа ПРЕДЫДУЩЕЙ.** `first_commit` искал по ВСЕЙ истории пути, а пути внутри `delivery/active/` **переиспользуются каждой поставкой** — значит «первый в истории» отвечает про первую работу репозитория, а не про эту. Класс: **переиспользуемый путь + поиск по всей истории = чужой ответ с видом своего.** ① `rework_after_done` печатал «3 commit(s) after first phase: handoff» на поставке, которая handoff НЕ объявляла — счёт шёл от handoff'а bootstrap'а; обе арки нашли это независимо и обе записали уроком L4, то есть механика воспроизводима. ② **Брат, которого не нашёл никто, кроме собственной пробы:** `time_to_accepted_spec` выдавал `4.0d` там, где спека и подпись сделаны в ОДИН день. Он опаснее первого: число настоящее и правдоподобное, просто не про эту работу — такое не вызывает подозрений и уезжает в отчёт как факт. Оба закрыты одним решением: `first_commit` получил `rev_range`, и все три вызова ограничены `merge_base..HEAD` — границей текущей поставки, которую §5.1 и так требует (ветка на поставку). **Дефект в собственной проверке правки:** первая фикстура держала вторую поставку на `main`, поэтому `main` двигался вместе с HEAD, диапазон выходил ПУСТЫМ, и «починено» проходило по неверной причине («ничего не нашли» вместо «нашли своё»). Поймано тем, что `time_to_accepted_spec` вдруг сказал «нет spec.md» при существующем файле; добавлен контрольный тест, что сужение области не отрезало своё |
| 2026-08-02 | **v1.46**: **§9.1a — поднятие лимита стало описанным ходом, а не лазейкой.** Правило и раньше разрешало «удаление одного ИЛИ письменное обоснование», но что обязано быть в обосновании — не говорило, и оно легко выродилось бы в «нам нужно». Теперь названы четыре обязательных элемента: **кто** занял слот, **за что заплатили** (какой класс дефектов и чем доказано, что оракула не было), **замер** (новый гейт ловит этот класс и не шумит на легаси), **почему не обмен** (чем плохи кандидаты на выбытие прямо сейчас). Формулировка лимита развязана с числом: число и счёт живут в одном месте (CQG §3), здесь их нет — иначе счётчики разъезжаются. Повод: 2026-08-02 лимит поднят 20 → 21 под `cpu-in-async` (cqg@1.47), и разбор lab-12 предлагал это правило как «слот: 0» — абзац про единицу счёта показал, что оно стоит слота. Записано и то, что правило проверено работой: единица счёта устояла против удобного пересчёта |
| 2026-08-02 | **v1.45** (lab-12, М3+М4 — по единственному исходу, подтверждённому как slipped ОБЕИМИ арками): **маячок §12.5 не видел производительность и покупал заголовок вместо содержания.** ① Новый класс риска **«производительность»**: признак — не слово «медленно», а ФОРМА (выборка без границы `list_all`/`fetchall`/`.all()`, разбор и поиск в цикле — `json.loads`, `re.search`). Замер lab-12: маячок тогда поднялся, но по чужому поводу («новый модуль» + `commit` у соседа), «classes matching perf/limit/scan: []»; та же правка в СУЩЕСТВУЮЩЕМ модуле не подняла бы его вовсе. ② **Каждый задетый класс обязан быть разобран**, а не «хотя бы один»: пробы аудита показали, что проходили и трёхстрочный блок с одним классом, и настоящий блок, из которого удалили ровно перф-строку — отписка по частям есть отписка. Класс считается разобранным, если названо его имя **ИЛИ токен, который его поднял** (`list_all`, `session.begin`, имя нового файла): второе честнее ярлыка, потому что показывает, что читатель видел конкретное место, и снимает хрупкость требования дословного русского ярлыка. Поэтому `risky_classes` возвращает КЛАСС → ТОКЕНЫ, а не список имён. «Новый модуль» из требования исключён сознательно: он структурный — поднимает маячок, но своей строки не требует. **Бюджет гейтов не тронут: это маршрутизация и ужесточение существующей проверки, а не новая строка каталога.** ⚠ **Собственный тест поймал переусердствование:** первая версия требовала строку с цитатой на КАЖДЫЙ класс и покраснела на своём же тесте «законный выход» — из-за структурного «нового модуля». Ровно ловушка §4.3b, из-за которой гейты снимают; исправлено до коммита. 3 новых теста, на старом каноне падают 2 |
| 2026-08-02 | **v1.44** (lab-12, найдено развёртыванием и обеими арками): **`delivery_check` умирал трейсбеком ВМЕСТО того, чтобы напечатать уже сформулированный диагноз.** `haystack` строился только в else-ветке разбора acceptance-примеров, а читался безусловно проверкой §6.5: spec.md без блока `## Acceptance examples` **или** с id вне грамматики `[A-Z]\d+` давали `UnboundLocalError: haystack`. В обоих случаях текст ошибки уже лежал в `errors` — исполнитель видел поломку инструмента там, где ему сообщали о его собственной ошибке. **Класс — зеркало «гейта, врущего зелёным»:** тот молчит, когда должен говорить, этот кричит не о том; оба лишают исполнителя диагноза. Мера — не «инициализировать переменную», а держать СБОР ДАННЫХ отдельно от РАЗБОРА ПРИЧИН: корпус (smoke + tasks + verify + тексты тестов) читают две независимые проверки, поэтому строится безусловно. Заодно **грамматика id названа в тексте ошибки** — она не выводима и её нарушение выглядело как «примеров нет» при полной таблице примеров (арка написала `EX1` и получила краш, вторая переименовала свои id, обходя соседнюю проблему вслепую). Класс закрыт оракулом-матрицей «любой вырожденный spec → диагноз, а не трейсбек» (5 форм), поэтому ловятся и будущие причины краха; статически подтверждено, что братьев нет: `mypy --enable-error-code possibly-undefined` по шести извлечённым скриптам даёт ровно одно попадание — это. На старом каноне падают 6 проверок из 6 |
| 2026-08-01 | **v1.43** (lab-11 F12/F13/F8 — дедлок handoff после bootstrap; обе арки, разными механизмами): пять правил — «kind: bootstrap», «одна active», «архивация на handoff», «нет человека → deferred», «merge_guard против main» — по отдельности верны, а композиция давала тупик: арка A получила 34 файла / 12 585 строк в диффе багфикса класса S (собственных — 3/190), арка B встала совсем, потому что мерж bootstrap'а законно блокировал дефект, найденный самим контуром. Три половины одного решения. ① §7.2 шаг 6: финал bootstrap'а — мерж В ТЕЧЕНИЕ активной поставки, до архивации и до следующей (выход, найденный аркой A, стал правилом); после squash следующая ветка — от свежего main. ② Механическая: `delivery_check` на specify/plan/tasks требует ПУСТОЙ стартовый дифф — непустой означает незамерженную предыдущую или ветку от старой базы, и тупик называется на входе за минуту вместо handoff ценой цикла; «слито в локальный main, не запушено» различается и чинится push'ем, не rebase'ом. ③ §7.2 шаг 6: дефект, найденный развёртыванием в существующем коде, bootstrap НЕ останавливает — точечный трёхполевой `filterwarnings`-ignore с TODO и строкой в spec следующей поставки законен, глобальное ослабление и стояние — нет (стоящий bootstrap блокирует всё). Парная правка — cqg@1.44: диагноз конфликта в merge_guard + цена squash в §8.8. Три прогона: 3 из 5 тестов падают на старом каноне, стражи (чистый старт, рабочий дифф на implement) проходят на обоих |
| 2026-08-01 | **v1.42** (lab-11 F7, парная к cqg@1.40 — один класс: «признак выбран под одну форму поля и распространён туда, где те же символы — законное содержание»): `is_placeholder()` объявлял незаполненным ЛЮБОЕ значение с `\|`, а зовут его 14 полей, и часть — свободнотекстовые. Точный `observe_signal` с jq-конвейером (`docker logs app \| jq …`) считался «пустым» — гейт наказывал точность, а сообщение врало направлением. Признак сужен до ГОЛОГО списка коротких альтернатив (вся строка — токены ≤16 символов через `\|`): «S \| M \| L» — по-прежнему шаблон и падает, конвейер в `observe_signal` — значение и проходит. Обе стороны закреплены тестами. Побочный эффект в честную сторону: `blockers:` со значением-списком через `\|` раньше молча считался пустым (и эскалация §12.3 не требовалась) — теперь это содержание |
| 2026-08-01 | **v1.41**: **новая §12.5 — маячок на рисковый дифф: маршрут к читателю, а не оценка.** Есть класс дефектов без оракула («архитектура неудачна», «делает не то»), и §3.1d отказывается судить его моделью. Здесь механизм другой формы: не оценивать качество, а **гарантировать, что рисковый дифф прочитан**. «Человек посмотрел» проверить нельзя — у заявления нет наблюдения для сверки, это анкета. Проверяется содержание: блок «## Ревью рисковых мест» в verify-report, где назван класс риска и процитировано ≥2 идентификатора **из диффа** (имена функций, полей, файлов — такого не написать, не открыв код; тот же приём, что `repro_test:` и признак цены §12.2a). Классы вычисляются по диффу: деньги · безопасность · транзакция БД · цепочка гейтов · интеграция · новый модуль. Законный выход — «риска нет, потому что …» с теми же цитатами: он тоже требует чтения, поэтому это не waiver (класс F4/F6/F13 закрыт по построению). Лестница как у `deferred`: предупреждение на verify, отказ на handoff — блокировать на каждом коммите нельзя, правка опечатки получала бы требование ревью. **Три ложных срабатывания поймал первый же прогон на живой арене**, и все три исправлены: `.claude/` считался продуктовым диффом (слово `permission` в собственном файле прав — контур ругался на свою машинерию, теперь в `BREAKER_EXCLUDE`); корень `доступ` совпадал внутри `недоступен`; голый `token` совпадал в `test_..._one_token`, потому что в этом проекте токен — единица стрима LLM. Отсюда правило, вписанное в §12.5: многозначные слова берутся только составными формами, а `RISK_CLASSES` — стартовый список, настраиваемый под домен, как `LINT_PY_SRC` под раскладку. Прямым текстом сказано, чего механизм НЕ даёт: доказательства, что смотрел человек, здесь нет — есть доказательство, что дифф прочитан |
| 2026-07-31 | **v1.40**: **признак «цены решения» перестал ошибаться в обе стороны.** Проверка §12.2a, введённая в v1.38, держала список глаголов — и в тот же день, на dogfooding'е, пометила «без цены» образцовую запись: «длина **съедала** легитимное `First one. Second one?`». Следствие названо предельно конкретно, просто глагола в списке не было. Теперь признак — **союз трёх**: число, ИЛИ маркер следствия, ИЛИ цитата из диффа (имя файла/функции/поля — такое не напишешь, не открыв код; тот же приём, что предложен для маячков). **Ошибка в замысле правки, которую поймал существующий тест:** первая версия делала цитату из диффа ЗАМЕНОЙ списку, и это неверно по построению — **дифф содержит то, что выбрали, а не то, что отвергли**, поэтому цена отвергнутого варианта естественно ссылается на то, чего в коде нет («B требует внешнего сервиса, которого у нас нет»), и проверка отвергала образцовую запись. Союз даёт наименьшую долю ложных срабатываний, а «дешевле» ему всё равно не удовлетворить. Без `--diff-base` основа берётся `HEAD~1`: иначе проверка молчала бы локально и работала в CI, то есть вела бы себя по-разному там, где решение и принимается; на первом коммите сравнивать не с чем — молчит. Сьют: **275 тестов** |
| 2026-07-31 | **v1.39**: **формо-зависимые проверки перестали врать диагнозом.** На dogfooding'е (lab-10) я трижды написал разумную разметку и трижды получил сообщение о СОДЕРЖАНИИ: «заполненных вариантов 0» при двух вариантах в `escalation.md` (я написал `## Вариант 1`, парсер ждал `**Вариант A:**`) и дважды «без заполненных вердиктов» в `diagnosis.md` — сначала на таблице «гипотеза \| вердикт», потом на пункте с вердиктом на второй строке. Ни одна из трёх подстроек не была про смысл: варианты и вердикты в файлах были, не совпадала разметка, и гейт об этом не говорил. Теперь: варианты принимаются заголовком, жирной строкой, пунктом и строкой таблицы; вердикты считаются по **структурным блокам**, а не строкам, поэтому перенос внутри пункта и таблица работают. Оба сообщения называют ожидаемую форму. **Дыра, от которой защищало прежнее ограничение, осталась закрытой** и закреплена тестом: строка шаблона «Вердикты: `ОПРОВЕРГНУТА` / `ПОДТВЕРЖДЕНА`» — проза о формате, структурным маркером не начинается, в блок не попадает. Два дефекта в самой правке поймали тесты, а не я: `\s*` вместо `\s+` сделал заголовок раздела «## Варианты» вариантом (одного хватало на два), а требование непробела сразу за `\|` отвергало ровно тот табличный формат, ради которого правка делалась. Сьют: **272 теста** |
| 2026-07-31 | **v1.38**: **шаги 2, 3, 5 плана против круга §3.1d.** ② **Порядок «пример раньше теста» стал проверяемым.** §3.1d говорила «примеры пишутся ДО кода, порядок принципиален», а принуждались наличие блока, наличие id и ссылки на них — три признака, ни один не про порядок. Память — история git (`git log -S<id>`), как в §3.5. **Исходов ТРИ, а не два, и это не мягкость:** агент часто кладёт спеку, код и тесты одним коммитом, и тогда порядок установить НЕЛЬЗЯ — честный ответ «не проверить» с числом, а не «нарушение»; требуй строгого старшинства, и проверка краснела бы рутинно, после чего её сняли бы (класс F4/F6/F13). Пример новее теста — вот это нарушение: ожидание, придуманное после реализации, есть описание реализации. ③ **Решение обязано называть цену, а не предпочтение** (§12.2a): «потому что дешевле» формат проходит, но неопровержимо, значит аудита не даёт, ради которого файл ведётся; «B потребовал бы миграции 40k строк» человек проверяет за секунды. Принимается число или конкретное следствие. ⑤ **Записан отказ от оценки решений моделью** с тремя причинами: ломает определение гейта («без участия модели»), это самооценка с лишними шагами, и балл становится целью оптимизации вместо архитектуры. Приведён замер дня: четыре случая, когда мои же проверки воспроизводили слепую зону, которую искали, и ни один не поймало рассуждение — все поймал прогон. **Багфикс в самой правке, того же класса:** проверка цены считала ценой **дату записи** — формат §12.2 требует дату в каждой строке, поэтому наивный поиск цифры не срабатывал бы никогда. Третий случай «цифра найдена не там» за сессию (хвост версии как количество, `A1` в проверке `observability`), закреплён отдельным тестом |
| 2026-07-31 | **v1.37**: **уровень 2 §3.1d перестал быть практикой в прозе.** «Класс M/L: минимум один реляционный оракул» стояло в §6.5 и не принуждалось ничем — та же форма, что F7. Теперь `delivery_check` предупреждает, если в тестах нет ни `@given` (hypothesis), ни `fc.property` (fast-check). Зачем именно свойство: тест на значение `f(2) == 4` закрепляет баг, если ожидание списано с кода, и **мутационный гейт этого не поймает** — мутант будет честно убит НЕВЕРНЫМ утверждением; в свойстве ожидаемого значения нет, поэтому подделать его нельзя. **Исправлена собственная формулировка §6.5** «слабое свойство гейтом не отличить»: отличить можно — декоративное свойство (`assert result is not None`) не реагирует на порчу кода, мутанты остаются живыми, доля убитых падает ниже порога, и это видно числом. Проверка описана как четыре слоя, каждый закрывает лазейку предыдущего: свойство (нельзя списать ответ) → генератор входов (нельзя выбрать удобные случаи) → mutation (нельзя заявить свойство, ни на что не реагирующее) → якорный пример снаружи (прибивает, какую именно функцию построили). Отсюда порядок работ: **свойства без работающего mutation неполны**, поэтому сначала `brew install coreutils` (CQG §5 шаг 3), потом требование свойств. Сама проверка названа честно: она смотрит на НАЛИЧИЕ и заполняется галочкой, её верификатор — mutation, и это сказано в тексте ошибки, чтобы не вышел ритуал вместо оракула |
| 2026-07-31 | **v1.36**: **новая §3.5 — долг во времени. Первый механизм контура, который смотрит не на коммит, а на тренд.** Ратчет запрещает снимку РАСТИ и ничего не требует от того, что снимок СТОИТ: двадцать поставок с неподвижным снимком зелёные все двадцать, каждый коммит честный, а долг не двигается. Платить по нему заставляло только соглашение «тронул легаси — почисти часть», то есть честность исполнителя — не заметил или схалтурил, и запись живёт вечно. **Памятью служит история git, а не файл:** свою «тетрадку» валидатора можно поправить тем же коммитом, что и нарушение, причём подделывать не надо — достаточно пересчитать; историю так не поправишь. Точки замера — коммиты, добавившие каталоги в `delivery/archive/`. **Метрик две, потому что одной мало:** сумма по снимкам за окно ловит «долг не тает вообще», а запись, прожившая всё окно **без движения веса**, ловит вечного жильца — сумму можно уронить, растопив лёгкие записи и оставив тяжёлые гнить. Вторая обязана различать «гниёт» и «чистят медленно»: `40→34→28→22→16` нарушением не является, `80→80→80→80→80` является; без этого различия проверка ругалась бы на честно чистимое легаси и её бы отключили. Бьёт **только на handoff**: на каждом коммите блокировала бы работу, не связанную со старым долгом, а предупреждением — не останавливала бы ничего. Три состояния проверка узнаёт сама (долга нет · данных мало, с печатью «накоплено K из N» · накопилось), иначе это был бы гейт, непроходимый в разрешённой каноном конфигурации — класс F4/F6/F13. Замер: 77 мс на пяти поставках. Два дефекта в самой правке поймал обратный прогон: порядок окна сортировался по времени коммита и разъезжался при совпадении секунды (началом окна становилась самая новая поставка, сравнение выходило вырожденным), и `git` работал в текущем каталоге вместо `ROOT`. **Единственный механизм, который нельзя проверить полигоном** — нужны десятки поставок; история архива в регрессии синтетическая, и это названо в §3.5 |
| 2026-07-30 | **v1.35**: **у автономного развёртывания появился честный выход при отсутствии CI.** Класс L + нет хостинга + нет человека давал `delivery_check` красным НАВСЕГДА: waiver требует человека, занижать класс запрещено §2.2b, а компенсация §10.4 п.4 и `blockers:` + `escalation.md` — выходы, которые канон сам называет законными, — гейтом не принимались (единственной ветвью снятия была строка waiver'а). Рядом лежали две дешёвые лазейки: занизить класс до M или написать `waivers: ci`, и обе давали зелёную приёмку — то есть канон **учил обходить себя законным с виду способом**, ровно то, против чего написана §2.2b. Асимметрия была видна в собственном выводе: для `human_ok_*` третье значение сделали, для `ci-oracles` нет. Теперь компенсация проверяется по ФАКТУ артефактов (`clean_clone_run:` в verify-report + непустые `blockers:` + `active/escalation.md`, где §12.3 сам требует два варианта с ценой), при этом `ci-oracles` остаётся `weak`, предупреждение громкое, а строка приёмки §6 закрывается как `weak`, не как `auto`. Нашло восьмое развёртывание, пройдя всеми законными путями и получив красный гейт |
| 2026-07-30 | **v1.34** (финальная приёмка): ① **CI-waiver класса L снимался ПОДСТРОКОЙ**: условие было `"ci" not in waivers`, поэтому `waivers: ci` снимало требование §10.4 без причины и без подписи. Форма waiver'а одна на весь контур (§4.3a): `reason=` и `by=human:`. ② **`docs/canon/` добавлен в `BREAKER_EXCLUDE`**: снимок канонов варианта C давал 10 743 строки из 13 743 пробитого лимита, при том что §7.1 велит «проверь, что путь снимка в исключениях» — инструкцию добавили в прошлой партии, а путь забыли. Обе находки — приёмочного развёртывания |
| 2026-07-30 | **v1.33** (приёмка перед заморозкой): **`harness_hardened` не видел оракулы, живущие в тест-сьюте проекта.** Словарь AGENT_STACK §1.1 говорит «oracle = гейты + **тесты** + smoke + артефакты», §5.4 считает усилением «добавить oracle», а `HARDENING_PATHS` знал только пути контура — поставка, добавившая два регрессионных оракула (включая мета-оракул «проверка, что защита подключена»), получала `harness_hardened: no`, и §9.2 по этой метрике советовал «добавь oracle», то есть добавить уже добавленное. Теперь считаются **добавленные** тест-файлы (`--diff-filter=A` + `TEST_PATH_MARKERS`): любая правка тестов сделала бы метрику вечным `yes`, и она перестала бы отвечать на свой вопрос |
| 2026-07-29 | **v1.32**: **`kind: bootstrap` breaker'ом не мерится (§3.4).** Приёмочное развёртывание намерило 30 файлов и 2437 строк **после** исключения механики контура — то есть даже с правильными исключениями развёртывание пробивало лимит 800, а совет гейта «split the PR» для bootstrap невыполним по построению: контур ставится целиком или никак. Единственным выходом оставался рутинный waiver, который §4.3a сам называет обесцениванием механизма — гейт учил себя обходить. Bootstrap — не поставка продукта: его DoD это §7.3 плюс приёмка §6, и мерить его объёмом поставки было категориальной ошибкой. Значение добавлено в шаблон A.2 и в шаг 6 процедуры §7.2 (иначе правило неисполнимо), объём по-прежнему печатается строкой |
| 2026-07-29 | **v1.31** (приёмочное развёртывание): ① **поправка к v1.30.** Строка ниже утверждает, что §2.2b распространена «на все поля-подписи» — **это было неверно**: развилку получили два поля из трёх, а `asserts_reviewed_by` остался с отдельной ветвью, принимавшей ЛЮБУЮ непустую строку (`посмотрел сам`, `by=agent:…`), и без значения `deferred` — то есть автономному агенту законного значения не оставалось вовсе. **Третье появление одной дыры**, и второй раз я объявил класс закрытым, закрыв случай. Теперь поле идёт через ту же `signature_verdict`, подпись распознаётся в обеих канонных формах (`yes (by=human:…)` и `human:<имя> at=…`), а оракул `test_signature_and_wrapping.py` перечисляет поля списком — четвёртое поле с отдельной ветвью упадёт на тесте, а не на развёртывании. ② **Вердикт гейта зависел от вёрстки.** Записи `decisions.md`/`diagnosis.md` и `plan.md` разбирались по ФИЗИЧЕСКИМ строкам: одно и то же решение проходило при переносе после «потому что» и краснело при переносе перед ним. Канонному примеру §12.2 повезло с вёрсткой, записям исполнителя — нет; два развёртывания нашли это независимо, в разных файлах. Введена `list_entries()` — склейка продолжений в логическую запись, одна на всех потребителей. Проверено, что склейка не глотает настоящую находку: пункт без альтернативы по-прежнему красный |
| 2026-07-29 | **v1.30** (пятое развёртывание, вторая волна — «человека нет» как КЛАСС): ① §2.2b распространена со `human_ok_spec` на **все поля-подписи** — `human_ok_plan` на классе L упирался в тот же тупик, потому что рядом стояла отдельная ветка с той же логикой минус `deferred`; развилка теперь одна (`signature_verdict`), и это пятое появление узора «починили случай, а не класс». ② **waiver breaker'а требует `reason=` и `by=human:`**: канон говорил «только человек», а парсер принимал `max_loc_diff=99999` без причины и подписи — одна арка честно записала, что подписалась `by=agent:` за неимением человека. Настройка проекта (`circuit_breakers:`) отделена от waiver'а: конфигурации подпись не нужна. Человека нет — waiver'а нет: разрезать поставку или `blockers:` + escalation.md (§12.3), потому что пробитый breaker к моменту мержа уже случился, в отличие от подписи под ожиданиями |
| 2026-07-29 | **v1.29** (пятое развёртывание — настоящий проект, три независимые арки `lab-5-real-{a,b,c}`): **развёртывание контура пробивало собственный circuit breaker.** `BREAKER_EXCLUDE` не включал четыре скрипта контура, а они дают 1527 строк против лимита 800 — причём один `delivery_check.py` (946) больше лимита, поэтому совет гейта «split the PR» невыполним по построению и оставался только рутинный waiver, который §4.3a сам называет обесцениванием механизма. Все три арки наступили независимо. Класс уже был закрыт у гейта длины (его grep эти четыре пути исключает) и **не перенесён сюда** — правка была на случай, а не на класс. Теперь пути в `BREAKER_EXCLUDE`; регрессия `tests/test_breaker_scope.py` держит обе стороны: механика контура не считается, 900 строк продуктового кода по-прежнему пробивают. Проверено обратным прогоном на подменённом каноне: без правки тест краснеет |
| 2026-07-29 | **v1.28** (системная дыра из lab-4): новая §2.2b «Агент работает, человека рядом нет». Канон требовал `human_ok_spec: yes` для класса M и **молчал** про случай, когда согласовать не с кем — и тем самым учил **занижать класс до S**, обходя требование спеки способом, который выглядит законным. Гейт при этом цел, поэтому дыра опаснее любого ложного срабатывания. Правило: **класс определяется работой, а не доступностью человека**; занижение запрещено, а у подписи появилось третье значение `deferred (reason=… at=…)` — спека с примерами есть, человека нет, работа идёт под запись долга. Оно пускает до `verify`/`converge`, но `delivery_check` на `handoff` требует именно `yes`: проверка не ослаблена, а перенесена туда, где человек всё равно есть — на мерж. Долг виден в PR и умирает с поставкой, как waiver'ы (§4.3a) и обязательство наблюдения (§13.1). Плюс запах занижения: класс `S` при диффе шире S-масштаба (>5 файлов или >200 строк) даёт warning — гейтом делать нельзя (законные большие багфиксы есть, §4.3b), но молчать нельзя, молчание эту дыру и создало. Нашло **независимое развёртывание**: агент, не знавший ни канона, ни находок, выбрал S и был формально прав |
| 2026-07-29 | **v1.27** (независимое развёртывание, lab-4): **`delivery/active/.gitkeep` стал обязательным.** §2.3a объявила пустой `active/` штатным состоянием — но git пустые каталоги не хранит, поэтому после первой архивации каталог исчезает из дерева, и в свежем клоне `delivery_check` падает `missing delivery/active/` жёстко, до всех прочих проверок. Канон объявил штатным состояние, которое git не умеет представить; нашёл это **агент, не участвовавший в разработке канона**, на первом же fresh checkout. Добавлено в дерево §2.3, в список файлов §7.2 и в DoD §7.3 с проверкой `git ls-files delivery/active/` |
| 2026-07-29 | **v1.26** (полевые находки F1, F3, F4, F5, F11): §7.2 переписан **со списка номеров приложений на список файлов дерева §2.3** — «шаблоны A.1–A.11» осталось от времён, когда приложений было одиннадцать, и шесть новых выпадали из развёртывания молча; номер не говорит, что внутри, поэтому пропуск не виден ни при развёртывании, ни при чтении DoD (я сам так потерял A.9). Добавлен шаг **создать `.claude/settings.json` из блока `agent-permissions`**: шаблон A.1 приезжает с заполненным блоком, и без этого шага свежий контур падал в каждом новом проекте — гейт требовал шага, которого в процедуре не было. Предупреждение о **вложенных фенсах** при скриптовом извлечении: наивная регулярка обрывает A.1 на внутреннем ```` ```text ````, теряя ровно тот блок, который требует §4.5. DoD §7.3 дополнен `decisions.md`, `INDEX.md` и настройками; пример версии обезврежен до `delivery@<version>` (тот же класс, что и число приложений). §3.4: `BREAKER_EXCLUDE` расширен механикой контура (`scripts/lint/**`, `merge_guard.sh`, `.pre-commit-config.yaml`, `.github/workflows/**`, `.claude/**`) — развёртывание и поставки `kind: refactor` по контуру пробивали breaker всегда, превращая waiver в рутину; проверено полем: 36 файлов/3163 строки → 8/430. Обратная сторона названа: объём гейт-скриптов теперь ничем не ограничен |
| 2026-07-29 | **v1.25** (полевые находки F9, F12 — первое развёртывание в lab-1): **`delivery_check` падал трейсбеком в любом проекте, где хоть раз запускали pytest.** Поиск id acceptance-примеров читал всё под `rglob("*test*")`, включая `tests/__pycache__/*.pyc` — `UnicodeDecodeError` вместо отчёта, шаг CI красный по причине, не связанной с поставкой. Лечение двойное: `read()` получил `errors="replace"` (снимает класс для всех вызовов), маска сужена до текстовых расширений с исключением `__pycache__`/`.venv`/`node_modules` (снимает причину здесь). **Новая §2.3a: пустой `active/` — штатное состояние, а не поломка.** После архивации поставки, как предписывает §2.3, отсутствие STATUS давало error — то есть контур красный между поставками, причём §13.1 сам приводит проект в это состояние, требуя архивации на handoff вместе с обязательством наблюдения. Теперь warning, а проверки уровня архива (полнота `INDEX.md`, просроченные наблюдения) продолжают работать — в idle они и важнее всего. Заглушка `phase: idle` сознательно не вводится: это ритуал заполнения ради зелёности |
| 2026-07-29 | **v1.24** (бэклог №12 — последний из двенадцати): новая **§13 «Post-merge»**. Третья исходная цель («быстро локализовать и решить») была закрыта наполовину: контур умел найти, какое изменение сломало, но заканчивался на мерже, а локализация начинается после. §13.1 — наблюдение сделано **обязательством, а не фазой**: фаза `observe` жила бы в `active/`, а она одна, и поставка, ждущая прода, заблокировала бы следующую. Поэтому на handoff в STATUS пишутся `observe_signal:` и `observe_until:`, поставка уходит в archive вместе с обязательством, а `observed.md` (A.17) появляется в её архивном каталоге; просрочку ловит `delivery_check` на следующей поставке — лаг в одну поставку, тот же приём, что §2.2a. §13.2 — замыкание круга §3.1d: те же id acceptance-примеров, что человек подписал **до** кода, проверяются **против прода**; «ошибок нет» сигналом не является, потому что молчание одинаково выглядит и при не вызываемом коде (§3.1a на уровне прода). Для «стало хуже» — одна метрика с замером ДО и порогом, реляционный оракул §6.5 между двумя моментами времени. §13.3 — лестница наблюдаемости (логи → ошибки и метрики → алерты с runbook'ами), `observability:` проверяется, а не декларируется. §13.4 честно делит механическое и нет; §13.5 — четыре остатка, включая «наблюдение можно подделать» и «если следующей поставки не будет, никто не проверит». DoD §3.2.9. **Бюджет гейтов исчерпан: 20 из 20** — и единица счёта не пересмотрена под результат (пять правил `check_grep_gate.sh` — пять гейтов, а не один файл), потому что это была бы подгонка метрики, а не уборка. Найдено при воспроизведении: проверка `observability: 2+` сначала проходила по неверной причине — наивный поиск цифры находил её в id примера `A1`; исправлено на цифру, которой не предшествует латинская буква (порог `180мс` отличается от `A1` и от имени метрики `p95`) |
| 2026-07-29 | **v1.23**: `### 3.1d` физически лежала **выше** `### 3.1c` — последний след ловушки «новая секция вставляется не по номеру» (её же разбор — в этом changelog у v1.17). Порядок восстановлен переносом блоков по границам `^### ` (ловушка «скрипт перестановки режет bash-блок»: перед переносом сверена чётность фенсов в каждом блоке, после — множество строк файла до и после совпадает, то есть ничего не потеряно и не добавлено). Номера секций не менялись, ссылки на них не тронуты; позиционные ссылки внутри блоков проверены — единственная («шаг выше») про шаг CI, а не про секцию |
| 2026-07-29 | **v1.22** (бэклог №8; №10 — в `cqg@1.16`): §4.6 «Опасную операцию — в песочницу, а не в HITL». Список HITL не сакрален: пункт в нём не потому, что операция опасна, а потому что **у неё нет оракула**. Правило перевода одно и жёсткое: **перевод обязан назвать оракул, заменивший человека** — иначе это оговорка 1 из `BACKLOG.md` («автономия без нового оракула = снятая проверка»). Таблица пяти переводов: миграция → эфемерная БД (`up`/`down`/`up` + сверка схемы), деструктивная команда → обёртка с dry-run по умолчанию, массовая правка → worktree (закрыт полностью), внешний API → фикстуры, прод-деплой → staging. Оформление — строка `sandbox: <правило> oracle=<путь> reason=…` в блоке `agent-permissions`; `delivery_check` проверяет **существование файла оракула**, потому что объявленная песочница без скрипта — снятая проверка с видом усиления (класс §3.1a «скрипт лежал, никто не вызывал»). Перечислено, что **не переводится никогда**: секреты в открытый вывод, платёжные и security-инварианты, `force-push` в main, всё, что видит пользователь под нашим именем — там нет обратимости, которую песочница могла бы доказать. Остаток назван: эфемерная копия — не прод (объём, блокировки, гонки), поэтому прод-строки остались за человеком. §9.1a: число гейтов убрано отсюда и держится в одном месте (каталог CQG §3), иначе счётчики расходятся; поле `new_dependency:` в шаблоне STATUS |
| 2026-07-29 | **v1.21** (бэклог №3/№4; №2 — в `okf@1.8`): §12.2a «Отклонённые варианты» — `plan.md` фиксировал выбранный подход и молчал про отброшенные, поэтому тот же тупик предлагается снова. Секция `## Rejected alternatives` в A.7, запись обязана содержать причину; класс L — error, класс M — warning; законна и запись «рассматривали только X, потому что Y исключён требованием Z». Граница с §12.2 задана явно (момент и срок жизни, не размер): на классе L причина уходит в `type: ADR` и переживает поставку. **Перенос в ADR гейтом не проверяется** — связать отказ с ADR механически можно только через ещё одну систему id, плата выше пользы; правило живёт, пока его читают. Новая §0.3a «Маршрутизация»: `kind` → что обязательно сверх базы класса, что читать, что упадёт. База по классу не дублируется — ссылка на §2.2, иначе таблицы разъедутся. У §0.3a **нет гейта**, и это сказано в ней самой: при расхождении с `delivery_check.py` прав скрипт. DoD §3.2.9. Проверялку ссылок `§N.M` строить не стал: измерил — 60 «висящих» ссылок оказались кросс-канонными, upstream-спекой и ссылками на пункты списка DoD, то есть гейт был бы стабильно красным (§4.3b) |
| 2026-07-29 | **v1.20** (бэклог №6/№7/№11): новая §12 «Три журнала поведения» — контур фиксировал результат и не показывал, **как** агент к нему шёл. `diagnosis.md` (§12.1, только `kind: bugfix`): гипотезы с обязательным вердиктом, поле `diagnosis:` в STATUS, `n/a reason=` по образцу `repro_test`. `decisions.md` (§12.2): greppable-лента, строка обязана называть альтернативу И причину — без альтернативы это отчёт о сделанном, аудит по нему невозможен; на handoff урок о системе переносится в `archive/INDEX.md`. `escalation.md` (§12.3, только при непустых `blockers:`): четыре части и **два варианта с ценой** — один вариант это просьба согласиться, а не выбор. §12.0 применяет бюджет §9.1a к артефактам: два журнала из трёх условны и в нормальной поставке не появляются. DoD §3.2.8, указатели из §3.1c/§3.3/§3.4, шаблоны A.14–A.16. **Найдено в собственной работе при воспроизведении:** все три проверки сначала удовлетворялись **незаполненным шаблоном** — та же дыра, что «`class: <S\|M\|L>` читался как class=S» (v1.5). Добавлен `unfilled()`: строка с `<…>` содержанием не считается, кандидаты сужены до list-item (гейт `diagnosis` удовлетворяла строка шаблона, объясняющая, какие бывают вердикты). Плейсхолдер распознаётся по кириллице или одиночной заглавной внутри скобок, чтобы `List<int>` и `<100ms` в живом тексте решения не ловились |
| 2026-07-28 | **v1.19** (бэклог №1/№9/№5): §2.2a — архив стал адресуемым: `archive/INDEX.md` (A.13) с путями и уроками под id `L<N>`; `delivery_check` требует индекс при непустом архиве, ловит отставший индекс (error) и неупомянутые уроки по затронутым путям (warning). Прежний ритуал §9.2 «раз в несколько поставок просмотри archive» был правилом без адреса и не выполнялся ни разу. §4.5 «Права на действия» — крупнейшее слепое пятно: контур контролировал выход и молчал про действия; блок `agent-permissions` в CONSTITUTION сверяется с `.claude/settings.json` в обе стороны, `allow` сознательно не сверяется (§4.3b), длинный `allow` при пустом `deny` = warning. Полевые данные: 3236 правил в `allow`, ноль в `deny`/`ask`. **Исправлена собственная дыра:** changelog v1.18 обещал «DoD §3.2.5 + `asserts_reviewed_by`», но правила в DoD не было — требование жило комментарием в шаблоне A.8 и не проверялось ничем. Теперь DoD §3.2.7 + §3.1d уровень 3: ревью пропорционально непроверяемой части, `n/a` законен только при `asserts_without_example: 0` из дайджеста |
| 2026-07-28 | **v1.18**: атака на главный потолок — §3.1d «откуда берётся ОЖИДАЕМОЕ значение»: круг «один исполнитель породил и код, и ожидание» разрывается тремя уровнями (acceptance-примеры в спеке до кода с подписью человека — проверяется `delivery_check`; реляционные оракулы §6.5, где ожидаемого значения нет вообще; ревью только утверждений через `assert_digest.sh`). §3.1b: differential против старой реализации вместо характеризационных тестов. §9.1a «ратчет на сам контур»: бюджет 20 гейтов, правило удаления по данным, `gate_kept_reason`. DoD §3.2.5 + `asserts_reviewed_by` в verify-report |

---

*Конец. Соседние каноны (подключать **после** этого файла):  
`CODE_QUALITY_GATES.md`, `OKF_KNOWLEDGE_BUNDLE.md`.  
Product-agent ABC (исследование): https://arxiv.org/abs/2602.22302*
