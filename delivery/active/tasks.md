# Tasks

- [ ] T0. **Подпись человека под спекой и примерами B1–B11** (§3.3 stop-gate) —
      до кода. Спека переписана 07.08 под правки владельца; дизайн-документ
      обновлён до v0.8, решения — в `decisions.md`
- [ ] T1. Долг прошлой поставки: `.astro` под eslint и prettier
      (`eslint-plugin-astro`, `prettier-plugin-astro`, маски хуков) **+ объявить
      `prettier` прямой devDependency** — сейчас транзитивный, гейт формата
      стоит на пакете без гарантий
- [ ] T2. **Смена локали:** `defaultLocale: 'ru'`, RU в корне, EN под `/en`,
      `x-default` = RU. Первым делом — позже это переезд всех URL
- [ ] T3. Оракул языковых пар: нет пары у slug → сборка падает с именем (B4)
- [ ] T4. Дизайн-система «жидкое стекло»: токены градиента, стеклянных
      поверхностей, акцента и текста; деградация `backdrop-filter` при
      `prefers-reduced-transparency` и на слабых устройствах
- [ ] T5. **Тест контраста** (B9): арифметика над токенами, проверка против
      светлейшего участка градиента. Первый тест проекта, `node --test`
- [ ] T6. `Base.astro`: head, OG, hreflang, JSON-LD `Person`, skip-link (B8)
- [ ] T7. `Nav` (проекты из коллекции + бургер на мобиле + переключатель),
      `Footer`, `404` (B2, B5, B7)
- [ ] T8. Контент: 4 проекта × RU + EN по §5.1–5.4 и схеме §8.2.1
- [ ] T9. `[slug].astro` (скелет §4.4, **место под видеофрейм**) + `/projects` +
      кольцо «Next» (B2, B3)
- [ ] T10. **Оракул артефакта** над `dist/`: состав обеих веток, `<head>`
      каждой страницы, вес страницы (B1, B8, B10). Снимает `artifact_oracle:
      n/a` в STATUS — к handoff строка обязана стать путём к файлу
- [ ] T11. Тест резолвера языка + реляционный оракул §6.5: для любой страницы и
      языка переключатель даёт существующий путь (B5)
- [ ] T12. **Headless-браузер** (одобрен владельцем, строка `new_dependency:`):
      B6 (reduced-motion, reduced-transparency) и B7 (360/390/414, бургер с
      клавиатуры)
- [ ] T13. Verify: `eval-smoke.md` по B1–B11, зелёный CI, мерж через
      `merge_guard.sh`. B11 — единственная ручная проверка, показывается
      человеку скриншотами
- [ ] T14. **Долг принципа 5:** обновить `4,200+` → `6,500+` в
      `ANTON_ASPIDOV_CV_EN.md` и `PORTFOLIO_LINKBUILDER_CASE.md`. Файлы вне
      репозитория — правку выполняет человек, задача открыта до подтверждения
