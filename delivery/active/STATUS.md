# Active delivery status

- **slug:** mvp-foundation
- **stack:** delivery@1.79, cqg@2.20, okf@absent
- **class:** M
- **kind:** feature
- **repro_test:** n/a reason=не bugfix, новая функциональность
- **diagnosis:** n/a reason=не bugfix
- **phase:** implement
- **builder:** agent:claude-code
- **verifier:** process:ci
- **human_ok_spec:** yes (by=human:anthony, at=2026-08-07)
- **human_ok_examples:** B1, B2, B3, B4, B5, B6, B7, B8, B9, B10, B11
- **human_ok_plan:** n/a (class M)
- **shape-oracles:** cqg-deployed
- **behavior-oracles:** weak — дёшево: словарь UI-строк и резолвер языка
  (чистые функции, `node --test`, нулевая зависимость); контраст токенов
  (арифметика над значениями, браузер не нужен); состав `dist/` и `<head>`
  каждой страницы (разбор собранного html). Дорого и потому останется ручным:
  раскладка на 360 px и видимость focus — нужен headless-браузер.
  <!-- §6.5a. Формулировка не декларативная: три первых пункта закрывают
       примеры B1, B8 и половину B5, и все три идут задачами этой поставки
       (T9, T3, T7a). «Weak» здесь значит «не развёрнуто», а НЕ «непроверяемо» —
       ровно то различие, которое правило и требует называть. -->
- **ci-oracles:** deployed (github-actions)
- **worktree:** none (ветка `mvp/shell-home`, дерево контура не трогается)
- **hooks:** not-deployed
- **stack-selftest:** ci (vendored)
- **blockers:** none
- **waivers:** none
- **new_dependency:** @astrojs/sitemap reason=sitemap на двух языковых ветках с hreflang обязан перечислять каждую страницу обоих языков; рукописный список расходится с коллекцией молча — сборка зелёная, страницы не в индексе by=agent:claude-code
- **new_dependency:** playwright reason=headless-браузер для примеров B6 и B7 (горизонтальный скролл на 360/390/414, бургер с клавиатуры, reduced-motion); одобрен владельцем 07.08. Окупился в первый же прогон: нашёл, что минификатор выбрасывает одно из двух написаний backdrop-filter — стекла не было бы либо в Firefox, либо в Safari до 18 by=human:anthony
- **new_dependency:** prettier reason=гейт формата уже стоит на нём, но пакет приезжает ТРАНЗИТИВНО — исчезнет у апстрима, и хук умрёт молча (найдено 07.08 при накате cqg@1.91) by=agent:claude-code
- **new_dependency:** eslint-plugin-astro + prettier-plugin-astro reason=весь код проекта в .astro, и сейчас его не судит ни линтер, ни форматтер — долг прошлой поставки, назван в её verify-report by=agent:claude-code
- **artifact_oracle:** scripts/lint/check_dist_oracle.py
  <!-- §6.5a закрыт по ФАКТУ. Судит собранный сайт: состав обеих языковых
       веток и зеркальность страниц (B1), мета-слой каждой страницы (B8),
       вес без медиа < 300 KB (B10). Фальсифицирован тремя способами:
       убранный og:image, x-default уведённый на EN, пропавшая EN-страница —
       каждый даёт красное с адресным диагнозом. -->
- **runtime_paths:** src/
  <!-- §12.6, и здесь это несущая строка: раскладку и вёрстку не проверяет НИЧТО
       из развёрнутых гейтов — сборка зелёная, а вёрстка может уехать. Весь
       `src/` объявлен одной поверхностью сознательно: разбивать её на
       `pages/`+`components/`+`styles/` значило бы притвориться, будто это
       независимые поверхности, тогда как ломаются они вместе. -->
- **canon_drift_waiver:** no
- **baseline_growth_waiver:** no
- **observability:** 1
- **observe_signal:** `npm run build` зелёный и `dist/` содержит обе языковые
  ветки; ручной проход пути рекрутёра (§4.2 дизайна) на мобильной ширине без
  горизонтального скролла
- **observe_until:** 2026-08-21
- **circuit_breakers:** default (25 файлов / 800 строк / 1 runtime-path)

## Что это

Первый слайс MVP витрины. Дизайн-документ `PORTFOLIO_SITE_DESIGN.md` v0.7 —
источник правды по содержанию (лежит вне репозитория, §8.3 того же документа).

Слайс отвечает на вопрос «витрина открывается и по ней можно ходить»: каркас
представления, Home по §4.2 и страницы проектов в структурном объёме. Полные
тексты кейса, медиа (тизеры, постеры, скриншоты), About и деплой — следующие
слайсы, перечислены в `plan.md`.

**Почему слайсами, а не целиком:** Phase 1 дизайна (Home + 4 страницы проектов +
About + деплой) не проходит circuit breaker §3.4 — только контент это 8 md-файлов,
плюс компоненты и стили. Разрезано по поверхностям, каждый слайс сам по себе
показуем.
