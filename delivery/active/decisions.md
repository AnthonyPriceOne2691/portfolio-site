# Decisions

Одна строка на выбор. Три части обязательны: **что выбрано**, **вместо чего**,
**потому что**. Строка без альтернативы — не решение, а отчёт о сделанном, и
`delivery_check` её отклонит.

- YYYY-MM-DD · выбрал <X> вместо <Y> — потому что <причина с числом или риском>
- 2026-08-07 · ставлю на сайте `6,500+` тестов вместо `4,200+` из CV (решение
  человека) — потому что 6,500+ подтверждено аудитом (зафиксированный прогон
  6,549 passed), а 4,200+ устарело. Цена названа и получила исполнителя: пока CV
  и `PORTFOLIO_LINKBUILDER_CASE.md` не обновлены, счётчик живёт в трёх местах
  и расходится — принцип 5 конституции нарушен, долг закрывает T13
- 2026-08-07 · смягчаю «one operator replaced a team of five» до «built to run
  with a single operator» вместо того, чтобы оставить исходную формулировку
  §10.4 (решение человека) — потому что §5.1 дизайна прямо фиксирует: замена
  команды в репозитории не подтверждена, источник — CV. Утверждение о свойстве
  системы защищается кодом, утверждение о пятерых — только памятью на интервью
- 2026-08-07 · не кладу CV-PDF в `public/` вместо того, чтобы выполнить §4.5
  дизайна целиком (решение человека) — потому что репозиторий публичный, а в CV
  телефон и адрес: публикация персональных данных требует, чтобы файл прочитал
  владелец, а не агент. Место под ссылку в nav резервируется, чтобы возврат не
  перекраивал вёрстку
- 2026-08-07 · беру `@astrojs/sitemap` вместо ручной генерации — потому что
  sitemap на двух языковых ветках с hreflang обязан перечислять КАЖДУЮ страницу
  обоих языков, и рукописный список расходится с коллекцией молча: сборка
  зелёная, а страницы в индекс не попали
- 2026-08-07 · RU становится языком по умолчанию и живёт в КОРНЕ, EN уходит под
  `/en`, вместо прежнего «EN в корне, RU под /ru» (решение владельца, отменяет
  зафиксированное §7.3 от 2026-07-20) — потому что основная аудитория ссылок
  русскоязычная. Редирект по локали браузера остался отвергнутым: одна ссылка
  открывалась бы у разных людей по-разному и шаринг перестал бы быть
  предсказуемым, поэтому default выражен структурой URL. Цена названа: ссылка
  для LinkedIn теперь `/en`, а не корень
- 2026-08-07 · визуальное направление «жидкое стекло» + небесно-голубой градиент
  вместо «тёмный ink + gate green, без градиентов» (решение владельца, отменяет
  §7.1.2) — потому что владелец хочет иную подачу. Две оговорки не отменяются
  вкусом и вписаны в дизайн-документ: контраст считается против СВЕТЛЕЙШЕГО
  участка градиента (на стекле текст проваливается до 3:1 незаметно), а
  `backdrop-filter` деградирует в заливку без блюра — на слабых Android он
  просаживает скролл, и это ровно то «зависание», которого владелец опасался
- 2026-08-07 · каждый проект отдельным пунктом меню + бургер на мобиле вместо
  «Projects одним пунктом, на мобиле без бургера» (§4.5) — потому что пунктов
  стало 8–9 вместо пяти, и в строку на 360 px они честно не помещаются. Пункты
  генерируются из коллекции, иначе ломается B2 «проект = пара md без кода»
- 2026-08-07 · отклонил требование «архитектура под 5–7 одновременных
  посетителей» вместо того, чтобы его закладывать — потому что проблемы не
  существует: сайт статический, сервера в схеме нет, CDN держит тысячи. Замена
  по существу: бюджет веса страницы < 300 KB как машинная проверка B10 и
  ленивая загрузка видео. Узкое место — вес и медиа, а не число людей
- 2026-08-07 · беру headless-браузер зависимостью (одобрено владельцем) вместо
  ручных проверок раскладки — потому что B6 и B7 иначе остаются «посмотрел
  глазами», а после смены визуальной системы на стекло проверять надо больше:
  деградацию блюра, отсутствие горизонтального скролла на трёх ширинах, бургер
  с клавиатуры
- 2026-08-07 · CV-файл не публикуется, кнопка ведёт на `mailto` вместо прямого
  PDF (§4.5) — потому что в CV телефон и адрес, а репозиторий публичный; файл
  кладём после того, как владелец прочитает оба документа
- 2026-08-07 · пере-снял снимок DRY с 0 на 2 вместо того, чтобы дальше дробить
  компоненты — потому что оставшиеся пары это каркасный обвяз Astro (импорт
  `Base`, `interface Props`, деструктуризация пропсов, вызов `useTranslations`),
  а не скопированная логика: примесей во фреймворке нет, каждый компонент
  объявляет своё. Настоящие дубли гейт нашёл, и они ВЫНЕСЕНЫ, а не узаконены:
  запрос «опубликованные проекты по порядку» жил в трёх местах (Nav, Home,
  ProjectsIndex) — стал `lib/projects.ts`; сетка карточек дублировалась на двух
  страницах — стала `ProjectGrid.astro`. Цена честная: снимок теперь 2, и рост
  сверх двух снова покраснеет
- 2026-08-15 · беру палитру из проекта `local-web-agent` вместо собственной
  небесно-голубой (решение владельца) — потому что так у витрины и у проектов,
  которые она показывает, один визуальный язык: рекрутёр видит систему, а не
  набор поделок. Оттуда же холст из четырёх радиальных пятен вместо линейного
  градиента — линейный на широком экране читается полосой, пятна светом
- 2026-08-15 · усилил стекло (заливка .55 вместо .72, блюр 32px вместо 20,
  внутренний блик) вместо косметической правки прозрачности — потому что
  «жидким» стекло делает не прозрачность, а блик по верхней кромке. Цена
  названа: на более прозрачной заливке текст обязан быть темнее, `--text` ушёл
  в oklch(24%); удержание порога 4.5:1 проверено тестом B9, а не на глаз
- 2026-08-15 · научил тест контраста разбирать `oklch` вместо перевода палитры
  в hex — потому что hex убил бы главное свойство: в градиенте между двумя
  oklch-цветами нет серой ямы посередине. Риск назван и закрыт: неверный
  конвертер не роняет ничего, а тихо превращает проверку в генератор
  случайных чисел, поэтому в тесте есть самопроверка на контрольных цветах
  (белый, чёрный, красный из спецификации CSS Color 4)
- 2026-08-15 · карточки на Home альбомные в одну колонку вместо сетки 2×2
  (решение владельца) — потому что на Home их всего три и они несут главный
  аргумент: строка таблицы результатов читается быстрее плитки. На `/projects`
  сетка осталась — там карточек может стать много. Горизонтальная раскладка
  включается только с 46rem: на 360 px она дала бы горизонтальный скролл,
  который сторожит B7
- 2026-08-15 · headless-тест поднимает статический сервер и ходит по http://
  вместо `file://` — потому что Astro подключает стили АБСОЛЮТНЫМ путём
  `/_astro/…`, а под `file://` он резолвится в корень файловой системы: страницы
  открывались БЕЗ CSS вовсе. Проверка «нет горизонтального скролла» на
  неверстанной странице проходит всегда — тест был зелёным именно потому, что
  ничего не проверял. **Урок о системе:** зелёный тест, который никогда не
  краснел, подозрителен сам по себе; фальсификация ловит «проверку не того»
  только если ломать ровно то, на что тест смотрит, — я ломал вёрстку, а он
  смотрел на страницу без вёрстки. Добавлена страховка: перед проверкой тест
  утверждает, что CSS применился (`--measure` не пуст)
- 2026-08-15 · две ширины колонки (`--measure` 72ch для чтения, `--measure-wide`
  68rem для витрины) вместо одной — потому что 72ch это предел удобного ЧТЕНИЯ,
  и он верен для прозы на странице проекта, но витрине читать нечего, ей надо
  показывать. Карточка на главной выросла с 704 до 1024 px
- 2026-08-15 · селектор `.grid:not(.wide)` вместо `.grid.grid` — потому что
  второе не значит «оба класса», это просто `.grid` с удвоенной специфичностью:
  правило матчило и альбомный список, разгоняя его в две колонки. Карточка
  выходила вдвое уже задуманного, а сборка была зелёной: вёрстку не судит ни
  один гейт формы (§12.6), поэтому её и меряют браузером
- 2026-08-15 · фрейм под видео в карточке Home — СТАТИЧНЫЙ, без автоплея и
  лупа, вместо того чтобы перенести туда тизер со страницы проекта (правка
  владельца отменяет запрет §6.1 и §7.1.3) — потому что запрет был не о
  вкусе: четыре играющих плеера на первом экране это вес трафика и разряд
  батареи на телефоне рекрутёра. Владелец просил фрейм, а не движение;
  компромисс отдаёт визуальную часть просьбы целиком и не берёт её цену.
  Полное видео по-прежнему живёт на странице проекта
- 2026-08-15 · фрейм рисуется в карточке ВСЕГДА, даже когда медиа нет, — в
  отличие от страницы проекта, где пустая секция не рендерится (B3). Причина
  разная у одного компонента: на странице пустой блок это дыра, в карточке —
  забронированное место, без которого появление файла перекроит раскладку
