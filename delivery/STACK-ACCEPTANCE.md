# Приёмка контура — portfolio-site

**Стек проекта:** Astro 7 / TypeScript, статическая генерация, без бэкенда.
**Stack:** `delivery@1.84 · cqg@2.28 · okf@absent`.
<!-- Метка именно `**Stack:**`, а не прежняя «Каноны»: доктор ищет третью запись о
     версии по МЕТКЕ строки (`doctor_versions.STACK_RECORDS`), и с прежней меткой
     он два обновления подряд печатал «нет записи в delivery/STACK-ACCEPTANCE.md»
     — третий источник правды о версии не сверялся ни с чем. Развёртывание шло на
     delivery@1.50 / cqg@1.79; разбор ролей ниже написан на том состоянии. -->

**Дата:** 2026-08-04. **Состояние:** развёртывание идёт, приёмка не закрыта.

---

## 1. Карта ролей: 22 гейта канона на этом стеке

Канон требует пройти каталог §3 **построчно** и на каждую строку ответить:
подключён / осознанно исключён с причиной. Это первое развёртывание контура на
не-Python проекте, поэтому таблица ниже — не формальность, а собственно
результат: она показывает, сколько механики канона переносится на чужой стек.

| Гейт | Здесь | Почему |
|---|---|---|
| `check-yaml` / `check-json` / `check-toml` | ✅ подключён | форматы универсальны, конфиги проекта именно такие |
| `check-merge-conflict`, `check-added-large-files` | ✅ подключён | язык не важен; для витрины с видео второе особенно уместно |
| `prettier` | ✅ подключён | `files: ^src/`, из корня. ⚠ До 06.08 инертен по той же причине; первый живой прогон нашёл настоящее — формат `src/content.config.ts` |
| `eslint` (ошибки) | ✅ подключён | `files: ^src/`. ⚠ До 06.08 стояла шаблонная маска `^frontend/src/` — каталога нет, хук был `Skipped` |
| `eslint-warning-ratchet` | ✅ подключён | снимок снят: warnings 0, ошибок 0. ⚠ До 06.08 — та же шаблонная маска и отсутствующий снимок |
| `file-length` | ✅ подключён, маска в `entry:` | `LINT_LENGTH_GLOBS="*.astro *.ts *.tsx *.js *.mjs *.css"` **и** `files:` под те же расширения. ⚠ До 06.08 маска была объявлена ЗДЕСЬ и отсутствовала в конфиге: файл на 900 строк проходил, exit 0. Проверено исполнением, гейт видит 4 файла |
| `jscpd-DRY` | ✅ подключён (`cqg@1.76`) | TS-половина: `-f typescript` + `LINT_JSCPD_TS_EXTS="typescript:ts,tsx,astro"` в `entry:`. Проверено исполнением — **просмотрено 2 файла**, снимок 0 clone-пар. ⚠ Прежняя ✅ была ложной: гейт был зашит на `-f python` и `$LINT_PY_SRC`, снимок снят по python-профилю и ничего не значил. Роль закрылась правкой КАНОНА, а не конфига: чинить одной переменной было нельзя — гейт нашёл бы бинарь и стал зелёным на пустоте |
| `detect-secrets-guard` | ✅ подключён | **репозиторий публичный**: это здесь несущий гейт, а не гигиена. Проверен не только на дереве, но и на истории перед раскаткой 07.08 — 9 коммитов, чисто |
| `gate-coverage` (мета) | ✅ подключён | без него любая строка этой таблицы — обещание |
| `baseline-ratchet` (мета) | ✅ pre-push + CI | снимки только вниз; нужен remote-ref |
| `ci-status` | ✅ внутри `merge_guard` | «у меня локально прошло» закрытием не считается |
| `new-dependency` | ✅ pre-push | на витрине это несущее правило: каждая прямая зависимость — решение (constitution §Product principles 3) |
| `deps-audit` | ⚠️ половина | npm-половина работает (`npm audit`, просмотрен 1 манифест), python-половины нет — питон-манифеста в проекте не существует. ⚠ До 07.08 не работала и npm-половина: в `entry:` не было `LINT_FE_DIR=.`, аудит уходил в несуществующий `frontend/`. В CI переменная стояла в `env:` workflow, поэтому дыра пряталась за зелёным CI и вскрылась только когда гейт научился печатать число (`cqg@1.79`) — доктор назвал `DEAD` |
| `ruff` / `ruff-format` | ⛔ исключён | Python-линтер, питон-кода в проекте нет |
| `mypy` | ⛔ исключён | типы TS проверяет `astro check` (в `npm run build`), это его роль на данном стеке |
| `complexity-gate` | ✅ подключён (`cqg@1.66`) | TS-половина на eslint: `LINT_TS_SRC=src LINT_FE_DIR=.` в `entry:`. Пороги навязывает гейт флагом `--rule`, конфиг проекта даёт только парсер. Проверено тремя прогонами на этом дереве: сложная функция → exit 1 с именем файла, снимок → exit 0, контроль → exit 0. **PLR0911 (число return'ов) замены не имеет — правила `max-returns` в ESLint нет** |
| `layers-gate` | ✅ подключён (`cqg@1.66`) | роль заняла обёртка `check_layers_gate.sh`, TS-половина на dependency-cruiser. **Контракт переписан под раскладку САЙТА, а не скопирован:** шаблон канона говорит про `src/{api,services,models}`, здесь слои `content` (лист) и `pages`. Проверено: `content` → `pages` даёт exit 1 с названным ребром, контроль зелёный |
| `mutation` | ⚠️ ветка есть, предмета нет | ветка на Stryker поставляется с `cqg@1.66`, но **тестов в проекте нет вовсе** — гейт честно печатает «Stryker не найден» и выходит нулём. Это не покрытие: роль остаётся непросуженной, пока не появится первый тест. Ставить Stryker до этого бессмысленно — мутантов некому убивать |
| `diff-coverage` | ⛔ исключён | тестов нет вовсе. Вернуть вместе с первым тестом, иначе гейт мерил бы пустоту |
| `config-access` (grep) | ⛔ исключён | правило написано под `os.getenv`; предмета нет — статический сайт не читает окружение в рантайме |
| `di-indirection` (grep) | ⛔ исключён | `importlib`-обходы — питоновская конструкция |
| `service-no-web` (grep) | ⛔ исключён | нет сервисного слоя: весь код — генерация страниц |
| `no-grab-bag-module` (grep) | ⛔ исключён **временно** | правило осмысленно и на TS (`utils.ts`), но паттерн под Python. Переписывать под язык — работа с замером; пока предмета нет (файлов `src/utils.*` не существует), вернуть при появлении |
| `blind-error` / `unstructured-log` (grep) | ⛔ исключён | нет рантайма и логов: сайт статический |
| `silent-except` / `inline-prompt` / `cpu-in-async` / `unbounded-list` (AST) | ⛔ исключён | разбор Python-AST; к `.astro`/`.ts` неприменим |

**Счёт после `cqg@1.79`: 15 гейтов из 22 работают, 1 наполовину, 6 исключены
с причиной.** DRY вернулся в строй TS-половиной; `mutation` остаётся наполовину
(ветка есть, тестов нет). Было 12 / 1 / 9 — три несущие роли закрыты правкой канона,
оплаченной этим же развёртыванием.

### Что это значит честно

Три дыры, названные первым заходом, **закрыты в каноне** (`cqg@1.66`), и закрыты
подстановкой инструмента, а не новыми гейтами — бюджет 22/22 остался нетронут.
Что здесь работает теперь и чего это стоит:

- **сложность функций** — судится (eslint), но предмет пока один файл
  (`src/content.config.ts`). Гейт доказал, что краснеет на этом дереве, — иначе
  «OK — просмотрено 1 файл» не отличался бы от неработающего;
- **направление зависимостей** — судится (dependency-cruiser), 3 модуля.
  Контракт пришлось написать под слои сайта: скопированный дословно шаблон канона
  не совпал бы ни с одним файлом и был бы зелёным всегда. Это названный предел
  §3.6, и подтвердился он здесь при первом же применении;
- **«тесты хоть что-то утверждают»** — по-прежнему НЕ судится, и причина
  сменилась: раньше не было инструмента, теперь нет тестов. Ветка на Stryker
  готова и ждёт первого теста.

Остаётся честным: `astro check` (типы), схема zod (сломанный frontmatter валит
сборку — примеры A2/A3), малый объём кода.

### Что в контуре красное и почему — состояние на 06.08

`pre-commit run --all-files`: **13 прошли, 0 упали, 3 пропущено** (пропуски —
`check-toml`, `ruff`, `ruff-format`: конфигов и python-кода нет, это честная
непокрытость). `gate-coverage`: OK — 15 скриптов, подключено 4, осознанно нет 11.
`contour_doctor`: `DEAD 0`, области — `complexity` 1 файл, `file-length` 4,
`layers` 3 модуля.

**Десять python-хуков сняты с коммита и объявлены в `scripts/lint/not-applicable.json`**
с причинами, которые печатаются на каждом прогоне. Раньше они были подключены и
смотрели в пустоту — роль числилась закрытой, а гейт не видел кода.

⚠ **Разбор, который стоит помнить: контур не работал с самого начала, и не из-за
скриптов.** Скрипты канона отработали верно — именно они и выдали улику
(«просмотрено 1 файл(ов)», «no files to check»). Сломана была **привязка к стеку**:
правки, доехавшие до `entry:` (complexity, layers), работали; те, что остались
только текстом в этой таблице (маски длины, пути фронта), — нет. Природа у всех
пяти находок одна: роль закрыли текстом, конфиг остался шаблонным.

Класс закрыт в каноне, а не только здесь (`cqg@1.69`–`1.70`): доктор гоняет
вписанные гейты в дереве и роняет прогон на «просмотрено 0», а `gate-coverage`
сверяет ЭТУ таблицу с `not-applicable.json` и деревом. Заявление, разошедшееся с
механикой, больше не молчит.
---

## 2. Чеклист приёмки (AGENT_STACK §6)

- [x] Delivery-каркас развёрнут, `delivery_check` проходит (0 error)
- [x] Constitution заполнен под продукт, не копирует CQG/OKF
- [x] `.claude/settings.json` совпадает с блоком `agent-permissions`
- [x] Версии канонов записаны в constitution и STATUS
- [x] Скрипты гейтов и конфиги разложены из payload'а (17 скриптов, 3 конфига)
- [x] Карта ролей заполнена (раздел 1 этого файла)
- [x] `npm ci` прошёл, инструменты фронта установлены (блокер снят 04.08: диагноз
      был неверен — деградация канала под 15 сокетами, лечится `maxsockets=2`)
- [x] Baseline'ы сняты с нуля: `complexity`, `file_length`, `eslint_warnings`, `jscpd`
- [x] `pre-commit install` + `pre-commit run --all-files` зелёный при чистом env
- [x] Каждый гейт формы печатает непустое число просмотренных файлов —
      `complexity` 1, `file-length` 4, `layers` 3, `jscpd` 2, `eslint-warnings` 1.
      ⚠ Пункт был невыполним до 06.08: четыре гейта смотрели в пустоту, а `jscpd`
      и ратчет числа вовсе не печатали. Закрыто правкой канона (`cqg@1.69`–`1.77`),
      а не подгонкой отметки
- [x] `contour_doctor.py` — `DEAD 0` (пример A6), «лжи нет»
- [x] **CI зелёный** — прогон #3 (`adf8bed`), обе джобы `success`, ни одного
      `skipped` шага, `pre-commit (all files, STRICT=1)` выполнился: 13 Passed,
      3 Skipped — состав тот же, что локально. Первые два прогона были красными
      по делу и нашли две поломки: `cache: pip` без python-манифеста и замкнутый
      круг `--require-ci` (разбор — в STATUS у `ci-oracles`). Задержка была
      внешней: у GitHub шёл major outage Actions, прогоны не создавались вовсе —
      проверено, что дело не в проекте (видимость, квота и содержимое workflow
      исключены поочерёдно, включая пробу из шести строк).
      ⚠ Третья находка того же прогона: в логе CI **не было чисел** «просмотрено
      N» — `pre-commit` без `--verbose` проглатывает stdout прошедших хуков, и
      критерий §6 оказывался проверяемым локально и НЕпроверяемым там, где по
      §10.4 живёт истина. Флаг добавлен
- [x] `merge_guard.sh` пропускает только зелёное — проверено на мерже PR #1:
      squash в `main` (`1b130a2`) после прогона гейтов на СЛИТОМ состоянии, не
      кнопкой. `quality` на main — зелёный (#6), `main-guard` — `skipped`, как и
      задуман (срабатывает только на красный).
      ⚠ Первое живое применение нашло в гейте мержа две дыры, обе теперь
      починены и фальсифицированы: ① разбор STATUS не читал значение вовсе
      (`**ci-oracles:**` против ожидаемого `**ci-oracles**:`), поэтому ветка «в
      STATUS `deployed`, а CI не подтверждён — красней» не срабатывала НИКОГДА;
      ② `gh auth status` ходит в сеть и на порванном канале печатал «gh не
      авторизован», отправляя чинить авторизацию вместо сети.
      Урок про канон крупнее обоих: над одним STATUS живут ДВА разбора, и
      `delivery_check.py` читает его верно — расхождение парсеров внутри одного
      контура (в BACKLOG)
- [x] Публичный репозиторий на GitHub: `AnthonyPriceOne2691/portfolio-site`,
      история проверена на секреты и NDA-следы перед раскаткой — чисто

**Непокрытые области, названные словами** (канон требует называть, а не
пропускать) — состояние на 06.08, сверено с прогоном:

- **«тесты хоть что-то утверждают» — не судится.** Тестов в проекте нет вовсе;
  ветка мутационного гейта на Stryker готова и ждёт первого теста. Сложность и
  направление зависимостей отсюда УБРАНЫ: с `cqg@1.66` их судят eslint и
  dependency-cruiser, и доктор подтверждает числами (1 файл и 3 модуля).
- **`.astro` вне eslint и prettier.** У обоих хуков `types_or: [ts, tsx, …]`, а
  плагинов `eslint-plugin-astro` / `prettier-plugin-astro` не поставлено — то
  есть единственный `.astro` не судит ни линтер, ни форматтер, и `.md` контента
  тоже вне prettier. Нигде не переобещано (таблица ролей этого не утверждает),
  но по правилу проекта непокрытое называется словами. Закрывается двумя
  devDependency, каждая — строкой `new_dependency:`; отложено сознательно:
  сейчас предмет — один файл, и цена плагинов выше пользы.
- **`ci-oracles: weak`** до первого зелёного прогона (remote нет).
