# Archive index

Что это: адресный указатель по `archive/`. Перед implement найди строки, чьи пути
пересекаются с твоим диффом, прочитай их уроки и сошлись на id в `plan.md`
(M/L) или `tasks.md` (S). Не применимо — напиши одной строкой почему.

Правила ведения:
- Строка добавляется на handoff, вместе с переносом поставки в `archive/`.
- Пути — префиксы от repo-root, в backticks. Только те, где жил урок.
- Урок — что мы **узнали о системе**, а не что сделали. «Добавил кэш» — не урок.
  «Пустой robots.txt и недоступный robots.txt — разные случаи, мы их слили» — урок.
- Урок без id не считается: id — то, чем на него ссылаются.
- Поставка без урока — строка с `—` в колонке. Это законно и означает «ничего
  нового не узнали»; частое `—` подряд означает, что разборы стали формальными.

| Дата | Поставка (каталог) | Пути | Урок |
|---|---|---|---|
| 2026-07-20 | `2026-07-20-robots-cache` | `backend/features/robots/` | **L1:** пустой robots.txt и недоступный (500) — разные случаи; слитые в один дают «запретить всё» на аварии апстрима. Тест A3 в той поставке |
| 2026-07-24 | `2026-07-24-billing-retry` | `backend/features/billing/`, `frontend/src/billing/` | **L2:** ретрай по таймауту без идемпотентного ключа удваивает списание; ключ обязан приходить от вызывающего, а не генерироваться внутри |
| 2026-07-26 | `2026-07-26-typing-pass` | `backend/features/` | — |
| 2026-08-07 | `2026-08-07-contour-bootstrap` | `scripts/lint/`, `.pre-commit-config.yaml`, `.github/workflows/`, `delivery/`, `docs/canon/` | **L3:** роль, закрытая ТЕКСТОМ в карте ролей, не судится ничем — путь в `entry:` остаётся шаблонным, и гейт зеленеет на пустоте. Класс повторился ВОСЕМЬ раз за три дня (маски длины, пути фронта, jscpd, deps-audit, `cache: pip`), то есть это не небрежность, а отсутствие перил: `gate-coverage` проверял «вписан ли гейт» и не проверял «видит ли он код». Перед правкой любого гейта: сначала заставь его напечатать число просмотренных файлов, потом верь зелёному |
| 2026-08-07 | `2026-08-07-contour-bootstrap` | `scripts/lint/check_ci_status.sh`, `scripts/delivery_check.py` | **L4:** над одним `STATUS.md` жили ДВА разбора, и они расходились: `delivery_check` читал `**ci-oracles:**` верно, а гейт мержа — нет, из-за чего его защитная ветка не срабатывала НИКОГДА и мерж проходил без проверки CI. Два парсера одного файла — всегда дефект, даже когда оба «работают»: расходятся они молча |
| 2026-08-07 | `2026-08-07-contour-bootstrap` | `.github/workflows/quality.yml`, `delivery/active/STATUS.md` | **L5:** предохранители контура образуют замкнутый круг на первой поставке: §10.4 велит ставить `ci-oracles: deployed` только по факту зелёного прогона, а `delivery_check --require-ci` краснеет на любом другом значении. Правило, у которого посылка — результат его же исполнения, на bootstrap неисполнимо; выход — аванс, валидируемый тем же прогоном, и он обязан быть НАЗВАН, иначе неотличим от поддавков |
