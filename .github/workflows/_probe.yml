name: _probe

# ВРЕМЕННЫЙ диагностический workflow. Отвечает ровно на один вопрос:
# создаёт ли GitHub прогоны для этого репозитория вообще. Живёт только в ветке
# contour/private-repo и удаляется сразу после ответа.
on: push

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - run: echo "actions работают"
