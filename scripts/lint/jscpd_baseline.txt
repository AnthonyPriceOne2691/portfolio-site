# jscpd_baseline.txt — снимок DRY-гейта. Генерируется --generate, НЕ руками.
# Одно число = кол-во clone-пар в прод-Python (python, min-tokens 50, без tests).
# Гейт падает при РОСТЕ; после консолидации дублей пере-снять вниз (--generate).
2
