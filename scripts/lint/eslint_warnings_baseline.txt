# eslint_warnings_baseline.txt — снимок eslint-ратчета. Генерируется --generate, НЕ руками.
# Первая строка-число = сумма ESLint warnings полного прогона фронт-src.
# Далее <errors>:<path> — пофайловый долг ОШИБОК: файл проходит при errors <= снимок,
# файл ВНЕ снимка (новый или ранее чистый) — hard 0. Оба списка только ТАЮТ.
0
